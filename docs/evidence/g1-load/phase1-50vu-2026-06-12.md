# G1 Phase 1 — Staging scale (50 VU × 60s)

**Date:** 2026-06-12  
**Project:** Mingla-dev `gqnoajqerqhnvulmnyvv` (G2 ✅)  
**Runner:** `scripts/load/run-staging.sh` (Docker `grafana/k6`)

## Summary

**PASS** — All business checks green; p95 latency within SLOs from `docs/load-profile.md`.

| Run | Script | VUs | Duration | Checks | p95 latency | SLO | Report |
|-----|--------|-----|----------|--------|-------------|-----|--------|
| Combined smoke | `smoke.js` | 50 | 60s | 100% (10,770) | **358 ms** | &lt; 3s (harness) | `reports/20260612T134722Z-scale-vus50.json` |
| Discover | `discover-merged-events.js` | 50 | 60s | 100% (4,308) | **632 ms** | &lt; 800 ms read | `reports/20260612T135139Z-discover-vus50.json` |
| Checkout status | `ticket-checkout-status.js` | 50 | 60s | 100% (4,643) | **223 ms** | &lt; 800 ms read | `reports/20260612T135241Z-ticket-checkout-status-vus50.json` |
| Checkout create | `ticket-checkout-create.js` | 50 | 60s | 100% (4,746) | **203 ms** | &lt; 2 s create | `reports/20260612T135403Z-ticket-checkout-create-vus50.json` |

**Throughput (combined smoke):** ~172 req/s, 2,154 iterations (~34 iter/s).

## Fixtures

Same as Phase 0 — `LOAD_TEST_EVENT_ID=a0000000-0000-4000-8000-000000002076`, ticket type `…3076`.

## Harness notes

1. **Per-path VU override:** `.env.load` defaults (`LOAD_VUS=2`) were overriding CLI exports. `run-staging.sh` now accepts optional `[VUS] [DURATION]` on any script (e.g. `./run-staging.sh discover 50 60s`).
2. **`discover` alias:** maps to `discover-merged-events.js` (file name mismatch fixed).
3. **Exit code 99 on checkout paths:** k6 `http_req_failed` counts 4xx/422 as failures. Synthetic checkout tokens and preview-mode responses are **expected 4xx** — all `not 5xx` checks pass. Align per-path thresholds with `smoke.js` (`checks` rate) before Phase 2.

## Commands

```bash
./scripts/load/run-staging.sh scale 50 60s
./scripts/load/run-staging.sh discover 50 60s
./scripts/load/run-staging.sh ticket-checkout-status 50 60s
./scripts/load/run-staging.sh ticket-checkout-create 50 60s
```

## Next

- [x] Phase 1 — 50 VU × 60s
- [x] Phase 2 — 1,000 VU × 5m — see [phase2-1000vu-2026-06-12.md](./phase2-1000vu-2026-06-12.md) (discover latency FAIL)
- [ ] Phase 3–4 — 10k → 100k (distributed k6)
