# Phase 1: no env vars required (no asset generation calls yet).
# Phase 2+ will add keys for: Stitch (via gcloud OAuth), Nano Banana / Veo 3 (GEMINI_API_KEY),
# 21st.dev Magic (TWENTY_FIRST_API_KEY), Supabase Storage for hosted assets.
