import React, { useEffect, useRef, useState } from "react";
import { AppState, Linking, Platform, Pressable, StyleSheet, Text, View } from "react-native";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { accent, canvas, semantic, text, typography } from "../../constants/designSystem";
import { checkAppVersionPolicy, type UpdateDecision } from "../../services/appVersionPolicy";
const REFRESH_MS=15*60*1000;
export function MandatoryUpdateGate({children}:{children:React.ReactNode}):React.ReactElement{const [decision,setDecision]=useState<UpdateDecision>({state:"unknown"});const [openError,setOpenError]=useState<string|null>(null);const checkedAt=useRef(0);const prior=useRef(AppState.currentState);const insets=useSafeAreaInsets();const check=()=>{checkedAt.current=Date.now();void checkAppVersionPolicy().then(setDecision);};useEffect(()=>{if(Platform.OS==="web")return;check();const sub=AppState.addEventListener("change",next=>{const wasBackground=prior.current==="background";prior.current=next;if(wasBackground&&next==="active"&&Date.now()-checkedAt.current>=REFRESH_MS)check();});return()=>sub.remove();},[]);if(decision.state!=="required")return <>{children}</>;return <View accessibilityViewIsModal style={[styles.page,{paddingTop:insets.top,paddingBottom:insets.bottom}]}><View style={styles.content}><Text accessibilityRole="header" style={styles.title}>Update required</Text><Text style={styles.body}>{decision.message||"Update Mingla to keep using the app."}</Text>{openError&&<Text accessibilityLiveRegion="polite" style={styles.error}>{openError}</Text>}<Pressable style={styles.button} accessibilityRole="button" accessibilityLabel="Update now" accessibilityHint="Opens the app store listing for Mingla" onPress={()=>void Linking.openURL(decision.storeUrl).catch(()=>setOpenError("Unable to open the store. Please update Mingla from your app store."))}><Text style={styles.buttonText}>Update now</Text></Pressable></View></View>;}
const styles=StyleSheet.create({page:{flex:1,backgroundColor:canvas.discover,paddingHorizontal:24,justifyContent:"center"},content:{alignSelf:"center",width:"100%",maxWidth:440},title:{...typography.display,color:text.primary,marginBottom:16},body:{...typography.body,color:text.secondary,marginBottom:24},error:{...typography.bodySm,color:semantic.error,marginBottom:16},button:{minHeight:52,borderRadius:999,alignItems:"center",justifyContent:"center",backgroundColor:accent.warm},buttonText:{...typography.buttonLg,color:text.inverse}});
