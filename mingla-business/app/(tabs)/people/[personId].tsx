import React from "react"; import { StyleSheet } from "react-native"; import { useLocalSearchParams,useRouter } from "expo-router";
import { SafeScreen } from "../../../src/components/ui/SafeScreen"; import { TopBar } from "../../../src/components/ui/TopBar"; import { PersonDetailView } from "../../../src/components/people/PersonDetailView"; import { useCurrentBrand } from "../../../src/hooks/useCurrentBrand"; import { useCurrentBrandRole } from "../../../src/hooks/useCurrentBrandRole"; import { useBrandPerson } from "../../../src/hooks/marketing/useBrandPeople"; import { PeopleServiceError } from "../../../src/services/peopleService"; import { canvas } from "../../../src/constants/designSystem";
export default function BrandPersonDetailRoute():React.ReactElement{const router=useRouter(),params=useLocalSearchParams<{personId:string|string[]}>(),brand=useCurrentBrand(),role=useCurrentBrandRole(brand?.id??null),personId=Array.isArray(params.personId)?params.personId[0]:params.personId??null;const roleResolved=!role.isLoading&&!role.isError;const q=useBrandPerson(brand?.id??null,personId,roleResolved&&role.accepted,role.rank);const e=q.error instanceof PeopleServiceError?q.error.code:null;return <SafeScreen edges={["top"]} style={s.host}><TopBar leftKind="back" title={q.data?.displayName??"Person"} onBack={()=>router.replace("/(tabs)/marketing/people" as never)} rightSlot={null}/><PersonDetailView person={q.data??null} loading={!roleResolved||q.isLoading} error={roleResolved&&role.rank<20?"forbidden":e==="people_not_found"?"not_found":q.isError?"error":null} onRetry={()=>void q.refetch()}/></SafeScreen>};const s=StyleSheet.create({host:{backgroundColor:canvas.discover}})
