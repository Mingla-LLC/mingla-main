import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";
const source=fs.readFileSync(new URL("../ToolLeadsPage.jsx",import.meta.url),"utf8");
const competitorOps=source.slice(source.indexOf("function CompetitorOpsPanel"));
test("Competitor Ops is a bounded tab with capability generations",()=>{for(const value of ["Competitor Ops","TikTok is link only and cannot be enabled here.","Generation {cap.generation}","Pause checking","Resume checking"])assert.ok(source.includes(value));});
test("Competitor Ops exposes redacted weekly cents and bounded retry only",()=>{for(const value of ["Watch ID","Brand ID","Weekly spend / remaining","weekly_actual_cents","weekly_reserved_cents","weekly_remaining_cents","measurement unavailable","Safe reason","Retry once","admin_retry_count < 1"])assert.ok(competitorOps.includes(value));for(const forbidden of ["row.normalized_url","row.caption","row.email","row.ip_hash","row.provider_object_id","raw_prompt","prompt_content","model_response"])assert.ok(!competitorOps.includes(forbidden));});
