/**
 * SKIP STUB — ORCH-1015 restructured the Overview tab from ORCH-1014's
 * three-column count layout into a 3-band readiness ladder with binary
 * badges. The original assertions referenced a row shape that no longer
 * exists.
 *
 * File preserved as skip stub to satisfy the append-only CI gate (ORCH-0840),
 * which forbids test-file deletion regardless of TEST-MOD-APPROVED.
 *
 * New coverage: orch1015_overview_readiness_ladder.test.js
 */
import { test } from "node:test";

test("ORCH-1014 three-column layout replaced by ORCH-1015 readiness ladder", { skip: "superseded by ORCH-1015" }, () => {});
