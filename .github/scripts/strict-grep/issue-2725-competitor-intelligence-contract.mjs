#!/usr/bin/env node
import fs from "node:fs";
const ROOT = new URL("../../../", import.meta.url);
const read = (path) => fs.readFileSync(new URL(path, ROOT), "utf8");
export function evaluate(files) {
  const failures = []; const migration = `${files.migration ?? ""}\n${files.amendment ?? ""}`; const worker = files.worker ?? ""; const compactWorker = worker.replace(/\s+/g, ""); const run = files.run ?? ""; const report = files.report ?? ""; const analytics = files.analytics ?? ""; const ui = files.ui ?? "";
  const requireAll = (source, label, needles) => needles.forEach((needle) => { if (!source.includes(needle)) failures.push(`${label}: missing ${needle}`); });
  requireAll(migration, "schema", ["tool_competitor_sources", "tool_competitor_refresh_jobs", "tool_competitor_observations", "tool_competitor_briefs", "tool_competitor_provider_capabilities", "tool_competitor_budget_ledger", "tool_competitor_admin_actions", "job_id uuid UNIQUE", "revision integer NOT NULL DEFAULT 1", "uq_tool_competitor_one_active_job", "uq_tool_competitor_job_idempotency", "member_retry_count BETWEEN 0 AND 1", "funding_lane IN ('scheduled','manual')", "FOR UPDATE OF j SKIP LOCKED", "RETURNING id INTO retry_job", "redacted_at", "watch_receipt_hash", "interval '90 days'", "interval '400 days'", "is_admin_user()", "issue_2725_watch_remove", "issue_2725_member_refresh", "issue_2725_finish_job", "tool_competitor_venue_week_budget_boundaries", "PRIMARY KEY(venue_listing_id,iso_week)", "used+p_amount>1000000", "p_amount<>50000", "measurement_failed", "tool_competitor_model_usage_receipts", "tool_competitor_synthesis_results", "ON CONFLICT DO NOTHING", "weekly_remaining_cents"]);
  requireAll(compactWorker, "worker", ["ANALYZED_PROVIDER_ALLOWLIST=newSet([\"website\",\"instagram\"]", "beforeproviderI/O", "immediatelybeforepublish", "META_COMPETITOR_IG_USER_ID", "META_COMPETITOR_ACCESS_TOKEN", "issue_2725_reserve_budget", "p_owner:job.lease_owner", "current_brief_id", "issue_2725_finish_job", "purgeOldLiveContent", "tool_competitor_observations\").delete", "rawprovider", "responseJsonSchema", "temperature:0", "thinkingBudget:0", "candidateCount:1", "MAX_SYNTHESIS_OUTPUT_TOKENS", "usage_metadata_missing", "issue_2725_record_model_usage", "issue_2725_accept_synthesis"]);
  if (/ANALYZED_PROVIDER_ALLOWLIST[\s\S]{0,240}tiktok/i.test(worker) || /observeTikTok|fetchTikTok|tiktok\.com.*fetch/i.test(worker)) failures.push("worker: TikTok analysis/network branch forbidden");
  requireAll(run, "run-api", ["watch_update", "watch_refresh", "expected_updated_at", "issue_2725_watch_upsert", "issue_2725_member_refresh"]);
  requireAll(report, "report-api", ["competitor_brief", "schema_version: 2", "watch.brand_id !== auth.brandId"]);
  requireAll(ui, "business-ui", ["THIS WEEK", "WORTH DOING NEXT", "Open weekly brief", "manualRefreshState", "TikTok · Link only"]);
  const forbiddenAnalytics = /(?:url|caption|observation|brief_text|competitor_name|city|provider_id|token)\s*:/;
  const analyticsStart = analytics.indexOf("type CompetitorAnalyticsProperties");
  const analyticsEnd = analytics.indexOf("captureCompetitorIntelligenceEvent");
  if (analyticsStart < 0 || analyticsEnd <= analyticsStart) failures.push("analytics: privacy allowlist anchors missing or reordered");
  else if (forbiddenAnalytics.test(analytics.slice(analyticsStart, analyticsEnd))) failures.push("analytics: forbidden raw/content property");
  return failures;
}
function fixtures() { return { migration: "tool_competitor_sources tool_competitor_refresh_jobs tool_competitor_observations tool_competitor_briefs tool_competitor_provider_capabilities tool_competitor_budget_ledger tool_competitor_admin_actions job_id uuid UNIQUE revision integer NOT NULL DEFAULT 1 uq_tool_competitor_one_active_job uq_tool_competitor_job_idempotency member_retry_count BETWEEN 0 AND 1 funding_lane IN ('scheduled','manual') FOR UPDATE OF j SKIP LOCKED RETURNING id INTO retry_job redacted_at watch_receipt_hash interval '90 days' interval '400 days' is_admin_user() issue_2725_watch_remove issue_2725_member_refresh issue_2725_finish_job tool_competitor_venue_week_budget_boundaries PRIMARY KEY(venue_listing_id,iso_week) used+p_amount>1000000 p_amount<>50000 measurement_failed tool_competitor_model_usage_receipts tool_competitor_synthesis_results ON CONFLICT DO NOTHING weekly_remaining_cents", worker: "ANALYZED_PROVIDER_ALLOWLIST=newSet([\"website\",\"instagram\"] before provider I/O immediately before publish META_COMPETITOR_IG_USER_ID META_COMPETITOR_ACCESS_TOKEN issue_2725_reserve_budget p_owner:job.lease_owner current_brief_id issue_2725_finish_job purgeOldLiveContent tool_competitor_observations\").delete raw provider responseJsonSchema temperature:0 thinkingBudget:0 candidateCount:1 MAX_SYNTHESIS_OUTPUT_TOKENS usage_metadata_missing issue_2725_record_model_usage issue_2725_accept_synthesis", run: "watch_update watch_refresh expected_updated_at issue_2725_watch_upsert issue_2725_member_refresh", report: "competitor_brief schema_version: 2 watch.brand_id !== auth.brandId", analytics: "type CompetitorAnalyticsProperties = { watch_id: string } captureCompetitorIntelligenceEvent", ui: "THIS WEEK WORTH DOING NEXT Open weekly brief manualRefreshState TikTok · Link only" }; }
function selfTest() { const good = fixtures(); if (evaluate(good).length) throw new Error(`GOOD failed: ${evaluate(good).join("; ")}`); for (const [file, needle] of [["migration", "uq_tool_competitor_job_idempotency"], ["migration", "used+p_amount>1000000"], ["migration", "measurement_failed"], ["worker", "immediately before publish"], ["worker", "ANALYZED_PROVIDER_ALLOWLIST"], ["worker", "thinkingBudget:0"], ["worker", "usage_metadata_missing"], ["run", "watch_refresh"], ["report", "competitor_brief"], ["ui", "TikTok · Link only"], ["analytics", "captureCompetitorIntelligenceEvent"]]) { const bad = { ...good, [file]: good[file].replace(needle, "") }; if (!evaluate(bad).length) throw new Error(`mutant survived: ${file}/${needle}`); } const forbidden = { ...good, analytics: good.analytics.replace("watch_id: string", "watch_id: string; url: string") }; if (!evaluate(forbidden).length) throw new Error("analytics privacy mutant survived"); const tiktok = { ...good, worker: `${good.worker}\nconst ANALYZED_PROVIDER_ALLOWLIST = new Set([\"website\",\"instagram\",\"tiktok\"]);` }; if (!evaluate(tiktok).length) throw new Error("TikTok mutant survived"); console.log("issue-2725 competitor intelligence contract self-test: PASS"); }
if (process.argv.includes("--self-test")) selfTest(); else { const failures = evaluate({ migration: read("supabase/migrations/20270606002725_issue_2725_competitor_intelligence.sql"), amendment: read("supabase/migrations/20270606002726_issue_2725_amendment_8_budget.sql"), worker: read("supabase/functions/competitor-intel-worker/index.ts"), run: read("supabase/functions/growth-tools-run/index.ts"), report: read("supabase/functions/growth-tools-report/index.ts"), analytics: read("mingla-business/src/analytics/competitorIntelligenceAnalytics.ts"), ui: read("mingla-business/src/components/venue/insights/CompetitorWatchSection.tsx") }); if (failures.length) { console.error(failures.join("\n")); process.exit(1); } console.log("issue-2725 competitor intelligence contract: PASS"); }
