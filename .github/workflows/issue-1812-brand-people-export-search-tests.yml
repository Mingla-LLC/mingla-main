name: Issue 1812 Brand People export search normalization

on:
  push:
    branches: [main]
    paths:
      - "supabase/migrations/20270313001812_issue_1812_brand_people_export_search_normalization.sql"
      - "supabase/migrations/__tests__/issue_1812_brand_people_export_search_normalization*.test.sql"
      - ".github/workflows/issue-1812-brand-people-export-search-tests.yml"
  pull_request:
    paths:
      - "supabase/migrations/20270313001812_issue_1812_brand_people_export_search_normalization.sql"
      - "supabase/migrations/__tests__/issue_1812_brand_people_export_search_normalization*.test.sql"
      - ".github/workflows/issue-1812-brand-people-export-search-tests.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  issue-1812-pg17:
    name: "Issue 1812 export search contract (fresh PG17)"
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: supabase/postgres:17.4.1.075
        env:
          POSTGRES_PASSWORD: ${{ format('issue-1812-ci-{0}', github.run_id) }}
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
          --health-start-period 30s
    steps:
      - uses: actions/checkout@v4
      - name: Wait for PostgreSQL 17
        env:
          PGPASSWORD: ${{ format('issue-1812-ci-{0}', github.run_id) }}
        run: |
          for attempt in {1..90}; do
            if psql -h localhost -U postgres -d postgres -Atc \
              "SELECT to_regnamespace('vault') IS NOT NULL AND to_regrole('supabase_admin') IS NOT NULL" \
              2>/dev/null | grep -qx t; then
              exit 0
            fi
            sleep 1
          done
          exit 1
      - name: Apply every migration in lexical order
        env:
          PGPASSWORD: ${{ format('issue-1812-ci-{0}', github.run_id) }}
        run: |
          set -euo pipefail
          find supabase/migrations -maxdepth 1 -type f -name '*.sql' -print0 \
            | sort -z \
            | while IFS= read -r -d '' migration; do
                psql -h localhost -U postgres -d postgres -v ON_ERROR_STOP=1 -f "$migration" >/dev/null
              done
      - name: Prove normalized export search and preserved boundaries
        env:
          PGPASSWORD: ${{ format('issue-1812-ci-{0}', github.run_id) }}
        run: |
          psql -h localhost -U postgres -d postgres -v ON_ERROR_STOP=1 \
            -f supabase/migrations/__tests__/issue_1812_brand_people_export_search_normalization.test.sql
          if test -f supabase/migrations/__tests__/issue_1812_brand_people_export_search_normalization.tester_adversarial.test.sql; then
            psql -h localhost -U postgres -d postgres -v ON_ERROR_STOP=1 \
              -f supabase/migrations/__tests__/issue_1812_brand_people_export_search_normalization.tester_adversarial.test.sql
          fi
