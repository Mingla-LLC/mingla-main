# Coverage Map

> **2026-05-05 — ✅ Cycle 17c (BIZ Refinement Pass mini-cycle 3 — WCAG AA kit-wide remediation) CLOSE Grade A on operator iOS live-fire "all passes lerts proceed".** **MOD primitive surface graded A:** `mingla-business/src/components/ui/IconChrome.tsx` (+20/-3 — `import type { PressableProps }` added; `accessibilityLabel: string` REQUIRED prop (no `?`) with verbatim JSDoc citing I-38/I-39; `hitSlop?: PressableProps["hitSlop"]` optional override prop with verbatim JSDoc; `DEFAULT_HIT_SLOP = { top: 4, bottom: 4, left: 4, right: 4 } as const` constant added; component destructure includes `hitSlop = DEFAULT_HIT_SLOP`; Pressable JSX adds `hitSlop={hitSlop}` and removes `?? icon` silent fallback at line 183 (now `accessibilityLabel={accessibilityLabel}`); visual `DEFAULT_SIZE = 36` UNCHANGED; non-interactive branch lines 166-172 UNCHANGED; **codifies NEW I-38 ICONCHROME-TOUCH-TARGET-AA-COMPLIANT + I-39 INTERACTIVE-PRESSABLE-ACCESSIBILITY-LABEL ACTIVE** post-CLOSE). **MOD scrim surfaces graded A:** `Modal.tsx:187` + `Sheet.tsx:260` + `TopSheet.tsx:275` (each +4 — `accessibilityLabel="Dismiss modal"` or `"Dismiss sheet"` + `accessibilityRole="button"` added; onPress handler unchanged). **MOD page-level surfaces graded A:** `payment.tsx:340/353` (+2 — explicit `accessibilityLabel` on dev test toggles); `PaymentElementStub.tsx:277/294` (+2 — caught by I-39 gate; explicit labels added); `CreatorStep1Basics.tsx:224` (+1 — caught by I-39; `accessibilityLabel={cat}`); `CreatorStep2When.tsx:1231` (+1 — `accessibilityLabel={opt.label}` per spec); `__styleguide.tsx:468-472` (+5/-5 — 5 dev fixtures gain explicit labels via tsc-driven enumeration). **MOD scanner motion surface graded A:** `app/event/[id]/scanner/index.tsx` (+28/-5 — `AccessibilityInfo` added to react-native imports; `reduceMotionRef = useRef<boolean>(false)` + useEffect subscribing to `AccessibilityInfo.isReduceMotionEnabled()` initial fetch + `addEventListener('reduceMotionChanged')` live subscription + cleanup with `sub.remove()` + `mounted` flag at lines 162-180; `dismissOverlay` branches on `reduceMotionRef.current` — true: `setValue(0)+setOverlay(null)` synchronous; false: existing `Animated.timing` path — at lines 225-245; `showResult` branches similarly at lines 247-270; reduce-motion-OFF path PRESERVED byte-identical to pre-17c; vestibular-sensitive users with iOS/Android Reduce Motion ON get instant overlay snap, no 250ms fade). **NEW CI surfaces graded A:** `.github/scripts/strict-grep/i38-icon-chrome-touch-target.mjs` (NEW ~225 LOC — Babel AST gate; walks `mingla-business/app/` + `mingla-business/src/`; for every `<IconChrome>` JSX element extracts `size=` numeric (default 36) + `hitSlop=` object/scalar (default `{top:4,bottom:4,left:4,right:4}` matching primitive); computes effective dimensions `width = size + slop.left + slop.right` and `height = size + slop.top + slop.bottom`; flags if either dim < 44 without `// orch-strict-grep-allow icon-chrome-touch-target — <reason>` allowlist; **SPEC §G.3 math correction encoded** per D-CYCLE17C-IMPL-1; rich error output with min dimension + per-axis math; exit codes 0/1/2; mirrors i37 walkTsx + skip dirs + Windows-path-safe pattern) · `.github/scripts/strict-grep/i39-pressable-label.mjs` (NEW ~205 LOC — Babel AST gate; walks `JSXElement` traversal (not OpeningElement) to inspect children for implicit-Text heuristic; for every `<Pressable>` or `<TouchableOpacity>`: skip if no `onPress=`; pass if `accessibilityLabel=` present; INFO log + pass if only direct child is `<Text>{StringLiteral|TemplateLiteral}</Text>`; else VIOLATION unless `// orch-strict-grep-allow pressable-no-label — <reason>` allowlist; rich error output; exit codes 0/1/2). **MOD workflow YAML graded A:** `.github/workflows/strict-grep-mingla-business.yml` (+26 — 2 new jobs `i38-icon-chrome-touch-target` + `i39-pressable-label` mirroring i37 job shape; `npm install --no-save @babel/parser @babel/traverse` reuse pattern; top-level registry comment lists I-38 + I-39 in "Currently registered gates"). **MOD README registry graded A:** `.github/scripts/strict-grep/README.md` (+6 — Active gates table includes I-38 + I-39 rows; "Other registered gate tags" lists I-38 + I-39 allowlist comment patterns). **MOD doc surfaces graded A:** `Mingla_Artifacts/INVARIANT_REGISTRY.md` (+44 — NEW I-38 entry at lines 1712-1729 + NEW I-39 entry at lines 1732-1751 immediately after I-37; both status flipped DRAFT→ACTIVE post-CLOSE 2026-05-05; codify kit-wide WCAG AA + cross-reference CI workflow + scripts + allowlist patterns). **NEW memory file:** `~/.claude/projects/c--Users-user-Desktop-mingla-main/memory/feedback_wcag_aa_kit_invariants.md` (NEW ~50 LOC — operator-readable doc; status flipped DRAFT→ACTIVE post-CLOSE; documents I-38 + I-39 + how-to-apply + CI enforcement + why-this-memory-exists + cross-references). **MEMORY.md index updated** with new section "WCAG AA Kit Invariants (post-Cycle-17c)". **Heatmap delta:** Mingla Business polish + structural-foundation surface coverage now extends to kit-wide WCAG AA touch-target + label coverage + reduce-motion respect on scanner page + I-38 + I-39 ACTIVE codification + 2nd registry-pattern CI gate re-use validating 17b D-17b-5 scaffold. Phase 5 polish closer THIRD mini-cycle complete. **Constitutional outcomes:** #1 No dead taps STRENGTHENED (every IconChrome interactive path now has explicit label; scrim Pressables labeled — removed dead-tap silent state) · #2 One owner per truth PRESERVED · #3 No silent failures STRENGTHENED (`?? icon` silent fallback REMOVED from IconChrome line 183; explicit-label contract is now load-bearing) · #4 PRESERVED · #5 Server state server-side PRESERVED · #6 Logout clears PRESERVED · #7 TRANSITIONAL labels HONORED+ (no new TRANSITIONAL markers introduced; existing untouched; I-38 + I-39 + memory file pre-written DRAFT then flipped ACTIVE at CLOSE per protocol) · #8 Subtract before adding APPLIED (`?? icon` removed BEFORE explicit-label additions; reduce-motion branch added BEFORE removing `Animated.timing` — preserved for false branch) · #9 No fabricated data PRESERVED · #10 Currency-aware UI PRESERVED · #11 One auth instance PRESERVED · #12 Validate at right time PRESERVED · #13 Exclusion consistency PRESERVED · #14 Persisted-state startup PRESERVED. **NEW invariants I-38 + I-39 ACTIVE** — kit-wide WCAG AA codified; CI grep gates enforce structurally. **6 forensics + 5 implementor + 3 QA discoveries surfaced; all dispositioned.** Effort ~3h vs 7.5h estimate (third pattern repeat after 17a 2.5h vs 6h, 17b 1.5h vs 4-5h); zero reworks; verbatim contracts paid off third time.

> **2026-05-05 — ✅ Cycle 17b (BIZ Refinement Pass mini-cycle 2 — TopBar IA reset + hardening-registry CI scaffold) CLOSE Grade A on operator device smoke "smoke pass".** **MOD surfaces graded A:** `mingla-business/src/components/ui/TopBar.tsx` (+47/-6 — NEW `extraRightSlot?: React.ReactNode` prop with verbatim JSDoc per SPEC §A.1 explaining I-37 enforcement; `rightSlot` JSDoc updated per SPEC §A.4 documenting 3 patterns (null suppress / View placeholder / page-specific replace) + I-37 cross-reference; `DefaultRightSlot` renamed → `DefaultRightSlotInner` returning Fragment without outer View wrap; render branch logic updated to `rightSlot !== undefined ? rightSlot : <View><DefaultRightSlotInner.../>{extraRightSlot}</View>` — **distinguishes `null` from `undefined`** so back-route consumers passing `rightSlot={null}` still suppress the default cluster; **codifies NEW I-37 TOPBAR-DEFAULT-CLUSTER-ON-PRIMARY-TABS ACTIVE** post-CLOSE) · `mingla-business/app/(tabs)/events.tsx` (+8/-31 net -23 — 17a tactical inline cluster block (lines 393-417 + TRANSITIONAL marker comment 394-399) DELETED + custom `topBarRightCluster` style entry (lines 632-638) DELETED + NEW `extraRightSlot={canCreateEvent ? <IconChrome icon="plus".../> : null}` block inserted at same prop position; visual outcome PRESERVED (`[search, bell, +]` for canCreateEvent / `[search, bell]` otherwise — same gating logic, same handler, byte-identical IconChrome shapes; only mechanism changed from inline-replace to compose-via-extraRightSlot); closes 17a tactical TRANSITIONAL marker EXIT condition). **NEW surfaces graded A:** `.github/workflows/strict-grep-mingla-business.yml` (NEW ~41 LOC — first strict-grep CI gate ever in repo; PR-triggered on `main` + `Seth` branches with path filter for `mingla-business/**` + `.github/scripts/strict-grep/**` + workflow itself; one job `i37-topbar-default-cluster` installs `@babel/parser` + `@babel/traverse` on-demand via `npm install --no-save` + runs `i37-topbar-cluster.mjs`; pinned versions `actions/checkout@v4` + `actions/setup-node@v4` matching existing repo workflows; top-level registry-pattern documentation comment listing currently-registered gates + future-gate placeholders for I-32/I-34/I-36 per operator D-17b-5 framing) · `.github/scripts/strict-grep/i37-topbar-cluster.mjs` (NEW ~190 LOC — AST-based gate; walks every .tsx file under `mingla-business/app/` + `mingla-business/src/`; parses via `@babel/parser` with `["typescript", "jsx"]` plugins; traverses via `@babel/traverse`; for every `<TopBar>` JSX element extracts `leftKind` value (string-literal only — dynamic values warn but don't fail) + checks `rightSlot=` attribute presence + checks line-immediately-above for `// orch-strict-grep-allow leftKind-brand-rightSlot — <reason>` allowlist; VIOLATION when `leftKind="brand"` AND `rightSlot=` present AND no allowlist; outputs rich error with file path + line number + suggested fix + INVARIANT_REGISTRY cross-reference + allowlist instructions; exit codes 0/1/2; walks defensively past `node_modules`, `.git`, `.expo`; uses Windows-path-safe `relative()` + `split(sep).join("/")`) · `.github/scripts/strict-grep/README.md` (NEW ~75 LOC — registry pattern documentation per operator D-17b-5 framing; "Active gates registered" table (currently I-37) + "Future gates" table (I-32/I-34/I-36 proposed) + "How to add a new gate (4 steps)" with verbatim workflow YAML template + script structure mirror; allowlist comment pattern documented with examples; conventions section (exit codes, error format, warning format, parse failures, no-new-deps rule)). **MOD doc surface graded A:** `Mingla_Artifacts/INVARIANT_REGISTRY.md` (+20 lines — NEW I-37 entry at lines 1692-1708 immediately after I-36 chronologically; status flipped DRAFT→ACTIVE post-CLOSE 2026-05-05; codifies primary-tab consumers MUST render default `[search, bell]` cluster + cross-references CI workflow + script + allowlist pattern). **NEW memory file:** `~/.claude/projects/c--Users-user-Desktop-mingla-main/memory/feedback_strict_grep_registry_pattern.md` (NEW ~50 LOC — operator-readable pattern doc; status flipped DRAFT→ACTIVE post-CLOSE; documents 4-step "How to add a new gate" rule + why-this-memory-exists section + active gates list + future gates queued + cross-references). **MEMORY.md index updated** with new section "Strict-Grep CI Gate Registry Pattern (post-Cycle-17b)". **15 back-route consumers UNTOUCHED** — verified via `git diff --stat` showing only TopBar.tsx + events.tsx + INVARIANT_REGISTRY.md + new `.github/` files + memory file in 17b's modification set; BrandProfileView.tsx 6-line change is pre-existing 17a §A.4.3 comment edit (J-A12 reference cleanup at lines 282-288) not 17b regression. **Heatmap delta:** Mingla Business polish + structural-foundation surface coverage now extends to `extraRightSlot` compose API + first CI invariant gate + registry pattern documentation + I-37 ACTIVE codification. Phase 5 polish closer SECOND mini-cycle complete. **Constitutional outcomes:** #1 No dead taps PRESERVED (search/bell inert per existing TopBar TRANSITIONAL contract; not introduced by 17b; `+` retains existing handler) · #2 One owner per truth STRENGTHENED (TopBar primitive owns cluster rendering; `extraRightSlot` ADDS via single ownership boundary; `rightSlot` REPLACES via documented escape-hatch) · #3 No silent failures STRENGTHENED (gate script outputs rich errors on violation; parseFailures tracked separately; exit code 2 reserved for inconclusive) · #5 Server state server-side PRESERVED · #6 Logout clears PRESERVED · #7 TRANSITIONAL labels HONORED+ (TopBar default IconChromes preserve original Cycle 0a marker; I-37 + memory file pre-written DRAFT then flipped ACTIVE at CLOSE per protocol) · #8 Subtract before adding APPLIED (events.tsx tactical block + style entry DELETED before extraRightSlot block inserted; DefaultRightSlot REMOVED before DefaultRightSlotInner added) · #9 No fabricated data PRESERVED (no new copy; no fake counts) · #10 Currency-aware UI PRESERVED · #11 One auth instance PRESERVED · #12 Validate at right time PRESERVED · #13 Exclusion consistency PRESERVED · #14 Persisted-state startup PRESERVED. **NEW invariant I-37 ACTIVE** — `<TopBar leftKind="brand">` consumers MUST render default `[search, bell]` cluster; CI grep gate enforces structurally. **5 forensics + 6 implementor + 5 QA discoveries surfaced; all dispositioned.** Effort 1.5h vs 4-5h estimate (pattern repeat from 17a 2.5h vs 6h); zero reworks; verbatim contracts paid off again.

> **2026-05-05 — ✅ Cycle 17a (BIZ Refinement Pass mini-cycle 1 — Quick Wins) CLOSE Grade A on operator device smoke (initial "+ not showing" diagnosed correctly: gate hides + when no brand selected per SC-A.1.2; with brand selected smoke PASS).** **NEW utility surface graded A:** `mingla-business/src/utils/paymentMethodLabels.ts` (~20 LOC — single-source-of-truth `Record<DoorPaymentMethod, string>` with 4 keys; replaces 3 inline copies across `door/[saleId].tsx` + `door/index.tsx` + `guests/[guestId].tsx`; closes D-CYCLE12-IMPL-4 and Const #2 one-owner-per-truth). **NEW canonical doc surface graded A:** `Mingla_Artifacts/FOUNDER_FEEDBACK.md` (append-only operator-feedback log; first canonical entry is the 2026-05-04 top-bar IA feedback with sub-items 1+2 routed to 17a/17b pipeline + sub-item 3 declined; closes D-17-2 and J-Z1 baseline gap). **MOD surfaces graded A:** `mingla-business/app/(tabs)/events.tsx` (TopBar `rightSlot` now composes inline `<View style={styles.topBarRightCluster}>` containing search + bell + conditional `+` IconChromes — preserves existing canCreateEvent gate + handleBuildEvent handler unchanged; TRANSITIONAL marker at 393-399 + new style entry at 632-638 with code comment "17b TopBar refactor will delete this when extraRightSlot prop ships"; closes operator's missing-`+` founder feedback sub-item 1 tactically; **NOT** a regression — initial "+ not showing" was correct gate behavior with no brand selected, smoke PASS with brand) · `mingla-business/app/(tabs)/events.tsx` styles (single `toastWrap` block with `zIndex: 100` + `elevation: 12` intact — duplicate clobbering block deleted; closes D-CYCLE12-IMPL-1 + restores Toast above-sheets z-order) · `mingla-business/src/services/brandMapping.ts` (mapBrandRowToUi return now includes `kind: "popup"`, `address: null`, `coverHue: 25` defaults — closes D-CYCLE12-IMPL-2 + dodges latent crash when B-cycle wires real-DB brand creation; TRANSITIONAL marker citing B-cycle EXIT) · `mingla-business/src/utils/recurrenceRule.ts` file header + `recurrenceRuleToRfc5545` docstring (both updated — `[TRANSITIONAL]` tag and "unused in Cycle 4" claim removed; cites Cycle 4 wizard validators + Cycle 9 publish flow as real consumers) · `mingla-business/src/components/brand/BrandProfileView.tsx:282-288` (J-A12 reference removed — Tax & VAT marker preserved; J-A12 onReports row already wired since Cycle 2) · `mingla-business/src/store/scannerInvitationsStore.ts` (4 `canManualCheckIn` allowlist comments inserted; preventive for future strict-grep gate per SPEC-DISCOVERY-1 — gate doesn't exist yet) · `mingla-business/src/components/scanners/InviteScannerSheet.tsx` (1 `canManualCheckIn` allowlist comment) · `mingla-business/app/event/[id]/door/[saleId].tsx` + `mingla-business/app/event/[id]/door/index.tsx` + `mingla-business/app/event/[id]/guests/[guestId].tsx` (each MOD: inline `const PAYMENT_METHOD_LABELS = ...` removed + import from new util added; 3 consumers + 1 new util = single owner per Const #2) · `Mingla_Artifacts/DECISION_LOG.md` (NEW DEC-100 row prepended at chronological top — `mingla-marketing/` canonical realisation of DEC-081; closes D-CYCLE15-FOR-2 doc drift). **§B.6 currency formatter discovered already-done** — `mingla-business/src/utils/currency.ts` pre-existed with 3 exports; both consumers (home.tsx:48, __styleguide.tsx:65) already imported; D-IMPL-A12-2 master-inventory stale entry closed-elsewhere. **Operator-side surfaces (NOT code-touch — flagged for deploy-window action):** §A.5 Supabase Magic Link Email Template body verification (5-step Dashboard check confirming `{{ .Token }}` body + no `{{ .ConfirmationURL }}`) + §B.1 Sentry env wiring (`mingla-business/.env` + EAS Secrets sync — DSN provided in IMPL report §C). **Heatmap delta:** Mingla Business polish + paper-trail surface coverage now extends to single-source-of-truth payment labels + canonical operator-feedback log + DEC-100 marketing-realisation lock + brandMapping latent-crash dodge + events.tsx tactical top-bar cluster bridge to 17b. Phase 5 polish closer SECOND mini-cycle complete. **Constitutional outcomes:** #1 No dead taps PRESERVED (search/bell inert until 17b matches existing TopBar TRANSITIONAL contract; `+` retains existing handler) · #2 One owner per truth STRENGTHENED (PAYMENT_METHOD_LABELS now single owner; closes 3-copy duplication) · #3 No silent failures PRESERVED (DEC-082 closure note SKIPPED per SPEC fallback — no fabrication; DEC-099 collision honestly documented inline) · #5 Server state server-side PRESERVED · #6 Logout clears PRESERVED · #7 TRANSITIONAL labels HONORED+ (events.tsx tactical cluster + brandMapping defaults both with EXIT conditions) · #8 Subtract before adding APPLIED (duplicate `toastWrap` deleted + 3 inline `PAYMENT_METHOD_LABELS` copies removed before new util added) · #9 No fabricated data PRESERVED (DEC-082 not fabricated; DEC-099 collision documented honestly) · #10 Currency-aware UI PRESERVED (currency.ts pre-existing util preserved; no inline formatters reintroduced) · #11 One auth instance PRESERVED · #12 Validate at right time PRESERVED · #13 Exclusion consistency PRESERVED · #14 Persisted-state startup PRESERVED. **NO new invariants Cycle 17a** (proposed I-37 TopBar default cluster DEFERRED to 17b CLOSE per SPEC §5 + tester recommendation). **D-IMPL-46 Apple JWT closed as already-mitigated** (autorotate spec + scheduled remote agent + GitHub Actions workflow already provide belt + suspenders + auto). **6 forensics + 7 implementor + 6 QA discoveries surfaced; 4 master-inventory items closed** (D-CYCLE12-IMPL-1/2 + D-CYCLE15-FOR-2 + D-IMPL-46) + 1 closed-elsewhere (D-IMPL-A12-2). **0 reworks** — verbatim contracts + accurate forensics paid off.

> **2026-05-04 (close++++++) — ✅ Cycle 16a (BIZ Cross-Cutting Polish quick wins) CLOSE Grade A on operator device smoke "all passes" — Phase 5 polish closer SHIPPED.** **NEW surfaces graded A:** `mingla-business/app/+not-found.tsx` (J-X4 ~115 LOC — Mingla-branded 404 with `LinearGradient` warm-orange background + Mingla logo + playful copy "Hmm, that's not a real page." + "Go home" Button → `router.replace("/")`; cross-platform parity native deep-link + web direct URL; reuses kit Button + designSystem tokens) · `mingla-business/src/hooks/usePermissionWithFallback.ts` (J-X6 ~95 LOC — consolidated permission request + denial-fallback hook; returns `requestWithFallback` + `settingsDialogVisible` + `openSettings` + `dismissSettingsDialog` + `dialogTitle` + `dialogDescription`; consumers render ConfirmDialog inside their own component tree per `feedback_rn_sub_sheet_must_render_inside_parent`; reusable for camera + image-picker + future surfaces). **MOD surfaces graded A:** `mingla-business/app/_layout.tsx` (+94 LOC — env-absent-guarded `Sentry.init({ dsn, enableAutoSessionTracking: true, debug: __DEV__, tracesSampleRate: __DEV__ ? 1.0 : 0.2 })` at module top + `SplashScreen.preventAutoHideAsync()` with web-no-op catch + NEW `RootLayoutInner` component reading `useAuth().loading` with `SPLASH_MIN_VISIBLE_MS = 500` gate + `<ErrorBoundary>` wraps `<Stack>` at root passing `Sentry.captureException` with React component-stack hint as onError; **codifies NEW I-36 ROOT-ERROR-BOUNDARY ACTIVE**) · `mingla-business/app/account/edit-profile.tsx` (+44/-1 LOC — wraps image-picker permission via `usePermissionWithFallback`; on denied with `canAskAgain=false`, ConfirmDialog "Photo library access needed" → `Linking.openSettings()`; closes Cycle 14 J-A1 silent-recovery gap that previously toast-only-failed) · `mingla-business/eas.json` (+1/-1 — `SENTRY_DISABLE_AUTO_UPLOAD: true → false` in production env ONLY; development + preview unchanged; enables source map upload on production EAS builds) · `mingla-business/src/components/ui/ErrorBoundary.tsx` (+6/-3 — `handleGetHelp` upgraded from dev-only `console.log` no-op to `Linking.openURL("mailto:support@mingla.app")` with catch-swallow; `[TRANSITIONAL]` marker preserved with EXIT condition "Sentry feedback widget integration"). **SKIPPED surface (per D-CYCLE16A-IMPL-1):** `mingla-business/app/event/[id]/scanner/index.tsx` — already has graceful full-screen permission UI (`permWrap` + `permIconBadge` + askable-vs-not branching button); ConfirmDialog overlay would have regressed UX; preserved verbatim. **Heatmap delta:** Mingla Business cross-cutting polish surface coverage now extends to monitoring + 404 + splash + permission consolidation. Phase 5 (Account + Polish) feature-complete on UI side: Cycles 14 + 15 + 16a compose the operator-facing baseline. **Constitutional outcomes:** #1 No dead taps PRESERVED · #2 One owner per truth STRENGTHENED (single ErrorBoundary at root) · #3 No silent failures STRENGTHENED (Sentry now captures component throws; permission deny path surfaces ConfirmDialog instead of toast-only; **3 documented platform/fallback exemptions are explicit not silent** — web SplashScreen no-op + Linking.openURL/openSettings rare-unavailability) · #5 Server state server-side PRESERVED · #6 Logout clears PRESERVED · #7 TRANSITIONAL labels HONORED (Sentry env-absent guard + ErrorBoundary mailto Get help; both with EXIT conditions) · #8 Subtract before adding APPLIED (ErrorBoundary `console.log` REMOVED before adding `Linking.openURL`) · #11 One auth instance PRESERVED · #12 Validate at right time PRESERVED · #14 Persisted-state startup STRENGTHENED (splash sync ensures bootstrap completes before render). **NEW invariant I-36 ROOT-ERROR-BOUNDARY ACTIVE** — `_layout.tsx` MUST wrap Stack with ErrorBoundary; CI grep gate enforces (T-G1). **Closes D-CYCLE14-IMPL-3 false alarm** (D-CYCLE16-FOR-3 confirmed `photosPermission` already configured in app.config.ts plugins block since Cycle 14). **7 forensics + 5 implementor discoveries registered** (6 closed at CLOSE; rest deferred to Cycle 16b / Refinement Pass / informational).

> **2026-05-04 (close++) — ✅ Cycle 15 (BIZ Organiser Login + Email-OTP) CLOSE Grade A on operator device smoke "all pass" — universal email-OTP sign-in SHIPPED.** **MOD surfaces graded A:** `mingla-business/src/context/AuthContext.tsx` (+99 LOC — NEW `signInWithEmail(email)` callback wrapping `supabase.auth.signInWithOtp({ email, options: { shouldCreateUser: true } })` with email regex guard + rate-limit error mapping; NEW `verifyEmailOtp(email, code)` wrapping `supabase.auth.verifyOtp({ email, token, type: "email" })` with 6-digit numeric guard + invalid/expired error mapping; AuthContextValue type extended; useMemo value + deps array updated; existing onAuthStateChange listener at lines 120-138 handles email-OTP SIGNED_IN identically to Google/Apple — I-35 recovery contract preserved without modification) · `mingla-business/src/components/auth/BusinessWelcomeScreen.tsx` (~+609 net LOC — NEW `WelcomeMode` type `idle | email-input | otp-input | otp-verifying`; 5 NEW state vars + 1 NEW resend-seconds-left state + 1 NEW keyboard-pad state; 2 NEW animation refs `emailOpacity` + `emailTranslateY` parallel to Apple/Google entrance pattern; 2 NEW useEffect — Keyboard listener gated to non-web + Resend cooldown countdown; 7 NEW handlers — `handleStartEmailFlow` + `handleSendCode` + `handleVerifyCode` + `handleOtpChange` auto-submit on 6th digit + `handleResendCode` + `handleEditEmail` + `handleBackToIdle`; centerZone now mode-aware with mode-specific heading + email-confirmation subtext; actionZone wraps existing 3 buttons + NEW Email button + terms inside `mode === "idle" && (<>...</>)` + 3 mode-specific render blocks; 14 NEW StyleSheet entries; ALL hooks BEFORE first conditional return per ORCH-0710) · `mingla-business/app/index.tsx` (+11/-3 LOC — destructure 2 new callbacks + 2 new prop wires) · `mingla-business/app/auth/index.tsx` (+9/-1 LOC v2 rework wire — second BusinessWelcomeScreen consumer Cycle 15 IMPL initially missed) · `mingla-business/src/utils/guestCsvExport.ts` (-44 LOC v2 net — bundled hotfix removed 19-line line-238 duplicate + 25-line lines-442-466 duplicate; canonical Cycle 13 v2 `Promise<ExportResult>` version preserved at lines 344-440 unchanged). **Heatmap delta:** Mingla Business email sign-in surface coverage now extends to anyone-with-email organisers; web bundle parses cleanly post-bundled-hotfix (D-CYCLE15-IMPL-1 dev-server crash RESOLVED at the source); Cycle 13 v2 paste-debris fully cleaned up by orchestrator-recommended bundling. **Constitutional outcomes:** #1 No dead taps PRESERVED · #2 One owner per truth PRESERVED (`creator_accounts` canonical; ensureCreatorAccount idempotent for OAuth + email-OTP) · #3 No silent failures PRESERVED + STRENGTHENED (rate-limit copy explicit; wrong-code copy explicit; D-CYCLE15-IMPL-1 silent-parse-error RESOLVED) · #5 Server state server-side PRESERVED (no new Zustand) · #6 Logout clears PRESERVED (existing signOut cascade unchanged) · #7 TRANSITIONAL labels — N/A (Cycle 15 ships production-grade) · #8 Subtract before adding APPLIED (44-line subtract from guestCsvExport.ts; pure additive otherwise) · #11 One auth instance PRESERVED · #12 Validate at right time STRENGTHENED (email regex on submit; 6-digit code auto-submits at exactly 6 chars) · #14 Persisted-state startup PRESERVED. **NO new invariants Cycle 15** (I-35 inherited unchanged). **7 forensics + 7 implementor discoveries registered** (6 closed at CLOSE, rest deferred to Refinement Pass / B-cycle / cosmetic).

> **2026-05-04 (final) — ✅ Cycle 14 (BIZ Account hub) CLOSE Grade A on operator device smoke + v2 rework retest — Phase 5 Account surface SHIPPED.** **NEW surfaces graded A:** `mingla-business/app/account/edit-profile.tsx` (J-A1 ~535 LOC — avatar picker via expo-image-picker + Storage upload to `creator_avatars/{userId}.{ext}` + name TextInput 1..80 + email read-only with provider badge + 8 states handled + keyboard discipline applied per memory rule) · `mingla-business/app/account/notifications.tsx` (J-A2 ~325 LOC — 4 ToggleRow + TRANSITIONAL banner + marketing DOUBLE-WIRE to `creator_accounts.marketing_opt_in`) · `mingla-business/app/account/delete.tsx` (J-A4 ~795 LOC — 4-step internal state machine Warn → Cascade preview → Type-to-confirm email → Soft-delete + sign-out; ALL hooks BEFORE early-return per ORCH-0710; Apple privacy-relay note conditional; active-events warn block per D-14-14; **v2 fix Bug A: `setTimeout(async () => { await signOut(); router.replace("/") })` blocks until SIGNED_OUT clears user before navigation evaluates index gate**) · `mingla-business/src/hooks/useCreatorAccount.ts` (~95 LOC — `creatorAccountKeys` factory + query + mutation with cache invalidation) · `mingla-business/src/hooks/useAccountDeletion.ts` (~85 LOC — `useRequestAccountDeletion` mutation + `tryRecoverAccountIfDeleted` non-hook helper) · `mingla-business/src/store/notificationPrefsStore.ts` (~95 LOC — Zustand persist v1; defaults GDPR-favored: transactional ON / marketing OFF) · `mingla-business/src/utils/accountDeletionPreview.ts` (~165 LOC — pure aggregator over 6 stores; `EMPTY_PREVIEW` constant; 11 fields including `hasActiveOrUpcomingEvents`) · `supabase/migrations/20260504_b1_phase5_creator_avatars.sql` (~80 LOC after hotfix — bucket + 4 RLS policies via `split_part(name, '.', 1) = auth.uid()::text` path-scoping). **MOD surfaces graded A:** `mingla-business/src/utils/clearAllStores.ts` (10 → 11 stores; notification reset added per Const #6) · `mingla-business/src/services/creatorAccount.ts` (NEW `updateCreatorAccount(userId, patch)` helper) · `mingla-business/src/context/AuthContext.tsx` (D-CYCLE14-FOR-6 recover-on-sign-in: `lastRecoveryEvent` flag + `tryRecoverAccountIfDeleted` invocation in bootstrap; **v2 fix Bug B: onAuthStateChange recovery call gated to `_event === "SIGNED_IN"` only — TOKEN_REFRESHED + USER_UPDATED + INITIAL_SESSION can no longer silently un-delete an in-flight delete-flow**) · `mingla-business/app/(tabs)/account.tsx` (additive +90 / -2 — preserved baseline; ADD Settings GlassCard with 3 NavRows + recovery toast useEffect + RENAME "Sign out" → "Sign out everywhere" + caption "Signs you out on every device.") · `Mingla_Artifacts/INVARIANT_REGISTRY.md` (NEW I-35 ~28 LOC). **Heatmap delta:** Mingla Business Phase 5 (Account) opens with full hub-and-spoke pattern matching Strategic Plan R4 (GDPR-compliant account deletion); UI side complete; B-cycle backend wires close 6+ TRANSITIONAL banners simultaneously. **Constitutional outcomes:** #1 No dead taps PRESERVED · #2 One owner per truth PRESERVED · #3 No silent failures STRENGTHENED post-v2 (Bug A was UX silent-failure: user thought signed out but wasn't — RESOLVED via `await signOut()`) · #5 Server state server-side PRESERVED (useCreatorAccount React Query / notificationPrefsStore Zustand TRANSITIONAL) · #6 Logout clears EXTENDED (cascade now 11 stores) · #7 TRANSITIONAL labels HONORED · #9 No fabricated data PRESERVED (cascade preview only non-zero rows) · #12 Validate at right time STRENGTHENED post-v2 (Bug B was data-integrity silent-failure: deleted_at silently cleared on token refresh — RESOLVED via SIGNED_IN gate) · #14 Persisted-state startup PRESERVED. **NEW invariant I-35:** `creator_accounts.deleted_at` soft-delete marker contract — mobile UPDATE self-only via existing RLS; recover-on-sign-in auto-clears (SIGNED_IN events only post-v2); 30-day window; permanent (no EXIT). **6 forensics + 6 implementor + 0 v2 discoveries registered.** Bug A + Bug B both fixed at the source — no scope creep beyond rework dispatch.

> **2026-05-04 (late) — ✅ Cycle 13 (BIZ End-of-Night Reconciliation Report) CLOSE Grade A on operator device smoke + v2 rework retest — cross-source reconciliation surface SHIPPED.** **NEW surfaces graded A:** `mingla-business/app/event/[id]/reconciliation.tsx` (J-R1 + J-R2 + J-R3 ~1030 LOC route — adaptive headline per event lifecycle status + 4 sections Tickets/Revenue/Scans/Discrepancies + advisory discrepancy detector + corrected payout split + CSV export with Gross/Refunded/Net columns + 5-line summary stanza prefix + permission-gated finance_manager+ rank 30 + defensive Set-based scan dedupe per I-27) · `mingla-business/src/utils/reconciliation.ts` (~340 LOC pure aggregator joining 4 client ledgers — `computeReconciliation` + `EMPTY_SUMMARY` + `headlineCopyFor` + 3 discrepancy detectors D1+D2+D3 ADVISORY-only) · `mingla-business/src/utils/eventLifecycle.ts` (extracted `deriveLiveStatus` predicate — Const #2 single source of truth shared by Event Detail + reconciliation route) · `mingla-business/src/components/event/ActionTile.tsx` (extracted shared component — Cycle 13 extraction unblocking ReconciliationCtaTile composition) · `mingla-business/src/components/event/ReconciliationCtaTile.tsx` (~33 LOC permission-gated null-return wrapper around ActionTile). **MOD surfaces graded A:** `mingla-business/src/utils/permissionGates.ts` (NEW `MIN_RANK.VIEW_RECONCILIATION = finance_manager (30)` constant) · `mingla-business/src/utils/guestCsvExport.ts` (3 NEW columns Gross/Refunded/Net at positions 12-14 + optional `summaryStanza` arg + new `exportReconciliationCsv` wrapper + new `ReconciliationCsvSummary` type + **v2 rework: NEW `ExportResult` discriminated union + `downloadCsvNative` returns sharedAction/dismissedAction discriminator — closes Const #3 silent-failure D-CYCLE13-IMPL-6**) · `mingla-business/app/event/[id]/index.tsx` (Reconciliation tile wired into action grid + extracted ActionTile + deriveLiveStatus imports + cancelled→past collapse for HeroStatusPill preserves existing 3-state UX) · `mingla-business/app/event/[id]/door/index.tsx` (D-CYCLE13-RECON-FOR-4 polish "View full reconciliation" CTA above TESTING MODE banner — ADDITIVE-only 60 insertions/0 deletions) · `mingla-business/app/event/[id]/guests/index.tsx` + `mingla-business/app/event/[id]/door/index.tsx` (v2 rework caller adaptations to `result.method` discriminator — silent dismiss path) · `Mingla_Artifacts/INVARIANT_REGISTRY.md` (I-32 amendment with VIEW_RECONCILIATION forward-compat note). **Heatmap delta:** Mingla Business Phase 4 (Event Management) reconciliation surface coverage now extends to PRD §6.2 + §7 expectations — cross-source aggregation across 4 ledgers (orderStore + doorSalesStore + guestStore + scanStore) with payment-method-attribution finance KPI + advisory discrepancy detection + accountant-friendly CSV export. **Constitutional outcomes:** #1 No dead taps PRESERVED (tile null-return on permission-miss + route NotAuthorizedShell on deep-link + Email PDF visibly disabled with B-cycle hint) · #2 One owner per truth IMPROVED (deriveLiveStatus + ActionTile extracted to single sources) · #3 No silent failures RESOLVED post-v2 (export toast no longer lies on share-sheet dismiss) · #7 TRANSITIONAL labels HONORED (payoutEstimate stub + Email PDF disabled + Stripe fee comment + B-cycle hints in revenue rows + native CSV degradation D-CYCLE10-IMPL-1 marker preserved) · #9 No fabricated data PRESERVED (empty state shows "—" em-dash; discrepancy section silent when clean — no false-positive warm-orange noise) · #14 Persisted-state startup PRESERVED (no schema bumps). **I-32 AMENDED:** VIEW_RECONCILIATION forward-compat note — server RLS counterpart MUST mirror finance_manager+ when B-cycle ships reconciliation RPC. **No new invariants Cycle 13** (read-only surface). **Const #3 silent-failure D-CYCLE13-IMPL-6 RESOLVED at the source** — Cycle 10 J-G6 + Cycle 12 J-D5 exports inherit honest dismiss-silent toasts as positive side effect. **5 forensics discoveries + 6 implementor discoveries registered.**

> **2026-05-04 — ✅ Cycle 13b (BIZ Permissions UI Depth) CLOSE Grade A on operator device smoke — permissions surface cleanup SHIPPED.** **DROPPED surfaces:** `permissions_matrix` table (PR #59 scaffolding — never consumed by mobile or backend RLS; replaced canonically by MIN_RANK + biz_role_rank); `canManualCheckIn` field from ScannerPermissions interface (decorative-only Cycle 11 design wart). **NEW surfaces graded A:** `audit_log` brand-admin SELECT policy (Brand admins now see all team actions on brands they belong to via `biz_is_brand_admin_plus_for_caller` helper; sub-rank users still see own only via existing self-only policy — multi-policy OR-merge). **MOD surfaces graded A:** `mingla-business/src/store/scannerInvitationsStore.ts` (persist v1→v2 + type-safe field strip migrate function); `mingla-business/src/components/scanners/InviteScannerSheet.tsx` (25-LOC toggle row block + state hook + payload field + useCallback dep deleted; canAcceptPayments toggle preserved); `mingla-business/app/event/[id]/scanners/index.tsx` (2 pill-render sites deleted); `mingla-business/app/brand/[id]/audit-log.tsx` (TRANSITIONAL banner copy updated honestly); `Mingla_Artifacts/INVARIANT_REGISTRY.md` (I-33 ACTIVE + I-34 NEW + I-24 amendment). **Heatmap delta:** Mingla Business permissions surface coverage now extends to PRD §11 with the role-rank approach as canonical authority both server-side (biz_role_rank + SECURITY DEFINER helpers) and client-side (MIN_RANK constants in permissionGates.ts) — single source of truth restored per Const #2. **Constitutional outcomes:** #1 No dead taps PRESERVED + IMPROVED (canManualCheckIn was a dead-state-no-consumer; now removed) · #2 One owner per truth RESTORED (permissions_matrix was a duplicate stale authority; now decommissioned) · #6 Logout clears PRESERVED (no new persisted store) · #7 TRANSITIONAL labels HONORED (audit-log route banner copy updated honestly per new RLS policy) · #8 Subtract before adding HONORED+ (Q1 + Q4 are pure subtractions; Q5 is a 4-line ADD on stable base) · #14 Persisted-state startup PRESERVED (v1→v2 migrate function explicitly destructures + rebuilds permissions object on cold-start). **NEW invariants ratified:** I-33 (permissions_override jsonb deny-list shape forward-compat) ACTIVE post-CLOSE; I-34 (permissions_matrix DECOMMISSIONED) NEW. **AMENDED:** I-24 (audit_log Option B append-only carve-out) — multi-policy OR-merge note added for new SELECT policy. **4 forensics discoveries registered:** D-CYCLE13B-FOR-1 (closed), FOR-2 (deferred — unconsumed permissionsOverride), FOR-3 (process lesson on PR-time client-table coordination), FOR-4 (P4 commendation). **1 implementor discovery:** D-CYCLE13B-IMPL-1 (P3 strict-grep gate vs documentation token — pattern lesson for future deprecation SPECs).

> **2026-05-04 — ✅ Cycle 13a (BIZ Permissions UI Foundations) CLOSE Grade A on operator device smoke — operator-side team management + audit log + 7-surface role-rank gating SHIPPED.** **NEW surfaces graded A on construction:** `app/brand/[id]/team.tsx` (J-T1 list view + synthetic self-row + section dividers + "+" invite gate on rank ≥ 50) · `app/brand/[id]/audit-log.tsx` (J-T5 read-only viewer + route-level gate + monospace action labels) · `src/components/team/{RolePickerSheet,InviteBrandMemberSheet,MemberDetailSheet}.tsx` (3 sheets + sub-sheet pattern per Cycle 12 verbatim) · `src/store/brandTeamStore.ts` (persisted Zustand mirroring Cycle 11 pattern) · `src/hooks/{useCurrentBrandRole,useAuditLog}.ts` (React Query hooks + DB synthesis chain + stub-mode fallback) · `src/utils/{brandRole,permissionGates}.ts` (constants + gate chokepoint) · `src/config/queryClient.ts` (first React Query consumer in mingla-business). **MOD surfaces graded A:** `app/_layout.tsx` (QueryClientProvider wired) · `src/utils/clearAllStores.ts` (useBrandTeamStore.reset cascade) · `src/store/currentBrandStore.ts` (DEC-092 Path A: J-A9 types/fields dropped + v11→v12 migration) · `src/store/brandList.ts` (J-A9 stub seeds removed) · `src/store/scannerInvitationsStore.ts` (header continuity comment only — 0 logic change) · `src/components/brand/BrandProfileView.tsx` (Audit log Operations row gated) · `src/components/brand/PublicBrandPage.tsx` (verifiedHostSinceYear suppressed pending B-cycle) · `app/brand/[id]/index.tsx` (handleOpenAuditLog wired) · `app/event/[id]/index.tsx` (G1 lifecycle gates) · `app/event/[id]/scanners/index.tsx` (G6 scanner mgmt gates) · `app/(tabs)/events.tsx` (G7 create event gate) · `src/components/event/EventManageMenu.tsx` (canEditEvent prop filters Edit/EndSales/Cancel/Delete) · `src/components/event/types.ts` (canEditTicketPrice prop) · `src/components/event/CreatorStep5Tickets.tsx` + `EditPublishedScreen.tsx` (G2 ticket price gate) · `src/components/orders/RefundSheet.tsx` (G3 refund gate) · `src/components/door/DoorRefundSheet.tsx` (G4 door refund gate) · `src/components/guests/AddCompGuestSheet.tsx` (G5 comp guest gate). **DELETED (Path A J-A9 subtract):** `app/brand/[id]/team/index.tsx` + `[memberId].tsx` + `src/components/brand/{BrandTeamView,BrandInviteSheet,RolePickerSheet,BrandMemberDetailView}.tsx`. **Heatmap delta:** Mingla Business operator-side surfaces now cover full PRD §11 Permissions UI foundations + §11.1 6-role enum (PRD reconciliation note added 2026-05-04). 7 J-T6 gate surfaces wired (G1-G7) + 4 team-management gates (J-T1..J-T4) + 1 audit-log gate (J-T5). **Constitutional outcomes:** #1 No dead taps PRESERVED (every gated action either hidden OR disabled-with-caption per gateCaptionFor) · #2 One owner per truth RESTORED via Path A (J-A9 BrandTeamView cluster subtracted; brandTeamStore is sole authority — Const #2 violation eliminated) · #3 No silent failures PRESERVED (useCurrentBrandRole isError surfaces; gates fail closed at rank=0) · #4 One query key per entity PRESERVED (brandRoleKeys + auditLogKeys factories) · #5 Server state in React Query PRESERVED (useCurrentBrandRole + useAuditLog use useQuery; not Zustand) · #6 Logout clears PRESERVED (useBrandTeamStore.reset cascade) · #7 TRANSITIONAL labels HONORED (4 markers: brandTeamStore header + InviteBrandMemberSheet banner + team list banner + audit log banner — all with verbatim copy + EXIT conditions) · #8 Subtract before adding HONORED (Path A deleted 6 J-A9 files BEFORE writing 13a code) · #9 No fabricated data PRESERVED (brandTeamStore starts empty; brandList stub members removed) · #14 Persisted-state startup PRESERVED (v11→v12 migration handles legacy J-A9 cache without crash). **NEW invariants ratified:** I-31 (UI-only brand invitation TRANSITIONAL until B-cycle) + I-32 (mobile UI gates mirror SQL biz_role_rank verbatim — CI grep gate). **DEC-092 logged** (Path A J-A9 subtract). **5 discoveries registered across cycle** (D-CYCLE13-IMPL-1/2/3/4/5); IMPL-1/4/5 closed at this CLOSE; IMPL-2/3 deferred to Refinement Pass; FOR-1..6 deferred to 13b.

> **2026-05-03 (late evening) — ✅ Cycle 12 (BIZ Door Sales / In-Person Payments) CLOSE Grade A on operator device smoke — operator-side door experience SHIPPED.** **NEW surfaces graded A on construction (mobile-side):** `app/event/[id]/door/index.tsx` (J-D2 list + J-D5 inline reconciliation card + DoorSaleRowCard + Export CSV + by-scanner expandable + TESTING MODE banner) · `app/event/[id]/door/[saleId].tsx` (J-D4 substantive detail route — hero + TICKETS + PAYMENT + BUYER + NOTES + REFUND HISTORY + Refund CTA gated on status) · `src/components/door/DoorSaleNewSheet.tsx` (J-D3 multi-line cart + 4 payment methods + 4 buyer fields + auto-check-in HIDDEN-1 contract + TESTING MODE banner + keyboard-discipline per memory rule) · `src/components/door/DoorRefundSheet.tsx` (per-line stepper + 10..200-char reason + **OBS-1 hard lock** — 0-hit grep for useScanStore in code paths) · `src/store/doorSalesStore.ts` (persisted Zustand v1 + frozen line snapshots + append-only refunds + 6-selector API + I-29 + I-30 + OBS-1 documented in header) · `src/utils/expandDoorTickets.ts` (`dt_` prefix avoids parseTicketId collision per HIDDEN-2). **MOD surfaces graded A:** `app/event/[id]/index.tsx` (J-D1 ActionTile gated on `inPersonPaymentsEnabled` + handleDoorSales + raw-entries useMemo for door KPIs + activity feed `event_door_refund` stream) · `app/event/[id]/guests/index.tsx` (J-G1 GuestRow union extended with `kind: "door"` + matchesSearch predicate + GuestRowCard door branch always-CHECKED-IN per OBS-1) · `app/event/[id]/guests/[guestId].tsx` (J-G2 parseGuestId `door-` branch + 2 new useMemos BEFORE early-return shell per ORCH-0710 + door PAYMENT section + Refund CTA + DoorRefundSheet mount) · `src/components/event/CreatorStep5Tickets.tsx` (per-tier `availableAt` 3-state picker via new AvailableAtSheet sub-component mirroring VisibilitySheet + state + reset + save) · `src/components/event/CreatorStep6Settings.tsx` (6th ToggleRow for `inPersonPaymentsEnabled`) · `src/components/event/EditPublishedScreen.tsx` (`editedSectionKeys` settings detection extended) · `src/components/door/DoorSaleNewSheet.tsx` + `src/components/event/PublicEventPage.tsx` (door-only Buy gated + "Pay at the door" copy) · `src/components/checkout/CartContext.tsx` (CheckoutPaymentMethod union extended with cash/card_reader/nfc/manual; door methods type-only — anon-tolerant routes have 0 useDoorSalesStore imports per I-29) · `src/components/guests/AddCompGuestSheet.tsx` (`availableAt === "both"` filter — I-30 enforcement) · `app/checkout/[eventId]/index.tsx` (`availableAt !== "door"` filter on J-C1 picker — I-30 enforcement) · `src/components/scanners/InviteScannerSheet.tsx` (canAcceptPayments toggle FLIP per Decision #4) · `src/utils/ticketDisplay.ts` (`formatTicketBadges` adds 6 chip categories: Door only / Online only / Sale window scheduled / Min/Max per buyer / Transfers disabled — single chokepoint propagating to 5 surfaces) · `src/utils/guestCsvExport.ts` (CSV extended with Kind + Payment method columns + new `exportDoorSalesCsv` helper) · `src/utils/clearAllStores.ts` (added useDoorSalesStore.reset cascade) · `src/store/draftEventStore.ts` (TicketStub.availableAt + DraftEvent.inPersonPaymentsEnabled + persist v5→v6 migrate) · `src/store/liveEventStore.ts` (LiveEvent.inPersonPaymentsEnabled + LiveEventEditableFieldKey union extension + persist v1→v2 migrate with privateGuestList backfill — heals Cycle 10 schema-bump-without-migrate gap) · `src/utils/liveEventConverter.ts` + `src/utils/liveEventAdapter.ts` (4-surface field cascade extension — including the rework v1 fix to `editableDraftToPatch` + undefined guard in `formatValueForKey`). **Heatmap delta:** Mingla Business operator-side surfaces now cover full PRD §7 in-person-payments + §14 sequence next slot ("In-person payments ← Cycle 12" complete; "Permissions UI" is next). PRD §5.3 manual check-in (gated on Cycle 11 + door auto-check-in) — door-side now has working manual check-in via auto-fire. **Constitutional outcomes:** #1 No dead taps PRESERVED (Pay-at-the-door button visibly disabled; card_reader + nfc visibly disabled with "Coming when backend ships" — non-interactive by design with explicit copy) · #2 One owner per truth PRESERVED (useDoorSalesStore is SOLE door-sale authority; T-39 grep PASS) · #3 No silent failures PRESERVED in main flows; D-QA-CYCLE12-1 P2 silent-fallback in DoorRefundSheet null-path queued for B-cycle (trigger unreachable in client-only) · #6 Logout clears PRESERVED (useDoorSalesStore.reset in clearAllStores) · #7 TRANSITIONAL labels HONORED (TESTING MODE banner verbatim in DoorSaleNewSheet + door route + card_reader/nfc captions + canAcceptPayments comment + doorSalesStore [TRANSITIONAL] header) · #8 Subtract before adding HONORED (Phase 1 InviteScannerSheet hardcoded-false subtracted before Pressable added; AvailableAtSheet mirrors VisibilitySheet pattern verbatim) · #9 No fabricated data PRESERVED ("Walk-up" only when buyerName empty; doorSalesStore empty at launch) · #10 Currency-aware UI PRESERVED (DoorSaleRecord.currency frozen "GBP"; formatGbp everywhere) · #13 Exclusion consistency PRESERVED (I-30 enforced via 4 filter chains using same `availableAt` field source-of-truth) · #14 Persisted-state startup PRESERVED (3 persist version migrations with safe defaults; cold-start hydration verified by operator device smoke). **NEW invariants ratified:** I-29 (door sales NEVER as phantom OrderRecord rows) + I-30 (door-tier vs online-tier separation via TicketStub.availableAt). **AMENDED:** I-28 (Cycle 12 amendment notes canAcceptPayments flip from type-locked false → operator-controllable; functional invite flow unchanged). **8 discoveries registered** across cycle (D-CYCLE12-IMPL-1/2/3/4/5/6 + D-QA-CYCLE12-1/2); D-CYCLE12-IMPL-3 closed by rework v2; rest queued for B-cycle / ride-along ORCH.

> **2026-05-03 (evening) — ✅ ORCH-0700 Phase 2 + Phase 3B CLOSE Grade A — Place Pool admin dashboard surface restored to A from F.** **Surfaces re-graded:** `admin_place_pool_mv.primary_category` (Grade A — all 69,599 rows now hold canonical 10-slug values verified by post-refresh assertion); admin Place Pool Categories panel + Bouncer-Approved Stats panel (Grade A — non-zero real counts for all 10 categories, was Grade F for 3 cells from 2026-05-03 morning matview rebuild until evening Phase 3B fix); `place_pool` schema (Grade A — lean: 6 stale interpretation columns physically dropped, single owner per truth restored per Constitution #2); 3 admin edge functions admin-seed-places + admin-refresh-places + admin-place-search (Grade A — scrubbed of all `place_pool.seeding_category` reads/writes, redeployed twice today); SQL helper `pg_map_primary_type_to_mingla_category` (Grade A — returns canonical 10-slug values only, 16 self-verify probes); TS twin `_shared/derivePoolCategory.ts` (Grade A — byte-aligned with SQL helper, 21-test invariant suite). **Heatmap delta:** the 4-taxonomy architectural picture is now mapped end-to-end (Seeding/Bouncer/Signal-Scoring/Display) — see [reports/INVESTIGATION_CATEGORY_PIPELINE_END_TO_END.md](reports/INVESTIGATION_CATEGORY_PIPELINE_END_TO_END.md). **Constitutional outcomes:** #2 RESTORED (matview column has single semantic owner); #9 PRESERVED (helper returns NULL on no-match, no fabricated default); #13 RESTORED (SQL + TS + DISPLAY_TO_SLUG all aligned). **NEW invariant I-CATEGORY-SLUG-CANONICAL ratified** with 3 regression gates. **D-T4 process improvement registered:** when forensics identifies a fix requiring a separate dispatch, orchestrator must track it explicitly in PRIORITY_BOARD or queued-fixes ledger so it doesn't get lost during scope pivots (LANDMINE-A1 was specified in the first audit then forgotten across 4 dispatches).

> **2026-05-03 — ✅ Cycle 11 (BIZ QR Scanner + Check-in + Scanner-Team UI + Per-Ticket QR) CLOSE Grade A pending operator device smoke.** **NEW surfaces graded A on construction (mobile-side):** `app/event/[id]/scanner/index.tsx` (camera + reticle + 6-kind result overlay + session log + TESTING MODE banner) · `app/event/[id]/scanners/index.tsx` (scanner-team list + action sheet + revoke flow + TRANSITIONAL banner) · `src/components/scanners/InviteScannerSheet.tsx` (sister of AddCompGuestSheet, keyboard-avoidance honored) · `src/components/checkout/TicketQrCarousel.tsx` (horizontal swipe carousel with single-ticket parity preservation) · `src/store/scanStore.ts` (persisted Zustand v1 + I-27 enforcement via getSuccessfulScanByTicketId guard) · `src/store/scannerInvitationsStore.ts` (persisted Zustand v1 + I-28 UI-only TRANSITIONAL stub) · `src/utils/expandTicketIds.ts` + `src/utils/qrPayload.ts` (per-seat ticketId derivation + scanner QR parser). **MOD surfaces graded A:** `app/event/[id]/index.tsx` (event_scan kind added to ActivityEvent union + recentActivity stream + activityKindSpec branch + Scanners ActionTile + handleScanTickets stub→router) · `app/event/[id]/guests/index.tsx` (J-G1 derived check-in pill 4-state) · `app/event/[id]/guests/[guestId].tsx` (J-G2 per-ticket section + Mark checked in CTAs + ORCH-0710 hooks reorder for cold-start safety) · `app/checkout/[eventId]/confirm.tsx` (multi-QR carousel + Const #8 SUBTRACT old QR styles) · `app/o/[orderId].tsx` (multi-QR carousel + DELETED local ticketIdFromOrder helper per Const #8) · `src/utils/clearAllStores.ts` (added scanStore + scannerInvitationsStore reset cascade). **Heatmap delta:** Mingla Business operator-side now covers door experience (8 J-S journeys + Cycle 8 carousel fix + Cycle 10 J-G1/J-G2 rewires + Cycle 9c-2 feed extension). PRD §6 features status: 8 GREEN/YELLOW shipped today (J-S1..J-S8); scanner-payments cluster (PRD §6.2) hard-line OUT to B-cycle; cross-device dedup (I-27 DB-enforcement) hard-line OUT to B-cycle. **3 device-dependent SC items remain unverified:** SC-1 (OS camera permission prompt visual) + SC-2 (`Linking.openSettings()` deeplink behavior) + SC-3 (`Haptics.notificationAsync` tactile feedback) — standard Expo APIs, very low risk; operator device smoke covers post-deploy. **Constitutional outcomes:** #1 No dead taps PRESERVED · #2 One owner per truth PRESERVED (scanStore + scannerInvitationsStore are sole authorities) · #3 No silent failures PRESERVED (recordScan return values consumed; failures surface via overlay + log) · #6 Logout clears PRESERVED (both new stores reset cascade) · #7 TRANSITIONAL labels HONORED (offlineQueued: true · I-28 UI-only · canAcceptPayments hardcoded false · TESTING MODE banner — all marked with EXIT conditions) · #8 Subtract before adding HONORED (handleScanTickets toast removed before router.push; ticketIdFromOrder helper deleted from /o/[orderId]; old QR styles removed from confirm.tsx + /o/) · #9 No fabricated data PRESERVED (stores empty at launch; no seeded scans/invitations) · #14 Persisted-state startup RESTORED for J-G2 detail (was at risk pre-ORCH-0710; cold-start crash fixed via hooks reorder). **NEW invariants ratified:** I-27 (single successful scan per ticketId; client-enforced + B-cycle DB-enforced) + I-28 (scanner-invitation UI without functional flow until B-cycle — TRANSITIONAL). **BACKFILLED:** I-25 + I-26 (Cycle 10 doc drift). **D-T3 process improvement registered:** `eslint-plugin-react-hooks` recommended addition to implementor verification gate; tsc alone misses Rules of Hooks violations.

> **2026-05-03 — ✅ ORCH-0706 (PR #59 B1.5 backend hardening) CLOSE Grade A — Mingla Business BACKEND now LIVE for the first time + 5 hardening guard rails enforced.** **NEW surfaces graded A on construction (DB-side):** PR #59's 19-table Mingla Business schema applied — `brands` + `brand_team_members` + `brand_invitations` + `creator_accounts` (extended) + `account_deletion_requests` + `events` + `event_dates` + `ticket_types` + `waitlist_entries` + `tickets` + `orders` + `refunds` + `door_sales_ledger` + `scan_events` + `event_scanners` + `scanner_invitations` + `audit_log` + `payment_webhook_events` + `stripe_connect_accounts` (full set). **NEW guard rails (ORCH-0706):** 3 immutability triggers (`trg_brands_immutable_slug` + `trg_events_immutable_slug` + `trg_events_immutable_created_by`) — verified live via metadata + behavioral test on brands.slug; 2 CHECK constraints (`refunds_status_check` + `door_sales_ledger_payment_method_check`) — verified live via INSERT bogus-value tests returning PG error 23514; `audit_log` + `scan_events` COMMENT ON TABLE updates honestly disclose service-role carve-out. **3 NEW invariants ratified (I-22 + I-23 + I-24) + I-17 PROMOTED from consumer-side to DB-enforced.** **DEC-089 ratified** (Option B audit-log carve-out). **Files touched:** 1 NEW migration `supabase/migrations/20260503100000_b1_5_pr_59_hardening.sql` (179 lines) + locally-fixed PR #59 migration `supabase/migrations/20260502100000_b1_business_schema_rls.sql` (function-ordering reorder; PR-back-to-main optional follow-up). **Constitutional outcomes:** #2 STRENGTHENED (DB-enforced one-owner-per-truth on brand/event slug + created_by) · #3 PRESERVED (RAISE EXCEPTION on slug/created_by changes is explicit error surface) · #7 HONORED ([TRANSITIONAL] markers preserved where applicable; no new TRANSITIONAL state introduced; this is permanent hardening) · #8 HONORED (DROP TRIGGER IF EXISTS before each CREATE TRIGGER; idempotent re-apply) · #12 PRESERVED (CHECK constraints validate at INSERT/UPDATE time; immutability triggers fire BEFORE UPDATE) · #13 PRESERVED (door_sales_ledger.payment_method excludes 'online_card' consistently with the table's physical-presence semantics).

> **2026-05-02 — ✅ Cycle 10 (Mingla Business Guest List) CLOSE Grade A — operator-side guest management surface added.** **NEW surfaces graded A on construction:** `app/event/[id]/guests/index.tsx` (J-G1 list view — merged orders ⊕ comp guests, search bar, EmptyState branches, GuestRowCard with status pill + check-in placeholder) · `app/event/[id]/guests/[guestId].tsx` (J-G2 detail with composite ID parsing, hero, tickets section, ORDER ACTIVITY for orders, ADDED BY for comps, OTHER ORDERS purchase history, inline remove sheet for comps) · `src/store/guestStore.ts` (NEW persisted Zustand store; raw-entries-plus-useMemo selector pattern enforced; logout-cleared) · `src/components/guests/AddCompGuestSheet.tsx` (J-G4 sheet with 6 inputs + reason validation + simulated processing + caller-side audit log fire) · `src/utils/guestCsvExport.ts` (J-G6 CSV serializer with RFC 4180 escape; web Blob+anchor / native RN Share fallback). **MOD surfaces:** `src/store/draftEventStore.ts` + `src/store/liveEventStore.ts` (added `privateGuestList: boolean` field) · `src/utils/liveEventConverter.ts` + `src/utils/liveEventAdapter.ts` (4-surface field extension) · `src/utils/clearAllStores.ts` (added useGuestStore.reset cascade) · `src/components/event/CreatorStep6Settings.tsx` (added 5th ToggleRow for J-G5) · `app/event/[id]/index.tsx` (handleGuests stub → router.push). **Net LOC delta:** ~+1876 added / -1 removed. **tsc EXIT=0 in mingla-business workspace.** **Heatmap delta:** Mingla Business operator-side surfaces now cover guest list + management. PRD §5.3 features status: 6 GREEN/YELLOW shipped (#3 #4 #6 #7 #8 #9 #10 partial via buyer-as-attendee model — count-shifted: feature #6 placeholder + #4 manual-add via separate store), 2 RED gated B4 (#1 #2 — approve/reject pending), 2 RED gated Cycle 11 (#5 manual check-in + #6 live-data display). Cycle 10 of BUSINESS_PRD complete. Cycle 11 (QR scanner) is the next forward dispatch on the BUSINESS_PRD pipeline. **Constitutional outcomes:** #1 No dead taps PRESERVED (every chrome button + row tap + sheet confirm + toggle + remove flow wired) · #2 One owner per truth PRESERVED (useGuestStore is sole comp authority; never duplicated into orderStore or liveEventStore) · #3 No silent failures PRESERVED (all catch paths surface via toast; no `catch () {}`) · #6 Logout clears PRESERVED (useGuestStore.reset in clearAllStores) · #7 TRANSITIONAL labels HONORED (guestStore.ts header + guestCsvExport.ts native path + EventManageMenu shortcut deferral all marked) · #8 Subtract before adding HONORED (handleGuests toast removed before adding router.push) · #9 No fabricated data PRESERVED (genuine "No guests yet" empty state; never seeds fake comps) · #10 Currency-aware UI PRESERVED (formatGbp on order amounts) · #14 Persisted-state startup PRESERVED (Zustand persist v1 hydrates cleanly). **NEW invariants ratified:** I-25 (comp guests in useGuestStore ONLY; CheckoutPaymentMethod NEVER includes "comp") + I-26 (LiveEvent.privateGuestList is operator-only with zero buyer surface in Cycle 10). **3 S3 discoveries surfaced:** D-CYCLE10-IMPL-1/2/3.

> **2026-05-02 — ORCH-0704 v2 CLOSE Grade A (Mingla Business — edit-after-publish surface).** **Surface-level grade additions:** EditPublishedScreen.tsx (sectioned 6-card accordion reusing CreatorStepN body components via local edit state) — NEW surface, grade A on construction. ChangeSummaryModal.tsx (severity-stripe diff list + tickets sub-renderer + required reason input + char counter + notification footer note) — REPLACED, grade A. CreatorStep5Tickets.tsx (price/free/unlimited lock UX + capacity floor + Delete-button-hide for sold tiers via optional `editMode.soldCountByTier` prop) — additive surface upgrade, grade A. EditAfterPublishBanner.tsx — copy refresh, grade A. liveEventStore.ts (`updateLiveEventFields` mutation with reason validation + 8 reject reasons + edit log record + notify) — replaces narrow `updateLiveEventEditableFields`, grade A. NEW surfaces: eventEditLogStore.ts (Zustand persisted append-only audit log; logout-cleared) + eventChangeNotifier.ts (4-channel notification stack with TRANSITIONAL email + SMS console-log stubs + push deferred) + scheduleDateExpansion.ts (active-date computation for dropped-date detection) + liveEventAdapter.ts (LiveEvent ↔ DraftEvent view adapter + rich diff helpers) + orderStoreHelpers.ts (TRANSITIONAL stub returning zeros; Cycle 9c flips live). **Net LOC delta:** ~+1380 added / -200 removed (~+1180 net). **Subtraction verified:** liveEventToEditPatch.ts DELETED; updateLiveEventEditableFields removed; narrow EditPublishedScreen body fully replaced. **tsc EXIT=0 across mingla-business workspace.** **Heatmap delta:** Mingla Business operator-side edit-after-publish surface area moves from "narrow-2-fields" (Cycle 9b-2) to "full editable surface with buyer protection." Cycle 9 (event management) journeys delivered: J-E13 ✓ J-E14 ✓ J-E15 ✓ J-E17 ✓ J-E9 ✓ J-E10 ✓ J-E11 ✓ Delete draft ✓. Remaining 6 of 8 Cycle 9 journeys all in Cycle 9c (J-M1..J-M6 Orders ops). **Constitutional outcomes:** #1 No dead taps PRESERVED (every section header / button / reject dialog CTA wired) · #2 One owner per truth PRESERVED (LiveEvent in liveEventStore; EditEntry in eventEditLogStore; OrderRecord forward-spec for 9c useOrderStore; CartLine in cart context) · #3 No silent failures PRESERVED (UpdateLiveEventResult discriminated union surfaces every reject; reject dialog maps all 8 reasons with refund-first copy) · #6 Logout clears PRESERVED (useEventEditLogStore.reset wired into clearAllStores) · #7 TRANSITIONAL labels HONORED (orderStoreHelpers + email/SMS/push stubs all labeled with EXIT CONDITIONs) · #8 Subtract before adding HONORED (narrow 9b-2 fully gone before new shipping path) · #9 No fabricated data PRESERVED (zero-soldCount stub is honest empty; never seeded fake orders) · #10 Currency-aware UI PRESERVED (currency forward-locked in OrderRecord shape spec'd for 9c). **NEW invariants ratified:** I-19 (immutable order financials) + I-20 (edit reason mandatory + audit log permanence). **DEC-087 ratified.** **Coverage observation:** the v1→v2 spec revision validated the "operator overrides forensics defaults" pattern — narrow recommendations got rejected in favor of industry-standard buyer-protection. Investigation stayed canonical; only the spec was rewritten. v1 obsolete-banner pattern + v2 file naming convention proven for future spec revisions.

> **2026-04-29 — ORCH-0696 CLOSE Grade A CONDITIONAL.** **Surface-level grade promotions:** ExpandedCardModal F→A across all 8 mount surfaces (chat-shared / Discover deck / Saved tab / Calendar tab / friend profile / collab session / solo deck / collab deck) — single chrome change cascades from one source of truth. Centered floating Modal converted to `@gorhom/bottom-sheet` with render-branch-conditional theme: Ticketmaster events → dark sheet (`rgba(12,14,18,1)` + white drag handle + EventDetailLayout poster hero + artist orange + above-fold Get Tickets CTA + secondary chip row) ; Place / curated cards → white sheet + original light theme content (sub-components reverted via `git checkout` after operator caught dark-token regression at first device test). 3 dead component files deleted (DescriptionSection / HighlightsSection / MatchFactorsBreakdown). nightOutStyles block (~256 LOC) + chrome styles + Shape 2a Modal hack at MessageInterface.tsx:1538-1554 deleted. New `glass.bottomSheet.*` token sub-namespace + `parseEventDateTime` defensive parser. 17 i18n keys × 29 locales = 493 translations. **Net LOC reduction:** ~-330 LOC removed before EventDetailLayout's ~470 LOC added across all sub-components affected. **F-13 SC-20 LIVE-FIRE verified PASS on iOS + Android** — toasts render above bottom sheet via React-tree z-ordering (Animated.View, not native Modal portal); Shape 2a hack from ORCH-0685 cycle-3 deleted with `[ORCH-0696 F-13 lock-in]` permanent comment. **Heatmap delta:** ExpandedCardModal surface F→A (8 mount surfaces inherit); MessageInterface chat-shared toast-above-sheet flow verified (regression class from ORCH-0685 cycle-2 dead-tap closed structurally). **Constitutional outcomes (combined ORCH-0696):** #2 STRENGTHENED (single chrome source via `glass.bottomSheet.*` + render-branch-conditional theme) · #7 STRONGLY UPHELD (Shape 2a's exit condition met + transitional fix deleted as designed) · #8 STRONGLY UPHELD · #1/#3/#12 UPHELD. **Coverage observation:** the ORCH-0696 chain validated the operator-confirmation-before-bulk-IMPL process invariant (D-PROC-3) — original spec assumed "all content goes dark" without operator verification; first device test surfaced the wrong assumption; render-branch-conditional theme was the correct architectural answer. Three cycles in a row (ORCH-0670 / ORCH-0698 / ORCH-0696) confirm the same pattern: bulk-IMPL on architectural assumptions = always at risk of operator-caught regression.

> **2026-04-29 — ORCH-0670 SLICE A + ORCH-0698 BUNDLED CLOSE Grade A CONDITIONAL.** **Surface-level grade promotions:** Discover (Concerts/Events) F→A — empty/error/no-match states no longer render literal i18n keys; tier filter chips actually filter via `TIER_BY_SLUG` canonical lookup (5 dead switch cases deleted, Constitution #8); 6 hardcoded English chip labels + screen title now i18n in 29 locales (280 native translations + 392 orphan deletions); title "Concerts & Events" → "Events" (CF-1 trade-off accepted as brand-language preference); Android header autoshrink eliminated, aligned to Friends-screen baseline (lineHeight 32→36 + textAlignVertical:'center'); 14 orphan i18n keys deleted across 29 locales. Map view F→DELETED — entire `app-mobile/src/components/map/` subtree (17 files) + `useMapSettings.ts` hook + 3 native deps (`react-native-maps` + `@maplibre/maplibre-react-native` + `react-native-map-clustering`) + iOS/Android Google Maps SDK config + MapLibre expo plugin + 2 EXPO_PUBLIC_MAPLIBRE_* extra vars + GOOGLE_MAPS_API_KEY env doc all removed; AccountSettings Privacy accordion's orphan Map Privacy section deleted. **Heatmap delta:** Discover surface F→A; Map surface F→DELETED (no longer counts in surface inventory); AccountSettings unchanged grade (orphan section removed but settings screen still functions normally). **Net LOC reduction across both fixes:** -2,068 lines (ORCH-0670 Slice A: +2,491/-459; ORCH-0698: +2/-4,100). **Constitutional outcomes (combined):** #1 / #3 / #9 RESTORED · #2 STRENGTHENED (PRICE_TIERS canonical + fewer parallel chrome owners) · #8 STRONGLY UPHELD · #12 UPHELD. **Coverage observation:** subtractive cleanup (ORCH-0698) is the largest LOC reduction in the chain to date — confirms the value of operator render-first audits; designer + audit had both propagated stale "MapBottomSheet/PersonBottomSheet are existing bottom sheets to retrofit" claim that was never grep-verified. D-PROC-2 process invariant codified to prevent recurrence.

> **2026-04-27 — ORCH-0690 CLOSED Grade A (CONDITIONAL — operator device PASS). Schedule date-picker UX surface F→A across all 8 ExpandedCardModal mount surfaces simultaneously.** Single fix in shared `ActionButtons.tsx` cascades to: CalendarTab + SavedTab + DiscoverScreen + MessageInterface (chat-shared) + ViewFriendProfileScreen (paired) + SessionViewModal (collab) + SwipeableCards solo + SwipeableCards collab. Pre-fix: iOS auto-committed first wheel tap; Android auto-advanced after calendar OK with no preview; no soft-escape from time-mode. Post-fix: iOS Next button gates date→time advancement; Android calendar-OK shows preview Alert with [Change Date]/[Pick Time]; iOS time-mode shows "Back to date" button; past-date check pre-scheduling; isAssumption surfacing (Constitution #9); closed-place re-prompt preserves date + reopens in time-mode. **Heatmap delta:** +8 A (every Schedule mount surface promoted via shared component); 0 deletions; 0 new invariants registered (state-machine bug class too narrow for CI gate; protective `[ORCH-0690 RC-1]` comment + spec-derived regression test serve as adequate prevention). Constitutional outcomes: #1/#3/#9/#12 RESTORED + #8 UPHELD. **Coverage observation — Shape A surgical discipline paid off:** ~30 LOC behavior change + ~50 LOC refactor (extract `confirmAndSchedule` shared helper) + 261 i18n translations across 29 locales. Single bundled commit `80b176fe`, OTA-eligible. Cycle-2 (5 surgical CSS changes for iOS Modal header overflow) bundled in same commit per dispatch §H "single OTA" — operator field-test caught header sizing issue, cycle-2 dispatch + fix landed in ~10 min wall time before any bug ticket needed to be filed. Pre-existing baseline `fetch_local_signal_ranked` CI gate failure persists as orthogonal item (ORCH-0691).

> **2026-04-26 — ORCH-0684 CLOSED Grade A (CONDITIONAL — operator finals). Paired-person view + paired-person personalization surface F→A.** Surface that shipped placeholder cards to every paired user since ORCH-0640 cutover (~3 days) is now production-grade. New mapper reads real `place_pool` snake_case Google fields; new RPC body uses `p_user_id` + `p_person_id` for joint-pair history boosts. **3 invariants registered:** `I-PERSON-HERO-RPC-USES-USER-PARAMS` (CI-gated structural JOIN check) + `I-RPC-RETURN-SHAPE-MATCHES-CONSUMER` (CI-gated mapper field-shape check via awk extraction; structurally prevents the ghost-field-reads class of bug going forward) + `I-PERSON-HERO-CARDS-HAVE-CONTENT` (documentary smoke contract; D-8 meta-discovery from investigation — recommend orchestrator process refinement to add visual-content gate to ALL future RPC-touching closure templates). **Heatmap delta:** +1 A (paired-person view); 0 deletions. Recently-closed grade-A items today: ORCH-0686 (photo backfill pipeline) + ORCH-0684 (paired-person view) — both via tester-CONDITIONAL-PASS pattern with operator-deferred environmental finals. **Pre-existing baseline `fetch_local_signal_ranked` CI gate failure persists** as orthogonal item (ORCH-0683). **Coverage observation — option-a HTTP combo planner deviation honored discipline:** spec §3.2 explicitly allowed it; implementor chose it for blast-radius reasons; T-24 regression-lock for Discover curated trivially N/A because `generate-curated-experiences` is byte-unchanged. Future scope-discipline pattern worth replicating (deviation explicitly anticipated by spec → not silent shrinkage).

> **2026-04-26 — ORCH-0686 CLOSED Grade A (CONDITIONAL — Path B). Photo-backfill pipeline write surface F→A.** Surface that ORCH-0678 inadvertently broke (every `create_run` attempt failed with SQLSTATE 23514 for 24 hours after deploy, 14,401 places stranded) is now production-grade. Constraint amended (3 values), default flipped, admin error-body unwrapper installed for two affected pages, CI gate `I-DB-ENUM-CODE-PARITY` structurally prevents the rename-without-SQL-update bug class going forward. **2 invariants registered:** `I-PHOTO-FILTER-EXPLICIT` (rewritten under 3-mode form — was previously only a migration comment which let it go stale through ORCH-0678; now persisted), `I-DB-ENUM-CODE-PARITY` (NEW, CI-gated, same class of failure as ORCH-0540 PL/pgSQL drift). **Heatmap delta:** +1 A (photo-backfill write surface); 0 deletions. **Cascade:** ORCH-0682 (Lagos operational recovery) Step 2 immediately unblocked — first run live at 05:24 UTC for 1039 Lagos pre-bouncer-approved places. **Coverage observation — third byte-for-byte forensics projection match in this program:** ORCH-0678's tester predicted 1039 ± 5 pre-photo survivors for Lagos; the operational recovery run for ORCH-0686 just used `total_places=1039` as the create_run input. Forensics→spec→impl chain producing predictions that empirically match reality is a coverage-map win pattern worth replicating. **Pre-existing baseline `fetch_local_signal_ranked` CI gate failure persists** as orthogonal item (ORCH-0683). Sub-cycle SC-9 verification (admin error helper unwraps real Postgres error on forced 500) UNVERIFIED but explicitly CONDITIONAL — that's a Constitution #3 *improvement*, not a fix-blocker. ORCH-0687 (PlacePoolManagementPage uses `supabase.functions.invoke` direct instead of `invokeWithRefresh` 401-retry wrapper) registered as S3 follow-up.

> **2026-04-26 — ORCH-0671 + ORCH-0678 BUNDLED CLOSE Grade A (CONDITIONAL).** **Surface grade movements:** **Bouncer Pipeline F→A** (ORCH-0678 two-pass design — pre-photo + final, single source of truth via opts flag, structurally-enforced single-writer per column, three new CI-gated invariants); **Admin Photo Pool surface DELETED** (ORCH-0671 — page removed from admin nav, 5 RPCs DROPped, 1 view DROPped, 4 historical photo_backfill runs archived with service-role-only RLS, CHECK constraint shrunk to single value, hash-route fallback verified); **Lagos serving inventory F→ pending Step 2+3** (1039 pre-photo survivors locked; final servable count produced when operator runs photo backfill + final bouncer). **Coverage win — empirical projection validation:** the forensics→spec→impl chain produced predictions (Lagos 1039 ± 5 pre-photo pass; B4=2888, B6=643, B5=160, B1=69) that reality matched **byte-for-byte**. This is a coverage-map first: a chain whose every quantitative prediction was independently verified at deployment. Pattern worth replicating for future serving-pipeline ORCHs. **3 new CI-gated invariants:** I-PRE-PHOTO-BOUNCER-SOLE-WRITER (negative-control PROVEN), I-PHOTO-DOWNLOAD-GATES-ON-PRE-PHOTO, I-TWO-PASS-BOUNCER-RULE-PARITY. **Heatmap delta:** +1 A (Bouncer Pipeline), -1 surface (Photo Pool admin deleted, no longer counted), +1 in-flight (Lagos pending operator finish). **Coverage observation:** 8 cities (Paris/NYC/Berlin/Barcelona/Toronto/Chicago/Dallas/Miami) have `passes_pre_photo_check IS NULL` for all rows — they sit at "not yet pipeline-recovered" status post-bundle-close; ORCH-0682 captures this. **Pre-existing baseline `fetch_local_signal_ranked` CI gate failure persists** as orthogonal item (ORCH-0683). **Bundled-close pattern continues to pay off:** cross-bundle integration test X-01 (BouncerPipelineButtons doesn't trip "AI Approved" labels gate from ORCH-0671) caught a class of cross-ORCH integration risk a single-ORCH tester would have missed. Same pattern as Wave 4 X-01..X-03.

> **2026-04-25 — ORCH-0669 cycle-2 CLOSED.** No surface grade movement — this is design-debt tuning on already-A-graded surfaces (Home pill bar, bottom nav, preferences chip, notification bell, "+" create pill, chat input capsule). Hairline alpha 0.12 → 0.06 across all 7 consumers (single shared token), dead `topHighlight` token removed (Constitution #8). **What strengthened structurally:** new invariant `I-CHROME-HAIRLINE-SUB-PERCEPTIBLE` with CI gate prevents the regression class (any future inline `borderColor: 'rgba(255, 255, 255, 0.X)'` with white alpha ≥ 0.09 on a chrome consumer file fails the gate — verified via T-13 forward + T-14 recovery negative-control). Constitution #2 reinforced (single owner per truth — the CI gate forces token reference instead of inline literal drift). **Coverage observation D-1:** 2 pre-existing inline literals at non-chrome surfaces in `MessageInterface.tsx` (action button + message bubble at 0.14 alpha) annotated with `// __not_chrome__` markers and excluded from the chrome gate; filed as ORCH-0679 candidate to introduce a `glass.chat.border.*` token namespace and migrate those consumers — Constitution #2 inline-literal cleanup, S3 cosmetic, not launch-blocking. **Heatmap unchanged. Total grade distribution unchanged.**

> **2026-04-25 — Wave 4 CLOSE: 4 chat-domain ORCHs to Grade A (single bundled CLOSE).** Surface grade movements: **DM Messaging F→A** (ORCH-0664 dedup ordering fixed; messages no longer silently drop), **Add Friend to Existing Session F→A** (ORCH-0666 RPC + cycle-2 prop migration; both Friends-modal + DM "Add to Board" entry points now real), **DM Saved-Card Sharing F→A** (ORCH-0667 picker + bubble + push pipeline live), **Discover/Singles Deck Distance Display C→A** (ORCH-0659/0660 honest haversine + per-mode travel time + null-safe UI). 4 invariants now active across these surfaces: I-DEDUP-AFTER-DELIVERY, I-INVITE-CREATION-IS-RPC-ONLY, I-CHAT-MESSAGE-TYPE-CARD-PAYLOAD-COMPLETENESS, I-DECK-CARD-CONTRACT-DISTANCE-AND-TIME — each with CI gate proven via tester negative-control reproductions. **Heatmap delta:** +4 A, -4 F across chat/DM/deck domains. Baseline `fetch_local_signal_ranked` CI gate failure persists as orthogonal parallel-chat issue (recommend separate ORCH for tracking). **Coverage observation:** the bundled-tester approach revealed cross-ORCH integration paths (X-01 card-share via realtime / X-02 invite push delivery / X-03 deck-share data preservation) that single-ORCH testers would have missed — recommend keeping bundled-test pattern for future multi-ORCH waves where surfaces share code paths.

> **2026-04-25 — ORCH-0668 CLOSED Grade A.** **Paired-Profile Recommendations surface: A reaffirmed** (was silently F since plpgsql plan-cache trap activated for ≥6 signals, restored by the `LANGUAGE sql STABLE` rewrite). Production perf p95 ~135 ms at 11-signal Raleigh confirms the surface is now production-grade across the full workload range tested by the dispatch (3 / 6 / 11 signals all sub-150 ms). **Sibling surfaces unchanged:** Discover/Singles (`query_servable_places_by_signal`) and Curated Experiences (`fetch_local_signal_ranked`) both verified `LANGUAGE sql STABLE` with body hashes captured for future byte-compare baseline (`cde9ef2edc7...` and `1fd57904a06...`). **New invariant `I-RPC-LANGUAGE-SQL-FOR-HOT-PATH` registered + CI gate active** — closes the regression class for hot-path RPCs that take array parameters. Pre-existing `fetch_local_signal_ranked (no defining migration found)` CI gate baseline failure is orthogonal (parallel chat) and tracked separately. **Heatmap delta:** Paired-Profile Recommendations F→A. Total grade distribution: +1 A, -1 F.

> **2026-04-25 — ORCH-0672 CLOSED.** No surface grade movement — this was a regression hotfix that restored the previously-audited state of the Home pill bar (originally graded under ORCH-0589 "unified glass home" + ORCH-0661 "pending pill states"). The `glass.chrome.pending` token sub-namespace was always intended to be HEAD truth; the 3911b696 partial-commit was the anomaly, not the prior state. Home/Chrome surface grade unchanged at A (per ORCH-0589/0661 closure). **Coverage observation:** the regression itself revealed a process gap — forensics + orchestrator capture of in-flight diffs did not classify coupled diffs (consumer + token must move together). New invariant `I-COUPLED-DIFF-NEVER-PARTIAL-COMMIT` registered in INVARIANT_REGISTRY.md to close the regression class structurally. **Heatmap unchanged. Total grade distribution unchanged.**

> **2026-04-23 late-night — ORCH-0641 FORMAL QA CLOSE (AH-175 CONDITIONAL PASS).** Discovery/Singles Serving surface grade: **A reaffirmed** (not A→A because no new grade earned; prior A was silently invalidated by the 2026-04-15 regression, now restored). Cross-function `_periods` audit across all 72 edge functions confirms the bug was isolated to `discover-cards` — no hidden copies elsewhere. Constitutional #3/#8/#13 preserved or improved. One coverage observation from D-1: Curated-stop hours filtering has been an unaudited no-op via `filterCuratedByStopHours` at `discover-cards:940` — grade on Curated Serving surface demoted to B-pending (was A via ORCH-0634 closure) until ORCH-0644 ships the shared hours helper replacement. No other grade movements.

> **2026-04-23 night — ORCH-0640 (Great Demolition and Rebuild) + ORCH-0641 (filter schema fix) BOTH CLOSED Grade A.** Device smoke 5/5 PASS on user phone. ORCH-0640: 14 migrations live, 5 edge fns deployed + 7 deleted, 2 admin pages deleted, 4 mobile files deleted, 7 new invariants registered. Discovery / Explore surface grade distribution **unchanged structurally** — the demolition collapsed 3 parallel card-generation systems into 1 without adding or removing audited flows. Surface coverage strengthens via consolidation: every serving path now goes through the 3-gate `query_servable_places_by_signal` RPC (G1 is_servable + G2 signal score + G3 stored_photo_urls). ORCH-0641 restores 7 previously-dead chips (brunch/casual_food/drinks/fine_dining/movies/play/icebreakers) — was a silent regression since 2026-04-15, already included in Discovery surface grade via ORCH-0434 closure, so no grade movement needed. Place Pipeline surface count UNCHANGED — AI validation layer deletion absorbed into signal-serving pipeline that was already audited A. Rollback archive lives until 2026-04-30. 11 deferred follow-ups queued for later dispatch.

> **2026-04-22 — ORCH-0636 (TS Build Hygiene) closed Grade A code-level.** No surface grade change (pure type-hygiene + dead-code removal affecting `RecommendationsContext.tsx`, `ViewFriendProfileScreen.tsx`, `useSessionManagement.ts`). Minor positive signal on Discovery/Collab surface: collab prefetch path converged onto the proven solo path, meaning both modes now share the same `['deck-cards', …]` query key — coverage improves via consolidation. ORCH-0636.1 (P3 residual) and ORCH-0639 (P1 Const #13 / curated two-gate) remain open. See WORLD_MAP.md.

> **2026-04-21 ~23:30 UTC — ORCH-0635 (Coach Mark Refresh) CLOSED Grade A.** Device-verified PASS after iterative cutout tuning. Commit `f284215d`. Tour rewritten from 10 steps to 9 to match post-glass-refresh app layout. 3 orphaned steps (old 2, 5, 10) fixed or dropped, 1 duplicate cutout merged (old 1+3), step 6 retargeted from FilterChip to header panel with centered bubble, NEW step 3 added for Likes tab (closes core-loop save-destination gap), NEW step 9 added targeting BetaFeedbackButton. New `bubblePosition?: 'auto' | 'center'` field on CoachStep. Legacy `coach_mark_step` values normalized to TOUR_COMPLETED on first post-deploy fetch (idempotent). New `__DEV__` warning in `useCoachMark` fires within 500ms if a step's targetRef never attaches — catches future refactor orphans. 14 files, +286/-151. Pure presentation + state-machine. OTA-safe for both platforms. Non-beta feedback universalization flagged as candidate follow-up (BetaFeedbackButton gate).

> **2026-04-21 ~22:00 UTC — ORCH-0627.1 (Profile Core Glass Refresh, Phase 1) CLOSED Grade A.** Device-verified PASS. Commit `e3dfb380`. Converts the last white-on-light screen to warm-charcoal glassmorphic bento. 5 floating cards (Hero elevated + Interests + Stats + Account + Footer) on `rgba(20,17,19,1)` canvas with orange radial glow breathing 0.85↔1.0 over 8s (static with reduce-motion). New `<GlassCard>` primitive (base + elevated) handles iOS BlurView + Android API<31 fallback + Reduce Transparency fallback. New `glass.profile.*` token namespace (~260 tokens: canvas, hero gradient + glow, card base/elevated, 14 text roles, avatar + dual ring, chips intent/category, stat tiles, level ring, tier badge, settings row, sign-out, sheet, motion). Stats card restructured from flat 3+3 to level-ring hero + 2×2 bento grid. Count-up short-circuits on zero. Phase 2 (ORCH-0627.2: sheets + ViewFriendProfileScreen + PairedProfileSection) QUEUED. 9 files, +1396/-452. Pure presentation + token layer. OTA-safe both platforms. Discoveries: placesVisited/streakDays still hardcoded to 0 (ORCH-0629/0630 candidates); BetaFeedbackButton still light-themed (ORCH-0634); AccountSettings nested screens still light (ORCH-0628).

> **2026-04-21 ~21:15 UTC — ORCH-0626 CLOSED Grade A.** Six empty-state surfaces promoted from F-invisible to A-legible on dark-canvas screens. Surfaces touched: Connections (title+subtitle), Messages (DM empty), Friends list, Requests, Blocked users, Deck errors (SwipeableCards — "Couldn't load your deck" + auth_error + "That didn't land" + "Waiting for friends" + "No places found nearby"). Grade-distribution impact: +6 A on empty-state coverage within the existing dark-glass audit. No new unaudited surfaces introduced. Adjacent light-theme screens (modals, sheets, board, onboarding) explicitly verified untouched with rationale.

> **2026-04-21 ~08:30 UTC — ORCH-0595 Slice 3 (Upscale Brunch signal) CLOSED Grade A.** Signal-library architecture extended with third signal; now proves the pattern across 3 distinct signal configs tuned through device-observed calibration. Brunch scoring 1,449 Raleigh places with 286 at 150-200 cap tier after v1.3.0 upscale tune. Discovery surface grade distribution unchanged — signal-library is reinforcing architecture, not changing grades.

> **2026-04-21 ~07:45 UTC — ORCH-0590 Slice 2 (Drinks signal) CLOSED Grade A.** Discovery surface: signal-library architecture extended with second signal. Drinks scoring 1,449 Raleigh places (172 ≥120 filter_min, 53 at 200 cap), cohort-gated serving via generalized `CATEGORY_TO_SIGNAL` map in discover-cards. Surface grade distribution unchanged (Discovery was already Strong pre-Slice-2; second signal reinforces architecture but doesn't move grade buckets). New invariant registered: `I-CATEGORY-SIGNAL-ALIAS-COMPLETE` — every cohort-eligible chip has BOTH display name AND slug keyed in the map. Slice 1 (fine_dining) backward-compat aliases preserved as `[TRANSITIONAL]` until 2026-05-05.

> **2026-04-21 03:45 UTC — ORCH-0558 CLOSED Grade A.** Collaboration Sessions surface: 1 additional A from match promotion structural hardening (advisory lock + unique constraint + atomic RPC + telemetry observable). Surface distribution: 7 total → 4 A / 0 B / 0 C / 0 D / 3 F (was 3 A / 0 B / 0 C / 0 D / 4 F). Match-promotion path verified live via 10 MCP deploy probes + tester Tier A/B/F + device test on iOS + Android. 6 new invariants registered.

> Last updated: 2026-04-18 (**ORCH-0503 CLOSED A** — v3 fix PASS; Discovery surface grade distribution unchanged: the fix restores correctness inside an already-Strong-graded deck pipeline, so no grade-count movement; new invariant `I-PROGRESSIVE-DELIVERY-INTERLEAVE-AUTHORITATIVE` reinforces Constitutional #2 "One owner per truth" at the deck-sync-effect boundary)

## Surface Coverage

| Surface | Total | A | B | C | D | F | % Unaudited | % Stale | Confidence |
|---------|-------|---|---|---|---|---|-------------|---------|------------|
| Auth & Session | 7 | 2 | 4 | 1 | 0 | 0 | 0% | 0% | Partial |
| Onboarding | 11 | 2 | 0 | 0 | 0 | 9 | 82% | 0% | Weak |
| Discovery / Explore | 55 | 39 | 10 | 0 | 0 | 6 | 11% | 0% | Strong (↑ ORCH-0474 closed B — ORCH-0469, ORCH-0472 previously B) |
| **Place Pipeline (Seeding + AI Validation)** | **11** | **8** | **0** | **0** | **0** | **3** | **27%** | **0%** | **Strong (NEW 2026-04-17)** |
| Collaboration Sessions | 7 | 4 | 0 | 0 | 0 | 3 | 43% | 0% | Partial (↑ ORCH-0558 closed A — match promotion structurally proven deterministic + observable) |
| Social / Friends | 7 | 1 | 1 | 0 | 0 | 5 | 71% | 0% | Weak |
| Notifications | 11 | 6 | 2 | 0 | 0 | 3 | 27% | 0% | Partial |
| Saved / Boards | 5 | 0 | 0 | 0 | 0 | 5 | 100% | 0% | Unaudited |
| Profile & Settings | 10 | 4 | 1 | 0 | 0 | 5 | 50% | 0% | Weak |
| Map & Location | 16 | 0 | 0 | 0 | 0 | 16 | 100% | 0% | Unaudited |
| Chat / DM | 8 | 0 | 0 | 0 | 0 | 8 | 100% | 0% | Unaudited |
| Payments & Subscriptions | 15 | 8 | 6 | 1 | 0 | 0 | 0% | 0% | Strong |
| Calendar & Scheduling | 8 | 0 | 0 | 0 | 0 | 8 | 100% | 0% | Unaudited |
| Holidays & Events | 8 | 1 | 1 | 0 | 0 | 6 | 75% | 0% | Weak |
| People Discovery | 10 | 0 | 0 | 0 | 0 | 10 | 100% | 0% | Unaudited |
| Pairing System | 10 | 3 | 0 | 0 | 0 | 7 | 70% | 0% | Weak |
| Sharing & Invites | 10 | 0 | 0 | 0 | 0 | 10 | 100% | 0% | Unaudited |
| Post-Experience | 9 | 0 | 0 | 0 | 0 | 9 | 100% | 0% | Unaudited |
| Booking | 2 | 0 | 0 | 0 | 0 | 2 | 100% | 0% | Unaudited |
| Network & Offline | 5 | 0 | 1 | 0 | 0 | 4 | 80% | 0% | Weak |
| State & Cache | 8 | 5 | 0 | 0 | 0 | 3 | 38% | 0% | Partial |
| Chat Responsiveness | 4 | 4 | 0 | 0 | 0 | 0 | 0% | 0% | Strong |
| Hardening Infrastructure | 3 | 3 | 0 | 0 | 0 | 0 | 0% | 0% | Strong |
| Error Handling | 5 | 0 | 0 | 0 | 0 | 5 | 100% | 0% | Unaudited |
| Security & Auth | 13 | 5 | 0 | 1 | 2 | 5 | 38% | 0% | Weak |
| Deep Linking | 4 | 0 | 1 | 0 | 0 | 3 | 75% | 0% | Weak |
| App Lifecycle | 11 | 2 | 0 | 0 | 0 | 9 | 82% | 0% | Weak |
| Analytics & Tracking | 11 | 1 | 0 | 2 | 2 | 6 | 55% | 0% | Weak |
| Weather & External | 6 | 0 | 0 | 0 | 0 | 6 | 100% | 0% | Unaudited |
| UI Components | 10 | 3 | 0 | 0 | 0 | 7 | 70% | 0% | Weak |
| **TOTAL** | **303** | **100** | **24** | **4** | **4** | **171** | **56%** | **0%** | **Weak → Slightly improving** |

## Heatmap Summary

### Strong (>70% at A/B)
- Chat Responsiveness (4/4 A)
- Hardening Infrastructure (3/3 A)
- Discovery / Explore (42/52 A+B = 81%)
- Payments & Subscriptions (14/15 A+B = 93%)

### Partial (40-70% at A/B)
- Auth & Session (6/7 A+B = 86%)
- Notifications (8/11 A+B = 73%)
- State & Cache (5/8 A = 63%)

### Weak (<40% at A/B)
- Profile & Settings (4/10 = 40%)
- Collaboration Sessions (3/7 = 43%)
- Pairing System (3/10 = 30%)
- Security & Auth (5/13 A+B = 38%)
- All cross-cutting except Chat-Resp, Infra, and State & Cache

### Unaudited (>80% at F)
- Saved / Boards (100%)
- Map & Location (100%)
- Chat / DM (100%)
- Calendar & Scheduling (100%)
- People Discovery (100%)
- Sharing & Invites (100%)
- Post-Experience (100%)
- Booking (100%)
- Error Handling (100%)
- Weather & External (100%)
- Onboarding (82%)
- Holidays & Events (86%)
- Analytics & Tracking (88%)
- App Lifecycle (82%)

## Staleness Check

All evidence dates are within 7 days (latest: 2026-03-31). No stale items.
7 payment bugs closed 2026-03-31 — audit velocity resumed. All payment bugs now closed.
Security Wave 2: 4 items regraded (D/B/C/D → D/A/C/D), 3 closed (ORCH-0253, ORCH-0258, ORCH-0252), 8 new bugs registered 2026-03-31.
Deterministic Deck Contract: 5 closed (ORCH-0266/0267/0268/0038/0048), 2 upgraded (ORCH-0065 F→B, ORCH-0066 C→B). Discovery now 81% A+B.
State Persistence: 4 closed (ORCH-0209 F→A, ORCH-0240 B→A, ORCH-0270 new→A, ORCH-0271 new→A). State & Cache now 63% A. App Lifecycle loses its only B, now 2A/9F.
