# Decision Log

> Last updated: 2026-05-04 (close+++) (**DEC-098 logged — Cycle 16 decomposed into 16a (quick wins, OTA-able) + 16b (resilience infra, native rebuild required); operator locks D-16-1 split + D-16-2 separate Sentry project (same org as consumer app, new project `mingla-business`). 16a IN-FLIGHT next; 16b deferred until operator commits to native rebuild window.** Forensics returned H confidence per thread + H overall (M-H on J-X6 due to scope collapse). **3 decisions resolved by orchestrator-recommendation default-accept** (D-16-7 playful 404 voice "Hmm, that's not a real page." · D-16-8 splash min-visible 500ms · D-16-9 cross-cutting analytics DEFER to analytics ORCH). **3 decisions deferred to Cycle 16b dispatch** (D-16-3 force-update strictness · D-16-4 min-supported-version · D-16-5 offline UX). Decomposition: 16a ~13-17h (J-X3 ErrorBoundary at root + Sentry init wiring + J-X4 404 page + J-X5 splash sync + J-X6 permission consolidation camera+image-picker; deferred notifications + location speculative without surfaces) · 16b ~24-28h (J-X1 offline + J-X2 force-update; new deps `@react-native-community/netinfo` + `expo-updates`; native rebuild required). 7 forensics discoveries registered (D-CYCLE16-FOR-1..7). **D-CYCLE14-IMPL-3 false-alarm CLOSED** — `photosPermission` already configured in app.config.ts plugins block. **NEW invariant proposal I-36 ROOT-ERROR-BOUNDARY** ratifies post-16a CLOSE.) + 2026-05-04 (final) (**DEC-097 logged — Cycle 15 = Organiser Login + Email-OTP additive sign-in; 6 operator decisions locked pre-IMPL dispatch ("all agreed").** Forensics returned H confidence per thread + H overall. **2 decisions resolved by ingest without operator input** (D-15-1 WEB2 already shipped Cycle 0b · D-15-2 6-digit OTP code paste-back operator-locked pre-forensics). **2 decisions collapsed by mode choice** (D-15-4 redirect URL N/A for code-paste · ~~D-15-2 mode~~ pre-locked). Operator locked all 6 remaining in single batch ("all agreed"): Supabase default email provider for Cycle 15 · login page stays at `/` (existing Index gate) · extend BusinessWelcomeScreen with internal state machine (NOT separate `/login` route) · same-button sign-in vs sign-up via `shouldCreateUser: true` · rate-limit copy "Too many attempts. Wait a minute before trying again." · email-prefix display_name fallback · orchestrator authors cross-marketing coordination note. Decomposition: SINGLE Cycle 15 ~12-16h (down from 56h epic budget after scope collapse via WEB2-already-fixed + marketing-owned-elsewhere). 7 forensics discoveries registered (D-CYCLE15-FOR-1..7). NO new schema migrations + NO new dependencies + NO mutations to existing stores. Cycle 15 is mostly UI wiring on top of stable schema + already-shipping web bundle.) + 2026-05-04 (late) (**DEC-096 logged — Cycle 14 = Account / edit profile / settings / delete-flow / sign out; 14 operator decisions locked pre-SPEC dispatch.** Forensics returned H confidence per thread (Thread 2 M-H due to push-infra gap) + H overall. **3 decisions resolved by PR #59 schema findings without operator input** (D-14-12 use existing `creator_accounts.deleted_at` · D-14-15 `account_deletion_requests` is service-role-only B-cycle audit pipeline · D-14-16 brands.account_id IMMUTABLE per trigger forces soft-orphan). Operator locked all 14 remaining decisions in single batch ("all agreed"): email read-only via OAuth · profile photo via reuse `brand_covers` bucket with `profiles/` path prefix · React Query direct mutation persistence · 4 notification categories TRANSITIONAL toggles · GDPR-favored opt-in pattern · sign-out renamed to "everywhere" (already global default Supabase v2.39+) · itemized cascade preview · account-email type-to-confirm · 30-day recovery window · warn-don't-block on active events · preserve account.tsx baseline + ADD 3 new rows. Decomposition: SINGLE Cycle 14 (~48h match epic budget exact). 6 forensics discoveries registered (D-CYCLE14-FOR-1..6). NO new schema migrations + NO new dependencies + NO mutations to existing stores. PR #59 already shipped 80% of Cycle 14's schema; this is a UI-wiring cycle.) + 2026-05-04 (late) (**DEC-095 logged — Cycle 13 = End-of-Night Reconciliation Report; 11 architectural decisions locked pre-SPEC dispatch.** Forensics returned H confidence per thread + H overall. Operator locked all 11 recommendations in single batch ("all agreed"). Route at `app/event/[id]/reconciliation.tsx` always-visible permission-gated; NEW `MIN_RANK.VIEW_RECONCILIATION = finance_manager (30)`; ADVISORY-only discrepancies (D1+D2+D3 warm-orange · D4 informational); CSV-only export with `Gross/Refunded/Net` column extension + 5-row summary stanza prefix; PDF DEFERRED to B-cycle email-attachment-via-Resend; settlement-stub corrected to split online×0.96 + door×1.0 fixing D-CYCLE13-RECON-FOR-3 locally; audit_log integration DEFERRED entirely. Single ~24h cycle; 1 NEW route + 1 NEW utility + 4 MOD files; ZERO migrations + ZERO new dependencies + ZERO mutations to existing stores.) + 2026-04-22 (**DEC-045..DEC-049 logged — 8 spec-binding questions answered.** Rules Engine carve-out = rename + narrow (A). PhotoPoolManagementPage reattached to new Supply nav group. **MAXIMUM CONSOLIDATION MANDATE** — every edge function / RPC / page gets audited "can one thing do what three do?" — delete anything redundant or doing a job a surviving component can already do. **NO SHIM WINDOW** — deleted edge fns go away immediately; family-and-friends user base means fix-forward is cheaper than maintain-backwards. 7-day archive retention. Per-stop engagement fan-out: 4 events per curated interaction (3 stops + 1 container) for every event kind. `saves`/`experiences` audit + mobile curated stop-swap chain audit + `discover-experiences` live-caller audit all AUTHORIZED pre-spec.) + 2026-04-22 (DEC-044 NORTH STAR pinned — 3 gates) + 2026-04-19 15:29 UTC (**DEC-036 logged** — ORCH-0526 production flag flip AUTHORIZED under CONDITIONAL PASS: flag flipped to `true` at 15:29:27 UTC; user accepted the 4-BLOCKED-SC post-flip spot-check condition (24h window); rollback is single-SQL `UPDATE admin_config SET value='false'::jsonb` taking <5s) + 2026-04-19 (**DEC-035 logged** — ORCH-0526 M2 concurrency: status-based check instead of pg_advisory_lock; pg locks don't span multi-call HTTP handlers in Deno+Supabase JS; same effective 409-on-contention behavior; M1 lock helpers stay as future tool) + 2026-04-19 (DEC-034 ORCH-0526 v1 scope locked) + 2026-04-19 (DEC-033 ORCH-0525 ships direct-to-prod without AH-151 Release-build pre-flip gate) + 2026-04-19 (DEC-032 Path B for ORCH-0526 Rules Engine) + 2026-04-19 (DEC-031 `mingla_last_mode` architecture)

| ID | Date | Decision | Context | Alternatives Rejected | Tradeoff Accepted | Exit Condition |
|----|------|---------|---------|----------------------|-------------------|----------------|
| DEC-102 | 2026-05-05 (ORCH-0733 CLOSE) | **`run-place-intelligence-trial` edge function locks Gemini 2.5 Flash as sole AI provider; Anthropic dropped from active code path; PROMPT_VERSION=v4 with anti-VETO discipline + contradictory-evidence weighting locked.** Operator runtime sweep `e15f5d8f-2d0d-4318-8980-8d125c37730f` (32 anchors, $0.1292, 10:59 wall time, 31/32 success) confirms v4 prompt fixes the two quality drifts surfaced in v3 Gemini A/B run `fe15cb99-7577-474c-a3de-a961df917ccb`: (a) Anthony's Runway 84 / `romantic` 40 → 65 (clears 65 cutoff per operator-anchored truth "romantic nice dinner spot"); (b) Mala Pata × 6 false-positive vetoes (theatre/play/groceries/picnic_friendly/nature/flowers) ALL eliminated — now score 1-15 NOT VETO. Structural vetoes preserved correctly: Bayfront Floral / `flowers` VETO (event-only); Calusso / `brunch` VETO (`serves_breakfast=false`); Wang's Kitchen + Taza Grill / `brunch` VETO. Anchors steady-or-stronger: Calusso / `fine_dining` 95→95, National Gallery / `creative_arts` 98→98, Boxcar / `lively` 90→95, Wang's / `casual_food` 85→95. **Numbering note:** SPEC dispatch and IMPL report referenced DEC-101 for this lock pre-collision; Cycle 17b CLOSE (parallel session) used DEC-101 first; bumped here to DEC-102 per OPEN_INVESTIGATIONS + MASTER_BUG_LIST collision flag. **Single non-fatal flake:** Harris Teeter / `flowers` row failed with Gemini's `MALFORMED_FUNCTION_CALL` — non-deterministic Gemini-side quirk (same row passed in v3 Gemini run); auto-retry-once mitigation bundled into next dispatch (city-runs SPEC). | A/B comparison `fe15cb99` revealed Gemini scored 5-15 points HIGHER than Anthropic on positive matches AND over-fired VETO ~3× more (58 false-positive vetoes vs Anthropic's 19). Cost gap material: Anthropic v3 sweep $0.4141 vs Gemini v3 sweep $0.1212 (-71%). Quality gap addressable via prompt redesign rather than provider switch — proven by v4 sweep where prompt fixes restored quality at retained cost profile. Operator decision 2026-05-05 ("Lets drop anthropic, and fix gemini") explicitly preferred sole-provider simplicity over dual-provider toggle ergonomics. v4 prompt additions: 11 explicit anti-VETO examples (Mala Pata × 6 + Wang's + Big Ed's + Taza Grill + National Gallery — directly addressing Gemini's documented over-VETO pattern); WEIGHING CONTRADICTORY EVIDENCE section codifying Anthony's Runway 84 / `romantic` = 70-80 as canonical example (mood-deduction = 5-15, NOT 30, when documentary evidence already grades the place); 2 new calibration examples (Anthony's + Calusso/brunch). Per Const #7 reversibility: Anthropic helpers (`callQuestion`, `callAnthropicWithRetry`, `AnthropicUsage` interface, `MODEL_ID`/`ANTHROPIC_VERSION`/`ANTHROPIC_MESSAGES_URL` constants, `Provider` type) preserved as block-commented historical reference in `supabase/functions/run-place-intelligence-trial/index.ts` — re-enabling requires NEW DEC entry citing specific Gemini regression. Admin UI radio toggle removed; static "AI Provider · Gemini 2.5 Flash · v4 prompt" label + helper text "Locked sole provider. Anthropic dropped 2026-05-05 after A/B comparison." Legacy v1/v2/v3 Anthropic rows still render via `model.startsWith("gemini")` conditional in PlaceResultCard. | (A) Keep both providers behind toggle — rejected: dual-provider tax (2× prompts to maintain, 2× calibration sweeps, 2× cost models in admin) for no clear winner once v4 prompt fixed Gemini quality. Const #2 (one owner per truth) violation. (B) Keep Anthropic, drop Gemini — rejected: Gemini matches/beats v4 quality at 30% the cost AND faster wall time (1s throttle vs 9s Anthropic Tier 1 floor). Production city-runs (next dispatch) need the cost+speed envelope. (C) Lock v3 Gemini without anti-VETO + contradictory-evidence prompt fixes — rejected: 6 false-positive Mala Pata vetoes + Anthony's romantic 40 (operator-anchored 70-80) too far from operator truth to ship. (D) Defer Anthropic-drop until production city-runs reveal Gemini regressions — rejected: dual-provider code paths increase city-runs SPEC complexity; cleaner to lock now, revert via DEC entry only if specific regression surfaces. | Cost: $0.13 per 32-anchor sweep (-69% vs Anthropic). Wall time: ~11 min (comparable to Anthropic v3 12:23). Reliability: 31/32 = 96.9% per-row success on `MALFORMED_FUNCTION_CALL` flakes; auto-retry-once in city-runs SPEC will push to ~99.9%. Quality (vs operator-anchored truth on 12 critical spot-checks): all 12 PASS — Anthony's romantic clears 65 cutoff; Mala Pata × 6 anti-VETO discipline holding; structural vetoes preserved (Bayfront/Calusso/Wang's/Taza Grill brunch); cross-signal scores stable on anchor places (Calusso/fine_dining 95, National Gallery/creative_arts 98, Boxcar/lively 95). Minor anti-VETO bleed-through accepted: a few weak-fit signals lost VETO and now score 5-10 (Big Ed's fine_dining/flowers, Wang's fine_dining) — functionally identical (well below cutoffs). | Permanent for trial pipeline scope. Re-enabling Anthropic requires NEW DEC entry citing specific Gemini regression (e.g., model deprecation, structural quality drift on a future calibration sweep, or pricing change ≥3× current). Cross-references: ORCH-0733 CLOSE entry in OPEN_INVESTIGATIONS · CALIBRATION_LOG Entry 004 (v3 Gemini A/B) + Entry 005 (v4 Gemini PASS) · SIGNAL_TAXONOMY.md "RE-DERIVATION PENDING" warning · `IMPLEMENTATION_ORCH-0733_GEMINI_FIX_AND_ANTHROPIC_DROP_REPORT.md`. **DEC-103 reserved** for Gemini-specific cutoff re-derivation dispatch (current cutoffs derived from Anthropic Haiku 4.5 v2 calibration; treat as +5-10 inflated for Gemini matching until DEC-103 locks fresh values from a Gemini v4 calibration sweep). |
| DEC-101 | 2026-05-05 (Cycle 17b CLOSE) | **Cycle 17b (TopBar IA reset — structural-only + hardening-registry CI scaffold) — 6 architectural decisions locked at CLOSE.** D-17b-1 decomposition strategy: **structural-only (Small) ~4-5 hrs** (chosen over structural+badge or structural+full-screens; ratchet-risk reasoning + founder ask is layout-not-function). D-17b-2 `rightSlot=` prop fate: **keep as escape-hatch** (15 back-route consumers legitimately replace; deprecating would break working UX or require third "suppressDefault" prop). D-17b-3 search icon tap action: **W1 stay decorative** (no empirical operator demand in 16 cycles; wire later if pain validates as 17b-W mini-cycle). D-17b-4 bell icon tap action: **W5 stay decorative** (real notification feed is B-cycle territory — OneSignal SDK + edge fn + DB table; structural-only preserves architectural freedom). D-17b-5 hardening-registry CI workflow: **NEW pattern** at `.github/workflows/strict-grep-mingla-business.yml` + `.github/scripts/strict-grep/` directory designed as **registry where every future invariant CI gate plugs in as one script + one workflow job, no scaffold rewrite**. 17b's I-37 gate is the FIRST registered. Future I-32, I-34, I-36 each becomes ~2-3hr task using this scaffold. D-17b-6 I-37 coverage scope: **`leftKind="brand"` consumers ONLY** (primary tabs home/events/account; back-routes are out of scope — they intentionally suppress for focused-task UX). **NEW invariant I-37 ratified ACTIVE post-CLOSE 2026-05-05.** **NEW memory file `feedback_strict_grep_registry_pattern.md` flipped DRAFT→ACTIVE post-CLOSE.** | Operator locked all 6 in single batch 2026-05-05 ("small. We will address the larger design later / Lets keep rightlsot / Nothing / Nothing / Can we make workflow file moving forward, where we register hardening changes so nothing else breaks it. Adding this will be the first") via direct chat after orchestrator surfaced the 6-decision matrix with forensics recommendations. Forensics report `INVESTIGATION_BIZ_CYCLE_17B_TOPBAR_IA_RESET.md` H confidence — caught scope-narrowing finding (15 back-route consumers intentionally suppress; I-37 must scope to `leftKind="brand"` only) before I-37 over-fired. SPEC + IMPL + tester pipeline: 0 reworks; effort ~1.5 hrs vs 4-5 hr estimate (pattern repeat from 17a 2.5h vs 6h). Operator runtime smoke confirmed `[search, bell, +]` cluster + visual parity vs home tab. | (D-17b-1) structural+badge (W7) — rejected: ratchet risk; bell badge UX locks before B-cycle backend ships; +2 hrs effort for low founder-validated value. structural+full-screens (W2+W6) — rejected: 14-18 hrs spans 2-3 sessions; defines "notifications"/"search" UX before operators have used the new top-bar shape; locks B-cycle architecture. (D-17b-2) deprecate `rightSlot=` entirely — rejected: 15 back-route consumers legitimately replace; would either introduce wrong default (search+bell on Edit Brand) or require new "suppressDefault" prop, doubling complexity. (D-17b-3) W2 new search screen — rejected: no empirical operator demand in 16 cycles. W3 inline filter — rejected: only useful on events tab; nonsensical on home/account. W4 toast "Coming soon" — rejected: Const #1 dead-tap + Const #9 fabrication-adjacent. (D-17b-4) W6 new notifications screen — rejected: real feed is B-cycle territory; locks architecture. W7 badge-only — rejected: ratchet risk before B-cycle backend lands. (D-17b-5) extend existing workflow file — rejected: no existing strict-grep gate to extend; 17b ships first. Warning-only mode — rejected: warnings get ignored; fail-CI = teeth. (D-17b-6) every TopBar consumer — rejected: 15 back-routes intentionally suppress; treating as violation breaks 15 working surfaces. | (D-17b-1) structural-only ships in 1 session (~4-5 hrs estimate, actual 1.5 hrs); search/bell click wiring deferred to 17b-W if/when operators validate pain. Founder feedback "constant on top bar at all times" satisfied by visual presence; click action is separate question. (D-17b-2) `rightSlot=` becomes escape-hatch with documented patterns (null suppress / View placeholder / page-specific replace) in JSDoc. CI gate scopes to `leftKind="brand"` so back-routes preserve. (D-17b-3/4) Search + bell stay inert until B-cycle (real notification feed) or operator-validated demand. (D-17b-5) `.github/scripts/strict-grep/` directory + README documents 4-step "How to add a new gate" procedure. Future I-32/I-34/I-36 each ~2-3hr cycle using scaffold. (D-17b-6) Back-routes preserved verbatim; 15 consumers untouched in 17b diff. | Permanent for Cycle 17b IMPL boundary. EXIT signals per decision: (D-17b-1) Future operator pain on search/bell click action surfaces → 17b-W follow-up mini-cycle ships W2/W3/W6/W7 with concrete UX based on observed need. (D-17b-2) If a future cycle introduces "always replace right slot" valid use case on a primary tab → escalate to NEW DEC; current escape-hatch contract holds otherwise. (D-17b-3/4) B-cycle ships OneSignal SDK + notification feed → bell W6 wiring becomes natural. Search W2/W3 ships when operator validates demand. (D-17b-5) Future invariants (I-32 rank parity, I-34 canManualCheckIn decommission, I-36 ROOT-ERROR-BOUNDARY) each register via the 4-step pattern; if 5+ gates registered, consider adding `package.json` + shared `_helpers.mjs` per D-CYCLE17B-IMPL-5. (D-17b-6) Permanent — back-route suppression patterns are intentional UX. If design system pivots to per-tab top-bar variations, supersede via NEW invariant; do not silently relax I-37. Cross-references: `INVARIANT_REGISTRY.md` I-37 (ACTIVE), `.github/workflows/strict-grep-mingla-business.yml`, `.github/scripts/strict-grep/{i37-topbar-cluster.mjs,README.md}`, memory `feedback_strict_grep_registry_pattern.md` (ACTIVE), `FOUNDER_FEEDBACK.md` 2026-05-04 sub-item 2. |
| DEC-100 | 2026-05-04 (Cycle 17a SPEC §B.3) | **`mingla-marketing/` (Next.js founder-owned repo) is the canonical realisation of DEC-081 (separate marketing surface); supersedes any Cycle 15-era ambiguity about marketing scope.** Resolves documentation drift surfaced in D-CYCLE15-FOR-2. **Numbering note:** SPEC §B.3 asked for `DEC-099` but `DEC-099` was already taken by the ORCH-0713 architecture revision (close++++++); next-available `DEC-100` used here. | DEC-081 originally framed `mingla-marketing` (Next.js separate codebase) as DISCONTINUED in favor of `mingla-business` Expo Web for marketing landing. Reality at HEAD: founder-owned Next.js repo at `mingla-marketing/` was shipped during Cycle 15 forensics phase; DEC-086 locks founder ownership of that workstream. The two DECs together describe a discontinue-then-revive arc that future investigations re-discover painfully unless this entry pins the canonical reading: `mingla-marketing/` IS LIVE and IS founder-owned. Cycle 17 master inventory forensics surfaced this drift as D-CYCLE15-FOR-2; locked here per Cycle 17a §B.3 dispatch. | (A) Defer indefinitely — rejected: drift compounds in future investigations and trips orchestrator re-discovery (Cycle 17 forensics inventory already burned forensics-time on the contradiction). (B) Retract DEC-081 — rejected: DEC-081's discontinuation was correct AT THE TIME and the audit trail value of preserving the original retraction outweighs the cleanup. Better to add DEC-100 as a forward-looking clarification that supersedes the discontinue-then-revive arc. (C) Add a "DEC-081 SUPERSEDED" annotation inline — rejected: less searchable than a standalone DEC; mid-DEC annotations are a maintenance burden. | Treat `mingla-marketing/` as LIVE + founder-owned across all future cycle planning. No BIZ-cycle work touches this repo unless explicitly handed off via orchestrator coordination. Future investigations referencing the marketing landing find this entry first via DEC chronology and stop re-discovering the contradiction. | Permanent. EXIT signals: (a) Founder pivots away from `mingla-marketing/` Next.js codebase → DEC-100 replaced by a new entry retracting it. (b) Future cycle integrates `mingla-business` Expo Web with `mingla-marketing/` deploy — at that point a coordination DEC formalises the URL convention + cross-codebase contracts. Cross-references: D-CYCLE15-FOR-2 (forensics drift discovery) · DEC-081 (original discontinuation) · DEC-086 (founder ownership lock) · D-CYCLE17A-IMPL-DEC100-NUMBERING (this collision with prior DEC-099 reuse). |
| DEC-098 | 2026-05-04 (close+++) | **Cycle 16 (Cross-Cutting Polish) — DECOMPOSED into 16a + 16b; 2 operator decisions locked + 3 orchestrator-recommendation default-accept + 3 deferred to 16b.** D-16-1 (decomposition strategy): **SPLIT into Cycle 16a (~13-17h, OTA-able) + Cycle 16b (~24-28h, native rebuild)**. 16a covers J-X3 ErrorBoundary at root + Sentry init wiring + J-X4 404 page + J-X5 splash sync + J-X6 permission consolidation (camera + image-picker only; notifications + location DEFERRED — speculative without surfaces). 16b covers J-X1 offline + J-X2 force-update (both require new deps + native rebuild). D-16-2 (Sentry project): **NEW project `mingla-business` in existing Sentry organisation** (operator already has Sentry for consumer app `app-mobile/`; same org reuses billing + auth; separate project gives clean dashboards + alert routing + release isolation). Operator-side 8-step setup: Sentry Dashboard → Create Project (React Native) → DSN issued → Auth Token (project:write + release:admin) → `EXPO_PUBLIC_SENTRY_DSN` to `.env` + EAS Secrets (`SENTRY_AUTH_TOKEN` + `SENTRY_ORG` + `SENTRY_PROJECT`) → flip `SENTRY_DISABLE_AUTO_UPLOAD` to `false` in eas.json production. D-16-7 (404 brand voice): **playful "Hmm, that's not a real page." + "Go home" CTA** (matches Mingla Business friendly-and-direct tone — orchestrator-recommendation default-accept). D-16-8 (splash min-visible time): **500ms minimum** to avoid hard blink on fast networks (orchestrator-recommendation default-accept). D-16-9 (cross-cutting analytics on offline/error/404/permission-deny events): **DEFER to analytics ORCH** (Cycle 17 or B-cycle); Cycle 16 ships UX only (orchestrator-recommendation default-accept). **3 deferred to Cycle 16b dispatch** (NOT NOW): D-16-3 force-update strictness · D-16-4 min-supported-version policy · D-16-5 offline UX shape. **NEW invariant proposal I-36 ROOT-ERROR-BOUNDARY** — `app/_layout.tsx` MUST wrap `<Stack>` with `<ErrorBoundary>`; CI grep gate enforces. Ratifies DRAFT → ACTIVE post-16a CLOSE. **D-CYCLE16-FOR-3 closes D-CYCLE14-IMPL-3 as false alarm** — `photosPermission` in app.config.ts plugins block already covers NSPhotoLibraryUsageDescription. | Operator locked D-16-1 + D-16-2 in single message 2026-05-04 ("split 16 a first. I already have sentry for the consumer app. Do we need to create a different project, or can we use it? How do we go about this?") via direct chat (not AskUserQuestion batch). Forensics report `INVESTIGATION_BIZ_CYCLE_16_CROSS_CUTTING_POLISH.md` H confidence per thread + H overall. Phase 0 ingest surfaced 3 critical findings reshaping the cycle: (1) Sentry SDK installed but never initialized — ErrorBoundary `onError` undefined, all crashes silently swallowed (D-CYCLE16-FOR-1, S1-high root cause); (2) `app/_layout.tsx` lacks ErrorBoundary at root — crashes hit Expo Router default not Mingla branded fallback (D-CYCLE16-FOR-2); (3) D-CYCLE14-IMPL-3 NSPhotoLibraryUsageDescription concern is FALSE ALARM — already configured (D-CYCLE16-FOR-3 closes the prior IMPL discovery). Forensics' Cycle 16a + 16b split rationale: 16a is OTA-able quality fixes (no new deps); 16b requires `@react-native-community/netinfo` + `expo-updates` deps + native rebuild via `eas build` (NOT OTA-able). Bundling them together would force quick wins to wait for the native rebuild window. Operator confirmed split + provided context that consumer Sentry exists. | (D-16-1 alternatives) Single Cycle 16 ~36h — rejected: forces OTA-able quick wins to wait for native rebuild window of 16b; loses fast-feedback advantage. Per-journey micro-cycles (16-J1..16-J6) — rejected: 6 forensics + SPEC + IMPL + tester rounds; coordination overhead exceeds the work. (D-16-2 alternatives) Single Sentry project shared with consumer app — rejected: dashboard noise (organiser errors mixed with consumer auth issues at 100x volume), unreadable triage, mixed releases (mingla-business@1.5.0 alongside consumer@3.2.0), conflated alert thresholds (organiser-side errors are higher-stakes), separate billing/quota harder to reason about. New separate Sentry organisation — rejected: doubles auth + billing surface for no separation benefit; same-org-different-project gets all the isolation without the overhead. | (D-16-1) 16a ships immediately post-IMPL via `eas update --platform ios/android`. 16b deferred indefinitely until operator commits to native rebuild window — acceptable per family-and-friends MVP scale (offline + force-update infra not yet justified by traffic). 16b's deferred decisions (D-16-3/4/5) re-surface at 16b SPEC dispatch authorship time. (D-16-2) Operator must complete 8-step Sentry Dashboard setup before 16a IMPL can fully wire (or 16a ships ErrorBoundary at root + Sentry init structural-only as TRANSITIONAL with EXIT condition "DSN provisioned + env set"). `SENTRY_DISABLE_AUTO_UPLOAD` flip in eas.json from `true` to `false` enables source map upload on production builds — without this, stack traces in Sentry are unreadable (minified bundle line numbers only). 16a shipping pre-source-maps is acceptable for MVP; flip pre-production-launch. | Permanent for Cycle 16 IMPL boundary. EXIT signals per decision: (D-16-1) 16a ships → operator commits to native rebuild window → 16b SPEC dispatch authored against deferred decisions D-16-3/4/5; alternative if operator never validates need for offline + force-update infra at MVP scale, 16b stays deferred and ORCH closes as "deferred indefinitely until traffic justifies." (D-16-2) Sentry consumer-vs-business project separation permanent unless operator surfaces a "single dashboard wins on small ops team" use case (~unlikely; not a 1-org migration but a project recreation). (D-16-7) Future cycle could A/B test 404 voice if landing-page-style metrics ever matter (low priority pre-MVP). (D-16-8) Splash min-visible 500ms reviewable post-launch if metrics show user-perceived-launch-time issue. (D-16-9) Analytics ORCH ships cross-cutting events at Cycle 17 or B-cycle — at that point D-16-9 reopens with concrete event taxonomy. |
| DEC-099 | 2026-05-04 (close++++++) | **ORCH-0713 architecture REVISION — radical schema simplification: drop photo_aesthetic; drop signal_anchors; single JSONB column on place_pool for production rerank.** Three cumulative cuts driven by operator across this conversation: **Cut 1 (operator-driven)** — `photo_aesthetic_data` is superseded by trial-style evaluation. Photo-only signal proved too thin (Compare-with-Claude tab on 30 fixtures showed brunch detection failed on Dame's, Cracker Barrel — brunch evidence lives in reviews, not photos; aesthetic_score systematically conservative). Trial pipeline (collage + 30 reviews + place_pool fields) produces 30/32 anchor agreement (94%) — strictly more powerful at 2.5× cost. **Drop photo_aesthetic_data column from production path entirely.** **Cut 2 (operator-driven)** — single JSONB column on `place_pool` mirrors existing `photo_aesthetic_data` shape pattern. Per-(place, signal) production evaluations live in `place_pool.claude_signal_evaluations` JSONB keyed by signal_id. Schema: `{"fine_dining": {"confidence": 9.5, "strong_match": true, "inappropriate_for": false, "vibe_phrases": [...], "reasoning": "...", "evaluated_at": "...", "prompt_version": "v2"}, ...}`. Up to 16 keys per place; only signals where this place was evaluated. **Zero new production tables.** **Cut 3 (operator-driven)** — `signal_anchors` table served only calibration purpose (proved Claude works on 32-anchor run). Calibration done. Production iterates `place_scores` directly (top-N per signal/city). Signal taxonomy comes from `signal_definitions` (the same source Signal Library uses). **Drop signal_anchors from forward plan.** **Net schema delta:** 1 NEW JSONB column on place_pool · 0 NEW production tables · 4 DEPRECATED (photo_aesthetic_data column, photo_aesthetic_runs, photo_aesthetic_batches, signal_anchors). Schema gets SMALLER across Phase 1. **REVISION supersedes DEC-098 D-0713-1 + D-0713-6:** D-0713-1 supersedes — production rerank uses JSONB column not new cache table; ranking rule unchanged (inappropriate_for=true is hard veto; confidence is primary sort; score is tiebreaker). D-0713-6 supersedes — ORCH-0708 Phase 2 (photo_aesthetic prefix matchers in scorer + 5 signal v_next migrations adding photo weights) NEVER SHIPS. Cap raise (200→1000) on the 5 dining signals stays valid (independent of photo_aesthetic). All other DEC-098 decisions stand. **Read path:** `query_servable_places_by_signal_v2` reads `place_pool.claude_signal_evaluations->signal_id` directly — no LEFT JOIN to a cache table, no separate index lookup. **Write path:** `evaluate-place-for-signal` per-place edge function (refactored from `run-place-intelligence-trial`) writes via `UPDATE place_pool SET claude_signal_evaluations = COALESCE(claude_signal_evaluations, '{}'::jsonb) || jsonb_build_object($signal_id, $eval) WHERE id = $place_id`. Idempotent. **Phase 3 collapses into Phase 1:** original Phase 3 was "build place_claude_evaluations cache + RPC v2 + mobile vibe chips." With JSONB-on-place_pool, Phase 1 ships all of it. Phase 1 wall-time grows from ~3-4 days to ~5-6 days; Phase 3 dissolves; original Phase 2 (signal expansion + flowers tightening + anti-signals) still queued post-Phase-1 close. **Net new: 2 phases, not 3.** | Operator drove the revision across 3 turns: (1) "Why dont we replace the photo aesthetic with new intelligence trail to feed the scoring? what is the use of the old photo aesthetic? what did you plan for it?" — surfaced photo_aesthetic obsolescence; (2) "How will this replacement work, i wnat to make it as simple as possible since we already have the idea. I wnat to cut down on needless tables. from a strategic perspective explain to me how we incorporate this into the signal scoring system without inflating our database?" — drove single-JSONB-column architecture; (3) "we dont need the signal anchors. I wanted to see how good claude was and we did. We also already have the signals we use them in signal library, how does this change things?" — drove signal_anchors deprecation. Forensics audit `INVESTIGATION_ORCH-0713_PIPELINE_AUDIT.md` already established trial pipeline strength (30/32 = 94% anchor agreement; flagged 2 real false positives in `place_scores`); revision is logical consequence of treating that finding seriously. H confidence on simplification (mirrors existing photo_aesthetic_data pattern; zero new conventions); H confidence on production-rerank read path (single JSONB lookup). | (Cut 1 alternatives) Keep photo_aesthetic alongside trial as cheap-fallback layer rejected — Const #2 violation (parallel sources of truth for similar production data); Const #8 violation (don't add when subtracting suffices). Mass photo_aesthetic backfill rejected — pays for systematically-weaker signal at 1/2.5× cost when 32-anchor trial already proved photo-only is structurally insufficient. (Cut 2 alternatives) Separate `place_claude_evaluations` cache table rejected — operator's "cut down on needless tables" preference; place_pool already has photo_aesthetic_data JSONB precedent; LEFT JOIN to a cache table is slower than direct JSONB lookup; storage cost trivial (14k places × 16 signals × ~500 bytes ≈ 110MB at full scale). Single `place_intelligence_trial_runs` table for both research AND production with `purpose` enum column rejected — different lifecycles (research = forever-append; production = TTL-refresh) on same table = confusing access pattern; Const #2 spirit violation. JSONB column without per-signal staleness tracking rejected — operator needs "find places where Claude evaluation is stale > 30 days" sweep query; entry-level `evaluated_at` timestamp inside JSONB sufficient (Option A from strategic answer). (Cut 3 alternatives) Keep signal_anchors active for production candidate selection rejected — production picks top-N from `place_scores` directly per (signal, city); anchors were calibration-only and that job is done. Hand-pick 32 places per future trial rejected — when adding new signal in Phase 2, can pick via SQL filter (top-2 per signal) ad-hoc; no need for persistent table. | Phase 1 wall-time: ~5-6 days (was 3-4 in DEC-098; absorbs old Phase 3). Net new code: ~+1500-2000 LOC (orchestrator edge fn + admin page + evaluate-place-for-signal worker + RPC v2 + mobile vibe chip component + Compare-with-Claude validation gate). Net schema delta: +1 JSONB column on place_pool · +2 orchestrator state tables (unified_pipeline_runs/batches) · 4 deprecated. Cost: ~$60 full Triangle backfill (~$0.019 × 3,234 servable). Cache TTL: 30 days. Background pre-compute: top-100 per signal per city. ORCH-0712 trial pipeline preserved as research-only (I-TRIAL-OUTPUT-NEVER-FEEDS-RANKING) — historical reference + occasional ad-hoc when adding new signal in Phase 2. NEW invariants: I-CLAUDE-EVAL-RERANK-NOT-GATE (Phase 1 ratify ACTIVE — Claude reranks shortlist via JSONB; SQL `place_scores` is the eligibility authority); I-CLAUDE-SIGNAL-EVAL-COLUMN-OWNERSHIP (Phase 1 ratify ACTIVE — `evaluate-place-for-signal` is the SOLE writer of `place_pool.claude_signal_evaluations`). | Permanent for ORCH-0713 SPEC + IMPL boundary. EXIT signals per cut: (Cut 1) photo_aesthetic_data column dropped in cleanup ORCH post-Phase-1-CLOSE; score-place-photo-aesthetics edge function removed from active set; photo_aesthetic_runs + photo_aesthetic_batches tables dropped. (Cut 2) Future scale (>1M places) might justify dedicated rerank cache table — JSONB column shape is compatible (one-time ETL to flatten). (Cut 3) signal_anchors table dropped in cleanup ORCH; place_intelligence_trial_runs + place_external_reviews preserved as research/cache infrastructure. (Phase 1 validation gate unchanged from DEC-098 D-0713-8) Compare-with-Claude tab on 30 Triangle fixtures must show Claude evaluations shifting top-decile rankings vs without; if shifts noise-level, ORCH-0713 reconsidered. |
| DEC-098 | 2026-05-04 (close+++++) | **ORCH-0713 architecture lock — Claude reranks SQL gates (does not replace them); 3-phase scope.** D-0713-1 division of labor: SQL `place_scores` GATES eligibility (top-50 shortlist per signal/city via `query_servable_places_by_signal`); Claude RERANKS + VETOES on the shortlist via new `place_claude_evaluations` cache table. Claude is much stronger at semantic judgment but expensive ($0.019/place); SQL is fast/cheap. Cache is keyed on (place_pool_id, signal_id, ttl_expires_at). `inappropriate_for=true` is HARD VETO (drops the row); `confidence_0_to_10` is PRIMARY SORT; existing `score` becomes tiebreaker; `review_count` becomes tertiary. Cache miss falls back to SQL-only ordering (does not block user search). D-0713-2 Vibes vs Signals separation: vibes (Claude-extracted descriptive adjectives) live on the place CARD as display chips, NOT in the preferences sheet. Signals (16 today + 3 new in Phase 2) are user-pickable in preferences. D-0713-3 `flowers` semantics tightened: qualifies if user can grab ready-stock bouquet in ~5 min (admits dedicated florists + grocery flower aisles like Harris Teeter/TJ's/Wegmans; rejects event/wedding florists requiring weeks-ahead consultation like Bayfront Floral). D-0713-4 Three new signals locked for Phase 2: `lgbtq_safe_space` (demographic safety), `family_friendly` (kids welcome — distinct from `picnic_friendly` which is BYO-blanket), `cocktail_destination` (drink program is the draw — distinct from `drinks` venue-type). D-0713-5 NEW conceptual category — anti-signals (why-NOT filters): `noise_fatigue`, `reservation_friction`, `service_inconsistent`, `security_risk`, `accessibility_gaps` — schema shape (separate table vs JSONB column) decided at SPEC. D-0713-6 ABSORB ORCH-0708 Phase 2 into ORCH-0713 Phase 1: photo-aesthetic prefix matchers in `signalScorer.ts` ship alongside the unified per-city orchestrator (single coherent pipeline reshape; avoids two scorer-extension migrations). D-0713-7 Web answer evidence: Google AI Overview / Knowledge Graph via Serper `/search` endpoint with per-signal Q template library (~16 templates). D-0713-8 Phase ordering: Phase 1 must validate (Compare-with-Claude tab shows aesthetic data shifting top-decile rankings) before Phase 2 ships. Phase 2 must update ALL signal-list locations atomically. Phase 3 only after Phases 1 & 2 are CLOSED. **Decomposition:** 3-phase ORCH chain. Phase 1 ~3-4 days (orchestrator + photo-aesthetic wiring + cap raise). Phase 2 ~2-3 days (signal expansion + flowers tightening + anti-signal schema). Phase 3 ~3-5 days (Claude rerank pipeline + RPC v2 + mobile vibe chips + background pre-compute). | Operator drove the architecture across 4 conversation turns: (1) recognized Claude trial outputs are stronger than SQL scorer ("Claude is much more better, so we have to give its findings more weight?"), (2) clarified vibes-vs-signals separation ("vibes don't go in preferences sheet"), (3) corrected `flowers` semantics ("grocery flower aisles ARE included — only event-only florists excluded"), (4) requested unified per-city orchestrator ("ONE function that runs pre-bouncer + downloads photos + bouncer + scores"). Forensics audit `INVESTIGATION_ORCH-0713_PIPELINE_AUDIT.md` confirmed pipeline structure (6 stages, 6 edge functions, no orchestrator) + identified ORCH-0708 Phase 2 deferral (R-1 root cause: photo_aesthetic_data column shipped but signalScorer.ts doesn't read it; 30 Triangle fixture aesthetic scores are inert). Trial run on 32 anchors validated Claude calibration (30/32 anchor agreement = 94%; flagged 2 real false positives in `place_scores`). H confidence on architecture; M on cost projections at Triangle scale. | (D-0713-1 alternatives) Claude as full scorer (replacing place_scores entirely) rejected (cost: 14k+ servable places × 16 signals × $0.02 = $4,480 per full-Triangle backfill; SQL place_scores eligibility filtering is fast/cheap and excludes obviously-bad candidates before Claude evaluates). Claude on every user search rejected (latency + per-search cost; 50 places × $0.02 = $1/search; cache pre-compute is strictly better). (D-0713-2 alternatives) Vibes as filter chips in preferences sheet rejected (subjective taxonomy; combinatorial chaos; vibe-meaning drift across users). (D-0713-3 alternatives) Strict florist-only `flowers` rejected (loses Harris Teeter / TJ's / Wegmans grab-and-go which are legitimate destinations; operator's actual ask is "ready-stock bouquets in 5 min" not "dedicated florist"). Loose any-flowers-mention rejected (admits Bayfront-class event-only operators which require weeks-ahead consultation — wrong destination type for date-planning). (D-0713-4 alternatives) Single new signal at a time rejected (Phase 2 needs all 3 atomically because mobile preferences sheet UI ships once + signal-list locations must update atomically). (D-0713-5 alternatives) Skip anti-signals rejected (Claude trial surfaced 5 distinct anti-signals on 32 anchors; operator validated as conceptually new dimension). Bake anti-signals INTO existing scorer cap rejected (anti-signals don't fit cap=200/1000 magnitude; conceptually "why-NOT" not "why-YES"). (D-0713-6 alternatives) Ship ORCH-0708 Phase 2 separately first rejected (creates two scorer-extension migrations + two pipeline-touch dispatches when one coherent pipeline reshape is cheaper). Drop ORCH-0708 entirely rejected (30 Triangle aesthetic scores already exist + ORCH-0708 R-1 cap compression on dining signals is real). (D-0713-7 alternatives) Skip web answer evidence rejected (operator's Harris Teeter Google AI Overview test demonstrated structural facts reviews miss + Serper integration cost is negligible at $0.03/trial). Use SerpAPI third-party rejected (Serper already integrated). (D-0713-8 alternatives) Ship all 3 phases together rejected (cycle wall too long + can't validate Phase 1 hypothesis until shipped + scope creep risk). Ship Phase 2 + 3 together skipping Phase 1 rejected (Phase 1 unblocks 14k+ aesthetic scores + Phase 2 expansion lands on a working orchestrator vs broken-pipeline base). | Phase 1 absorbs ORCH-0708 Phase 2: ~+200 LOC scorer extension + 7 prefix matchers + 5 signal v_next migrations (cap raise to 1000 + photo-derived field weights). Phase 1 ~+800-1200 net LOC (orchestrator edge fn + admin page). Phase 2 ~+400-600 net LOC (3 signal definition migrations + flowers rule rewrite + anti-signal schema). Phase 3 ~+1500-2000 net LOC (rerank edge fn + RPC v2 + mobile vibe chip component + background pre-compute job + Google AI Overview evidence + Q template library). Cost ceiling: $50/month Triangle ramp-up. Cache TTL 30 days (re-evaluates on Serper review-count delta). Background pre-compute: top-100 places × 16 signals × city × $0.02 = ~$32 per city per refresh. ORCH-0712 trial pipeline preserved as research-only (I-TRIAL-OUTPUT-NEVER-FEEDS-RANKING) — feeds Phase 3 prompt iteration via human-readable Compare tab. NEW invariants ratified at each phase CLOSE: Phase 1 — `I-CLAUDE-EVAL-RERANK-NOT-GATE` (Claude reranks shortlist but never widens it; SQL `place_scores` is the eligibility authority). Phase 2 — invariant TBD at SPEC (signal-list-locations-atomic). Phase 3 — `I-VIBE-PHRASES-DISPLAY-ONLY` (vibe_phrases never enter preferences sheet). | Permanent for ORCH-0713 SPEC + IMPL boundary. EXIT signals per decision: (D-0713-1) Future cycle surfaces "real-time per-search Claude evaluation is acceptable cost" → background pre-compute can be reduced to top-N + on-demand fill. (D-0713-3) Future trial validates additional `flowers` admission criteria (e.g., farmers markets) → rule extends additively. (D-0713-4) Future Mingla signal expansion (e.g., `late_night`, `solo_friendly`, `kid_friendly_brunch`) — research trial validates demand first per ORCH-0712 Q1 pattern; signal-list-locations-atomic invariant ensures clean addition. (D-0713-5) Anti-signal dimension matures into structured filter UI (today: rerank-time downrank only; tomorrow: explicit user "exclude noisy places" toggle in preferences sheet — preserves D-0713-2 vibes-display-only by living in signals taxonomy). (D-0713-6) ORCH-0708 Phase 2 deferral closed at ORCH-0713 Phase 1 CLOSE. (D-0713-7) Phase 3 SPEC validates per-signal Q template quality on the same 32 anchors before production-ramps; if ≤70% useful answer rate, rework templates. (D-0713-8) Phase 1 validation gate: Compare-with-Claude tab on 30 Triangle fixtures must show aesthetic data shifting top-decile rankings vs without. If shifts are noise-level, ORCH-0713 Phase 2/3 reconsidered. |
| DEC-097 | 2026-05-04 (final) | **Cycle 15 (Organiser Login + Email-OTP additive sign-in) — 6 architectural decisions locked pre-IMPL.** D-15-3 email provider: Supabase default (Resend wires defer to B-cycle alongside notification infra). D-15-5 login page URL: stay at `/` (existing Index gate already routes signed-out users to BusinessWelcomeScreen; no separate `/login` route needed). D-15-6 UI placement: extend BusinessWelcomeScreen with internal state machine `idle → email-input → otp-input → otp-verifying` (matches Cycle 14 4-step delete-flow precedent; avoids RN sub-sheet rule by NOT using parallel sheets). D-15-7 sign-in vs sign-up UX: same button + auto-create on first sign-in via `shouldCreateUser: true` (matches existing OAuth pattern via `ensureCreatorAccount` upsert). D-15-8 rate-limit copy: "Too many attempts. Wait a minute before trying again." (explicit + actionable + 60s Resend cooldown UX). D-15-9 first email-OTP `display_name`: fallback to `user.email.split('@')[0]` (existing `ensureCreatorAccount` behavior; operator changes via Cycle 14 J-A1 edit-profile post-sign-in). D-15-10 cross-marketing coordination: orchestrator authors a one-liner coordination note for the other chat (URL convention for marketing site CTAs to point at) post-CLOSE. **2 decisions resolved by ingest without operator input:** D-15-1 WEB2 fix scope — RESOLVED (already shipped Cycle 0b commit `b2cc5daa` Apr 29; AuthContext Platform-gated + signInWithOAuth web branches + `detectSessionInUrl: Platform.OS === "web"` + auth/callback.tsx route + Apple button web gate + signOut navigation rework all live at HEAD). D-15-2 OTP mode — operator-locked pre-forensics 2026-05-04 to 6-digit code paste-back (NOT clickable link) for brand consistency with consumer-app SMS OTP UX. **1 decision collapsed by mode choice:** D-15-4 redirect URL N/A for code-paste mode (user stays in app and pastes code). **Decomposition:** SINGLE Cycle 15 ~12-16h (3 NEW AuthContext callbacks: `signInWithEmail` + `verifyEmailOtp`; 1 BusinessWelcomeScreen state machine extension; 1 index.tsx callback wire; 0 schema; 0 deps; 0 stores). | Operator locked all 6 remaining in single batch 2026-05-04 ("All agreed (Recommended)") via AskUserQuestion DEC-097 lock prompt after orchestrator REVIEW APPROVED forensics + SPEC. Forensics report `INVESTIGATION_BIZ_CYCLE_15_ORGANISER_LOGIN.md` H confidence per thread + H overall — Phase 0 ingest caught WEB2-already-fixed (Cycle 0b shipped Apr 29 per `IMPLEMENTATION_CYCLE_0b_WEB_AUTH_UNBLOCK.md`); marketing landing owned by `mingla-marketing/` Next.js separate codebase per DEC-086 founder-owned-workstream lock. Cycle 15 scope collapsed from epic 56h → 12-16h. Recovery on email-OTP sign-in inherits Cycle 14 v2 SIGNED_IN gate (I-35 contract preserved). | (D-15-3 alternatives) Resend now rejected (couples Cycle 15 to B-cycle backend infra; ~+1day cycle wall + new Supabase Auth → SMTP config + DNS verification; brand-consistent for now is Supabase default deliverability). (D-15-5 alternatives) Separate `/login` web route rejected (DEC-086 founder-owned-workstream split + existing Index gate already handles unsigned-in routing; doubles route count without value). (D-15-6 alternatives) Sheet overlay rejected (RN sub-sheet rule preempted; internal state machine matches Cycle 14 precedent + cleaner navigation). (D-15-7 alternatives) Separate sign-up flow rejected (matches OAuth pattern of auto-create-on-first-signin; doubles UX without value; Supabase v2 `shouldCreateUser: true` is canonical). (D-15-8 alternatives) Generic "Try again" rejected (Const #3 honesty — explicit cause + countdown is industry-standard); aggressive rate-limit gating rejected (Supabase defaults are appropriate for Cycle 15). (D-15-9 alternatives) Forced "What's your name?" first-sign-in step rejected (extra friction; J-A1 edit-profile already covers it; Cycle 14 J-A1 ships display_name editing). (D-15-10 alternatives) Operator handles handoff manually rejected (orchestrator-owned coordination — single source of truth for URL convention preferable). | (D-15-3) Supabase default email deliverability is acceptable for Cycle 15 organisers (low volume); B-cycle wires Resend for transactional volume. (D-15-5) Pre-deploy login URL is `localhost:8081` per DEC-077 local-only-dev; production URL deferred to post-Cycle-17 deploy per DEC-076 + DEC-081 + DEC-086. (D-15-6) Internal state machine increases BusinessWelcomeScreen LOC by ~+280 (current 547 → ~827); maintainable per existing animation/state pattern. (D-15-7) First email-OTP sign-in produces auth.users + creator_accounts row in same flow as Google/Apple — UX consistent. (D-15-9) `display_name = "joe"` from `joe@example.com` is acceptable initial copy; operator changes in J-A1 within minutes of first sign-in. ~+340 net LOC (3 callbacks + state machine + render branches + handlers). 0 NEW migrations + 0 NEW dependencies + 0 mutations to existing stores. EAS OTA-able post-CLOSE. | Permanent for Cycle 15 IMPL boundary. EXIT signals per decision: (D-15-3) B-cycle wires `Resend` as Supabase Auth SMTP provider → email-OTP delivery upgrades to Resend's deliverability/analytics with no Cycle 15 code change. (D-15-6) Future cycle introduces additional sign-in modes (e.g., phone-OTP, passkey) → state machine extends additively. (D-15-7) Future cycle differentiates sign-in vs sign-up UX (e.g., for marketing tracking) → splits into 2 routes; current shape preserves the choice. (D-15-9) Future polish cycle adds optional "What's your name?" first-sign-in step (~3-5 LOC). (D-15-10) Founder marketing site (`mingla-marketing/`) integrates with deployed mingla-business URL post-Cycle-17. |
| DEC-096 | 2026-05-04 (late) | **Cycle 14 (Account: edit profile, settings, delete-flow, sign out) — 14 architectural decisions locked pre-SPEC.** D-14-1 email read-only via OAuth (no email-OTP path; Mingla Business is Google + Apple only). D-14-2 profile photo storage via reuse `brand_covers` bucket with `profiles/{userId}.{ext}` path prefix (verify bucket existence at SPEC §4 + add path-prefix RLS). D-14-3 profile persistence via direct React Query mutation against `creator_accounts` (UPDATE RLS allows self-write; matches Cycle 13a `useCurrentBrandRole` React Query pattern; NO new Zustand store). D-14-4 notification categories: tight 4 (Order activity / Scanner activity / Brand team / Marketing). D-14-5 push infrastructure: TRANSITIONAL toggles only — Cycle 14 ships UI persisted in Zustand; B-cycle wires OneSignal SDK + Resend + edge fn + `user_notification_prefs` table. D-14-6 default opt-in: transactional ON · marketing OFF (GDPR-favored; matches existing `creator_accounts.marketing_opt_in DEFAULT false`). D-14-7 notification persistence via Zustand `useNotificationPrefsStore` TRANSITIONAL persist; sync `marketing_opt_in` to existing `creator_accounts` schema column ONLY (other 3 categories Zustand-only until B-cycle). D-14-8 sign-out-everywhere: rename existing button "Sign out" → "Sign out everywhere" with caption "Signs you out on every device" (Supabase JS v2.39+ defaults `signOut()` to `{ scope: 'global' }` — current button is already everywhere; cosmetic relabel only; no separate single-device button). D-14-9 active sessions list: omit (Supabase Auth doesn't expose; not industry-standard; sign-out-everywhere without list mirrors Stripe/Apple/GitHub). D-14-10 cascade preview: itemized full counts (brands owned + brands team-member-of + live events + sold orders with £total + comps + scanner invitations + team invitations). D-14-11 type-to-confirm: account email (industry-standard; harder to typo than literal "DELETE"). D-14-13 recovery window: 30 days per cycle-14.md + industry standard (Apple/Google/Stripe). D-14-14 active events check: warn loudly with itemized cascade preview, do NOT block (block is GDPR-tense paternalism + would conflict with right-to-erasure). D-14-17 account.tsx baseline: PRESERVE existing structure (TopBar + email + brand list + dev seeds gated `__DEV__`); ADD 3 new rows (Edit profile / Notifications / Delete account) + RENAME sign-out button. **3 decisions resolved by PR #59 schema findings (no operator input):** D-14-12 use existing `creator_accounts.deleted_at` column (already shipped) — UPDATE RLS allows self-write; D-14-15 `account_deletion_requests` table audit pipeline already exists with service-role-only writes (B-cycle edge fn writes it); D-14-16 `brands.account_id IMMUTABLE` per `biz_prevent_brand_account_id_change` trigger forces Option B soft-orphan (no force-transfer possible at DB level). **Decomposition:** SINGLE Cycle 14 ~48h matching epic budget exact (tight UX coupling + hub-and-spoke nav + cascade preview aggregator small ~120 LOC + schema 100% ready). | Operator locked all 14 in single batch 2026-05-04 ("all agreed") after forensics presented recommendations + alternatives in side-by-side table. Forensics report `INVESTIGATION_BIZ_CYCLE_14_ACCOUNT.md` H confidence per thread + H overall. PR #59 already shipped 80% of Cycle 14's schema (`creator_accounts.deleted_at` + `account_deletion_requests` + `marketing_opt_in` + `phone_e164`) — Cycle 14 is mostly UI wiring to existing infrastructure, NOT new schema work. Push notification deps verified absent (0 hits to `react-native-onesignal` / `expo-notifications` / `@notifee` in package.json) — TRANSITIONAL-only push toggles is the right call given infrastructure gap. Email-OTP path verified absent in mingla-business — Google + Apple only — collapses D-14-1 to read-only. Supabase JS v2.39+ default scope is `global` per upstream docs — collapses D-14-8 to rename-only. Existing consumer-app `delete-user` edge function (touches `profiles`/`collaboration_sessions`/`place_reviews`) provides B-cycle architectural model — confirms business-schema delete-account edge fn is genuinely B-cycle territory. | (D-14-1 alternatives) full email change with re-verification rejected (no email-OTP path means change requires switching OAuth providers — not a Cycle 14 surface). (D-14-2 alternatives) NEW `profile_photos` bucket rejected (reuse simpler; mirrors brand cover pattern); LOCAL-only TRANSITIONAL rejected (storage works today via existing self-write RLS). (D-14-4 alternatives) wide 7 categories rejected (operator hasn't validated event-day reminder + weekly digest demand); just-1 rejected (too thin for "settings cluster" UX). (D-14-5 alternatives) wire OneSignal NOW rejected (~+1 day cycle wall + new dep + edge fn + RLS scope expansion; cycle-14.md ~48h budget already tight); defer entirely rejected (account tab needs visible settings cluster). (D-14-8 alternatives) add separate "single device" button rejected (security-favored default + power-user feature operator hasn't surfaced); explicit "everywhere" CTA on top of existing rejected (UX duplication). (D-14-10 alternatives) tight count-only rejected (GDPR right-to-know mandates itemization); custom prose rejected (Const #9 fabricated-data risk). (D-14-11 alternatives) literal "DELETE" rejected (typo-confirmable); both rejected (friction overhead). (D-14-14 alternatives) block-deletion rejected (GDPR-tense + paternalism; right-to-erasure should not be blocked by user-owned data); force-transfer-first rejected (impossible per `biz_prevent_brand_account_id_change` IMMUTABLE trigger). (D-14-17 alternatives) rewrite-from-scratch rejected (preserves Cycle 0a/1 invest + dev seeds operator uses + sign-out + email + brand list all serve users today). | (D-14-1) Operators using Apple privacy-relay email may need to also remove Mingla from appleid.apple.com; document in delete-flow Step 1 warning copy. (D-14-5) Native push delivery deferred to B-cycle; toggles ship as TRANSITIONAL placeholders with explicit "delivery wires up in B-cycle" caption per Const #7 + #9 honesty. (D-14-13) 30-day window during which operator can recover account by signing in (D-CYCLE14-FOR-6 SPEC-handoff: lock recover-on-sign-in to auto-clear marker UX). (D-14-14) Operator with active events deleting account loses brands + events on hard-delete day 31 unless they sign in within 30 days; UI must surface this loudly. ~+1100-1400 net LOC (3-4 NEW routes + 1-2 NEW utilities + 2-3 NEW components + ~5-6 MOD touches). 0 NEW migrations + 0 NEW dependencies + 0 mutations to existing stores. EAS OTA-able post-CLOSE. | Permanent for Cycle 14 IMPL boundary. EXIT signals per decision: (D-14-2) `brand_covers` bucket exists confirmed at SPEC §4 → ships; absent → SPEC pivots to NEW `profile_photos` bucket OR LOCAL-only TRANSITIONAL. (D-14-5) B-cycle ships `OneSignal SDK + user_notification_prefs table + edge fn + Resend wire-up` → 4-toggle TRANSITIONAL Zustand store contracts to React Query consumer of real prefs schema. (D-14-12) B-cycle ships `request-account-deletion` edge fn + 30-day cron + `auth.admin.deleteUser` + CASCADE through ~80 tables (mirrors consumer app `delete-user` pattern) → mobile soft-delete UI gains automatic hard-delete pipeline. (D-14-15) B-cycle audit_log writers ship → reconciliation route + brand audit-log views populate; Cycle 14 doesn't write audit_log entries today. (D-14-16) Future cycle (Cycle 15+ or B-cycle) operator surfaces "transfer brand ownership to co-founder" use case → migration drops or relaxes `biz_prevent_brand_account_id_change` trigger; D-CYCLE14-FOR-4 documents this for future-cycle awareness. |
| DEC-095 | 2026-05-04 (late) | **Cycle 13 (End-of-Night Reconciliation Report) — 11 architectural decisions locked pre-SPEC.** D-13-1 NEW route at `app/event/[id]/reconciliation.tsx` (mirrors Cycle 12 J-D5 pattern; SUPERSETS not duplicates). D-13-2 always-visible permission-gated CTA on Event Detail; copy adapts per status (live/upcoming/past/cancelled). D-13-3 NEW `MIN_RANK.VIEW_RECONCILIATION = finance_manager (30)` matching refund-action ranks. D-13-4 discrepancy taxonomy: D1 auto-check-in mismatch + D2 method-sum mismatch + D3 refund-status mismatch ALL ADVISORY-only; D4 unscanned reframed as "no-shows" (informational, NOT discrepancy). D-13-5 warm-orange `accent.warm` for D1/D2/D3. D-13-6 "No-shows" terminology on past events. D-13-7 PDF DEFERRED to B-cycle email-attachment-via-Resend; ship CSV-only via existing `serializeGuestsToCsv` extension (verifies no `expo-print` / `expo-sharing` / `expo-file-system` in `mingla-business/package.json`). D-13-8 CSV extends with `Gross / Refunded / Net` columns + 5-row summary stanza prefix for accountant-import friendliness. D-13-9 filename `{slug}-reconciliation-{YYYY-MM-DD}.csv` (mirrors Cycle 10 + 12). D-13-10 settlement-stub corrected: split online×0.96 + door×1.0 (fixes D-CYCLE13-RECON-FOR-3 over-deduction for door-heavy events locally; Event Detail KPI card stays simplified as separate follow-up candidate). D-13-11 audit_log integration DEFERRED entirely (B-cycle territory; Cycle 13b RLS gate exists but no client writers yet). | Operator locked all 11 in single batch 2026-05-04 ("all agreed") after forensics presented recommendations + alternatives in side-by-side table. Forensics report `INVESTIGATION_BIZ_CYCLE_13_END_OF_NIGHT_RECONCILIATION.md` H-confidence per thread + H overall. Cycle 13 is reads-only over 4 already-shipping persisted Zustand stores (orderStore + doorSalesStore + guestStore + scanStore) — clean-slate (no existing reconciliation route beyond Cycle 12 J-D5 door-only card). Permission rank choice deliberate: matches refund-action ranks since reconciliation is the audit lens on those refunds; brand_admin (50) was rejected as over-restrictive (every venue's finance team needs view access; Cycle 13a finance_manager rank already covers that authority). PDF deferral keeps cycle OTA-able (no `eas build` required) + accountant-handoff workflow is better served by CSV (Excel-importable) than locked-down PDF. | (D-13-1 alternatives) inline-on-Event-Detail rejected (already heavy 7-tile + KPI + activity feed surface) · auto-modal rejected (heavyweight UX). (D-13-3 alternatives) event_manager (40) rejected (too permissive for finance-flavoured surface) · brand_admin (50) rejected (over-restrictive; every venue finance team needs view access). (D-13-4 alternatives) blocking on D1 rejected (no current "close-event" lifecycle to block) · different taxonomy rejected (4-class set covers all data integrity surfaces forensics could derive from the 4-store data shapes). (D-13-7 alternatives) add `expo-print` rejected (requires `eas build` not OTA; +1 day cycle wall · pulls ~3MB native dep) · HTML-to-Share rejected (cosmetic only, no real PDF artifact). (D-13-8 alternatives) keep current shape rejected (accountants need refund attribution per row) · summary-only rejected (loses per-row drill-down). (D-13-10 alternatives) hide payout entirely rejected (operator wants visibility) · "B-cycle pending" placeholder rejected (less useful than corrected stub). (D-13-11 alternatives) include audit_log with TRANSITIONAL banner rejected (audit_log writers don't exist in client today; surfacing empty / stub-data violates Const #9 no fabricated data). | (D-13-7) accountants on iOS/Android share-sheet receive CSV as text content (degraded native UX per pre-existing TRANSITIONAL D-CYCLE10-IMPL-1) until B-cycle ships email-attachment-via-Resend. (D-13-10) Event Detail KPI card payout (line 416 of `app/event/[id]/index.tsx`) keeps the simplified `revenueGbp × 0.96` while reconciliation route uses corrected split — divergence acceptable because reconciliation is the audit source-of-truth; Event Detail KPI is at-a-glance number; future small ORCH (D-CYCLE13-RECON-FOR-3-FIX) optional to align them. (D-13-11) audit_log entries from operator team-management actions (Cycle 13b) won't surface on reconciliation route until B-cycle wires audit_log writers — until then the screen has no audit_log section at all (no banner, no stub). Cycle 13 ships ~24h; 1 NEW route file (~700 LOC) + 1 NEW utility (~150 LOC) + 4 MOD files (event/[id]/index.tsx ActionTile + permissionGates VIEW_RECONCILIATION constant + guestCsvExport.ts extension + door/index.tsx polish CTA per D-CYCLE13-RECON-FOR-4) + ZERO migrations + ZERO new dependencies + ZERO mutations to existing stores. | Permanent for Cycle 13 IMPL boundary. EXIT signals per decision: (D-13-3) B-cycle wires server-side reconciliation RPC → RLS policy MUST mirror finance_manager+ rank-gate on backend (matches `MIN_RANK.VIEW_RECONCILIATION = 30` per I-32); operator can re-evaluate whether VIEW_RECONCILIATION should escalate to brand_admin (50) at that boundary. (D-13-7) B-cycle email-attachment-via-Resend ships → "Email PDF report" CTA enables (today disabled with caption "B-cycle"); HTML-to-PDF render on backend. (D-13-10) Real Stripe payout API integration replaces 4% stub for online; Stripe Terminal SDK (B-cycle) provides real card_reader/nfc fee schedules → split refines further. (D-13-11) B-cycle ships audit_log writers → reconciliation route adds optional "Operator timeline" section; gates remain finance_manager+ but section content reads from `audit_log` via React Query. (D-13-4) If operator surfaces real "must close night before next event" workflow → Cycle 13b polish dispatch flips D1 + D2 to BLOCKING with "Resolve to close" CTA. |
| DEC-094 | 2026-05-04 | **Cycle 13b CLOSE — permissions_matrix table DROPPED + audit_log brand-admin SELECT policy ADDED + canManualCheckIn field SUBTRACTED.** Migrations `20260504100000_b1_phase7_drop_permissions_matrix.sql` + `20260504100001_b1_phase7_audit_log_brand_admin_select.sql` applied via `supabase db push` 2026-05-04. Mobile-side: 4 files modified (scannerInvitationsStore.ts persist v1→v2 + InviteScannerSheet.tsx + scanners/index.tsx + audit-log.tsx banner copy). 2 invariants ratified post-CLOSE: I-33 (permissions_override jsonb deny-list shape) flipped DRAFT → ACTIVE; I-34 (permissions_matrix DECOMMISSIONED) NEW. I-24 amended with multi-policy OR-merge note for audit_log. Memory file `feedback_permissions_matrix_decommissioned.md` flipped DRAFT → ACTIVE. Operator device smoke ALL PASS. | Operator device smoke ALL PASS post-deploy 2026-05-04. Cycle 13b forensics + SPEC + IMPL + reworks-zero pipeline ran clean (rare for a BIZ cycle — testament to the YAGNI-driven scope discipline + verbatim-contract SPEC). | DEC-093 already locked the architectural decisions; DEC-094 codifies the EXECUTION + CLOSE event for the audit trail. | Operator runs `supabase db push` once; no rollback path beyond manual `CREATE TABLE permissions_matrix` re-creation (which would require explicit DEC-094-override) since the 5 sentinel seed rows are gone. Acceptable per Cycle 13b forensics — no operational data loss. | Permanent. EXIT signals: (a) Future cycle re-introduces permissions_matrix with operator-validated runtime-mutable use case → DEC override + new migration. (b) B-cycle ships audit_log writers → Cycle 13a TRANSITIONAL banner retires + audit log starts populating. (c) D-CYCLE13B-IMPL-1 (P3 process improvement re strict-grep gates) feeds future deprecation SPEC review checklist updates. |
| DEC-093 | 2026-05-04 | **Cycle 13b — Per-event role assignments for non-scanner roles DEFERRED ("brand-level only" until validated use case).** Cycle 13b forensics investigation thread 3 found ZERO empirical operator demand for per-event role scoping for `event_manager`/`finance_manager`/`marketing_manager`/`brand_admin`/`account_owner` roles (only `scanner` role has per-event scoping today via `event_scanners` table). PRD §11.2 mentions "restrict by event" as a permission DIMENSION but no entry on PRIORITY_BOARD or in operator-research feedback channel validates a real workflow. Three architectural options surveyed: (A) extend `brand_team_members` with optional `event_id` column · (B) parallel `event_team_members` table mirroring `event_scanners` shape · (C) encode event scope in `permissions_override` jsonb. All three deferred — building any without a use case is over-engineering per Const #8 (subtract before adding) + YAGNI. | Operator-locked 2026-05-04 ("3. Agreed") on forensics recommendation. Cycle 13a's brand-level role + per-action MIN_RANK gates already covers all empirically-surfaced operator workflows. Building per-event scoping speculatively risks schema lock-in on a shape that may not match the eventual real ask. | (A) extend brand_team_members with event_id — rejected speculatively without use case; doubles unique-index complexity + forces every RLS policy to consider eventId. (B) parallel event_team_members — rejected; duplicates data shape (role + override) across two tables; 2 invitation flows; 2 stores. (C) jsonb event_scope in permissions_override — rejected speculatively; would couple Threads 2+3 prematurely. | Per-event role distinction for non-scanners is unavailable until operator validates a real workflow. Acceptable: Cycle 13a + 13b cover the brand-level cases; if/when operator surfaces "I need to give Tunde event_manager role for ONLY the December launch," Option C (jsonb encoding) ships in <1 day on top of locked I-33-DRAFT shape. | Deferred until operator surfaces a real per-event role use case. EXIT signals: (a) operator says "I need event-scoped non-scanner roles" with a concrete example → unlock thread 3; pick from A/B/C based on use-case scale. (b) Multiple operators independently ask → upgrade to native schema (Option A or B). (c) Single operator with 1-2 events → Option C jsonb encoding. |
| DEC-092 | 2026-05-04 | **Cycle 13a Path A — Subtract J-A9 BrandTeam cluster, replace with 13a contracts.** Mid-IMPL discovery: Cycle 13a SPEC §4.8/§4.10/§4.11 collide with pre-existing J-A9 (§5.3.9) team cluster. Resolution: DELETE 5 files (`app/brand/[id]/team/index.tsx`, `app/brand/[id]/team/[memberId].tsx`, `src/components/brand/{BrandTeamView,BrandInviteSheet,RolePickerSheet}.tsx`); v8 migration in `currentBrandStore` drops `members?`, `pendingInvitations?`, `BrandMember`, `BrandInvitation`, `BrandMemberRole`, `InviteRole`; new SPEC files land at `src/components/team/...` + `app/brand/[id]/team.tsx` + `app/brand/[id]/audit-log.tsx` (keep `[id]` segment to avoid expo-router conflict — SPEC's `[brandId]` was editorial). New `brandTeamStore` is canonical authority; `useCurrentBrandRole` synthesizes self-row for solo operators. Establishes D-CYCLE13-IMPL-1 (forensics gap: J-A9 missed by Cycle 13 investigation; pattern → forensics MUST grep `app/brand/` AND `components/brand/` AND `currentBrandStore` for any team/permissions work). | Operator directive 2026-05-04: "PATH A LETS PROCEED" after layman explanation surfaced the collision. J-A9 was incomplete vs SPEC (no audit log, no synthesis fallback, no rank-based gating, wrong role enum `'owner'` instead of `'account_owner'`); replacing it with Cycle 13a's stricter contract is a net upgrade. Const #2 (one owner per truth) + #8 (subtract before adding) demand consolidation. | (B) Layer 13a permissions over J-A9 in place — rejected: SPEC v4 rewrite of §4.7-§4.11; preserves wrong-shape role enum; ~30min SPEC churn. (C) Coexist (move new SPEC files to non-colliding paths) — rejected: explicit Const #2 violation; tester would correctly P0. | Persisted local state in `currentBrandStore.pendingInvitations` (UI-only TRANSITIONAL since J-A9) loses entries on next launch — acceptable per I-31. v7→v8 persist migration silently strips dropped fields. ~30-45min added to Cycle 13a IMPL wall. | Permanent for Cycle 13a. `[id]` route segment + `brandTeamStore` are canonical. EXIT signals: (a) B-cycle wires `invite-brand-member` + `accept-brand-invitation` per I-31; brandTeamStore contracts to cache. (b) Cycle 13b extends `BrandTeamEntry` if per-event role assignments land. |
| DEC-089 | 2026-05-03 | **ORCH-0706 — Audit-log + scan_events append-only carve-out (Option B).** `audit_log` and `scan_events` tables remain append-only for non-service-role callers, but service_role retains UPDATE/DELETE permission for reconciliation jobs and migration scripts. Carve-out documented verbatim in `COMMENT ON TABLE` for both tables. Trigger functions UNTOUCHED — PR #59 already correct under Option B semantics; only the table comments rewritten to honestly disclose the carve-out so future readers don't mistake the tables for strictly tamper-proof. | Reconciliation jobs are an operational reality (partial scanner sync repair, double-charged refund recovery, mis-attributed door sale). Strict-no-mutations-ever (Option A) creates real on-call pain the first time bad data lands and we cannot fix it. PR-author Fehintola general green-light (PR #59 issuecomment-4364474041) authorized SPEC defaults; Q2 audit-log intent question deferred to operator default = Option B. | (A) Strict no-mutations-ever (drop service-role short-circuit; require migration-script SET ROLE postgres for any fix) — rejected: Option B costs only one comment string; Option A creates midnight-emergency migration risk. (C) Hybrid (allow service_role UPDATE but not DELETE) — rejected: arbitrary partial restriction; either trust service_role or don't. | Slight risk of service-role code accidentally mutating audit data via reconciliation bugs. Mitigation: COMMENT ON TABLE makes the carve-out visible to any future investigator reading schema; future SOC2-grade audit could flip to Option A in ~8 LOC change per trigger function. | Permanent for the audit_log + scan_events tables. EXIT signals: (a) SOC2 Type II / forensic-audit-trail certification needs flip to strict Option A — drop the service-role short-circuit, ~8 LOC per function. (b) Schema evolves to extract reconciliation-write paths into a separate edge-function-only API surface, at which point Option A becomes free. |
| DEC-088 | 2026-05-02 | **ORCH-0708 — Score `cap` raised 200 → 1000 on 7 dining/family signals (fine_dining, brunch, romantic, lively, casual_food, drinks, icebreakers); other 9 signals stay at cap=200. Driven by Wave 2 Phase 1 photo-aesthetic-scoring integration.** Audit (live DB, Mingla-dev) showed cap-compression at top is real and material on 4 dining signals: casual_food 1,008 places at exactly score=200 (7.0%), brunch 720 (5.0%), drinks 508 (3.5%), icebreakers 384 (2.7%). Raw uncapped scores recomputed from `contributions` JSONB reach 459 (casual_food), 456 (drinks), 375 (brunch), 361 (icebreakers) — meaning a flagship venue raw=400 and a mediocre venue raw=200 both currently clamp to the same 200. Adding photo-derived field weights (up to ±170 swing per place per signal) requires headroom. cap=1000 buys 5x headroom and preserves all current discrimination. Other 9 signals (movies, theatre, scenic, picnic_friendly, creative_arts, play, nature, flowers, groceries) have cap-compression rates ≤0.4% — no current need to raise. clamp_min stays 0 across all signals; `min_rating` + `min_reviews` hard gates unchanged. filter_min thresholds at the serving layer (typically 80–120) STAY ABSOLUTE in this dispatch — re-evaluate per signal after first re-score in Raleigh/Cary/Durham. | Operator approved photo-aesthetic-scoring integration (Wave 2 Phase 1). Photo-derived field weights need a budget that today's 200-cap suffocates — adding 7-12 photo weights inside a 200 budget that already gives 60 to popularity + 50 to text patterns would force every photo weight to ±5 to ±10, too small to materially shift ranking. Cap raise unlocks meaningful photo contribution. | (A) Keep cap=200, fit photo weights inside existing budget — rejected: every photo weight forced to ±5 to ±10 magnitude; won't move ranking. (B) Move to per-city percentile serving (Option C) — architecturally correct but requires touching every consumer of `place_scores` (5 functions + 1 RPC) + nightly percentile compute job + re-tuning across 16 signals. Quarter-long project, not a one-cycle ship. Filed as ORCH-0709 candidate for Wave 2 Phase 2. (C) Raise cap to 500 — rejected: split-the-baby; 1000 buys clean 5x headroom for the casual_food raw=459 ceiling without tight-fit anxiety. | filter_min thresholds become a smaller fraction of the cap (filter_min: 120 / cap: 1000 = 12% vs 60% before). Need re-evaluation per signal after first re-score in Raleigh/Cary/Durham — operator verifies "is filter_min still excluding the right things?" If too many places clear, bump threshold (e.g., 120 → 200 for fine_dining). Acceptable cost: this is a one-time tuning pass, not a recurring expense. The 9 untouched signals still respect cap=200 — if their photo weights ever land (currently out of scope), they'll need their own cap raises. | Permanent for the 7 in-scope signals' v_next configs. Exit signals: (a) ORCH-0709 ships per-city percentile — at that point absolute filter_min becomes obsolete on the 7 signals and the cap is decoupled from the serving threshold entirely. (b) Future signals receive photo weights → revisit cap raise on a case-by-case basis (no blanket commitment beyond the 7 named here). (c) If after Raleigh+Cary+Durham smoke the cap=1000 is materially under-utilized (max raw still <300 post-photo for any signal), consider lowering it to 500 in v_next+1 to avoid score-distribution fatigue in admin UIs. |
| DEC-087 | 2026-05-02 | **ORCH-0704 v2 — Full edit-after-publish + buyer protection + multi-channel notification stack + reason audit.** Published Mingla Business events are FULLY editable post-publish (every field except identity + lifecycle metadata). Existing buyers protected via I-19 (immutable order financials) + buyer-side displayable info reads LIVE from current LiveEvent. Material changes (date / venue / format / tickets / etc.) fire a 4-channel notification stack: in-app banner (Cycle 9c renders from `useEventEditLogStore`) + email stub (B-cycle wires Resend) + SMS stub for web purchases (B-cycle wires Twilio) + push deferred to consumer-app cycle. Refund-first as the unified destructive-change pattern: 8 reject reasons (capacity_below_sold, tier_delete_with_sales, tier_price_change_with_sales, tier_free_toggle_with_sales, multi_date_remove_with_sales, when_mode_drops_active_date, recurrence_drops_occurrence, plus event_not_found / missing_edit_reason / invalid_edit_reason for input validation) each mapped to a "Refund first" dialog with "Open Orders" CTA (stub-toast until Cycle 9c builds Orders ledger). whenMode + recurrenceRule unlocked entirely — only blocked when the change DROPS a previously-active date with sales. Reason mandatory on every save (10..200 chars trimmed). Permanent edit audit log via new `useEventEditLogStore` (Zustand persisted, append-only, logout-cleared). Stub-mode (ORCH-0704) ships zero soldCount + console-log notifications; guard rails inactive until Cycle 9c wires `useOrderStore` + B-cycle wires Resend/Twilio. SUPERSEDES narrow Cycle 9b-2 + obsoletes v1 spec (`SPEC_ORCH-0704_FULL_EDIT_AFTER_PUBLISH.md` v1). | Operator decision 2026-05-02 — narrow 9b-2 (description + coverHue only) was unshippable for real organisers (wrong address, mispriced tier, missing FAQ, etc.). Operator overrode v1's 6 conservative defaults: notification stack everywhere · whenMode unlocked · refund-first as unified pattern · reason on every change. v2-A and v2-B follow-up defaults locked: SMS only fires for material+ AND only when web-purchase orders exist (additive-only edits → banner + email; no SMS to avoid notification fatigue) · reason length 10..200 chars. Industry-standard buyer-protection pattern (Eventbrite/Ticketmaster). | (A) Narrow 9b-2 (description + coverHue only) — rejected by operator: half-measures leak into every downstream cycle. (B) Wizard mode prop refactor (Option A from v1) — rejected: wrong UX shape (sequential gating wrong for random-access edit), 769 LOC EventCreatorWizard churn. (C) Per-section sub-routes (Option C) — rejected: navigation overhead, save-per-section worse UX. (D) Block all whenMode changes once published (forensics conservative recommendation) — rejected by operator: too restrictive; allows immediate "oh I meant recurring" fix when zero sales. (E) Banner-only notifications (no email/SMS) — rejected by operator: real material changes need real notification reach. | Stub-mode guard rails are inactive (`getSoldCountByTier` returns zeros) until Cycle 9c wires `useOrderStore` — operator can fully edit until then with no enforcement. ChangeSummaryModal now requires every save to enter a reason (10–200 chars; multiline TextInput, char counter); friction acceptable in exchange for buyer-trust audit trail + notification copy. Email + SMS console.log stubs add log noise during dev — acceptable, swapped for real Resend/Twilio at B-cycle. "Open Orders" reject CTA shows toast "Orders ledger lands Cycle 9c — your refund flow is coming." until 9c builds the route — graceful degradation accepted. ~+1180 LOC net (6 NEW + 5 MOD + 2 REPLACE + 1 DELETE). | Permanent for the Mingla Business operator-side edit-after-publish surface. EXIT signals: (a) Cycle 9c builds `useOrderStore` + buyer order detail page — `getSoldCountContextForEvent` flips from stub to live; "Open Orders" CTA swaps to real route; banner renders from `useEventEditLogStore.getEditsForEventSince`. (b) B-cycle wires real Resend (email channel) + Twilio (SMS channel). (c) Consumer-app cycle adds OneSignal push channel. ORCH-0705 (no-show + post-event ops + dispute paths + post-Stripe refund flows) is the explicit follow-up cycle (deferred until post-Stripe integration). |
| DEC-086 | 2026-05-01 | **ORCH-BIZ-WEB-WORKSTREAM-SPLIT — Public-facing website (`mingla.com` marketing / landing / SSR-grade share pages) is a SEPARATE FOUNDER-OWNED WORKSTREAM. The orchestrator's cycle pipeline (Cycles 6 through 17) builds the organiser product + public share pages inside `mingla-business` Expo Web ONLY — including Cycle 8 Checkout. The orchestrator does not initiate, plan, or dispatch work for the public-facing marketing website; the founder builds that separately on their own timeline.** Reaffirms DEC-081 (mingla-web Next.js codebase discontinued for orchestrator's lane) and adds the explicit workstream-ownership split. The original case for SSR/Next.js stands as a real future-state need (social link previews via OG meta tags pre-JS, SEO, sub-2.5s LCP on 3G, lighter bundle for marketing-page first impressions, two clean deploy targets `business.mingla.com` vs `mingla.com`) — but those costs do not bite at our current stage (zero traffic, MVP stub data, family-and-friends user base). Founder may stand up an SSR public site whenever they choose; orchestrator integrates with whatever the founder ships when the time comes. Until then, public share pages (`/b/[slug]`, `/e/[brandSlug]/[eventSlug]`) live in `mingla-business` Expo Web and are accepted as having degraded social previews (OG tags inject post-JS-boot, most crawlers give up first). Cycle 8 Checkout therefore lives at `mingla-business/app/checkout/...`. Cycles 9–17 follow the same rule. | Founder directive 2026-05-01 (post-Cycle-7-FX3 close, when discussing whether Cycle 8 should bootstrap mingla-web): *"I will work on the UI for the front facing website separately."* + earlier-in-conversation Q-and-A clarifying the SSR rationale: founder asked "why was it even needed in the first place?" — orchestrator laid out social-preview / SEO / LCP / bundle-weight / two-deploy-targets reasoning; founder concluded those concerns are real but not present at MVP stage and chose to own the public-website lane personally, separately from cycle work. | (A) Bootstrap `mingla-web/` now and pay the SSR cost upfront — rejected: zero traffic at MVP means SEO + social-preview + LCP costs aren't yet bite-sized real; the day spent bootstrapping is a day not spent learning whether the buyer flow makes sense. (B) Roll public-website work into the orchestrator cycle pipeline (e.g., insert mingla-web setup as Cycle 8.5) — rejected: founder explicitly wants to own the public-facing UI as a separate workstream. (C) Continue ignoring the topic and let future-orchestrators rediscover the question — rejected: trust contract requires the WHY to be on the canonical paper trail so it isn't re-litigated. | Public share pages in `mingla-business` Expo Web have degraded social link previews (most crawlers don't run JS so OG meta tags injected by `expo-router/head` arrive too late). Mitigated by (a) Mingla acquisition is currently App Store + Play Store + word-of-mouth, not viral-share-driven, and (b) when the founder's separate Next.js site ships, share links can be flipped to point at SSR'd routes. Slightly slower public-page LCP on 3G — acceptable pre-traffic. SEO foregone — non-blocker pre-traffic. | Permanent for the orchestrator's cycle pipeline through Cycle 17. Re-evaluation triggers: (a) founder's separate mingla-web ships and provides SSR routes the cycle product can link to, (b) measurable social-share virality demands real OG cards, (c) organic SEO becomes a real acquisition channel, (d) buyer drop-off on public-page LCP becomes a measurable conversion problem. When any of those fire, orchestrator re-opens the question and proposes a migration cycle (mechanical port: copy JSX from `mingla-business/app/...` to `mingla-web/app/...`, swap RN primitives for HTML, ~1 day not a rewrite). |
| DEC-085 | 2026-04-30 | **ORCH-BIZ-CYCLE-3-KIT-EXTENSION-004 — Modal primitive native-portal extension (additive bug-fix; parity with Sheet's J-A8 polish RC-1).** `mingla-business/src/components/ui/Modal.tsx` is now wrapped in React Native's native `<Modal>` (aliased `RNModal`) with `transparent + statusBarTranslucent`. Without this portal, `StyleSheet.absoluteFill` resolves to the nearest positioned ancestor — for ConfirmDialog (which uses Modal under the hood) rendered inside a wizard step body inside a ScrollView, the dialog centered within the scroll content rect rather than the screen. Existing call sites' visible behaviour is unchanged or *corrected* — all dialogs now center on screen as designed. DEC-079 closure: this is an additive bug-fix to satisfy the I-13 overlay-portal contract that Sheet already met as of Cycle 2 J-A8 polish. Modal + Sheet both portal-correct now; promoting I-13 to global INVARIANT_REGISTRY in this same close cycle. | Founder smoke 2026-04-30 hit the centering bug when tapping the Delete button on a ticket card (deep inside Step 5 → wizard body → ScrollView). ConfirmDialog appeared centered within the scroll body, not the screen. Pattern matches Sheet's J-A8 polish (RC-1) fix verbatim. | (A) Render ConfirmDialog at wizard route root via prop drilling — rejected: invasive prop chains across 7 step bodies + multiple sheets; doesn't solve future Modal-based primitives. (B) Wrap each ConfirmDialog call site in a native RNModal — rejected: duplicate machinery; better to fix at the kit-primitive layer. (C) Document as known limitation — rejected: dialog centering is a foundational UX expectation; founder caught it on first smoke. | None — additive bug-fix with no behavioural cost. Existing Modal callers (e.g., ConfirmDialog in J-A9 brand team RolePickerSheet, J-A8 brand-edit discard) see corrected behaviour rather than regression. | Permanent. If future kit primitives mount overlays, they must use the same portal pattern (Sheet + Modal both compliant). I-13 overlay-portal contract is now globally tracked in INVARIANT_REGISTRY (this cycle's close). |
| DEC-084 | 2026-04-30 | **ORCH-BIZ-CYCLE-3-KIT-EXTENSION-003 — Sheet primitive accepts numeric snap value (additive prop extension per DEC-079).** `mingla-business/src/components/ui/Sheet.tsx` `SheetSnapValue = SheetSnapPoint \| number`. Numeric values clamped to `[120px, 95% screen]` — pass a measured content height (via `onLayout`) for content-fit sheets with no wasted bottom padding. Existing string-preset callers (peek/half/full) unaffected. Used by `TicketStubSheet` in Step 5 to auto-fit its form (Name + Free toggle + optional Price + Unlimited toggle + optional Capacity + Save row) — sheet height matches content exactly, animates smoothly when toggles change, expands to "full" when keyboard rises. | Founder smoke surfaced "wasted space at bottom of ticket sheet" repeatedly across Cycle 3 reworks v3.1 → v3.6. The Sheet's three-step snap (peek/half/full) couldn't match a form that varied between 312px (free+unlimited) and 452px (paid+capacity-bound). Ratio-based snaps either over-padded ("full" for small forms) or clipped content ("half" for full forms requiring scroll). Numeric snap = single declarative way to size sheet to content. | (A) Add a "three-quarter" string preset (75%) — rejected: still mismatches dynamic content; same fundamental shape problem just shifted. (B) Build a separate `AutoSheet` primitive — rejected: extra primitive in DEC-079-closed kit; numeric extension is strictly additive. (C) Move TicketStubSheet to a custom inline component (skip Sheet primitive) — rejected: loses pan-gesture + portal consistency that Sheet primitive provides. | None — strictly additive. Existing Sheet consumers (BrandSwitcherSheet, RepeatsSheet, CategorySheet, RolePickerSheet, every Sheet in the wizard) keep their string-preset snaps and behave identically. | Permanent. Future content-varying sheets (ticket editor in Cycle 5 with all 27 PRD §4.1 fields, recurrence picker in Cycle 4) will use numeric snap. If a fourth-tier need emerges (e.g., always-fit-content with auto-shrink on keyboard), wrap into a higher-level `AutoFitSheet` consumer that wraps Sheet — no further Sheet primitive change needed. |
| DEC-083 | 2026-04-30 | **ORCH-BIZ-CYCLE-2-KIT-EXTENSION-002 — Avatar primitive added to kit (post-closure additive carve-out per DEC-079).** A new circular/rounded-square avatar primitive (`mingla-business/src/components/ui/Avatar.tsx`) is added to the kit AFTER Cycle 0a CLOSED. Built specifically to consolidate 4 inline avatar implementations across J-A7 (BrandProfileView 84 hero), J-A8 (BrandEditView 84 hero with pencil overlay), and J-A9 (BrandTeamView 40 row + dimmed variant; BrandMemberDetailView 84 hero) — all sharing identical visual contract (circular wrap with `accent.tint` bg + 1px `accent.border` + first-letter-uppercase initial in `accent.warm`) but differing only in size / border-radius / initial-font-size. API: `<Avatar name size="row"|"hero" photo? dimmed? accessibilityLabel? style? />`. Atomic primitive — no children prop, no overlay support baked in; consumers wrap in their own `position: relative` View to absolute-position siblings (e.g., the photo-edit pencil button on BrandEditView). Kit closure rule (DEC-079) remains in force for ALL OTHER primitives — this is a watch-point-triggered carve-out following DEC-082 (Icon set additive expansion) precedent, NOT permission to add primitives ad-hoc. Future kit additions still require explicit orchestrator approval + a DEC entry. | D-IMPL-A9-3 watch-point THRESHOLD REACHED 2026-04-30: 4 inline avatar implementations across 4 files = lift-now-vs-rewrite-later trade-off tipped toward lift. J-A10 (`/brand/:id/payments`) + J-A11 (payments KPI tiles) + Cycle 14 (account profile) + Cycle 6 (public brand page) all expected to need avatars; consolidating before more inline copies accumulate prevents 3+ more drift sources. Founder approved 2026-04-30 ("B, then A"). | (A) Keep inline composition forever; document the duplication as accepted tech debt — rejected: drift risk increases each cycle; pixel parity across 4 files is fragile when each is hand-tuned. (B) Build the Avatar with `children` prop supporting overlays — rejected: bakes a specific use case (edit-pencil) into the primitive's API; cleaner to keep avatar atomic and have consumers compose overlays via positioned wrappers. (C) Two separate primitives `AvatarRow` + `AvatarHero` — rejected: redundant style logic; size variants in a single primitive matches Button's `sm/md/lg` precedent. | One-off kit carve-out: kit is now 26 primitives instead of 25 (TopSheet was 25 per DEC-080). Slight inconsistency cost: Cycle 0a was advertised as "the kit is closed" but is now closed-plus-three (TopSheet, Icon-set-expansion, Avatar). Documented mitigation: each carve-out has its own DEC entry citing the trigger. Implementor must continue treating the kit as closed unless another orchestrator+founder ratification produces a similar DEC. | Permanent. The primitive stays in `src/components/ui/Avatar.tsx`. If future cycles surface new size variants (e.g., `xs` 24px for nested mentions, `xl` 120px for profile heroes), extend the `AvatarSize` union additively and add a SIZE_TOKENS row — no new primitive needed. If 3+ overlay patterns emerge (pencil-edit + status-dot + presence-ring), reconsider whether the children-prop-with-positioned-anchor approach (Option B) becomes warranted. |
| DEC-081 | 2026-04-29 | **ORCH-BIZ-WEB-CONSOLIDATION — `mingla-web` (Next.js separate codebase) DISCONTINUED. The Mingla Business web product is `mingla-business` running on Expo Web only. No Next.js codebase will be built. Marketing landing page (`mingla.com`), public event share pages (`mingla.com/e/[slug]`), public brand share pages (`mingla.com/b/[slug]`), and organiser login redirect handler — all originally scoped to a separate Next.js codebase under DEC-075 — are either (a) hosted in `mingla-business` Expo Web, or (b) deferred until explicit founder request. Cycle 0b (web auth wiring + WEB2 unblock) is now SCOPED PURELY to `mingla-business/AuthContext.tsx` swapping to Supabase `signInWithOAuth` redirect on web; no separate codebase setup. RETRACTS DEC-075. AMENDS DEC-076 (web auth still uses OAuth-redirect but lives entirely in `mingla-business`, not split across two codebases). AMENDS DEC-077 (local-only dev now applies to one codebase, not two). | Founder directive 2026-04-29: *"we will delete mingla-web for marketing and clean up so you stop picking it up. I am asking about fully setting up the web component for Mingla business."* Web parity (per `project_business_plans.md` build rule #1) is preserved on `mingla-business` Expo Web — that's where web smoke runs. The separate Next.js codebase added complexity (two codebases to maintain, design-token sharing concerns, dual deploy pipelines) without proven value at our current stage. | (A) Keep both codebases as planned (DEC-075 original) — rejected: complexity cost not yet justified by user-facing value. Marketing + share pages can be served from `mingla-business` Expo Web for v1; revisit only when SEO / OG cards become measurably load-bearing. (B) Defer the discontinuation decision until Cycle 6 — rejected: user wants the noise gone now so dispatch prompts and orchestrator memory stop referencing a dead direction. (C) Soft-mark DEC-075/076 as "stale" without retraction — rejected: trust contract requires explicit retraction in the canonical paper trail. | Loses SEO-optimised marketing landing + Lighthouse-tier share pages until web-marketing is reopened. Mitigation: when Marketing actually ships (per DEC-073's adaptive-4th-tab + DEC-070 Q-A3 build-when-ready), reopen the question of standalone marketing site at that point. For now, Mingla relies on App Store + Play Store + organiser word-of-mouth, not marketing-page-driven acquisition. | Permanent unless founder reopens. The roadmap's Cycle 6 (Public event page), Cycle 7 (Public brand page), Cycle 8 (Checkout) are now ALL `mingla-business` Expo Web — or deferred altogether. Stale references in roadmap / specs / handoffs to `mingla-web/` are superseded by THIS DEC and noted via header banner in those files; full surgical cleanup is housekeeping work that may or may not happen — the canonical state is this entry. |
| DEC-080 | 2026-04-29 | **ORCH-BIZ-CYCLE-1-KIT-EXTENSION-001 — TopSheet primitive added to Cycle 0a kit (post-closure carve-out).** A new top-anchored drop-down primitive (`mingla-business/src/components/ui/TopSheet.tsx`) is added to the kit AFTER Cycle 0a CLOSED. Built specifically for the brand-switcher dropdown UX after iterating through bottom Sheet (felt heavy / overflowed) and centered Modal (overflowed with many brands) — both wrong patterns. TopSheet drops down from below the topbar with: fixed 70% screen height, full topbar-matching width (`marginHorizontal: spacing.md`), pinned full-width footer button, internal ScrollView for body content, swipe-up gesture to dismiss with mirror-symmetric close animation. Kit closure rule (DEC-079) remains in force for ALL OTHER primitives — this is a one-off carve-out, NOT permission to keep adding primitives ad-hoc. Future kit additions still require explicit orchestrator approval + a DEC entry. | Founder UX iteration 2026-04-29 across Cycle 1 mid-cycle: bottom Sheet at snap=full felt "weird" (full-screen for a quick brand pick); centered Modal grew unbounded with brand count, hiding the create button. After founder described the desired UX ("drop down from top bar, fixed height, brands scroll within viewport, create button pinned at bottom, swipe-up to dismiss"), confirmed inline spec via Q1 (drops below topbar) + Q2 (slides from below topbar) + Q3 (matches topbar width). | (A) Reuse Modal with fixed maxHeight constraint — rejected: maxHeight inside centered Modal still feels wrong UX-wise; vertical position is wrong relative to trigger. (B) Build a TopSheet variant inside the existing Sheet primitive (e.g. `anchor="top"` prop) — rejected: would significantly fork Sheet's gesture/animation logic; cleaner as a standalone primitive. (C) Don't fix; ship Cycle 1 with the centered Modal — rejected: overflow bug + UX feel are both load-bearing for organiser daily flow. | One-off kit carve-out; the kit is now 25 primitives instead of 24. Slight inconsistency cost: Cycle 0a was advertised as "the kit is closed" but is now closed-plus-one. Documented mitigation: this DEC + the primitive's own header comment cite the carve-out reason. Future cycles must continue treating the kit as closed unless another orchestrator+founder ratification produces a similar DEC. | Permanent. The primitive stays in `src/components/ui/`. If future cycles surface the need for more anchored-overlay variants (e.g., bottom-of-screen toast stack, side drawer), consider a unified `OverlayHost` architecture (D-IMPL-33) instead of accumulating one-off primitives. |
| DEC-079 | 2026-04-29 | **ORCH-BIZ-KIT-CLOSURE-CARVEOUT — Cycle 0a kit (24 primitives) is "closed for new primitives or new behaviours" but NOT closed for retiring transitional behaviours via additive props.** Primitives that ship with `// [TRANSITIONAL]` markers in their own header comments (e.g., TopBar's Cycle 0a Toast on brand-chip-tap saying "lands in Cycle 2") MAY be extended in subsequent cycles with **additive props** that retire the transitional behaviour. Example: TopBar gains `onBrandTap?: () => void` in Cycle 1 — when undefined, Toast still fires (backward compatible); when defined, Toast is suppressed and the caller's handler runs. NEW behaviours not previously anticipated by the primitive's header comments still hard-stop and require orchestrator escalation. Implementor dispatches that touch primitives must explicitly cite this DEC plus the line of the primitive's `// [TRANSITIONAL]` marker that anchors the carve-out. | Founder approved 2026-04-29 after Cycle 1 implementor surfaced contradiction: AC#4 of Cycle 1 dispatch required TopBar's brand-chip to open BrandSwitcherSheet, but kit-closure rule from Cycle 1 dispatch §3.6 forbade extending primitives. The TopBar header comment already marked the Toast as Cycle 0a-transitional ("lands in Cycle 2"), so the carve-out is structurally implicit but needed explicit ratification. | (A) Kit fully closed — every transitional behaviour requires a brand-new primitive in subsequent cycles. Rejected: explodes primitive count and creates duplicate components for the same logical control. (B) Kit fully open — any cycle freely modifies primitives. Rejected: defeats kit-closure's scope-creep guard. (C) Kit hard-replaceable — Cycle 1+ may rewrite Cycle 0a primitives. Rejected: breaks existing call sites and re-opens settled foundation. | Implementors must distinguish "additive prop to retire transitional" (allowed) from "behaviour change to existing consumer" (hard-stop). The line is bright: if the primitive's existing call sites continue to render identical UX without code changes, the prop is additive; if they don't, it's a behaviour change. Slight discipline tax — every additive prop must be justified against a `// [TRANSITIONAL]` anchor in the primitive's source. | Permanent process rule. The kit's "closed for new primitives" guard remains. The carve-out applies ONLY to retiring transitionals via additive props, ONLY when the primitive's own source explicitly anticipated future wiring. |
| DEC-078 | 2026-04-29 | **ORCH-BIZ-CYCLE-0a-WEB-DEFERRAL — Cycle 0a (foundation) closes with iOS + Android verified. Web smoke is DEFERRED to Cycle 0b when Supabase OAuth-redirect web auth (DEC-076) is wired. Web is registered as KNOWN-BLOCKED with two surfaced root causes: (WEB1) SSR crash from AsyncStorage `window` access — FIXED in commit `<E.5 hotfix>` via SSR-safe storage adapter at `mingla-business/src/services/supabase.ts`. (WEB2) `app/index.tsx` AuthProvider loading state never resolves on web — auth gate hangs indefinitely on web bundle. ROOT CAUSE NOT INVESTIGATED — likely candidates: `getSession()` network/CORS hang, React hydration mismatch silently swallowing the useEffect, or RNGoogleSignIn module-level configure() side-effect (the web-not-implemented-method warning fires but may corrupt module state). NOT FIXED in Cycle 0a. The milestone-level "mobile + web parity in every milestone" build rule (BUSINESS_STRATEGIC_PLAN.md) remains intact for M0–M21 feature milestones; this deferral is a foundation-cycle exception, NOT a parity-rule amendment.** | Founder directive 2026-04-29 (after E.5 SSR hotfix shipped successfully but exposed a second-layer auth-gate hang on web): *"When will we actually fix the web version? How about we defer till then?"* + Founder explicit approval of trigger: defer web smoke to Cycle 0b where web auth is the focus. (a) DEC-076 already commits web auth implementation to Cycle 0b — investigating WEB2 now produces throwaway work since the AuthProvider will be rewritten when Supabase OAuth-redirect replaces native ID-token flow on web. (b) Cycle 0a's deliverable is foundation primitives + styleguide proven on the primary platforms (iOS + Android per organiser-mobile-first rule). (c) WEB1 + WEB2 stay registered for tracking; un-defer trigger fires when Cycle 0b dispatches web auth. | (A) Block Cycle 0a closure until WEB2 root-cause-investigated and fixed — rejected: throwaway work since AuthProvider will be rewritten in 0b. (B) Investigate WEB2 in parallel with Cycle 1 feature work — rejected: pulls focus from organiser feature delivery, and the fix without web auth context is exploratory. (C) Drop web from the project entirely — rejected: DEC-076 already commits web to Cycle 0b, and milestone-level parity rule is preserved. | Cycle 0a closes with iOS + Android verified, web flagged DEFERRED-TO-0b in the implementation report and AGENT_HANDOFFS. Sub-phase F dispatch rewritten to iOS + Android smoke only. WEB2 carries forward as a known blocker with documented un-defer trigger ("Cycle 0b dispatch authorised"). Risk: Cycle 0b investigators will need to re-discover the WEB2 hang context — mitigated by DEC-078 itself documenting the symptom + suspected root causes. | Permanent for Cycle 0a closure decision. WEB1 fix permanent. WEB2 un-defers when Cycle 0b authorises web-auth implementation; at that point WEB2 becomes a Cycle 0b task and the AuthProvider is rewritten holistically rather than patched. |
| DEC-077 | 2026-04-28 | **[AMENDED 2026-04-29 — see DEC-081] ORCH-BIZ-LOCAL-ONLY-DEV — No Vercel deployment during UI cycles 0a–17. Both codebases run locally only.** `mingla-business/` smoke-tested via `npx expo start` (mobile) + `npx expo start --web` (browser); `mingla-web/` smoke-tested via `npm run dev` (localhost:3000). DNS records, Vercel project setup, `business.mingla.com` / `mingla.com` subdomain wiring, and the `.mingla.com` cookie domain (DEC-076) are all DEFERRED until UI is shippable-grade and we explicitly authorise production deployment. For dev, auth cookies set on `localhost` and Supabase OAuth callback URLs include `http://localhost:3000/auth/callback`. | User directive 2026-04-28: *"No vercel yet. We will test it all with npm run dev"* (after authorising Cycle 0a dispatch). | (A) Set up Vercel + DNS now in parallel — rejected: adds preflight friction; user wants to see UI working locally first before paying for / configuring infra. (B) Skip web testing entirely until production deploy — rejected: parity is a hard build principle (DEC-071); web smoke must run per cycle. | Local-only dev means cookie-domain `.mingla.com` portability cannot be validated until production deploy. Mitigation: code is written to support both `localhost` (dev) and `.mingla.com` (prod) cookie domains via env-driven config. Cross-subdomain session test deferred to production cutover. Apple Tap to Pay entitlement application can still proceed in parallel — it's Apple Developer Portal work, not Vercel-coupled. | Permanent until UI is shippable. When UI is signed off (post-Cycle-17), orchestrator surfaces the production-deploy checklist (Vercel project, DNS, cookie-domain swap, environment variable migration) as its own dispatch. |
| DEC-076 | 2026-04-28 | **[AMENDED 2026-04-29 — see DEC-081 — web auth lives in `mingla-business` Expo Web only, not a separate Next.js codebase] ORCH-BIZ-AUTH-ARCH — Auth architecture across mobile + web. Mobile uses native ID-token flows (Google Sign-In SDK + Apple Authentication, already shipping). Web uses Supabase OAuth-redirect flow via Next.js route handler at `/auth/callback`. Same Supabase project = same `auth.users` row across both. Auth cookie domain set to `.mingla.com` so session is portable across `business.mingla.com` (Business app) and `mingla.com` (marketing + share pages).** Web flow: `business.mingla.com/login` → `supabase.auth.signInWithOAuth({ provider, options: { redirectTo: 'https://business.mingla.com/auth/callback' } })` → Google/Apple sign-in page → Supabase callback → exchanges code → cookie set on `.mingla.com` → redirect to `/`. Built with `@supabase/ssr`. Mobile flow unchanged: native ID-token → `supabase.auth.signInWithIdToken({ provider, token })`. Sign-out: per-device by default; "Sign out everywhere" uses `signOut({ scope: 'global' })` which revokes all refresh tokens. | User question 2026-04-28: *"the web should be accessible the same sign in methods via app i.e apple and google. How would that work?"* | (A) Native ID-token flow on web via Google Identity Services + Sign in with Apple JS — rejected: extra SDK weight, more glue code, doesn't gain us anything since mobile already handles native flow well; OAuth-redirect is the canonical Next.js + Supabase pattern. (B) Magic-link only on web — rejected: organisers expect Google/Apple parity with mobile. (C) Separate auth providers per surface — rejected: defeats the same-user-everywhere principle. | One full-page redirect on web (vs popup) — accepted, this is standard Supabase + Next.js pattern. Two redirect URIs to maintain in Google/Apple consoles (`*.supabase.co/auth/v1/callback` + `business.mingla.com/auth/callback` + dev localhost). Cookie-domain `.mingla.com` requires DNS setup but unlocks cross-subdomain session detection (e.g., logged-in user on a public share page sees personalised affordances). | Permanent. Implemented at Cycle 0b in the Frontend Journey Build Roadmap. Configuration checklist: Supabase dashboard adds web redirect URIs to Google + Apple providers; Google Cloud Console adds `mingla.com` + `business.mingla.com` to authorized JavaScript origins; Apple Developer Service ID adds web domains + return URLs; `mingla-web/` installs `@supabase/ssr`, builds `lib/supabase/{client,server}.ts` + `middleware.ts` + `auth/callback/route.ts`. Mobile config unchanged. |
| DEC-075 | 2026-04-28 | **[RETRACTED 2026-04-29 — see DEC-081 — `mingla-web` Next.js codebase discontinued; everything consolidates onto `mingla-business` Expo Web] ORCH-BIZ-WEB-STACK — Next.js for marketing / login / public-share pages; Expo Web for the Business app itself.** Recommended and approved 2026-04-28. Two web codebases under one Vercel project: (1) `mingla-business/` (Expo Web) serves the Business product at `business.mingla.com` (or `app.mingla.com` — subdomain TBD); (2) `mingla-web/` (Next.js, NEW) serves `mingla.com` (marketing landing), `business.mingla.com/login` (organiser login), `mingla.com/e/[slug]` (public event share page), `mingla.com/b/[slug]` (public brand share page), `mingla.com/o/[slug]` (public organiser showcase). Dev: `cd mingla-business && npx expo start` for mobile + Expo Web; `cd mingla-web && npm run dev` for Next.js. | Recommendation rationale: (a) Public share pages are first-impression UX — SSR + OG-card rendering needs Next.js. (b) `mingla.com` landing needs SEO + Lighthouse scores; Expo Web's bundle is heavier than necessary for marketing. (c) Vercel + Next.js is canonical pairing (Vercel-built; fastest builds, edge functions, image optimization). (d) Two codebases simpler than it sounds — each has a clear job. (e) Design tokens can be shared via npm package or copied. | (A) Expo Web for everything — rejected: bundle too heavy for marketing pages; weaker SEO; worse share-link UX. (B) Vite SPA + Expo Web — rejected: still no SSR, OG cards need prerender workaround, less mature ecosystem for marketing-grade sites. | Two codebases to maintain. Mitigation: design tokens shared as `packages/tokens` workspace package or copied verbatim; component-library packages where feasible. Slightly slower iteration on cross-stack changes; acceptable given marketing/app are different cadences. | Permanent. Subdomain choice (`business.` vs `app.`) deferred to a v2 brand decision; default `business.mingla.com`. |
| DEC-074 | 2026-04-28 | **ORCH-BIZ-NO-ONBOARDING — Onboarding screens KILLED. Sign-in routes straight to Home. Brand creation is a topbar-chip action.** Sign-in (Google / Apple / email — DEC-073 nav-agnostic) lands the organiser on the Home dashboard. If they have no brand yet, the topbar brand chip reads "Create brand" with a chevron; tapping opens the brand-creation sheet inline. Until a brand exists, event-creation actions are disabled with inline copy "Create a brand first." Stripe Connect prompted via Account banner whenever it's missing for any brand. No 4-step onboarding wizard. No operator-type capture. No brand-bio capture upfront. | User Q-A9 directive 2026-04-28: *"no kill onboarding totally."* + Q-A9 selected option: *"Sign-in → Home. Brand creation is a tap on the brand chip in the topbar."* | (A) 4-step onboarding from the design package — rejected by user. (B) Modal on first sign-in — rejected. (C) Operator-type capture for analytics — rejected. | Loses optional taxonomy capture (operator type, event type — was M1 onboarding step 4). Mitigation: capture later via Account → Profile if analytics demand surfaces; not a blocker. Loses the design package's friendly onboarding sequence — replaced with a more confident "you're already here, get started" pattern. | Permanent. The design-package screens-auth.jsx OnboardingScreen + step content are now DESIGN-PACKAGE-DELETE (not absorb) per Q-A9. AuthScreen itself stays. |
| DEC-073 | 2026-04-28 | **ORCH-BIZ-NAV — Nav structure: 3 fixed tabs (Home / Events / Account) + adaptive 4th tab (Marketing) that materialises ONLY when Marketing ships.** No 5-tab. No Marketing locked-state placeholder per DEC-070 (Q-A3 = build-and-ship-when-ready). Scan accessible from: (1) Event Detail screen "Scan tickets" primary action button, (2) Home dashboard "Live tonight" KPI card → Scanner CTA when an event is live today, (3) deep link from event-staff invite email. The BottomNav primitive built in M0 takes a `tabs` config prop; adding/removing tabs is a config change, not a refactor. Web sidebar at `lg+` mirrors the same nav: 3 fixed items + adaptive 4th when Marketing ships. | User Q-A1 directive 2026-04-28: *"No, we need to brainstorm here what is the best way to structure the nav"* + Q-A2 directive: *"Putting it in the nav menu is a total waste of real estate"* + selected option: *"3 fixed tabs + adaptive 4th"*. | (A) 5-tab Claude Design package default — rejected: real-estate waste, Marketing tab without product is dishonest. (B) 4-tab with Chat as 4th — rejected: chat agent is M19+, no placeholder. (C) 2-tab heavy-contextual — rejected: Home as separate tab earns its keep. (D) 3-tab + center FAB — rejected for v1; FAB pattern reserved as a future enhancement if Quick Actions become demanded. | Loses the 5-tab discoverability of Marketing/Scan that the design package signals. Mitigation: (a) Scan is contextual where it's needed (Event Detail, Live tonight card) which is more accurate to the workflow. (b) Marketing tab arrives when Marketing ships — no placeholder lying about a feature. (c) Chat agent (M19+) decision deferred — could become 4th or 5th when shipped, replacing/joining Marketing. | Permanent for v1. Revisit nav structure when (a) Marketing ships (DEC-071 staging), (b) chat agent ships (M19+), (c) usage data shows organisers want different prominent tools. |
| DEC-072 | 2026-04-28 | **ORCH-BIZ-PRODUCER-MODEL — Mingla Business is the EVENT-INJECTION SOURCE for the consumer Mingla mobile app. Organisers create events here; events flow into the consumer app's Discover screen (alongside Ticketmaster events) AND into the swipeable deck. Mingla Business has no event-browsing surface.** Two distinct products with one shared backend: organisers PRODUCE here, attendees CONSUME on the consumer app. Event visibility flags on the producer side (`show_on_discover`, `show_in_swipeable_deck`) control which consumer surfaces ingest each event. Public event/brand pages on `mingla.com/[slug]` are share-link surfaces, not browse surfaces. | User directive 2026-04-28: *"This app will feed the user facing mingla mobile app for explorers. It will feed both the discover screen where the ticketmaster events currently live, and the swipeable deck. Events can be injected. Please register this so we are clear. users will not browse events on the mingla business app. This is for Organizers and businesses only."* | (A) Mingla Business as standalone partner SaaS with its own discovery surface — rejected: duplicates consumer-app discovery, fragments demand. (B) Mingla Business as a generic event creation tool with no consumer feed — rejected: defeats the wedge (consumer-app demand graph is the differentiator). | Producer-consumer architecture. Backend table for events must include consumer-feed flags from day one. Eligibility/inclusion rules for Discover vs deck must be deterministic. Mingla Business UI nowhere lists "events you might attend" — only events YOU run, YOUR brand runs, or events you're a scanner on. | Permanent. The producer-consumer split is foundational to the Mingla architecture and should govern every backend schema, every ingestion path, and every public-page UX. |
| DEC-071 | 2026-04-28 | **ORCH-BIZ-FRONTEND-FIRST — Build sequence is UI-FIRST across mobile + web. Forensics specs every user journey first. Backend deferred until UI is shippable-grade.** Order of operations: (1) Forensics specs every user journey for Mingla Business, unifying with the absorbed design package + locked plans. (2) Implementor builds UI screen-by-screen, journey-by-journey, mobile + web parity, with stub data. (3) Refine to perfection through iteration cycles — user decides per-cycle when each is "nailed." (4) Foundations: domain (already secured), Vercel (already secured), `npx expo start` (Mingla Business app) and `npm run dev` (Mingla web stack — landing site + Business subdomain + organiser login) working side-by-side. (5) ONLY THEN: backend (Supabase schema, edge functions, RLS, Stripe, etc.) lights everything up. | User directive 2026-04-28: *"I suggest we slowly implement just the UI incrementally, based on user journeys… do a thorough analysis and forensics specs all user journeys, we unify with the designs we have, work out the flows, and the screens, implement accordingly, and refine till we nail it to perfection. Only then do we proceed with any backend work… No rush to backend at all. Pure UI nail it with the journey. A shippable product then we do the backend and make it actually live."* | (A) Backend-and-frontend interleaved per-milestone (locked plan default M0–M13) — rejected: front-end refinement cycle requires no backend dependency, and locking schema before UX is settled creates rework. (B) Front-end with mock data only, never integrate — rejected: backend is the eventual destination. (C) Backend-first to lock contracts — rejected: no UX clarity yet. | Strategic Plan milestones rebalance: M0–M(N) are pure-UI cycles producing a clickable, refinable, shippable-feeling app on mobile + web with stub data. Backend cycles M(N+1) onwards. Per-cycle handoff to user for "is this nailed?" judgment. Risk: stub-data may hide schema issues — mitigated by preserving the locked Project Plan's data model as the contract the eventual backend cycle implements. | Permanent for this build. Future Mingla projects default to this UI-first cadence unless a backend constraint is specifically blocking. |
| DEC-070 | 2026-04-28 | **ORCH-BIZ-DESIGN-AUDIT-001 — Founder resolutions on 14 audit questions. 9 ADOPTED, 1 KILLED outright (onboarding), 1 RESERVED-FOR-BRAINSTORM (nav structure), 1 PLACEMENT-PENDING (Scan), 1 BUILD-WHEN-READY (Marketing — no placeholder), 1 ADOPT-WITH-REFINE (wizard labels).** Specific resolutions: Q-A1 (5-tab nav) = NO, brainstorm needed; Q-A2 (Scan tab) = yes feature, but NOT in nav (waste of real estate); Q-A3 (Marketing placeholder) = NO placeholder, build-and-ship-when-ready; Q-A4 (Public Brand Page in MVP) = YES; Q-A5 (slow per-cycle absorption) = YES, user-decided per cycle; Q-A6 (brand-chip topbar trigger) = YES; Q-A7 (status pill copy) = YES; Q-A8 (KPI tile vocabulary) = YES; **Q-A9 (onboarding) = KILL TOTALLY** — sign-in routes straight to home; brand creation happens on first event-create or via Account; Q-A10 (wizard step labels) = ADOPT as starting point but plan for extensive refinement; Q-A11 (microcopy) = ADOPT; Q-A12 (sample data UI-only) = ADOPT; Q-A13 (Mingla M monogram) = ADOPT; Q-A14 (icon library) = ADOPT. | User answered Q-A1 to Q-A14 verbatim 2026-04-28 with two clarifications layered on top (DEC-071 frontend-first, DEC-072 producer model). | DEC-058 draft (5-tab locked) — rejected by Q-A1 NO. DEC-064 draft (4-step onboarding) — rejected by Q-A9 KILL. DEC-057 draft (general absorption) — replaced by this DEC-070 with specific outcomes. | Audit's recommended absorption diverges from founder's actual decisions on 4 of 14 questions. Patches in `SPEC_BIZ_DESIGN_ABSORPTION.md` need rewriting to reflect: nav structure deferred, Scan placement deferred, Marketing tab not rendered, onboarding screens DELETED-not-absorbed. Wizard labels adopted-with-refine flag. The remaining 9 patches (Q-A4, Q-A6, Q-A7, Q-A8, Q-A11, Q-A12, Q-A13, Q-A14) apply as drafted. | Permanent. New DECs DEC-058 (nav), DEC-061 (brand-chip), DEC-062 (status pill), DEC-063 (KPI), DEC-065 (wizard), DEC-066 (microcopy), DEC-067 (sample), DEC-068 (monogram), DEC-069 (icon) supersede the audit's drafts with the actual founder-approved scope. DEC-058 nav decision will be drafted after Q-A1 brainstorm resolution. |
| DEC-056 | 2026-04-28 | **ORCH-BIZ-DESIGN-AUDIT-001 — DEC-055 SUPERSEDED. Premature M0 implementor prompt retired (renamed `_SUPERSEDED_IMPLEMENTOR_M0_DESIGN_FOUNDATION.md`). Replaced with forensics audit dispatch BEFORE any implementor work runs.** New flow: forensics audits design package against locked plans → produces absorption strategy (`SPEC_BIZ_DESIGN_ABSORPTION.md`) + audit report (`AUDIT_BIZ_DESIGN_PACKAGE_ABSORPTION.md`) → orchestrator applies patches to PRD/Strategic Plan/Project Plan/Designer Handoff under SYNC mode → THEN implementor dispatches in sequenced cycles. Founder explicit directive 2026-04-28: *"this is a prototype but this is not the final product. This is an idea of what it should look like. We need to see what we can bring in, and tune to fit the business app… we need to audit this, and absorb it into our plan and strategy then delete what is not needed."* | Founder rejected the M0 implementor dispatch as premature: implementing before absorbing the design into our planning trinity risks codifying a divergent direction, introducing scope creep (Marketing module is M14 per Strategic Plan), or carrying decisions that contradict the locked plans without flagging them. The design package is an IDEA, not a contract — it needs forensic audit + line-by-line absorption before any line of code follows. | (A) Proceed with M0 implementor dispatch as originally scoped — rejected: implementor would treat 5-tab nav as default, carry Marketing placeholder, and codify divergences D1–D5 from DEC-055 without founder sign-off. (B) Apply absorption patches directly without forensic audit — rejected: orchestrator without audit-grade evidence cannot honour "documents before reasoning" prime directive. (C) Delete design package and continue on locked plans — rejected: founder authorized ingest and explicitly wants assimilation. | Audit phase adds 1 cycle before any implementation. Trade-off accepted: the audit prevents N rework cycles where N is the number of divergences absorbed without explicit founder decision. Forensics agent's investigate-then-spec mode is the right tool — produces both audit report (evidence) and absorption spec (patches). | Audit returns → orchestrator reviews → founder approves absorption strategy → orchestrator applies patches under SYNC mode → THEN implementor dispatches resume in sequenced cycles per Strategic Plan milestones. Permanent procedural rule: no design package gets ingested into Mingla Business plans without a forensic audit + founder-approved absorption strategy first. |
| DEC-055 | 2026-04-28 | **[SUPERSEDED BY DEC-056]** ORCH-BIZ-M0-DESIGN-001 — Claude Design package ingested. M0 implementor dispatch scoped to design tokens + glass primitives + 5-tab nav shell ONLY. No screen-level work this dispatch. Marketing tab routes to locked-state placeholder until M14. Design-package source path: `Mingla_Artifacts/design-package/mingla-business-app-screens/`. Implementor prompt: `Mingla_Artifacts/prompts/_SUPERSEDED_IMPLEMENTOR_M0_DESIGN_FOUNDATION.md`. Existing Business `designSystem.ts` extended additively (no deletions, no renames) — new exports: full glass token set (tints, borders, highlights), semantic colors with tints, full shadow stack including glass shadows, full typography roles, easings, durations. Glass primitives created: `GlassCard` (base+elevated), `GlassChrome`, `BottomNav` (5-tab with config prop), `TopBar`, `IconChrome`, `Button`, `Pill`, `Input`, `MinglaMark`, `Icon`. Tab nav shell wires `(tabs)/_layout.tsx` with placeholder routes for all 5 tabs. | Founder uploaded `HANDOFF_BUSINESS_DESIGNER.md` to Claude Design (claude.ai/design); design assistant produced clickable HTML/CSS/JSX prototype across 30+ screens spanning auth/onboarding/home/events/creator/scanner/account/marketing/brand/journeys; founder authorized fetch + ingest 2026-04-28; orchestrator extracted 21 files into Mingla_Artifacts/design-package/; cross-referenced against locked plans; identified 5 divergences (D1–D5); scoped first implementor cycle to M0 strictly per "small increments, perfection over large writes." | (A) Implement screens in same dispatch — rejected: violates throughput rule and small-increments memory directive; risks rework when D1 (4-tab vs 5-tab) lands a different way. (B) Implement tokens-only without primitives — rejected: tokens without primitives can't be QA'd; primitives prove the tokens work end-to-end on iOS+Android+web. (C) Adopt 5-tab as permanent without founder sign-off — rejected: locked plans + Designer Handoff have 4-tab; this is a strategic UX decision the founder must explicitly approve. (D) Skip the design package entirely and continue on locked plans — rejected: founder explicitly authorized the ingest and instructed "implement the relevant aspects." | (a) Five divergences logged as D1–D5 in implementor prompt: D1 (5-tab vs 4-tab) — implementor builds tabs as a config prop so a future flip is config not refactor; D2 (Marketing module designed despite Post-MVP per Strategic Plan M14) — placeholder only this dispatch; D3 (BrandSwitcher in TopBar) — primitive built without sheet, sheet ships in later cycle; D4 (light vs dark default) — confirmed dark-default, decision closed; D5 (token coverage gap) — additive extension only. (b) 30+ screen designs in design package treated as REFERENCE for follow-on dispatches, not for this cycle. (c) Marketing as a tab in nav (vs locked plan's Chat-tab placeholder) — placeholder rendered as locked-state copy "coming in M14"; if 4-tab path wins later, Marketing tab gets removed entirely. | When founder resolves D1, D2, D3 explicitly via DEC-056 (or sub-decisions): if 5-tab approved → no rework; if 4-tab approved → BottomNav config switches to 4-tab via prop, Marketing route deletes, Chat placeholder replaces. Either way is a one-cycle change because the primitive was built config-aware. |
| DEC-049 | 2026-04-22 | **ORCH-0640 — 7-day archive retention. No 14-day shim window for deleted edge fns.** Post-cutover, dropped tables rename to `_archive_*` for 7 days then DROP. Deleted edge functions (discover-experiences if audit-confirmed dead, generate-single-cards, warm-cache, new-generate-experience-, refresh-place-pool, ai-verify-pipeline, and any others found redundant) are REMOVED immediately at cutover. No thin shims. No backward-compat stubs. | User directive 2026-04-22: *"No. Cause confusion. If there are any gaps. And it is broken, we can fix it. We are not marketing and only have family and friends. We must get it right now. No harm if it's broken for some people. We will fix it ASAP."* | (A) 14-day shim window — rejected: creates two truths during migration, defeats the point of big-bang. (B) 30-day archive — rejected: we already have git revert + the archive rename itself is reversible in seconds. | An old mobile bundle calling a deleted edge fn gets HTTP 404. Family-and-friends users see a broken deck, report, fix lands within hours. Acceptable blast radius given the pre-launch user base. | Permanent. Reevaluate archive / shim policy when paid users exist. |
| DEC-048 | 2026-04-22 | **ORCH-0640 — NO SHIMS. Deletion is immediate at cutover.** No backward-compatibility stubs for deleted edge functions. No parallel read paths. No feature flags gating old vs new. | Same user directive as DEC-049. Combined with DEC-042 big-bang, the cutover is a clean break. | Shim layer (A7 original recommendation) — rejected by user. | Fix-forward cost if post-cutover issue. Mitigated by small user base and immediate rollback via git revert + archive rename. | Permanent. |
| DEC-047 | 2026-04-22 | **ORCH-0640 — Engagement fan-out: 4 events per curated interaction for every event kind.** When a user `serves` / `seen-deck-renders` / `seen-expands` / `saves` / `schedules` / `reviews` a curated card, engagement_metrics fires FOUR events: (a) 1 event on the experience container (experience_id = curated_experiences.id), (b) 3 events on each stop's place_pool_id. Per-kind. | User directive 2026-04-22: *"4. 3 for the stops, and 1 for the curated card itself. same when the user expands the card, saves the card, schedules the card, or reviews the card."* DEC-039 "track across stops" clarified. | Single-container-only event — rejected (loses per-place engagement signal that drives Scorer feedback). Stop-only events — rejected (loses curated-as-product analytics). | Write amplification 4× on curated interactions. Acceptable under current QPS. `engagement_metrics` schema must accommodate both identity types (experience UUID vs place_pool UUID). | Permanent. |
| DEC-046 | 2026-04-22 | **ORCH-0640 — MAXIMUM CONSOLIDATION MANDATE. Every surviving edge function / RPC / admin page audited: "can one thing do what three do?" Delete anything redundant or doing a job a surviving component can already perform.** Spec writer must produce a consolidation pass alongside the demolition: (a) collapse redundant edge functions (e.g. if `discover-experiences` duplicates `discover-cards`, merge), (b) collapse redundant RPCs into parameterized variants where possible, (c) collapse admin pages that preview the same data into one, (d) collapse mobile service/hook pairs that wrap each other. | User directive 2026-04-22: *"we must trace the mobile chain and make sure everything works with the new serving path simplifying things if they are already too complex. we have so many functions doing something that one function could do making the application bulkier than it needs to be"* + *"audit, and delete everything that is redundant, or doing a job that something else we are keeping already does or can do"* | Minimal-touch migration (only delete card_pool + AI-validation, leave everything else) — rejected: perpetuates the bulk. | Bigger spec scope, bigger implementation pass. Counter-balanced by simpler architecture permanently. | Permanent — "can one thing do what three do?" becomes a standing review question for all future Mingla work. |
| DEC-053 | 2026-04-22 | **ORCH-0640 × ORCH-0634 coordination CLOSED + ALIGNED. ORCH-0634 spec v3 locked, accepting both surgical asks. ORCH-0640 scope tightens.** ORCH-0634 session confirmed via paste-back 2026-04-22: (a) Ask-1 G3 photo-gate patch ACCEPTED + VERIFIED LIVE. Leak quantified to 9 rows, all `__backfill_failed__` sentinel (not null-photos). New migration added to their spec with identical predicate pattern as legacy `query_pool_cards`. (b) Ask-2 SC-11 scope NARROWED to `discover-cards` + `generate-curated-experiences` edge fns only (non-serving callers explicitly out of scope, delegated to our cleanup). (c) `cache_key` formula LOCKED as `sha256(experience_type + ':' + sorted_stop_place_pool_ids.join(','))` with protective code comment. (d) I-NO-CURATED-PERSISTENCE already in their spec, so DEC-052 reversal was a no-op delta for them. (e) Their "Reference D" section now permanently records this coordination. **ORCH-0634 final scope:** 3 migrations (curated_teaser_cache, deprecation comments, G3 patch), 2 edge fns modified, 1 helper, 1 mobile service tweak. 6-8h impl + 2-3h test. Awaiting Implementor dispatch on their side. Ping-back promised on ship. **ORCH-0640 scope inheritances (now OUT of our spec):** G3 patch on `query_servable_places_by_signal`, `curated_teaser_cache` creation, deprecation comments on `card_pool`/`card_pool_stops`/`query_pool_cards`, discover-cards legacy branch removal, generate-curated-experiences rewire, `fetchSinglesForSignalRank` place_pool-direct rewrite, mobile deckService round-robin. **ORCH-0640 scope retained:** physical DROP of `card_pool`+`card_pool_stops`, full AI validation demolition (DEC-043), Rules Engine carve-out (DEC-045), `admin_place_pool_mv` rebuild, admin redesign (17 pages), `engagement_metrics` table + RPCs + triggers + dual-key schema (DEC-039/047/052), legacy `experiences`/`saves` demolition (DS-6, DEC-050), mobile file deletions, doc updates. **ORCH-0640 physical-drop cutover explicitly gated on ORCH-0634 shipping first.** | Confirmation from ORCH-0634 paste-back 2026-04-22. Live DB verification (9-row leak confirmed `__backfill_failed__` pattern). Both specs acknowledge the sequential handoff. | — | Sequential dependency: ORCH-0640 implementor dispatch cannot land migrations until ORCH-0634 ships. Spec can still be WRITTEN in parallel by the spec-writer — no blocker at spec phase. | Permanent — closes the cross-session coordination; both specs reflect the same architecture. |
| DEC-052 | 2026-04-22 | **ORCH-0640 — DEC-038 REVERSED. Curated experiences do NOT persist. Adopt ORCH-0634's I-NO-CURATED-PERSISTENCE.** Curated cards are assembled fresh from `place_pool` + `place_scores` on every request. No `curated_experiences` table. No `curated_experience_stops` table. No cascade-on-stop-loss trigger. The only persisted curated-related artifact is `curated_teaser_cache` (shipped by ORCH-0634) — it caches GPT teaser text keyed by `cache_key = sha256(experience_type + ':' + sorted_stop_place_pool_ids.join(','))`. Engagement tracking (DEC-039/047) uses dual-key schema: single-card events set `place_pool_id` only, curated-stop events set `place_pool_id` + `container_key` (= the same cache_key), curated-container events set `container_key` only. All analytical questions from DEC-039/047 remain answerable — aggregation by hash is cleaner than UUID fragmentation. Admin "browse curated" visibility preserved via `curated_teaser_cache` (list distinct cache_keys + their stop compositions). | ORCH-0634 spec `outputs/SPEC_ORCH-0634_SIGNAL_ONLY_SERVING_AND_INTERLEAVE.md` has been in parallel dispatch and converges on exactly the architecture the DEC-044 North Star + DEC-046 Consolidation Mandate require. User acknowledged 2026-04-22: *"If there is a better way to do it without your tables then we go for it."* Fresh assembly + teaser cache eliminates the second source of truth for "what curated exists," aligning with Const #2 (one owner per truth) more strictly than DEC-038 did. | Maintain DEC-038 (persisted curated tables) — rejected: reintroduces the second-source-of-truth pattern we spent the whole session demolishing; extra schema for zero analytical gain given the hash-keyed engagement alternative. | (a) Collab parity depends on signal-RPC determinism — probed H confidence via `ORDER BY score DESC, review_count DESC NULLS LAST` (deterministic), to be formally verified by ORCH-0634 spec tests. (b) Latency per request rises ~4× (50ms lookup → 200-500ms assembly) — acceptable at family/friends scale; benchmarkable. (c) Admin "browse curated" migrates from hypothetical table-scan to cache_key-scan — same visibility, different source. | Permanent — this is the final curated architecture for Mingla. |
| DEC-051 | 2026-04-22 | **ORCH-0640 — Scope clarified: Discover screen is Ticketmaster-only. No For-You tab. No hero cards. No featured cards. `discover-experiences` edge fn and `experienceGenerationService.discoverExperiences()` method are fully orphan and DELETE outright.** Verified 2026-04-22: `DiscoverScreen.tsx` header declares it *"Tinder-Explore glass events surface (ORCH-0590 Phase 3). Dark-glass, single-purpose concerts & events browser"* — reads only `NightOutExperiencesService` + Ticketmaster. Zero reference to `discoverExperiences`, hero cards, or featured cards. The 2 remaining LIVE methods on `experienceGenerationService.ts` are `fetchCompanionStrollData()` (called by `ExpandedCardModal.tsx:1426` for curated stroll companion) and `fetchPicnicGroceryData()` (called by `ExpandedCardModal.tsx:1474` for picnic shopping list). These power curated-card expanded-view features. Both survive as REWRITE (read from new `curated_experiences` tables). The `discoverService.ts` consolidation target is ONLY deck/save concerns — NOT a Discover surface replacement. `get-companion-stops` and `get-picnic-grocery` edge fns survive as REWRITE (confirmed live). | User directive 2026-04-22: *"This is gone. The only thing on the discover screen right now is the Ticketmaster events. Everything else was deleted. No more For-You tab. We don't need anything from there."* | Rebuild a For-You surface post-cutover — rejected: user explicitly removed it from scope; product direction has pivoted Discover to events-only. | Simpler spec: one less surface to rebuild. `experienceGenerationService.ts` shrinks from 3 methods to 2; those 2 methods move to `stopReplacementService.ts` (already exists for `replace-curated-stop`), then `experienceGenerationService.ts` is DELETED entirely. Mobile service count consolidates further. | Permanent. Discover remains events-only unless product reverses. |
| DEC-050 | 2026-04-22 | **ORCH-0640 — Scope expanded: delete the entire legacy `experiences` + `saves` + `saved_experiences` + `experiencesService` + `experienceGenerationService` + `useExperiences` + `savesService` + `places` edge fn + `discover-experiences` edge fn parallel architecture.** Post-audit discovery 2026-04-22: Mingla has TWO live card-generation architectures running simultaneously. Old (2025-era): mobile calls `experiencesService.ts`/`experienceGenerationService.ts` → writes to `saves` (573 rows, +383 in last 7d) / `experiences` (1,088 rows, +170 in last 7d) tables directly via mobile-side Supabase client OR via `places`/`discover-experiences` edge fns. New (2026-era): mobile calls `deckService.ts`/`pairedSavesService.ts` → `discover-cards` → `place_pool`/`card_pool`. The OLD architecture is STILL BEING WRITTEN TO DAILY. `discover-experiences` edge fn has 2 live call sites in mobile (`experienceGenerationService.discoverExperiences()` at lines 157-209 + `useForegroundRefresh.ts:38`). `useExperiences.ts` reads `saves` 3× and `experiences` 1×. `experiencesService.ts` reads both 7×. This is the bulk user felt. Full scope now includes: (a) DROP tables `saves`, `experiences`, `saved_experiences` (0 rows, semi-dead); (b) DELETE mobile services `savesService.ts`, `experiencesService.ts`, `experienceGenerationService.ts`, `useExperiences.ts`; (c) DELETE edge fns `places` (writes `experiences`), `discover-experiences` (reads `query_pool_cards`); (d) MIGRATE hero/featured-card surface on Discover screen to call new signal-serving RPC(s) via `discoverService.ts` (NEW — consolidated replacement); (e) ACCEPT 573 user saves will be lost at cutover per DEC-049 (family/friends pre-launch); (f) pre-cutover export dump of `saves` + `experiences` + `saved_experiences` to CSV as safety net. | Live DB probes 2026-04-22: saves_last_7d=383, experiences_last_7d=170, saves_fk_live_experiences=573 (all valid in old architecture). Live mobile grep: 9+ distinct service/hook files writing to old tables. DEC-046 consolidation mandate requires removal; DEC-049 no-shims makes fix-forward acceptable. | Keep the old parallel architecture indefinitely — rejected: perpetuates the bulk DEC-046 is rejecting. Migrate `saves` → `saved_card` format — rejected: family/friends data is disposable per DEC-049, migration effort not worth it. Split into two separate ORCH-IDs (card_pool demolition vs experiences demolition) — rejected: same Const #2 root cause, same cutover, one spec. | 573 user saves vanish at cutover. Accepted per DEC-049. Discover screen's For-You hero/featured surface must be rebuilt in the spec using signal-serving RPC calls instead of `discover-experiences`. Any mobile flow touching experiences/saves tables must be rewired to deckService/pairedSavesService pattern. | Permanent. The architecture simplifies from "triple-parallel card+save systems" to "one card pipeline + one save pattern." |
| DEC-045 | 2026-04-22 | **ORCH-0640 — Spec-binding decisions for OQ-A1, A2, A4.** (A1) Rules Engine carve-out via **rename + narrow**: `ai_validation_jobs` → `rules_runs` (purge rows where `rules_version_id IS NULL`), `ai_validation_results` → `rules_run_results` (cascade via job_id), `ai_validation_batches` → DROP entirely (pure-AI, no rules coupling). 12 `admin_rules_*` RPCs rewritten to new table names. (A2) **`PhotoPoolManagementPage.jsx` REATTACHED** to new "Supply" nav group alongside SeedPage + RefreshTab + SeedTab (the latter two extracted from the deleted AIValidationPage). Photos are Gate 3 — the page is how admins see per-city G3 coverage. (A4) **`saves` (573 rows) and `experiences` (1,088 rows) legacy table audit AUTHORIZED** — 15-min targeted investigation completed pre-spec (results in Master Bug List + spec prompt). | User directives 2026-04-22 (8-question round): A1 "a", A2 "yes", A4 "we must audit. No harm in always auditing. It is a must!" | A1 option B (new tables + migrate) — rejected: more complexity, more RLS to write. A2 delete — rejected: photos are Gate 3, page is needed. A4 skip audit — rejected by user directive. | Rename operation is atomic + transactional; `admin_rules_*` RPC rewrite is 12 mechanical find/replaces. Reattaching PhotoPoolManagementPage is a one-line App.jsx import + one NAV_GROUPS entry. The audit itself is read-only probes + grep. | Permanent. |
| DEC-044 | 2026-04-22 | **ORCH-0640 — NORTH STAR locked: the ONE card-generation pipeline is the 3-gate signal path. Every engineering decision is evaluated against whether it serves this goal.** The gates, in order: **(G1) Does the Bouncer approve this?** → `place_pool.is_servable = true`. **(G2) How high is the score for this signal? Show higher scores first.** → `place_scores.score DESC`, filtered at or above the signal's minimum threshold. **(G3) Does it have downloaded photos?** → `place_pool.stored_photo_urls` is non-null, non-empty, not `['__backfill_failed__']`. A card is ELIGIBLE if and only if G1 ∧ G2 ∧ G3 all hold. Every serving RPC in the repo must enforce all three. Every consolidation, every deletion, every migration is oriented to make this the single, efficient, scalable path. | User directive 2026-04-22: *"The ultimate goal for card generation is to generate cards from signals. 1. Does the Bouncer approve this. 2. How high is the score for this. Show higher scores first. 3. Does it have downloaded photos. Every single cleanup and engineering is geared towards this is the most efficient and scalable way possible. I need you to pin this down as the goal."* Live probes 2026-04-22 confirm: 9,989 places pass all 3 gates currently (out of 10,010 scored × 12,356 Bouncer-approved × 17,477 photo-complete); every one of 15 signals has full coverage (100% of scored places pass photo+Bouncer gates per signal). **Photo-gate leak discovered:** 9 places are `is_servable=true` with no real photos; `query_servable_places_by_signal` LIVE body MISSING gate G3 (photo check) — see §Finding-G3 below. | Alternative gate orderings (photo-first, score-first, bouncer-first) — rejected: the user's ordering is the operational priority. Multi-gate AND-semantics (all must hold) vs OR (any passes) — AND is the only sane policy given "eligible-to-serve" is a hard constraint. Adding a 4th gate (e.g. "has valid opening hours") — rejected for now; if needed, surfaces as a serve-time filter downstream, not a pool-eligibility gate. | Universal simplification trade-off: a place that is scored + photo-complete but temporarily Bouncer-not-yet-run is invisible until Bouncer catches up. A place that is Bouncer+scored but photo-failed is invisible until backfill succeeds. Three pipelines (Bouncer, scorer, photo backfill) must all reach each place for it to serve. This is a deliberate quality gate, accepted cost. | Permanent. This is the architecture of Mingla card serving. |
| DEC-043 | 2026-04-22 | **ORCH-0640 — Delete the AI-validation layer entirely + redesign the admin dashboard.** `ai_validation_jobs` (26 rows), `ai_validation_batches` (347 rows), `ai_validation_results` (9,866 rows) tables dropped. `ai-verify-pipeline` edge function deleted. `place_pool.ai_approved` + `card_pool.ai_override` columns dropped. `ai_validation_page.sql`, `ai_validation_coverage_rpc.sql`, `fix_ai_validation_rpcs.sql`, `ai_validation_jobs.sql` migrations superseded. Bouncer (`run-bouncer`) + signal scorer (`run-signal-scorer`) + Rules Engine (ORCH-0526, `rule_sets`/`rule_set_versions`/`rule_entries`/`rules_versions`) become the only quality/eligibility surface. Admin pages retired: `CardPoolManagementPage.jsx`, `ContentModerationPage.jsx` card_pool flows, AI Validation page (if present), `UserManagementPage` card_pool_id columns. Admin rebuilt around: place_pool health, place_scores by signal, Bouncer run history, Rules Engine tab (ORCH-0526, already shipped), curated_experiences editor, engagement_metrics analytics. Live probe 2026-04-22 confirmed AI and Bouncer diverge (990 places is_servable=true AND ai_approved=false; 33,365 ai_approved=true but not Bouncer-approved). | User directive 2026-04-22: *"delete almost everything about card pool, and AI validation. It is causing confusion. The old architecture is doing more harm than good."* Live DB probes confirm AI validation runs produce results that disagree with Bouncer. Running two quality gates that disagree breeds drift. Rules Engine (ORCH-0526 shipped 2026-04-19) is the replacement admin surface for deterministic rule management. | Keep ai_validation tables as historical — rejected: user explicit deletion directive; 90-day `_archive_ai_*` acceptable compromise if needed. Hybrid (drop AI validation, keep card_pool) — rejected: they share the same Const #2 root cause. | Lose the GPT-4o-mini quality-gate signal (9,866 result rows). Rules Engine + Bouncer must absorb the semantic work. Any legitimate AI-override case needs a new path. Admin users retrained on new dashboard. | Permanent. |
| DEC-042 | 2026-04-22 | **ORCH-0640 — Big-bang migration, NOT strangler.** One coordinated ship: all DDL applied, all edge functions rewritten pool-only, all admin pages retired or rebuilt, all mobile OTA-pushed, all docs updated, `card_pool`+`card_pool_stops`+`ai_validation_*` tables archived as `_archive_*` then dropped after 7d soak. Single release gate instead of 6-phase strangler. | User directive 2026-04-22: *"B big bang. The swipeable deck already uses the main serving path. We need to redesign the entire admin. Delete almost everything about card pool and AI validation. It is causing confusion. The old architecture is doing more harm than good."* Key enabler: the deck is already on `query_servable_places_by_signal` at 100% cohort for 14 signals (F-2 from 0640 investigation), so the "risky path" is already the default path. Staying in the intermediate state prolongs the harm. | (A) Strangler 6-phase over 3 weeks — rejected: user assessed that the current architectural duplication is actively causing confusion; prolonging it is worse than the big-bang risk. Small dataset (saved_card=30, user_interactions=2,155, person_card_impressions=0, user_visits=0, place_scores=150K, card_pool_stops=1,944) makes big-bang tractable. | Higher risk of a release-day issue; no per-surface rollback; all rollback is single git revert + migration revert. Mitigated by: 7-day archive retention on every dropped table, mobile OTA pre-landed, full tester pass required before flip, pre-cutover `mcp__supabase__create_branch` for DB rollback test. | Permanent for this migration. Future deprecations default to strangler unless similar harm-from-duplication applies. |
| DEC-041 | 2026-04-22 | **ORCH-0640 — Signal-serving RPC family is the ONE authoritative card-generation path. Update all docs + README.** After the `card_pool` drop (DEC-037–040), every card served to the app — swipeable deck, curated carousel, friend-hero, paired partner view reads, discover — flows through a `place_pool`-driven RPC surface. `query_servable_places_by_signal` (already live for singles at 100% cohort across 14 signals) and its curated/hero siblings (to be specced) are the only code paths. Legacy `query_pool_cards`, `query_person_hero_cards`, and every `card_pool`-touching edge function either get rewritten pool-first or retired. Documentation updates are in spec scope: repo `README.md`, `app-mobile/README.md`, `Mingla_Artifacts/PRODUCT_SNAPSHOT.md`, `WORLD_MAP.md`, `COVERAGE_MAP.md` all declare signal-serving authoritative and `card_pool` dropped. | User directive 2026-04-22: *"Update all documents and the readme that the signal-serving path is the new and only way."* Signal-serving has been proven in prod for weeks (14 slices shipped across ORCH-0588 through ORCH-0601 + ORCH-0628, each at 100% cohort, device-verified). Making it authoritative is formalizing what's already true for the bulk of the traffic. | Maintaining dual serving paths (signal + legacy `card_pool` pipeline) indefinitely — rejected as the root Const #2 violation we are resolving. | Docs now a load-bearing artifact; any future edge function that reads `place_pool` for serving must conform to the signal-serving contract. New category chips can only ship after their signal + chip mapping is registered. | Permanent — this is the architecture for Mingla card serving going forward. |
| DEC-040 | 2026-04-22 | **ORCH-0640 — Wipe all Google-API fallback code from USER CARD-SERVING paths. EXEMPTIONS (preserve): (a) Admin intake paths — `admin-seed-places`, `admin-refresh-places`, `admin-place-search` — these ARE the pool supply chain. (b) PreferencesSheet custom-location autocomplete — `app-mobile/src/services/geocodingService.ts`, `app-mobile/src/utils/throttledGeocode.ts`, any PreferencesSheet consumer. Google Places Autocomplete here is a legitimate geocoding helper, not a card fallback.** The investigation confirmed `discover-cards/index.ts` has zero Google refs today; residual `googleApiKey` params survive on `serveCardsFromPipeline`/`serveCuratedCardsFromPool` (always passed as `''`), and shared helpers (`placesCache`, `textSearchHelper`, `photoStorageService`) still carry Google-fetch code paths that are reachable only from admin-intake. Spec scope: remove the dead `googleApiKey` param from every non-admin-intake call site, audit + delete the legacy edge functions `warm-cache`, `new-generate-experience-`, `get-picnic-grocery`, `get-companion-stops` unless proven live, tag remaining Google-calling helpers as `[ADMIN-INTAKE-ONLY]` or `[GEOCODING-ONLY]` in file headers. | User directive 2026-04-22: *"wipe out ANY GOOGLE FALLBACKS"* + explicit clarification *"The preferences sheet uses google for custom locations. Find out which feature is used there. That stays."* Rationale: one deterministic source of truth (place_pool) + one deterministic serving path (signal-serving RPC family) for CARDS. Geocoding for user-entered custom locations is a different concern (address → coordinates) and stays. | Keeping Google fallback as a "safety net" for pool-empty scenarios — rejected because `query_servable_places_by_signal` returning zero rows is a legitimate signal (seeding gap) that should surface to admin, not be masked by a live API call. | Cannot live-enrich user deck with fresh Google data; seed + Bouncer must produce pool-ready rows before a city can launch. This is the same constraint that drove ORCH-0598.11 launch-city pipeline. PreferencesSheet geocoding usage documented as permanent exemption. | Permanent — single-source-of-truth is a Const #2 commitment for card serving. Geocoding exemption permanent. |
| DEC-039 | 2026-04-22 | **ORCH-0640 — Engagement metrics move to a new dedicated table.** Place-level granularity (keyed on `place_pool_id`). Five event kinds tracked: **served** (card delivered to the deck), **seen** (two sub-kinds counted separately: card rendered in the deck viewport AND card expanded — both count as seen but on distinct counters), **saved**, **scheduled**, **reviewed**. For curated cards, engagement aggregates across every stop's `place_pool_id` — a curated card that includes place X produces a `served`/`seen` increment for X, just like a direct single-card impression does. Existing `user_card_impressions` and `person_card_impressions` FKs pivot from `card_pool_id` to `place_pool_id` (+ `category` for context preservation). Engagement triggers move to the new table. `user_visits.experience_id` + `place_reviews.place_pool_id` remain the write sources; they fan into the new counter home. | User directive 2026-04-22: *"engagement metrics can have their own table. Because we want to track a single place's served, seen (card viewed on the deck or card expanded both count as separate views), saved, scheduled and reviewed, and we want to also track it across stops in curated cards."* | (A) Move counters to `place_pool` — rejected, pollutes the master table with transactional counters. (B) Per-(place, category) key — partially chosen via the `category` context column but the primary engagement identity is place-level. (C) Aggregate-on-read from `user_interactions` — rejected, adds read-path cost and makes signal scorer inputs fragile. (D) Drop counters entirely — rejected, scorer + admin analytics depend on them. | Extra table + triggers to maintain. Per-stop fan-out on curated increases write volume ~2-3× per curated interaction, acceptable under current QPS. | Permanent — engagement is a first-class place-level concept. |
| DEC-038 | 2026-04-22 | **ORCH-0640 — Curated experiences move to a dedicated `curated_experiences` + `curated_experience_stops` table pair. Replaces the current `card_pool` curated rows + `card_pool_stops`.** `curated_experiences` carries `experience_type`, `tagline`, `total_price_min/max`, `estimated_duration_minutes`, `shopping_list`, `curated_pairing_key`, `stops` (JSONB denorm for serve-time reads). `curated_experience_stops` joins each experience to its ordered `place_pool_id` stops, with the same cascade-delete-on-stop-loss semantics currently on `card_pool_stops`. All current curated-specific read paths (`generate-curated-experiences`, hero cards, discover curated) rewrite to read the new tables. | User directive 2026-04-22: *"curated cards can have their own table."* Smallest behavioural delta — preserves multi-stop composition, keeps collab parity (deterministic experience IDs), and keeps the "invalid if any stop disappears" safety that the 2026-03-19 normalization established. | (B) Regenerate curated on every request with no persistence — rejected: adds latency, breaks collab parity (non-deterministic IDs across devices), blocks admin visibility into "what curated exists in city X." (C) Retire curated as a product surface — rejected: a product-level call, not an engineering simplification. | Yet another pair of tables to own. Migration path must backfill existing curated rows from `card_pool` (type='curated') + `card_pool_stops` — a one-shot job gated on the drop. | Permanent — curated is a distinct product concept that deserves its own schema home. |
| DEC-037 | 2026-04-22 | **ORCH-0640 — Drop `card_pool` entirely. `place_pool` becomes the single authoritative source of truth for every card surface in Mingla.** Every read that today touches `card_pool` moves to `place_pool`-driven equivalents (signal-serving RPCs, new curated tables per DEC-038, new engagement table per DEC-039). `card_pool` archived as `_archive_card_pool` for 90 days then dropped. All FKs pointing to `card_pool.id` (`user_card_impressions.card_pool_id`, `person_card_impressions.card_pool_id`, `card_pool_stops.card_pool_id`) pivot to `place_pool_id`. All TEXT `experience_id` columns adopt the forward-only rule: post-cutover, every new value is `place_pool.id::TEXT` or `place_pool.google_place_id`. | Forensics returned feasibility YES with high confidence (ORCH-0640 investigation report 2026-04-22). Three enablers already proven: (F-1) `card_pool` singles are strictly 1:1 with `place_pool` (live probe 8,142:8,142), (F-2) signal-serving path already live at 100% cohort for 14 categories, (F-3) paired view already decoupled via `saved_card` snapshots. Mobile diff is trivial (2 file refs, neither a DB read). Constitutional #2 (one owner per truth) makes the intermediate warehouse the root cause of drift risk. | Keep `card_pool` indefinitely — rejected as the Const #2 violation we are resolving. Hybrid (drop for singles, keep for curated) — rejected, DEC-038 gives curated its own home so we can make a clean break. | 90-day archive retention, 3-table backfill migration, mandatory mobile OTA ahead of final drop, Realtime/cache-key invalidation cycle. All bounded and well-understood. | Permanent — the drop is the end state. |
| DEC-036 | 2026-04-19 15:29 UTC | **ORCH-0526 production flag flip AUTHORIZED under tester CONDITIONAL PASS.** `admin_config.enable_rules_filter_tab` flipped from `false` → `true` at 2026-04-19 15:29:27 UTC via MCP. Active admin users now see the full Rules Filter admin tab (all 16 components + 13 RPCs + CI script + drift detector). User explicitly accepted the 4-BLOCKED-SC post-flip browser spot-check condition (SC-04 panel open / SC-12 Esc / SC-13 Cmd+S / SC-21 drift card click) with a 24-hour verification window. | Tester returned CONDITIONAL PASS with 0 blockers, 18/22 SCs code-level PASS, 4 BLOCKED purely because headless tester cannot click animated UI. Code forensics found no defects in those 4 paths. DB + build + CI + migration deploy all verified clean. Alternative path was "flip gated on human test first" — rejected because (a) the BLOCKED items need the flag ON to test anyway, (b) rollback is single-line and <5 seconds, (c) no production user sees the tab unless they're in `admin_users` (scoped blast radius). | (a) Hold the flip, require human browser test with a local dev-flag flip first — rejected as redundant: admin_config is the only source of truth + local flip would need admin_config edit anyway; (b) Deploy on a subset of admins via email filter — rejected as over-engineering for a scoped admin feature. | If any BLOCKED SC fails during the 24h spot-check window, user reports back, orchestrator writes a rework prompt, flag stays ON while fix lands (no user harm — the tab renders, just with a broken interaction that can be worked around). | Permanent for this flag. For future flags with broader user blast radius (mobile app features, etc.), default recommendation: require release-build + human smoke before flip. |
|----|------|---------|---------|----------------------|-------------------|----------------|
| DEC-035 | 2026-04-19 | **ORCH-0526 M2 concurrency: status-based check, NOT pg_advisory_lock.** `handleRunRulesFilter` refuses new run with HTTP 409 + active_job_id when an existing run for the same `(stage IN [rules_only, rules_only_complete], city_id)` is in `status IN [ready, running, paused]`. Same pattern as `handleCreateRun` (lines 944-950). The M1-built `try_advisory_lock_rules_run` + `release_advisory_lock_rules_run` RPCs remain available but unused in v1. | Spec §5.1 originally said "Acquire advisory lock at handler entry via pg_try_advisory_xact_lock". M2 pre-flight discovered pg advisory locks (xact-scoped) only hold within a single transaction; the Supabase JS client wraps each `db.from().*()` and `db.rpc()` in its own transaction, so the lock auto-releases between operations and provides zero protection across the multi-step handler. Session-level locks (`pg_advisory_lock`) span statements but require explicit unlock + risk orphan locks if the edge function crashes before unlock. | (a) `pg_try_advisory_xact_lock` — lock auto-releases between statements (no protection); (b) `pg_advisory_lock` (session-level) — risks orphan locks on crash + adds complexity; (c) full PL/pgSQL transaction wrapper — wholesale handler rewrite. | Same effective behavior (409 on contention) as the spec intended; loses the architectural cleanliness of "lock = primitive owns the protection" in favor of "DB row state is the lock." | Permanent for v1; revisit if (i) status-based check ever races (extremely unlikely — single edge fn per request), (ii) we want non-rules-filter actions to coordinate with rules-filter runs (not v1 scope), or (iii) the M1 advisory-lock RPCs find another consumer. |
|----|------|---------|---------|----------------------|-------------------|----------------|
| DEC-033 | 2026-04-19 | **Skip AH-151 Release-build pre-flip gate for ORCH-0525 flag flip.** User elects to ship the 2-line flag flip (`FEATURE_FLAG_PROGRESSIVE_DELIVERY` + `FEATURE_FLAG_PER_CONTEXT_DECK_STATE` → `true`) directly to prod via AH-152 implementor + OTA, without first running a separate EAS Release build + manual 7-test matrix. | User already has Sentry on the app (crash observability); all code paths are tester-PASSED on Debug builds (AH-135, AH-137, AH-146); prod rollback is a 1-line revert + OTA (< 5 min); avoiding a second EAS build saves ~15-20 min of build time + credentials cost. | AH-151 full Release-build pre-flip test matrix — rejected in favor of trust-the-Debug-testing + Sentry-on-ship. | Any Debug-only regression (minification behavior, Hermes Release optimizations, dead-code-elimination interactions) would not be caught until users hit it + Sentry fires. If that happens, rollback is immediate but user impact window is the crash-report lag (minutes to hours). Acceptable because existing prod code is ALREADY broken (ORCH-0491, ORCH-0498, etc.) — any Release-only regression is bounded by "can't be worse than status quo for those flows" since flag-off path IS the status quo. | Permanent for THIS flip. For future flag flips, default recommendation remains: run AH-151-shaped gate on Release build first. |
| DEC-034 | 2026-04-19 | **ORCH-0526 v1 scope locked** — 5 designer-open-question resolutions: (Q1) role-based rule editing DEFERRED to v2; (Q2) JSON export of current rules manifest INCLUDED in v1 via `admin_rules_export()` RPC + UI button; (Q3) `reason` field REQUIRED for blacklist + demotion entry-additions, OPTIONAL for whitelist/promotion/strip/keyword-set adds; (Q4) "AI suggests entries" sub-tab DEFERRED to v2; (Q5) "Sandbox" run-against-copy mode DEFERRED to v2. | Designer surfaced 5 open questions in `outputs/DESIGN_ORCH-0526_RULES_FILTER_TAB.md` §13 needing product judgment before spec. | Approving Q1+Q4+Q5 in v1 (each adds 2-5 extra days for marginal value). Refusing Q2 (small RPC + button, useful for backup/sharing). Removing reason requirement on whitelist (low impact, audit trail not critical). | Bigger v2 backlog (3 deferred features). v1 ships faster + tighter. Audit log via `admin_audit_log` already captures who-changed-what for v1; role tier is additive later. | Q1+Q4+Q5: revisit after 4 weeks of v1 production usage. If admin team requests, build then. |
| DEC-032 | 2026-04-19 | **Path B for ORCH-0526 Rules Engine** — migrate the 18 deterministic rule sets out of `ai-verify-pipeline/index.ts` TypeScript constants into DB tables (`rule_sets`, `rule_set_versions`, `rule_entries`, `rules_versions` manifest). Admin UI reads + writes rules directly. Edge function reads rule sets from DB at job start, caches per-run. Versioning is explicit (rules_version_id FK on ai_validation_jobs). Per-result rule attribution via rule_id FK on ai_validation_results. | Need to ship "Rules Filter" admin tab; vibes will multiply rule count ~50%; orchestrator §13 audit recommended Path B for long-term correctness. | Path A (extend tables, keep rules in code, ~1 week — closes visibility + versioning but NOT tweakability or 4-source drift) and Hybrid (A first, B later — risk of B never shipping). | ~3 weeks build vs 1 week. Lose TypeScript compile-time safety on rule_entries values (runtime validation only). One extra DB read at edge function job-start (cacheable). | Permanent — vibes will reuse this same architecture. |
| DEC-001 | 2026-03-20 | Pool-first card serving — all card-serving functions use card_pool only, zero Google/OpenAI at serve time | Performance + cost + reliability. 1,463 lines removed. | Keep Google fallback for cache misses | No real-time data freshness (hours, prices from generation time) | When real-time enrichment pipeline exists |
| DEC-002 | 2026-03-21 | AI validation as sole quality gate — remove all SQL type-exclusion blocks | Type exclusions were too coarse (blocked entire categories). AI gives per-card judgment. | Keep SQL exclusions as safety net | Unvalidated cards hidden instead of shown with bad data | Never — AI gate is permanent |
| DEC-003 | 2026-03-22 | OneSignal android_channel_id disabled — was killing all push delivery | Root cause: channel not configured in OneSignal dashboard → 400 error | Configure the channel properly | No Android notification channel customization | When OneSignal dashboard is properly configured |
| DEC-004 | 2026-03-23 | Single entry point for collaboration (pill → SessionViewModal) | CollaborationModule + BoardViewScreen were redundant code paths causing confusion | Keep both paths with shared state | More complex modal, but single code path | Permanent |
| DEC-005 | 2026-03-23 | Service error contract deferred — TRANSITIONAL logging instead | ~60+ call sites need ServiceResult<T>. Too much blast radius now. | Fix all 60+ sites immediately | Silent failures still possible in ~4 services | Next hardening cycle |
| DEC-006 | 2026-03-24 | Remove invalidateQueries from preference save — use prefsHash matching | invalidateQueries caused race condition with batch fetch | Keep invalidation with delay/debounce | Slightly more complex batch validation logic | Permanent |
| DEC-007 | 2026-03-25 | Constitution principles (14 rules) locked into README | Prevent regression of hard-won fixes | Informal guidelines without enforcement | Protective comments in code may slow development | Permanent — can add, not remove |
| DEC-008 | 2026-04-06 | Collab aggregation = UNION for all preferences | User directive: everything combined, widest possible set | Median (balanced), MIN (most restrictive) | May show more results than any individual user wanted — acceptable for collab | Permanent |
| DEC-009 | 2026-04-06 | Travel mode: widest radius for card search, each user sees their own mode's travel time in UI | Radius = Math.max of all constraint values using most permissive mode for query | Majority vote on mode | Server queries with driving radius even if one user walks — more cards, user sees their own travel time | Permanent |
| DEC-010 | 2026-04-06 | Time slots: UNION array — show places open during ANY selected slot | If one picks Dinner and one picks Brunch, show places open for either | Majority vote (current), ignore time filtering | More results but all match at least one participant's preference | Permanent |
| DEC-011 | 2026-04-06 | Date option: UNION — show cards available at ANY selected date/time window | If one says "Now" and one says "This Weekend", show both windows | Most future wins, majority vote | Broader card set, no participant's date preference is ignored | Permanent |
| DEC-012 | 2026-04-06 | Visibility = who sees YOU, not what you see. "Show on map" toggles control what you see. | User's visibility_level controls who can see them on the map. It does NOT restrict what the user can see — that's the "Show on map" toggles (people/places/feed/heatmap). | Bidirectional matching (both must opt in) | Simpler mental model, matches user expectation of "I set everyone = everyone sees me" | Permanent |
| DEC-013 | 2026-04-06 | Friend requests to seed users: show "request sent" UI, silently skip DB insert | FK constraint on friend_requests.receiver_id blocks inserts for mock users. Server-side: detect is_seed, return success without inserting. Client sees normal haptic + state change. Mock user never accepts — natural "ghosting". | Disable button for seeds (breaks interaction), Create auth.users rows (complex, billing risk) | User gets full interactive experience. No fabricated acceptance — just a pending state that never resolves. | Permanent |
| DEC-014 | 2026-04-06 | Grid-based stranger seeding (~1.5M rows) across global land grid every ~10km | Uniform global coverage. Users see strangers everywhere, not just near cities. One-time generation. | On-demand (dynamic complexity), Major cities only (rural gaps), 5.3M uniform (too large) | Large dataset but feasible in PostgreSQL. Complete coverage at launch. | Until real user count exceeds seeded density |
| DEC-015 | 2026-04-11 | i18n architecture: i18next + react-i18next + expo-localization with namespace-per-domain JSON files | Need runtime language switching, RTL support path, server-side push translations. | react-intl (heavier), custom solution (maintenance burden) | Adds ~50KB bundle, 171 keys to maintain in each locale | Permanent |
| DEC-016 | 2026-04-11 | OTP multi-channel: SMS + WhatsApp + voice call via Twilio Verify | Users in some regions can't receive SMS reliably. WhatsApp and voice call are Twilio Verify built-in channels. | SMS-only (simpler), separate WhatsApp API (more complexity) | Three channel buttons on OTP step, slightly more complex UI | Permanent |
| DEC-017 | 2026-04-11 | Analytics strategy: product-first scope — define what questions matter before wiring events | Mixpanel is dead (no token), AppsFlyer has 25 events but never dashboard-verified, services don't talk to each other. | Quick-win (just set the token), full integration sweep (build unified layer first) | Delays analytics activation until product skill defines requirements | When product skill completes analytics strategy |
| DEC-018 | 2026-04-11 | Queue docs (SPEC/IMPL/TEST/RETEST) deprecated in favor of AGENT_HANDOFFS.md | Queue docs were skeletal and 12 days stale. AGENT_HANDOFFS tracks the full pipeline in a single dispatch model with 79+ completed dispatches. | Populate retroactively (too much work), delete (lose structure) | Lose per-stage tracking granularity | If pipeline model changes |
| DEC-019 | 2026-04-11 | Dead code elimination before documentation sweep — subtract first, document what survives | Constitution rule #8: subtract before adding. Can't document accurately if dead code inflates counts. | Document first then clean (risk documenting things that get deleted) | Extra session of work before docs start | One-time |
| DEC-020 | 2026-04-17 | `place_pool.ai_categories` column canonically stores **app slugs** (e.g. `brunch_lunch_casual`), not seeding IDs (e.g. `casual_eats`). Fix the consumer (`generate-single-cards`), not the data. | ORCH-0470 — `generate-single-cards` queries using the seeding ID while `ai-verify-pipeline` writes the app slug. Silent zero-card failure on every re-seed of Brunch/Lunch/Casual. Two identifiers exist because seeding splits one app category across multiple Google API calls (casual_eats + casual_eats_world + casual_eats_extended). | Direction B (migrate column to seeding IDs, update all writers + readers) — cost/benefit unfavorable | Lose the ability to preserve which sub-recipe produced a place in the column. No current consumer needs that distinction. | When any consumer requires sub-recipe provenance — re-evaluate then |
| DEC-021 | 2026-04-17 | Pivot from ORCH-0480 (partial-expression index approach) to ORCH-0481 (materialized view layer) as the admin-RPC perf fix. Do not rework ORCH-0480; do not apply ORCH-0480 migration to production. | ORCH-0480 tester verdict FAIL on Mingla-dev: all 3 admin RPCs still 4–9s (spec target <500ms / <2s). Root cause proven: partial expression index accelerates filter-narrowing (53ms pure COUNT via Index-Only Scan) but cannot help when the query also projects heap-resident columns (`stored_photo_urls`, `rating`). Bitmap Heap Scan + detoast is the bottleneck, and no index shape on `place_pool` can eliminate it without an MV-like pre-computation. | Rework ORCH-0480 with a covering index (INCLUDE clause) — rejected because `stored_photo_urls` and `ai_categories` are text[] that TOAST out-of-line; INCLUDE won't help. Also rejected: bumping `work_mem` per-function — band-aid only fixes the sort, not the heap scan. | Admin dashboard stays broken on production for another dispatch cycle (~1 day) instead of shipping a ~45% improvement stepping stone. Acceptable because partial fix still 500s on prod (~9s > 8s timeout). | Permanent — MV pattern becomes the standard for admin aggregate RPCs (ORCH-0483 will codify this as a process rule). |
| DEC-022 | 2026-04-17 | ORCH-0481 cycle 2 migration removes the planned one-shot `SELECT cron_refresh_admin_place_pool_mv()` validation call at migration end. Deploy now lock-free. | First cycle 2 deploy attempt failed with `canceling statement due to lock timeout` (SQLSTATE 55P03) because Supabase's migration runner has a session-level lock_timeout that's tighter than the function's `SET lock_timeout TO '15min'`. When cron had an active REFRESH in-flight (confirmed via `cron.job_run_details.end_time IS NULL`), the one-shot call blocked on the lock and got killed by the migration's lock_timeout before the function's own setting could take effect. | Add `SET lock_timeout = 0` at top of migration — rejected because it's a session-wide change that could mask legitimate lock issues on the `cron.schedule` and `cron.unschedule` calls. Wait for in-flight cron attempts to fail before retrying — rejected because cron fires every 10 min, hard to time. Rely on the function's own SET — didn't work because migration's session lock_timeout kills the blocking call before the function's attribute applies (function-level SET only activates once the function starts executing, not while the planner is still acquiring locks). | Lose immediate deploy-time validation of the function body. Validation now happens at the next cron tick (≤10 min after deploy). If the function is broken, `cron.job_run_details.status='failed'` surfaces it within 10 min — same visibility mechanism as any other cron failure. | Permanent — this decision applies to all future migrations that try to one-shot call a function that acquires locks on a concurrently-refreshing MV. |
| DEC-023 | 2026-04-18 | ORCH-0481 hit stuck-in-loop threshold (cycle 3). Pause all implementor dispatches targeting pg_cron on this ORCH. Strategic review required before any further SQL migration against the cron layer. | Three consecutive rework cycles on the same underlying technology (pg_cron) each fixed their specific problem and surfaced a new one: cycle 0 FAIL (wrong query shape), cycle 1 FAIL (cron timing out on raw REFRESH), cycle 2 FAIL (cron jobid=13 registered correctly but never fires — 0 runs in 4+ hours, other cron jobs fire normally). Cycle 2 tester report `QA_ORCH-0481_ADMIN_MV_LAYER_REPORT_RETEST_2.md` triggered the escalation. Per tester discipline rule "retest cycle >2 → flag as stuck-in-loop," this is the expected safety valve to prevent compounding complexity. | Continue with cycle 3 implementor — rejected because the failure pattern (each cycle surfaces a new cron failure mode) suggests the dependency itself is the issue, not any specific migration. File a support ticket with Supabase — parked as Alternative 4, not recommended as primary path (time-unbounded). | Delay admin dashboard unblock until user steers between: (1) drop cron, add admin-UI refresh button — preserves cycle 0/1 query wins, ships today, zero cron dependency; (2) narrow the MV so REFRESH fits in 2 min natively — cleaner architecture, larger scope; (3) ship current state to prod + measure (prod's environment may behave differently). Best combo: (1) + (3) in parallel. Cycle 0/1 wins (country_overview 87ms hot, pool_overview ~400ms, category_breakdown 107ms) preserved in all alternatives. | User selects alternative path OR evidence surfaces explaining why pg_cron skipped jobid=13 (at which point cycle 3 implementor becomes viable). |
| DEC-024 | 2026-04-18 | Mingla uses a single-environment Supabase setup: the "Mingla-dev" project (`gqnoajqerqhnvulmnyvv`) IS the production database. Prior orchestrator/tester documentation that distinguished "Mingla-dev" from a separate "production" environment was incorrect. All migrations applied via `supabase db push` ship directly to production. | User clarified 2026-04-18 that there is no separate prod Supabase project. Orchestrator had been writing artifacts assuming a 2-environment setup (dev + prod). This caused incorrect status language ("NOT applied to production") in earlier docs, when in reality every migration was already live for real users. Concrete consequences: (a) DEC-021's "don't ship ORCH-0480 to production" directive was moot — it shipped to Mingla-dev which is production. (b) ORCH-0481 cycles 0/1/2 have all been in production since their respective deploy times. (c) The pg_cron failure on Mingla-dev IS the production behavior — the "ship to prod + measure" alternative from DEC-023 was already answered by the existing Mingla-dev measurements. (d) ORCH-0480 closes A (user-reported admin 500s are resolved on prod). (e) ORCH-0481 status downgraded from stuck-in-loop to partially-shipped D (query layer works in prod; auto-refresh residual). | Maintain the 2-env fiction — rejected because it misrepresents reality and leads to misdirected "push to prod" asks. Rename "Mingla-dev" to "Mingla-prod" at the Supabase project level — rejected because it's a larger rename touching CI/env vars with no functional benefit; just document the truth. | The name "Mingla-dev" sticks as a historical artifact; all future status docs explicitly call out "Mingla-dev = production." Any ORCH that claims "not applied to production" must be interpreted literally as "not applied to Mingla-dev" which means the migration file exists in the repo but has not been `db push`ed. | When Mingla adds a true staging environment (separate Supabase project) — re-evaluate the doc pattern then. |
| DEC-025 | 2026-04-18 | **ORCH-0490 OPD-1 resolution — dedup scope is CONTEXT-SCOPED, not permanent user-scoped.** Rejected-cards memory is scoped to `(mode × sessionId × preferencesRefreshKey)`. Within the same context, dedup applies (no rejected card reappears while the user is still in that swipe session). Context boundary change (mode toggle, session change, preferences save, app close/reopen via a restarted swipe session) CLEARS rejection memory — rejected cards become eligible to reappear in the new context. | User's exact answer 2026-04-18: *"Should never be hidden. Once the swipe session is over, and you change preferences."* Interpreted as: rejections do NOT cross context boundaries. Constitutional #13 (exclusion consistency) is satisfied WITHIN a context, not across. The charter's risk #1 ("cross-mode dedup bleed") is hereby deemed NON-blocking — cross-mode reappearance is expected and correct per product. | Permanent user-scoped AsyncStorage persistence (spec's §3 Phase 2.4 default) — rejected. Time-decayed 30-day horizon — rejected. | **Phase 2.4 scope REDUCED:** no new `userRejectedCardsService`, no new AsyncStorage key, no logout sweep addition. Simpler + smaller (~30 LOC instead of ~100 LOC). **Charter §8 risk #1 neutralized.** **Phase 2.3's parallel-hooks design simplified** — cross-mode dedup protection not required. Only fix required: `RecommendationsContext.tsx:836` within-context `batchSeed===0` replace → merge-preserving (union-update) so progressive delivery (singles→interleaved) doesn't wipe within-session dedup memory. Keep `sessionServedIdsRef` ref. | Permanent, unless user returns with a product-level change of mind. |
| DEC-026 | 2026-04-18 | **[RETRACTED 2026-04-19 — see DEC-031]** ~~ORCH-0490 OPD-4 resolution — persist `currentMode` + `activeCollabSessionId` DIRECTLY in Zustand partialize. Bump Zustand schema from v4 to v5 with forward-only migration.~~ | Retraction reason: AH-148 Phase 2.5 verification (`outputs/INVESTIGATION_ORCH-0490_PHASE_2.5_COLD_LAUNCH_RESTORE.md`) proved that (a) the `mingla_last_mode` AsyncStorage key at `AppStateManager.tsx:168,391` already provides mode+session cold-launch persistence, predates this decision, and is working correctly in production; (b) the Zustand partialize approach prescribed here conflicts with the explicit ORCH-0209 constraint documented at `appStore.ts:242-244`: *"currentSession, isInSolo, availableSessions, pendingInvites are intentionally NOT persisted. They are refreshed from the database on every app open via loadActiveSession(). Persisting them would risk showing stale session state."* Executing DEC-026 would have reversed an earlier load-bearing constitutional decision and exposed users to stale-session bugs. The `mingla_last_mode` design achieves the same product goal WITHOUT breaking ORCH-0209. | (See DEC-031.) | None — decision never executed (Phase 2.5 dispatch was blocked for verification before implementor landing). No migration shipped. No Zustand schema bump. | N/A — retracted. |
| DEC-027 | 2026-04-18 | **ORCH-0490 OPD-5 resolution — progressive delivery is CLIENT-SIDE (option a).** `deckService.fetchDeck` kicks off both singles and curated promises in parallel, races them via `Promise.race`, fires `onPartialReady(cards, { source })` callback per settled promise. No edge function change. No HTTP chunked streaming. | User answer: Client-side (a) (spec's recommended default). ~1 week implementation. Lower risk. | Server-side (b) HTTP chunked streaming — rejected (2-3 weeks, rewrites the most-important edge function, higher risk). Defer (fix only RC#3 zero-singles skip) — rejected (loses RC#2 fix, leaves singles-first hardcoded). | Coarser-grained than true streaming but fine-grained enough to hit the P-L-01 latency targets. If tester measures shortfall post-ship, option (b) becomes a follow-up ORCH. | Permanent unless latency targets prove unreachable via option (a). |
| DEC-030 | 2026-04-19 | **ORCH-0490 R4 cold-launch intent LOCKED as "resumption."** Cold launch into same `(currentMode, refreshKey)` restores prior `removedCards` from AsyncStorage. User explicitly chose (a) resumption 2026-04-19 after AH-147 investigation flagged the ambiguity. Rationale: closing + reopening an app should feel like continuation, not restart; explicit preference change is the "new session" trigger; consistent with ORCH-0492 "session persistence across app close" goal. | User decision post-AH-147 investigation §4 R4 verdict. | Interpretation (b) "cold launch always = fresh session" — rejected. Would have required ~20 LOC to scope baseKey by per-launch session ID. | None — current code already implements (a). No ORCH-ID needed. R4 verdict upgrades from DEGENERATE to ✅ HOLDS. | Permanent unless JTBD analysis surfaces a user complaint. |
| DEC-029 | 2026-04-18 | **ORCH-0490 Phase 2.4 CLOSED as "structurally satisfied — no code change."** Verification AH-147 (`outputs/INVESTIGATION_ORCH-0490_PHASE_2.4_CONTEXT_SCOPED_DEDUP.md`) proved all 5 DEC-025 requirements (R1 within-session dedup, R2 pref-change clears, R3 mode isolation, R5 progressive-delivery dedup) already hold post-commit `841d9fb7`. R4 (cold launch = fresh) is DEGENERATE pending user clarification on "resumption vs new session" intent — code currently treats cold launch as resumption via AH-142 Layer 6 persisted `preferencesRefreshKey`. Master spec `SPEC_ORCH-0490_DECK_RELIABILITY_AND_PERSISTENCE.md` Phase 2.4 section (user-scoped service plan, lines 304–366) was SUPERSEDED by DEC-025 scope reduction, and DEC-025's narrower "fix sessionServedIdsRef replace" is itself MOOT because `sessionServedIdsRef` was never the visible-queue rejection filter (actual filter is `removedCards` in SwipeableCards with mode+refreshKey-scoped AsyncStorage at `mingla_card_state_*_removed`). | AH-147 forensics investigation verdict + DEC-025 locked context-scoped intent + AH-146 ORCH-0503 v3 device retest structurally proved R5 (mid-swipe preservation). | Implement a user-scoped or time-decayed rejection service — rejected (DEC-025). Implement DEC-025's narrow sessionServedIdsRef union fix — rejected (moot per AH-147 §4 R5). | No Phase 2.4 code landing. ORCH-0490 program scope shrinks by ~30 LOC. Phase 2.5 (session position persistence + mode/session cold-launch restore) is the next ORCH-0490 program action — OR close out ORCH-0490 entirely and move to next Priority Board top item. | Permanent — unless user clarifies R4 with "cold launch = fresh session" intent, at which point register a new ORCH-ID for app-launch-scoped baseKey change (~20 LOC, small, standalone). |
| DEC-031 | 2026-04-19 | **ORCH-0490 mode/session persistence architecture — the existing `mingla_last_mode` AsyncStorage key (shape `{ mode: 'solo' \| <sessionName>, sessionId: string \| null }`) is the ACCEPTED and PERMANENT architecture for persisting user mode + active session across cold launches.** Written at `AppStateManager.tsx:168` via `safeAsyncStorageSet`, read at line 391 on mount, verified against live DB session at lines 429-453 (graceful fallback to Solo on deleted session). Replaces DEC-026. | AH-148 forensics verification (`outputs/INVESTIGATION_ORCH-0490_PHASE_2.5_COLD_LAUNCH_RESTORE.md`) discovered that this mechanism ALREADY existed, predates DEC-026, and was undocumented in the Phase 0-B baseline (`INVESTIGATION_ORCH-0492_PERSISTENCE_BASELINE.md` §2 enumerates 5 persistence mechanisms; this is a 6th, unlisted). Satisfies JTBD-3 ("reopen into the mode+session I left") for cold launch. Coexists with ORCH-0209's DB-authoritative `currentSession` object model — mode name + session UUID persist to AsyncStorage, but the full `CollaborationSession` object is re-fetched from DB on every open via `SessionService.getActiveSession`, so stale-session risk is eliminated. | Zustand partialize direct (DEC-026) — REJECTED (retracted) because it reverses ORCH-0209 and exposes stale-session bugs. Separate `@mingla/lastResumeIntent` key — rejected (redundant; `mingla_last_mode` already does this). Don't persist at all — rejected (JTBD-3 would be broken; but code already persists, so this was moot). | The name is `mingla_last_mode` (legacy — predates `@mingla/` namespace convention). Future namespace cleanup can rename to `@mingla/lastMode` as a follow-up ORCH at the time other `mingla_*` → `@mingla/*` migrations run. The logout prefix sweep at `AppStateManager.tsx:792` (`key.startsWith("mingla_")`) already covers this key, so no Constitutional #6 gap. | Permanent — unless a future architectural reason forces mode/session off AsyncStorage. At that point, DEC-026 could be re-evaluated AFTER auditing whether ORCH-0209's stale-session constraint still applies. |
| DEC-028 | 2026-04-18 | **ORCH-0490 Phase 2.1 — `QueryPoolCardsError` uses `name = 'RPCError'` instead of adding a new `instanceof` catch in `discover-cards/index.ts`.** The existing outer catch at `discover-cards/index.ts:696-714` already extracts `errorClass` from `err.name` and routes to `path:'pipeline-error'`. By setting the new error class's runtime `name` to `'RPCError'`, the existing mechanism produces the dispatch-specified response shape (`errorClass: 'RPCError'`) without adding a parallel error handler. One catch, one owner — Constitutional #8 (subtract before adding) + INV-043 (no unconditional fall-throughs). | Phase 2.1 implementor report §4.1 "Principled deviation." Dispatch §2.1 item 13 described a specific `instanceof QueryPoolCardsError` catch inside `serveCardsFromPipeline`. Implementor chose the leaner route after spotting that the existing outer catch does exactly the right thing when `err.name === 'RPCError'`. | Add explicit instanceof catch — rejected because it creates two paths to `path:'pipeline-error'`, risking divergence if future edits touch one but not the other. Throw with name `'QueryPoolCardsError'` (JS class name) — rejected because dispatch specified wire-level tag `'RPCError'` and that's what the outer catch would produce from this runtime name. | The JavaScript class name is `QueryPoolCardsError` (compile-time identity) but the runtime `.name` property is `'RPCError'` (wire-level tag). Two identities for the same object is a minor cognitive overhead but produces the cleanest response contract. | Permanent. |
| DEC-090 | 2026-05-03 | **ORCH-0700 ai_categories family + seeding_category column DECOMMISSIONED. 6 columns physically dropped from `place_pool`: `seeding_category, ai_categories, ai_reason, ai_primary_identity, ai_confidence, ai_web_evidence`.** Migration 6 applied 2026-05-03 with atomic ALTER TABLE DROP COLUMN x6 + 30-day archive backup (`_archive_orch_0700_doomed_columns`, 69,599 rows). All 5 admin RPCs that previously read `seeding_category` rewired (Migration 3) to read matview's `primary_category` derived column instead. The matview itself rebuilt (Migration 4) without the doomed columns. 3 admin edge functions (admin-seed-places, admin-refresh-places, admin-place-search) scrubbed (Phase 3 + redeployed Phase 3B) to use the new `derivePoolCategory()` TS helper. | Operator directive 2026-05-01: "we already changed the place pool to read from is_servable. AI categories should be deleted." 4 forensics passes today: cycle-1 (Movies pill leak), cycle-2 (live-fire pre-decommission), Phase 2 LANDMINE_AUDIT (caught Migration 5 OUT-param drift + 3 admin edge function reads), Phase 3 TAXONOMY_REGRESSION + Phase 3B CATEGORY_PIPELINE_END_TO_END (caught helper invented-taxonomy bug + corrected). | Keep all 6 columns alive — rejected (Constitution #2 violation: 3-way category-source ambiguity between primary_type, seeding_category, ai_categories). Drop only ai_categories family without seeding_category — rejected (incomplete cleanup; same architectural problem). | The legacy `seeding_category` snapshot column (which preserved "what shopping list found this place") is gone — now derivable only via FK from `seeding_batches.seeding_category` joined back via a future audit query if needed. Acceptable loss; current product doesn't expose that lineage. | Permanent. Archive table drops 2026-06-02 (scheduled wakeup). |
| DEC-091 | 2026-05-03 | **`pg_map_primary_type_to_mingla_category(primary_type, types)` + matview `admin_place_pool_mv.primary_category` is the canonical authority for any admin-display Mingla-category derivation, returning ONLY canonical 10-slug values per `DISPLAY_TO_SLUG` in `categoryPlaceTypes.ts`.** TS twin `_shared/derivePoolCategory.ts` mirrors the SQL helper byte-for-byte. Signal-scoring contexts continue using the separate 16-signal taxonomy via `place_scores.signal_id` + `query_servable_places_by_signal` RPC (orthogonal axis, intentionally different granularity). | Phase 3B forensics (`INVESTIGATION_CATEGORY_PIPELINE_END_TO_END.md`) identified 4 legitimate parallel category taxonomies in the codebase: (1) seeding (10-slug + 14 granular Google-API configs), (2) bouncer (boolean + B-codes), (3) signal-scoring (16 signals, 11 type-grounded + 5 quality-grounded), (4) display (10 canonical slugs). Each serves a different job. The Phase 1 helper had INVENTED an 11-slug taxonomy that matched none of these — admin Place Pool dashboard rendered 0 for Brunch+Casual / Movies+Theatre / Groceries for ~6 hours until Phase 3B (Migration 7) rewrote the helper to emit canonical 10. Operator confirmed Path A (display-label semantic) for matview `primary_category`. | Path B (translate at admin RPC read time) — rejected (Constitution #2 violation: matview column would mean "scoring or display depending who reads"; 8 translation sites at risk of drift). Path C (two columns: display + top-signal) — rejected (over-engineering; YAGNI). | Granular signal-scoring data (`brunch` vs `casual_food` separately, `movies` vs `theatre` separately) lives in `place_scores`, NOT in the matview. Admin can't filter "show me casual_food only, not Brunch+Casual combined" from the Place Pool dashboard alone — would need to JOIN to place_scores. Acceptable; admin doesn't currently need that filter. | Permanent. Future ORCH-0597/0598 if/when display ever splits from 10→12 slugs would require helper rewrite again (small change, well-understood pattern). |
