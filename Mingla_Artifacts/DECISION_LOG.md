# Decision Log

> Last updated: 2026-04-17

| ID | Date | Decision | Context | Alternatives Rejected | Tradeoff Accepted | Exit Condition |
|----|------|---------|---------|----------------------|-------------------|----------------|
| DEC-001 | 2026-03-20 | Pool-first card serving — all card-serving functions use card_pool only, zero Google/OpenAI at serve time | Performance + cost + reliability. 1,463 lines removed. | Keep Google fallback for cache misses | No real-time data freshness (hours, prices from generation time) | When real-time enrichment pipeline exists |
| DEC-002 | 2026-03-21 | AI validation as sole quality gate — remove all SQL type-exclusion blocks | Type exclusions were too coarse (blocked entire categories). AI gives per-card judgment. | Keep SQL exclusions as safety net | Unvalidated cards hidden instead of shown with bad data | Never — AI gate is permanent |
| DEC-003 | 2026-03-22 | OneSignal android_channel_id disabled — was killing all push delivery | Root cause: channel not configured in OneSignal dashboard → 400 error | Configure the channel properly | No Android notification channel customization | When OneSignal dashboard is properly configured |
| DEC-004 | 2026-03-23 | Single entry point for collaboration (pill → SessionViewModal) | CollaborationModule + BoardViewScreen were redundant code paths causing confusion | Keep both paths with shared state | More complex modal, but single code path | Permanent |
| DEC-005 | 2026-03-23 | Service error contract deferred — TRANSITIONAL logging instead | ~60+ call sites need ServiceResult<T>. Too much blast radius now. | Fix all 60+ sites immediately | Silent failures still possible in ~4 services | Next hardening cycle |
| DEC-006 | 2026-03-24 | Remove invalidateQueries from preference save — use prefsHash matching | invalidateQueries caused race condition with batch fetch | Keep invalidation with delay/debounce | Slightly more complex batch validation logic | Permanent |
| DEC-007 | 2026-03-25 | Constitution principles (14 rules) locked into README | Prevent regression of hard-won fixes | Informal guidelines without enforcement | Protective comments in code may slow development | Permanent — can add, not remove |
| DEC-008 | 2026-04-06 | Collab aggregation = UNION for all preferences | User directive: everything combined, widest possible set | Median (balanced), MIN (most restrictive) | May show more results than any individual user wanted — acceptable for collab | Permanent |
| DEC-009 | 2026-04-06 | Travel mode: widest radius for card search, each user sees their own mode's travel time in UI | Radius = Math.max of all constraint values using most permissive mode for query | Majority vote on mode | Server queries with driving radius even if one user walks — more cards, user sees their own travel time | Permanent |
| DEC-010 | 2026-04-06 | Time slots: UNION array — show places open during ANY selected slot | If one picks Dinner and one picks Brunch, show places open for either | Majority vote (current), ignore time filtering | More results but all match at least one participant's preference | Permanent |
| DEC-011 | 2026-04-06 | Date option: UNION — show cards available at ANY selected date/time window | If one says "Now" and one says "This Weekend", show both windows | Most future wins, majority vote | Broader card set, no participant's date preference is ignored | Permanent |
| DEC-012 | 2026-04-06 | Visibility = who sees YOU, not what you see. "Show on map" toggles control what you see. | User's visibility_level controls who can see them on the map. It does NOT restrict what the user can see — that's the "Show on map" toggles (people/places/feed/heatmap). | Bidirectional matching (both must opt in) | Simpler mental model, matches user expectation of "I set everyone = everyone sees me" | Permanent |
| DEC-013 | 2026-04-06 | Friend requests to seed users: show "request sent" UI, silently skip DB insert | FK constraint on friend_requests.receiver_id blocks inserts for mock users. Server-side: detect is_seed, return success without inserting. Client sees normal haptic + state change. Mock user never accepts — natural "ghosting". | Disable button for seeds (breaks interaction), Create auth.users rows (complex, billing risk) | User gets full interactive experience. No fabricated acceptance — just a pending state that never resolves. | Permanent |
| DEC-014 | 2026-04-06 | Grid-based stranger seeding (~1.5M rows) across global land grid every ~10km | Uniform global coverage. Users see strangers everywhere, not just near cities. One-time generation. | On-demand (dynamic complexity), Major cities only (rural gaps), 5.3M uniform (too large) | Large dataset but feasible in PostgreSQL. Complete coverage at launch. | Until real user count exceeds seeded density |
| DEC-015 | 2026-04-11 | i18n architecture: i18next + react-i18next + expo-localization with namespace-per-domain JSON files | Need runtime language switching, RTL support path, server-side push translations. | react-intl (heavier), custom solution (maintenance burden) | Adds ~50KB bundle, 171 keys to maintain in each locale | Permanent |
| DEC-016 | 2026-04-11 | OTP multi-channel: SMS + WhatsApp + voice call via Twilio Verify | Users in some regions can't receive SMS reliably. WhatsApp and voice call are Twilio Verify built-in channels. | SMS-only (simpler), separate WhatsApp API (more complexity) | Three channel buttons on OTP step, slightly more complex UI | Permanent |
| DEC-017 | 2026-04-11 | Analytics strategy: product-first scope — define what questions matter before wiring events | Mixpanel is dead (no token), AppsFlyer has 25 events but never dashboard-verified, services don't talk to each other. | Quick-win (just set the token), full integration sweep (build unified layer first) | Delays analytics activation until product skill defines requirements | When product skill completes analytics strategy |
| DEC-018 | 2026-04-11 | Queue docs (SPEC/IMPL/TEST/RETEST) deprecated in favor of AGENT_HANDOFFS.md | Queue docs were skeletal and 12 days stale. AGENT_HANDOFFS tracks the full pipeline in a single dispatch model with 79+ completed dispatches. | Populate retroactively (too much work), delete (lose structure) | Lose per-stage tracking granularity | If pipeline model changes |
| DEC-019 | 2026-04-11 | Dead code elimination before documentation sweep — subtract first, document what survives | Constitution rule #8: subtract before adding. Can't document accurately if dead code inflates counts. | Document first then clean (risk documenting things that get deleted) | Extra session of work before docs start | One-time |
| DEC-020 | 2026-04-17 | `place_pool.ai_categories` column canonically stores **app slugs** (e.g. `brunch_lunch_casual`), not seeding IDs (e.g. `casual_eats`). Fix the consumer (`generate-single-cards`), not the data. | ORCH-0470 — `generate-single-cards` queries using the seeding ID while `ai-verify-pipeline` writes the app slug. Silent zero-card failure on every re-seed of Brunch/Lunch/Casual. Two identifiers exist because seeding splits one app category across multiple Google API calls (casual_eats + casual_eats_world + casual_eats_extended). | Direction B (migrate column to seeding IDs, update all writers + readers) — cost/benefit unfavorable | Lose the ability to preserve which sub-recipe produced a place in the column. No current consumer needs that distinction. | When any consumer requires sub-recipe provenance — re-evaluate then |
| DEC-021 | 2026-04-17 | Pivot from ORCH-0480 (partial-expression index approach) to ORCH-0481 (materialized view layer) as the admin-RPC perf fix. Do not rework ORCH-0480; do not apply ORCH-0480 migration to production. | ORCH-0480 tester verdict FAIL on Mingla-dev: all 3 admin RPCs still 4–9s (spec target <500ms / <2s). Root cause proven: partial expression index accelerates filter-narrowing (53ms pure COUNT via Index-Only Scan) but cannot help when the query also projects heap-resident columns (`stored_photo_urls`, `rating`). Bitmap Heap Scan + detoast is the bottleneck, and no index shape on `place_pool` can eliminate it without an MV-like pre-computation. | Rework ORCH-0480 with a covering index (INCLUDE clause) — rejected because `stored_photo_urls` and `ai_categories` are text[] that TOAST out-of-line; INCLUDE won't help. Also rejected: bumping `work_mem` per-function — band-aid only fixes the sort, not the heap scan. | Admin dashboard stays broken on production for another dispatch cycle (~1 day) instead of shipping a ~45% improvement stepping stone. Acceptable because partial fix still 500s on prod (~9s > 8s timeout). | Permanent — MV pattern becomes the standard for admin aggregate RPCs (ORCH-0483 will codify this as a process rule). |
| DEC-022 | 2026-04-17 | ORCH-0481 cycle 2 migration removes the planned one-shot `SELECT cron_refresh_admin_place_pool_mv()` validation call at migration end. Deploy now lock-free. | First cycle 2 deploy attempt failed with `canceling statement due to lock timeout` (SQLSTATE 55P03) because Supabase's migration runner has a session-level lock_timeout that's tighter than the function's `SET lock_timeout TO '15min'`. When cron had an active REFRESH in-flight (confirmed via `cron.job_run_details.end_time IS NULL`), the one-shot call blocked on the lock and got killed by the migration's lock_timeout before the function's own setting could take effect. | Add `SET lock_timeout = 0` at top of migration — rejected because it's a session-wide change that could mask legitimate lock issues on the `cron.schedule` and `cron.unschedule` calls. Wait for in-flight cron attempts to fail before retrying — rejected because cron fires every 10 min, hard to time. Rely on the function's own SET — didn't work because migration's session lock_timeout kills the blocking call before the function's attribute applies (function-level SET only activates once the function starts executing, not while the planner is still acquiring locks). | Lose immediate deploy-time validation of the function body. Validation now happens at the next cron tick (≤10 min after deploy). If the function is broken, `cron.job_run_details.status='failed'` surfaces it within 10 min — same visibility mechanism as any other cron failure. | Permanent — this decision applies to all future migrations that try to one-shot call a function that acquires locks on a concurrently-refreshing MV. |
| DEC-023 | 2026-04-18 | ORCH-0481 hit stuck-in-loop threshold (cycle 3). Pause all implementor dispatches targeting pg_cron on this ORCH. Strategic review required before any further SQL migration against the cron layer. | Three consecutive rework cycles on the same underlying technology (pg_cron) each fixed their specific problem and surfaced a new one: cycle 0 FAIL (wrong query shape), cycle 1 FAIL (cron timing out on raw REFRESH), cycle 2 FAIL (cron jobid=13 registered correctly but never fires — 0 runs in 4+ hours, other cron jobs fire normally). Cycle 2 tester report `QA_ORCH-0481_ADMIN_MV_LAYER_REPORT_RETEST_2.md` triggered the escalation. Per tester discipline rule "retest cycle >2 → flag as stuck-in-loop," this is the expected safety valve to prevent compounding complexity. | Continue with cycle 3 implementor — rejected because the failure pattern (each cycle surfaces a new cron failure mode) suggests the dependency itself is the issue, not any specific migration. File a support ticket with Supabase — parked as Alternative 4, not recommended as primary path (time-unbounded). | Delay admin dashboard unblock until user steers between: (1) drop cron, add admin-UI refresh button — preserves cycle 0/1 query wins, ships today, zero cron dependency; (2) narrow the MV so REFRESH fits in 2 min natively — cleaner architecture, larger scope; (3) ship current state to prod + measure (prod's environment may behave differently). Best combo: (1) + (3) in parallel. Cycle 0/1 wins (country_overview 87ms hot, pool_overview ~400ms, category_breakdown 107ms) preserved in all alternatives. | User selects alternative path OR evidence surfaces explaining why pg_cron skipped jobid=13 (at which point cycle 3 implementor becomes viable). |
| DEC-024 | 2026-04-18 | Mingla uses a single-environment Supabase setup: the "Mingla-dev" project (`gqnoajqerqhnvulmnyvv`) IS the production database. Prior orchestrator/tester documentation that distinguished "Mingla-dev" from a separate "production" environment was incorrect. All migrations applied via `supabase db push` ship directly to production. | User clarified 2026-04-18 that there is no separate prod Supabase project. Orchestrator had been writing artifacts assuming a 2-environment setup (dev + prod). This caused incorrect status language ("NOT applied to production") in earlier docs, when in reality every migration was already live for real users. Concrete consequences: (a) DEC-021's "don't ship ORCH-0480 to production" directive was moot — it shipped to Mingla-dev which is production. (b) ORCH-0481 cycles 0/1/2 have all been in production since their respective deploy times. (c) The pg_cron failure on Mingla-dev IS the production behavior — the "ship to prod + measure" alternative from DEC-023 was already answered by the existing Mingla-dev measurements. (d) ORCH-0480 closes A (user-reported admin 500s are resolved on prod). (e) ORCH-0481 status downgraded from stuck-in-loop to partially-shipped D (query layer works in prod; auto-refresh residual). | Maintain the 2-env fiction — rejected because it misrepresents reality and leads to misdirected "push to prod" asks. Rename "Mingla-dev" to "Mingla-prod" at the Supabase project level — rejected because it's a larger rename touching CI/env vars with no functional benefit; just document the truth. | The name "Mingla-dev" sticks as a historical artifact; all future status docs explicitly call out "Mingla-dev = production." Any ORCH that claims "not applied to production" must be interpreted literally as "not applied to Mingla-dev" which means the migration file exists in the repo but has not been `db push`ed. | When Mingla adds a true staging environment (separate Supabase project) — re-evaluate the doc pattern then. |
