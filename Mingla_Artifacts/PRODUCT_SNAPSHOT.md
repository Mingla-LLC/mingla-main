# Product Snapshot

> **2026-05-06 — 🎉 ORCH-0743 CLOSED PASS (DEC-121) — cold-start polish shipped; ORCH-0742 fallout cleaned up.** Plain-English: when an organiser opens the app fresh in the morning, they no longer see the "Create brand" / "No brands yet" flash before their actual brand pops up. The splash screen now waits for the brand to actually load (with a 2-second hard cap so the splash never gets stuck on slow networks). The `/event/create` deep-link redirect-loop edge case is fixed — synchronous Zustand read replaces the async wrapper. Web hero text shadow now renders correctly (the Metro deprecation warning is gone). 15 leftover diagnostic console.error markers from 5 closed cycles are deleted with all live error-handling preserved. The architectural require cycle ORCH-0742 SPEC tried to prevent is structurally broken — `Brand` types extracted to a leaf file, neither store nor hook depends on the other anymore. **The dangerous one (RC-2):** the orphan-key reaper whitelist that was pointing at `currentBrand.v12` (would have wiped ORCH-0742's live blob if anyone flipped reaper to delete-mode) is fixed AND locked by 5 unit tests so future persist-key bumps can't drift again. **What's now strong:** mingla-business currentBrand surface architecturally clean post-ORCH-0742 + ORCH-0743 (Const #14 ELEVATED PARTIAL→PASS); cross-device propagation works correctly (ORCH-0742) AND no cold-start flash anymore (ORCH-0743); web target rendering parity restored on event hero. **What's still queued:** **META-ORCH-0744-PROCESS** (highest leverage — 5 missing CLOSE-protocol + CI gates: require-cycle baseline, DIAG-marker reaping, persist-key whitelist sync, TRANSITIONAL exit-condition lint, web-deprecation parser; prevents the *classes* of bug ORCH-0743 just fixed from recurring) · ORCH-0746 (4 pre-existing AuthContext require cycles; structural refactor, lower urgency) · ORCH-0747 (8 unsafe form-input typecast; type-safety hygiene) · Cycle 3 Realtime push (queued — drops cross-device propagation lag from ~30s+foreground to <2s steady-state) · Cycle 4 per-store Zustand classification audit (verifies remaining 10 persisted stores against I-PROPOSED-J or are appropriately TRANSITIONAL with documented exit). **Most operator-trust-building moment:** implementor handled SPEC §3.7.4 Note A escalation textbook-perfectly — pre-flight verified callers, found the gap (AuthContext.tsx callsites not wrapped in try/catch), announced clearly with recommended Option (a) wrap matching existing `getSession` pattern, awaited operator approval, implemented exactly as approved. No silent scope creep, no SPEC violation, exemplary discipline. Tester explicitly praised this as P4-2 positive note. **Most surprising during ORCH-0743:** the implementor's report cited 52 routes from `expo export -p web`, but the tester's independent re-run produced 44. Cosmetic copy-paste from prior ORCH-0742 verification context — bundle is identical, doesn't affect functional correctness. Caught and documented as P3-2.

> **2026-05-06 — 🎉 ORCH-0742 CLOSED CONDITIONAL PASS (DEC-119 + DEC-120) — currentBrand cross-device staleness bug class structurally fixed in mingla-business.** Plain-English: when an organiser picks a brand on Phone, the app no longer keeps a local photocopy of the entire brand record. It now keeps just a coat-check ticket (the brand's ID), and asks the server fresh whenever a screen needs the actual brand details. Result: rename a brand on iPad and Phone shows the new name on next foreground refresh; delete a brand on iPad and Phone auto-clears the selection cleanly; cold-starting the app after a brand was deleted shows a clean null state instead of a flash of stale data. iOS, Android, and Expo Web all use the same single code path. **What's now strong:** mingla-business currentBrand sync architecturally clean (ID-only persist + React Query for fresh data + auto-clear `useEffect` on deleted brand + Cycle 1 focusManager refetch on foreground); `I-PROPOSED-J ZUSTAND-PERSIST-NO-SERVER-SNAPSHOTS` codified as ACTIVE invariant — the persisted-Brand-snapshot pattern is structurally banned across all future Zustand stores; new `getBrandFromCache(brandId)` helper centralizes outside-component Brand lookup for 5 sites (RefundSheet, CancelOrderDialog, order detail, liveEventStore, liveEventConverter); cross-domain blast clean (zero spillover into admin/supabase/app-mobile). **What's still fragile / queued:** **ORCH-0743** (queued — cold-start polish: the wrapper hook returns null during the React Query fetch window, causing TopBar to briefly show "Create brand" + home tab to show "No brands yet" empty state on cold-start with persisted brand ID; bounded by network speed ~100ms-1s; SPEC §4.7 acknowledged but splash gate at `_layout.tsx:55-75` doesn't actually wait on `useBrand`; ~1 day implementor effort to fix via splash-gate extension OR React Query persistence wiring OR migration to `useCurrentBrandId()` for the 3 ID-only consumers; SPEC dispatch prompt drafted at `prompts/SPEC_ORCH_0743_CURRENT_BRAND_COLD_START_POLISH.md`) · **`event/create.tsx` rare redirect-loop edge case** (rolls into ORCH-0743) · **Cycle 3 Realtime push** (queued — replaces 30s polling + foreground-refetch model with Supabase Realtime subscription, drops cross-device propagation lag from ~30s+foreground to <2s steady-state) · **Cycle 4 per-store Zustand classification audit** (queued — verifies remaining 10 persisted stores comply with I-PROPOSED-J or are appropriately TRANSITIONAL with documented exit). **Most operator-trust-building moment:** tester ran independent build gates from scratch (didn't trust implementor's claimed exit codes — re-ran tsc + expo export -p web and captured real exit codes), then walked all 11 grep gates green, traced the v13→v14 migration across 7 input shapes, and was honest about the cold-start flash being a real UX trade-off the SPEC underestimated. Verdict CONDITIONAL PASS not PASS because the architecture is right but the polish is incomplete. **Most surprising during ORCH-0742:** SPEC §4.5.2's referenced "dead-code mirror block at lines 71-73" of `payments/onboard.tsx` was already deleted by the prior B2a Stripe Connect refactor before Phase 2 started. Implementor caught this and correctly deleted the orphaned line-33 selector entirely instead of migrating it (Const #8 — subtract before adding). Process note for orchestrator: SPECs that lock literal line numbers go stale fast when other cycles touch the same files; consider pattern-matchers or unique anchors instead.

> **2026-05-06 — 🎉 ORCH-0737 v6 + v6.1 CLOSED PASS (DEC-118) — place-intelligence trial pipeline redesign ratified production-trustworthy on Cary-class full-city runs.** Plain-English: the admin's "Run Trial" full-city mode now correctly grinds through 761 places in ~78 min wallclock without crashing (vs ~17 hours pre-v6 + frequent memory failures). The trick was to ask Supabase Storage and Google's photo CDN for tiny pre-resized images (~10-15 KB instead of native ~50-200 KB) before downloading, which cut compose-call memory from ~50 MB peak to ~5 MB and let us run 12 places in parallel safely. Cary 761 ran clean: 760 places scored successfully; 1 place hit a known Gemini schema-violation flake (~0.13% rate, not a Mingla bug); 0 memory crashes; both ancient stuck rows from earlier eras auto-recovered via the 5-min stuck-cutoff. **v6.1 hotfix shipped same session:** dropped Gemini score parallel batches from 12 → 6 to mitigate rate-limit storms; deployed bumping edge function 17→18 ACTIVE 23:54 UTC; throughput projection 10-15 rows/min (up from 5.75/min on v6). Live-fire verification of v6.1 happens organically on the next operator-triggered city run. **What's now strong:** trial pipeline architecturally clean for Cary-class cities (~78 min for 761 places; calibration sweeps schedulable overnight or in parallel without operator-blocking); URL-transform photo pipeline canonical (`transformPhotoUrlForTile` helper + 8/8 Deno unit tests pinning behavior); budget-loop worker pattern eliminates pg_cron 60s dead-air; cancel-mid-budget check preserves SC-08 ≤90s observability; v2/v3/v4 patches all preserved verbatim through v6/v6.1; new invariant `I-COLLAGE-PHOTO-URL-AT-TILE-RESOLUTION` ACTIVE structurally prevents regression. **What's still fragile / queued:** **ORCH-0737 v7** (queued non-blocking — London-scale 3,495 places projects ~5 hr under v6.1 = above operator's ≤60-min target; v7 forensics will scope Gemini File API replacement for `inline_data` + cache hit-rate improvements + parallel-tuning beyond v6.1; prompt drafted at `prompts/FORENSICS_ORCH-0737_V7_LONDON_SCALE.md`) · **ORCH-0737-followup-3** (pg_net score-response capture; cosmetic monitoring) · **the 1 Gemini failure rate** (~0.13% per this run; structurally unfixable from Mingla code; ORCH-0737 v2's auto-retry-once handles most cases per DEC-110; this row's retry must have also returned malformed). **Most operator-trust-building moment:** v6 deployed mid-Cary-run while operator was transitioning Windows → Mac, AND the 5-min stuck-row recovery system reclaimed both ancient pre-v6-era stuck rows (process_min 0.3 / 0.4 — micro-seconds to recover after 21,800-22,800 sec stuck). Self-healing worked exactly as designed. Sequential Pace honored — one orchestrator decision at a time, evidence-first, no compounding (v6.1 was a one-line edit + 30s deploy + Mac-side unit-test verification before CLOSE). **Most surprising during ORCH-0737 v6:** existing fingerprint cache survived the URL-transform change (cache key is `sha256(stored_photo_urls.slice(0,5))`; URLs themselves aren't part of the key, so transformed downloads produce same cache hits as native). Favorable D-1 deviation observed in v6 IMPL — operator got immediate ~40% cache-hit benefit on first-run Cary acceleration. Documented at `I-COLLAGE-PHOTO-URL-AT-TILE-RESOLUTION` cross-references — if URL transforms ever change structurally (e.g., Supabase Storage migrates render-image endpoint), ALL existing fingerprints invalidate.

> **2026-05-05 — 🎉 ORCH-0734 CLOSED PASS (DEC-110) — place-intelligence trial pipeline ratified production-trustworthy.** Plain-English: the admin's "Run Trial" button on the Place Intelligence Trial page now runs city-scoped Gemini sweeps over the actual servable place pool — pick a city + sample size (50/100/200/500), click Run, get cost + duration + per-row Gemini scores back in ~75 min for sample-200. Auto-retry-once on Gemini's `MALFORMED_FUNCTION_CALL` flake gets per-row reliability to ~99.9%. The old 32-anchor calibration scaffold (`signal_anchors` table + its admin tab) is DECOMMISSIONED — backup snapshot retained 14 days as rollback safety, then dropped 2026-05-19. **What's now strong:** trial pipeline architecturally clean (one owner of trial-run records via `place_intelligence_city_runs`); upstream pool quality gated by `I-BOUNCER-EXCLUDES-FAST-FOOD-AND-CHAINS` (ORCH-0735) holding (verified zero real chain leakage across 9 cities + 14,400 rows); Gemini reliability + cost predictability proven on Cary 50 smoke ($0.21, 19 min, 3/3 retries succeeded); admin UI form-required city + sample-size pickers prevent operator errors. **What's still fragile / queued:** **ORCH-0737** (operator-confirmed Option B TOGGLE — keep sampled-sync as default + add "Run whole city" mode for batch sweeps; full-city math is 4-21 hours / $3-15 per city which exceeds edge function 10-min sync timeout, so async architecture required — forensics dispatch next) · **Cycle 17e-A close-pending** (anomalous: brand-CRUD migration shipped LIVE this session but ~25 mingla-business code files uncommitted) · **ORCH-0736** (queued ~1 week — rule_sets DB decommission) · **ORCH-0738** (queued — run-bouncer streaming-write refactor) · **2026-05-19 backup snapshot DROP reminder** for `_archive_orch_0734_signal_anchors`. **Most operator-trust-building moment of this CLOSE:** the explicit choice to authorize CLOSE on existing evidence chain (Cary smoke + ORCH-0735's 9-city probe) instead of re-running a fresh smoke. ORCH-0735's SC-16 evidence across 14,400 rows is materially stronger than re-running the 50-place Cary test, and saving $0.21 + 19 min for negligible additional certainty is the right call. Sequential Pace honored — one CLOSE at a time, evidence-first, no compounding. **Most surprising during ORCH-0734:** the deploy hit 2 fixup migrations (FK already exists at §10 then signal_id NOT NULL) caught by orchestrator runtime smoke + handled via follow-up migration `20260505000002_orch_0734_signal_id_nullable.sql` and §10 strikethrough in original migration. Headless QA didn't catch either; live-fire smoke did. Validates `feedback_headless_qa_rpc_gap` rule.

> **2026-05-05 — 🎉 ORCH-0735 CLOSED PASS (DEC-107) — bouncer fast-food + chain + cheap-snack exclusion ratified production. Net product impact: Phase 1 production rerank no longer at risk of shipping fast-food chains as date destinations.** Plain-English: Mingla's bouncer (the gate that decides which places are good enough to show in the app) now correctly excludes Dairy Queen, Bob Evans, Papa Johns (any spelling), Firehouse Subs (any spelling), Cold Stone (worldwide), plus all v1/v2 chains (McDonald's, Chick-fil-A, Starbucks, Cheesecake Factory, IHOP, Outback, Buffalo Wild Wings, Texas Roadhouse, Cracker Barrel, etc. — ~107 known chain rows + ~600 fast-food-adjacent primary_type rows excluded vs pre-ORCH-0735 baseline). Bouncer correctly admits explicit upscale chains (Cipriani, Hawksmoor, Gordon Ramsay restaurants, Capital Grille, Ruth's Chris, Houston's, J. Alexander's) plus operator-locked cases Cava + Le Pain Quotidien. Servable place_pool reduced 14,400→13,671 (~5% pool shrink — chain credibility-protection at minor coverage cost). **What's now strong:** the bouncer surface is structurally correct (4 invariants preserved + 1 new `I-BOUNCER-EXCLUDES-FAST-FOOD-AND-CHAINS` ACTIVE); chain policy lives in code as canonical authority (Path A per DEC-107); 58 test fixtures cover positive matches + negative independents + allowlist bypass + two-pass parity + operator-locked admit guards (Cava, Le Pain Quotidien) + v2/v3 false-positive admits (Pupuseria Maria de Leon Bus, Paul and Jack bakery, Wasabi sushi indie, Quick Bites cafe, Perkins Orchard, Saladelia Duke café, wellness clinic, indie Romanos pizzeria, Sonic Room nightclub). **What's still fragile / queued:** **ORCH-0734 CLOSE** still pending (gated on this — ratifies place-intel-trial city-runs sampled-sync); **Cycle 17e-A close-pending** (anomalous: brand-CRUD migration `20260506000000_brand_kind_address_cover_hue_media.sql` shipped LIVE during this session via `supabase db push` but ~25 mingla-business code files uncommitted — operator confirmed intentional but needs CLOSE pass); **ORCH-0736** queued (~1 week deferred — decommissions parallel rule_sets / rule_set_versions / rule_entries Postgres tables + admin RPCs that have ZERO production consumer post-ORCH-0735); **ORCH-0738** queued (~1 week deferred — refactors `run-bouncer/index.ts` to streaming write-as-you-go after Washington 5,542-row pool surfaced WORKER_RESOURCE_LIMIT memory bug during v3 sweep; Promise.all of 500 concurrent UPDATEs + in-memory write accumulation OOMs on response-serialize phase). **No EAS OTA needed** for ORCH-0735 — bouncer is backend-only edge function. **Most operator-trust-building moment:** dry-run-as-safety-net pattern validated three times — caught false positives BEFORE any DB writes (v1→v2 caught 4 high-collision patterns paul/leon/wasabi/quick saving ~95 false-rejected independents at ~5-7 chain-admit cost; v2→v3 SC-16 caught 12 pluralization-pattern leaks across 9 cities). Worth codifying as memory pattern for future bouncer/scoring rule changes. **Most surprising:** Washington's HTTP 546 partial-write didn't catastrophically corrupt — `bouncer_validated_at` got updated for all 5,542 rows, but the response-serialize phase OOMed; implementor traced via SQL and confirmed writes committed. Filed as ORCH-0738.

> **2026-05-05 — ✅ Cycle 17d Stage 2 (BIZ Refinement Pass mini-cycle 4 §F — LOC decompose 3 fattest .tsx files) CLOSED PASS via tester independent verification (17/17 test cases PASS; 0 P0; 0 P1).** Plain-English: the three densest mingla-business screen files (event creator wizard Steps 2 + 5, plus event detail) are now split across 6 focused sibling sub-component files. **Visual UI 100% unchanged** — same accessibility labels, same hitSlop values, same conditional rendering, same icon names, same Sheet snapPoints — but underneath, the 2148-LOC `CreatorStep5Tickets.tsx` is now a 367-LOC orchestration shell, the 1354-LOC `event/[id]/index.tsx` is now 877 LOC (in target range 800-1000), and the 2271-LOC `CreatorStep2When.tsx` is now 1986 LOC (above the 1300-1500 target ceiling — implementor honest-partial; the other 4 sheets in that file couple to 12-20 parent state items each, so extracting them under the SPEC's no-prop-bloat constraint would worsen code quality without proportional benefit; tester accepted as principled). **The mapped-array rows now have `React.memo` boundaries** (`TicketTierCard` / `EventDetailTicketTypeRow` / `EventDetailActivityRow`) so future React profiling can verify per-row re-render reductions. **Caveat surfaced as P2 non-blocking discovery:** the parent passes 5 inline arrow handlers to `TicketTierCard`, defeating its memo at the prop layer. NOT a regression (pre-Stage-2 was unmemoized too) — flagged as future polish opportunity (D-CYCLE17D-S2-QA-1 — reshape row prop signature to `onEdit?: (ticketId: string) => void` + stable parent ref). **Sub-sheet positioning rule (`feedback_rn_sub_sheet_must_render_inside_parent` codified after Cycle 13a v3 RolePickerSheet invisibility bug) honored on first pass** — `<VisibilitySheet>` and `<AvailableAtSheet>` correctly nested inside `<TicketTierEditSheet>`'s parent Sheet children (not as siblings). Tester P4 praise. **Cross-domain impact verified clean:** all 4 external grep hits for pre-Stage-2 sub-component names (`TicketCard`, `VisibilitySheet`, `AvailableAtSheet`, `HeroStatusPill`, `TicketTypeRow`, `ActivityRow`, `TicketStubSheet`) are comment-only references in unrelated files (`RolePickerSheet`, `InviteBrandMemberSheet`, `eventLifecycle`, `ticketDisplay`) — no broken imports. `ToggleRow` consumers (`CreatorStep6Settings`, `CreatorStep3Where`, `account/notifications`) each define their own local copy — no broken imports there either. **Implementor's discretionary cleanup verified:** 4 unused imports removed (`Icon` + `IconName` + `Pill` from event/[id]/index.tsx + `formatDayOfMonth` from CreatorStep2When.tsx) — tsc didn't enforce (`noUnusedLocals` not on) so this was good-citizenship discipline. **Pattern repeat — fifth in a row:** effort ~1.5-2h vs SPEC's 4-5h estimate (mirrors 17a 2.5h vs 6h, 17b 1.5h vs 4-5h, 17c 3h vs 7.5h, 17d Stage 1 1.5h vs 6-8h). Zero reworks. Zero retests. Five cycles of pristine pipeline integrity is now an extremely reliable signal. **What's now strong:** the 3 fattest screens are structurally focused; future LOC growth on event creator + event detail won't exceed 2148 LOC mono-file because the natural extraction points are now precedents. Mapped-array rows have `React.memo` boundaries even if not realized at every call-site (scaffolding is in place). All 3 strict-grep CI gates (i37 + i38 + i39) continue to hold against the new file shapes — gates' `SCAN_DIRS` cover both `mingla-business/app/` and `mingla-business/src/` so all 6 new files are within scope. tsc clean throughout (verified at every per-file checkpoint plus final). **What's still fragile / forward-deferred:** **17e-A brand CRUD wiring** — entire brand lifecycle still phone-only TRANSITIONAL despite schema being fully ready (RLS DELETE policy + cascades + `deleted_at`); founder feedback D-17d-FOUNDER-1 SPEC dispatch ~12-16h IMPL. **17e-B event cover media picker** — events `cover_media_url` + `cover_media_type` columns ready (accept `'image'|'video'|'gif'`); brands needs additive cover_media columns if Tier 2 ships; founder feedback D-17d-FOUNDER-2 SPEC dispatch ~10-14h IMPL Tier 1. **`npm audit` triage** — 7 pre-existing vulnerabilities (6 mod / 1 high) surfaced post Stage 1 `npm install`; separate ORCH recommended. **Memo prop pattern future polish** — D-CYCLE17D-S2-QA-1 row-handler reshape (~20 LOC change in CreatorStep5Tickets + TicketTierCard) for future cycle when handler stability is fixed. **Doc-drift comments** — D-CYCLE17D-S2-QA-2 4 comments in unrelated files cite pre-Stage-2 names; bundle into next relevant cycle's tidy pass. **SecureStore migration** — Supabase auth tokens still live in plain AsyncStorage; deferred to security-hardening ORCH. **Sentry env still pending** (Cycle 16a TRANSITIONAL) — operator pre-deploy adds DSN. **Supabase Magic Link template** — operator dashboard verification still pending. **Apple JWT D-IMPL-46** belt+suspenders+auto in place. **Launch posture:** Mingla Business Phase 5 polish closer FOURTH MINI-CYCLE FULLY COMPLETE. Refinement Pass (4 mini-cycles: 17a + 17b + 17c + 17d Stage 1 + 17d Stage 2) **structurally complete**. 17e-A + 17e-B remain as DIFFERENT-PHASE work (founder-feedback feature additions, NOT Refinement Pass scope). **DEC-106 logged** (DEC-105 by 17d Stage 1; DEC-103 reserved by ORCH-0735 line: ORCH-0735 CLOSE → DEC-107 + ORCH-0736 → DEC-108 per OPEN_INVESTIGATIONS reminder; collision avoidance honored). **NO new invariants, NO new memory file, NO new migrations, NO new dependencies, NO native module changes.** **EAS OTA-able** — operator dispatches dual-platform OTA per `feedback_eas_update_no_web` (separate `--platform ios` + `--platform android`, NEVER comma-combined). **0 commits pushed yet** — operator commits + dual-platform OTA pending post-CLOSE. **Most operator-trust-building moment:** the implementor's honest SC-F1 partial labeling. SPEC §F.2's 1300-1500 LOC ceiling target was based on forensics' "700-900 LOC moved" projection; in practice, only 285 LOC moved cleanly without prop-interface bloat. The implementor explicitly framed it as PARTIAL with multi-paragraph rationale (state-coupling cost > LOC-target benefit) instead of soft-pedaling to PASS. Tester accepted as principled (not a bug-mask). This is exactly the failure-honesty discipline the orchestrator skill demands and that compounds operator trust over cycles. **Most surprising during Stage 2:** the SC-F2 over-achievement. SPEC's target was 1200-1400 LOC for `CreatorStep5Tickets.tsx`; the actual extraction landed at 367 (well below floor) because the natural boundary (TicketCard + TicketStubSheet + 2 picker sub-sheets + ToggleRow + co-domain helpers) was much larger than forensics estimated. Parent retains only orchestration logic + summary card + add CTA + ConfirmDialog — pure shell. Tester verdict: PASS (over-achieved is stronger than in-range; SC's intent was orchestration shell which is exactly what we got).

> **2026-05-05 — ✅ Cycle 17d Stage 1 (BIZ Refinement Pass mini-cycle 4 — perf pass + storage hygiene) CLOSED PASS via tester independent verification; §F LOC decompose deferred to Stage 2 per implementor honest-deferral.** Plain-English: mingla-business is now hardened against the 12-month "app gets slow after months of use" phenomenon iOS organisers especially notice. **The most user-felt protection:** every event the organiser ran ≥30 days ago gets its phone-cached entries (orders, comp guests, scans, edit log, door sales) automatically pruned at app start. Without this, a heavy organiser running 50 events × 200 orders × 100 scans/year would hit AsyncStorage's ~6MB soft-cap inside 12 months → cold-start hydration tax → laggy app. The pruning has a deliberate guardrail: events that the organiser hasn't yet confirmed-ended (e.g., a concert delayed by 2 hours but `end_at` is in the past) are NEVER evicted — only events with explicit `endedAt` confirmation AND TTL elapsed. Heavy operator at 12 months: still runs fast. **Three unused npm dependencies removed.** They were installed by earlier cycles "in case" but never wired up — pure bundle bloat (~30-50KB gzip). The fix is invisible to operators but every cold-start gets faster. **The `currentBrandStore` migrate function lost 145 lines of v1-v11 dead code** that nobody runs anymore (all live users are on v12). The audit trail is preserved via inline comment citing commit `aae7784d` so any future investigator can recover the chain via git history. **Two of three `DoorPaymentMethod` imports cleaned up** — leftover dead imports from Cycle 17a's `PAYMENT_METHOD_LABELS` consolidation. The third file (`door/index.tsx`) genuinely uses the type 6 different ways, so it stays — no fabricated cleanup. **Most operator-trust-building moment:** the implementor's honest §F LOC decompose deferral. SPEC §F.1-§F.4 estimated ~1900-2500 LOC structural extraction across 3 fattest .tsx files (CreatorStep2When 2271, CreatorStep5Tickets 2148, event/[id]/index.tsx 1354). Sub-components share a single `StyleSheet.create` object → per-extraction style-object decomposition required → adjacent-regression risk on the densest screens (event creator wizard typing flow, event detail) under time pressure. Implementor flagged it transparently; tester accepted as principled per failure-honesty pattern; orchestrator recommends Stage 2 focused dispatch (~4-5h). Tester also verified Stage 2 readiness — CreatorStep5Tickets has 5 inline sub-components (TicketCard / VisibilitySheet / AvailableAtSheet / TicketStubSheet / ToggleRow) **already structurally separated** at lines 95/307/381/451/1401, so Stage 2 extraction is mostly file-move + style-decomp, not deep refactor. Lower-risk than initially estimated. This is exactly the discipline `feedback_post_pass_protocol` "zero bugs zero glitches" + auto-mode "never half-finished implementations" demands. **Pattern repeat — fourth in a row:** effort came in ~1.5h vs SPEC's 6-8h estimate. Mirrors 17a 2.5h vs 6h, 17b 1.5h vs 4-5h, 17c 3h vs 7.5h. Zero reworks. Zero retests. Forensics + SPEC + IMPL + tester pipeline ran clean. Four cycles of pristine pipeline integrity is now a reliable signal. **What's now strong:** TTL eviction prevents AsyncStorage drift permanently (no operator action required). Orphan-key safety net detects future store renames before they accumulate dead keys. CurrentBrandStore migrate is lean. Bundle is ~30-50KB lighter. CI gates I-37 / I-38 / I-39 from 17b/c continue to hold (regression check exit 0). All 4 verification gates green (tsc + 3 CI gates). **What's still fragile / forward-deferred:** **Stage 2 §F LOC decompose** — 3 fattest .tsx files unchanged; CreatorStep5Tickets typing-lag perf benefit pending. Recommended ~4-5h focused dispatch. **17e-A brand CRUD wiring** — entire brand lifecycle (create + read + update + delete) is currently phone-only TRANSITIONAL despite schema being fully ready (RLS DELETE policy + cascades + `deleted_at`). Founder feedback D-17d-FOUNDER-1 queued. **17e-B event cover media picker** — events `cover_media_url` + `cover_media_type` columns are READY (accept `'image'|'video'|'gif'`); brands needs additive cover_media columns if Tier 2 ships. Founder feedback D-17d-FOUNDER-2 queued. **`npm audit` triage** — 7 pre-existing vulnerabilities (6 moderate / 1 high) surfaced post `npm install`; NOT 17d's responsibility. Separate ORCH recommended. **SecureStore migration** — Supabase auth tokens still live in plain AsyncStorage; deferred to security-hardening ORCH per D-17d-5. **Sentry env still pending** (Cycle 16a TRANSITIONAL) — operator pre-deploy adds DSN. **Supabase Magic Link template** — operator dashboard verification still pending. **Apple JWT D-IMPL-46** belt+suspenders+auto in place. **Launch posture:** Mingla Business Phase 5 polish closer FOURTH MINI-CYCLE STAGE 1 COMPLETE. Stage 2 §F + 17e-A + 17e-B remain in pipeline before Refinement Pass declares full closure. EAS dual-platform OTA pending operator post-CLOSE; commit covers Stage 1 only (8 files). **DEC-105 logged** (DEC-103 reserved by ORCH-0733 Gemini cutoff re-derivation; DEC-104 used by 17c CLOSE). **Most surprising during the cycle:** the implementor caught + flagged the §F deferral with concrete reasoning + Stage 2 readiness path. This is the failure-honesty pattern executed correctly — not "I ran out of time" but "I assessed the risk vs the time budget vs the quality bar and concluded honest deferral is the right call, here's the Stage 2 path." Tester confirmed CreatorStep5Tickets readiness (5 inline sub-components already separated → file-move + style-decomp lower-risk than initially estimated). Compounds value: future cycles know §F is queued + scoped + readiness-confirmed, not "we tried and got blocked."

> **2026-05-05 — ✅ Cycle 17c (BIZ Refinement Pass mini-cycle 3 — WCAG AA kit-wide remediation) CLOSE Grade A on operator iOS live-fire "all passes lerts proceed".** Plain-English: mingla-business is now WCAG AA-compliant on the three pillars App/Play Store reviewers actually flag — touch-target size, screen-reader labels, and reduce-motion respect — without changing the visual design at all. Every glassy icon button (search, bell, back, +, scanner controls — 58 of them across 28 screens) is still 36×36 visually but gets a 4-pixel "secret" tap zone on every edge that makes the touchable area effectively 44×44 (Apple HIG / Material floor). The fix is one line in the primitive: bake `hitSlop={top:4,bottom:4,left:4,right:4}` as a default. 58 buttons fixed in one place. **The TypeScript-enforced contract:** `accessibilityLabel` is now a REQUIRED prop on `IconChrome` — the `?? icon` silent fallback is removed. If anyone adds a new IconChrome and forgets the label, the build fails with "Property 'accessibilityLabel' is missing" — they can't ship. This kills an entire class of regression at the type system. **Three dim-backdrop dismiss zones now announce.** Modal/Sheet/TopSheet scrims used to be silent to VoiceOver — blind users landed on them and heard nothing. Now they announce "Dismiss modal, button" or "Dismiss sheet, button" — confident dismissal possible. **Five page-level Pressables explicit-labeled** — payment.tsx dev test toggles + PaymentElementStub dev toggles + CreatorStep1Basics category picker + CreatorStep2When preset selector. The I-39 gate caught 3 of these violations the §D forensics scan misclassified as false positives — gates are more authoritative than sub-agent label scans. (This is now codified as a process improvement for future cycles.) **Scanner overlay reduce-motion guard wired** — when iOS/Android Reduce Motion is ON, the 250ms fade collapses to instant set via `AccessibilityInfo.isReduceMotionEnabled()` subscription pattern (the canonical workaround for RN's old `Animated` API which lacks Reanimated's `useReducedMotion` hook). Vestibular-sensitive users get instant overlays. **The big structural win: first re-use of the 17b hardening-registry CI scaffold validates the operator's primary 17b ask.** Two new gates plug in: I-38 (touch-target ≥ 44 effective via AST math) + I-39 (label coverage with implicit-Text-child heuristic). 4-step "How to add a new gate" pattern works as designed. Future invariants (I-32 rank parity, I-34 canManualCheckIn decommission, I-36 ROOT-ERROR-BOUNDARY) each become ~2-3 hour follow-up cycles. **Most operator-trust-building moment:** the implementor caught a math bug in SPEC §G.3 (per-side `size + slop_side` would have returned 40 not 44 for kit default) and reported it transparently rather than silently fixing. Spec-is-law-unless-wrong-then-escalate discipline validated. The corrected math (`width = size + slop.left + slop.right`, `height = size + slop.top + slop.bottom`) is encoded in i38 lines 251-258. **Pattern repeat — third in a row:** effort came in ~3h vs 7.5h SPEC estimate (mirrors 17a 2.5h vs 6h, 17b 1.5h vs 4-5h). Zero reworks. Zero retests. Forensics + SPEC + IMPL + tester + iOS live-fire ran clean end-to-end. Three cycles of pristine pipeline integrity is strong signal. **What's now strong:** kit-wide WCAG AA touch + label compliance permanent (CI gates enforce). Reduce-motion 100% across 13 animated UI primitives + scanner page-level. I-38 + I-39 ACTIVE permanently. New memory `feedback_wcag_aa_kit_invariants.md` ACTIVE — future skill sessions reference it for kit-wide a11y discipline. Three CI gates green (i37 + i38 + i39); registry pattern proven. **What's still fragile / forward-deferred:** **Color contrast not yet audited** — WCAG SC 1.4.3 (4.5:1 body text, 3:1 large text) deferred to a dedicated later pass; out of 17c scope. **Web platform a11y deferred** — D-17c-7 explicitly out of scope (mobile-primary product); web focus-order + keyboard nav awaits a dedicated mini-cycle when web traffic justifies. **Dynamic Type / iOS text-scaling support deferred** — separate concern; mobile-primary launch floor doesn't require it. **Voice Control / Switch Control deferred** — depth-3 a11y; not launch-floor. **Android live-fire deferred per device availability** — iOS confirmed PASS; Android per operator device access; does NOT block CLOSE on single platform per `feedback_eas_update_no_web` two-separate-commands principle. **I-40 motion gate deferred** — D-17c-4 deferred; existing kit-wide compliance is already 100%, gate would be low-ROI vs implementation cost + false-positive friction; revisit if a regression appears. **JSDoc terminology slight ambiguity** — IconChrome.tsx:60-61 says "44 per side" while i38 gate uses "44 effective dimensions"; cosmetic doc tweak deferred (D-CYCLE17C-QA-1). **Sentry env still pending** (Cycle 16a TRANSITIONAL) — operator pre-deploy adds DSN. **Supabase Magic Link template** still needs operator dashboard verification. **Apple JWT D-IMPL-46** belt+suspenders+auto in place. **Launch posture:** Mingla Business Phase 5 polish closer THIRD MINI-CYCLE COMPLETE. 17a + 17b + 17c shipped; 17d perf pass remains in pipeline (bundle size + AsyncStorage hygiene + LOC decompose + DoorPaymentMethod cleanup). EAS dual-platform OTA pending operator post-CLOSE. **DEC-104 logged** (DEC-103 reserved by ORCH-0733 Gemini cutoff re-derivation; collision avoidance flagged at SPEC dispatch authoring + locked at CLOSE). **Most surprising during the cycle:** the implementor's tsc-driven §B enumeration shrank scope from SPEC's anticipated 28 IconChrome consumer files to 5 actual styleguide demo fixtures. Production code was already labeled by 17a/17b carry-over. The "TS will tell you" approach saved ~1.5 hours of file-by-file SPEC reading and codifies a future-cycle process improvement: tsc-driven enumeration is the binding completion signal, not pre-counted file inventories.

> **2026-05-05 — ✅ Cycle 17b (BIZ Refinement Pass mini-cycle 2 — TopBar IA reset + hardening-registry CI scaffold) CLOSE Grade A on operator device smoke "smoke pass".** Plain-English: the events tab top bar now uses a proper architectural pattern instead of the 17a tactical hack. The visible result is identical to before — same search + bell + plus icons in the same positions — but underneath, the TopBar primitive gained a new `extraRightSlot` prop that lets pages ADD icons to the default cluster instead of REPLACING it. events.tsx switched to the new prop and dropped 23 lines of tactical code. **The big structural win:** first strict-grep CI gate ever shipped in this repo. From now on, any pull request that tries to break I-37 ("primary tabs must keep [search, bell] visible") fails CI immediately with a clear error message + suggested fix. The gate was designed as a **registry pattern** per the operator's specific framing — every future invariant CI gate plugs in as one Node.js script + one workflow job. No re-architecting needed. Three pre-existing invariant proposals (I-32 rank parity, I-34 canManualCheckIn decommission, I-36 ROOT-ERROR-BOUNDARY) that were proposed in earlier cycles but never enforced now have a clear path: each becomes a ~2-3 hour follow-up cycle using the new scaffold. **Most operator-trust-building moment:** the forensics pass caught a scope-narrowing finding that would have over-fired the invariant. Original I-37 draft said "every TopBar consumer must keep the default cluster." Reality: 15 back-route consumers (Edit Brand, Audit Log, Brand Payments, Brand Profile, Brand Finance Reports, Event Detail, Team list) intentionally suppress the cluster for focused-task UX. Treating them as violations would have broken 15 working surfaces. Forensics caught it; SPEC scoped I-37 correctly to `leftKind="brand"` consumers only (3 production routes); CI gate enforces accordingly. **Pattern repeat from 17a:** effort came in 1.5h vs 4-5h estimate (mirrors 17a 2.5h vs 6h). Zero reworks. Zero retests. The forensics + SPEC + IMPL + tester + smoke pipeline ran clean end-to-end. This is now two cycles in a row where verbatim contracts paid off — strong signal of pipeline integrity. **What's now strong:** the events tab top bar matches the home tab cluster exactly (founder-feedback resolution from 2026-05-04). I-37 codifies the rule permanently. The hardening-registry pattern means the next time you ratify an invariant, the implementor adds one .mjs file + one workflow job — not "build a new CI workflow from scratch." Future skill sessions discovering "new invariant needs CI gate" find the registry pattern via `feedback_strict_grep_registry_pattern.md` (now ACTIVE) and follow the documented 4-step procedure. **What's still fragile / forward-deferred:** **Search and bell icons stay decorative** — operator-locked W1+W5; no click action wired in 17b. If operators surface pain post-ship, 17b-W follow-up mini-cycle wires them based on observed need (W2 search screen / W3 inline filter / W6 notifications screen / W7 badge-only). **Real notification feed deferred to B-cycle** — needs OneSignal SDK + edge fn + DB table; structural-only 17b preserves architectural freedom. **36px touch target stays kit-wide** — pre-existing pattern below WCAG 44pt minimum across TopBar IconChromes. NOT introduced by 17b; surfaced for 17c WCAG audit ORCH register. **Workflow YAML actionlint validation** deferred to GitHub Actions runtime on first PR after CLOSE — first PR will surface any real parse errors. **Sentry env still pending** (Cycle 16a TRANSITIONAL) — operator pre-deploy adds DSN to .env + EAS Secrets. **Supabase Magic Link template** still needs operator dashboard verification (Cycle 15 §A.5). **Apple JWT D-IMPL-46** belt+suspenders+auto in place. **Launch posture:** Mingla Business Phase 5 polish closer SECOND mini-cycle complete. 17a quick wins + 17b TopBar IA reset shipped; 17c WCAG audit + 17d perf pass remain in pipeline. EAS dual-platform OTA pending operator post-CLOSE; combined commit covers 17a + 17b code (per QA D-CYCLE17B-QA-3 disambiguation: BrandProfileView 6-line diff is pre-existing 17a comment edit, not 17b regression). **Most surprising during the cycle:** the operator's framing of D-17b-5 reshaped the entire scope. Original SPEC dispatch sketched "build a CI workflow with one I-37 gate." Operator's "Can we make workflow file moving forward, where we register hardening changes so nothing else breaks it. Adding this will be the first" turned a one-off enforcement into a registry pattern. The SPEC author honored that framing — every gate is now a modular script + workflow job + README cross-reference, not embedded in a monolithic workflow. Future invariants ship trivially. This is exactly the kind of small framing nudge that compounds across cycles. **DEC-101 collision flag for orchestrator awareness:** ORCH-0733 (parallel session, place-intelligence Gemini A/B comparison + Anthropic drop) planned DEC-101 for "Anthropic dropped"; this 17b CLOSE used DEC-101 first (locked at the top of DECISION_LOG); ORCH-0733 close needs to bump to DEC-102 or next available. Surface to that session's orchestrator at their close.

> **2026-05-05 — ✅ Cycle 17a (BIZ Refinement Pass mini-cycle 1 — Quick Wins) CLOSE Grade A on operator device smoke.** Plain-English: 11 small mechanical fixes that have been queuing across cycles 2–16a now ship together. **The one visible change:** the events tab top bar now shows `[search, bell, +]` together — search and bell are the same icons that show on the home tab; `+` is conditional on whether the operator has permission to create events. Pre-17a the events tab showed ONLY `+` (when permitted) or fell back to default `[search, bell]` (when not permitted) — never all three. Post-17a, the cluster matches the founder's mental model. Search and bell don't actually do anything yet (they're decorative until 17b wires them up properly with a new `extraRightSlot` prop on TopBar) — but they're visually there, which is what the founder noticed was missing. **The latent crash that almost shipped:** `mapBrandRowToUi` (the function that converts database brand rows to UI brands) was missing 3 fields the UI Brand type required (`kind`, `address`, `coverHue`) — Cycle 7 FX2 added them to the UI but neither the database row interface nor the mapper was updated. Real-DB brand creation isn't wired yet, so this has been a hidden ghost-path bug. The moment B-cycle wires real brand creation, it would have crashed with a TypeScript error. 17a patches it defensively with safe defaults (`popup` kind / null address / `25` warm-orange hue) and a TRANSITIONAL marker pointing to B-cycle for the real fix. **Cleanup work:** duplicate style block in events.tsx deleted (was silently stripping `zIndex: 100` from toasts — toast risked rendering behind dialogs) · 3 stale TRANSITIONAL marker comments updated (recurrenceRule.ts no longer claims it's unused; BrandProfileView.tsx no longer references the long-shipped J-A12 cycle) · payment method labels (`Cash`, `Card reader`, `NFC tap`, `Manual`) consolidated from 3 inline copies into 1 util · 5 allowlist comments placed for a future strict-grep CI gate that doesn't exist yet (preventive). **NEW canonical doc:** `FOUNDER_FEEDBACK.md` — append-only log where founder feedback gets recorded with date, verbatim quote, and disposition. First entry is the operator's 2026-05-04 top-bar IA feedback (sub-items 1+2 routed to 17a/17b pipeline; sub-item 3 "move Account to top bar" DECLINED per operator decision). **Most operator-trust-building moment:** the smoke test caught what looked like a regression ("+ not showing up") — turned out to be SPEC-correct gate behavior (no brand selected → permission gate hides `+`). Operator confirmed hypothesis 1 within seconds; smoke PASS with brand selected. **Most surprising during the cycle:** SPEC §B.6 (consolidate currency formatter) was already done — `currency.ts` pre-existed with 3 exports; both consumers already imported; the master-inventory aggregate had a stale D-IMPL-A12-2 entry that another cycle had silently closed. Pipeline saved ~30 min and surfaced a recommendation to audit the master-inventory aggregate against current code at next Refinement Pass. Also surprising: SPEC asked for `DEC-099` row but DEC-099 was already taken by the ORCH-0713 architecture revision; implementor used DEC-100 instead and documented the collision inline. AND DEC-082 turned out not to exist as a row in the table at all (numbering gap honestly accepted as historical per tester's recommendation — the audit-trail value of "we skipped it" outweighs retroactively backfilling). **Pipeline ran cleanly** — 1 forensics + 1 SPEC + 1 IMPL + 1 tester + 1 smoke-diagnosis loop = 5 stages, 0 reworks, ~2.5 implementor hours vs SPEC's 6-hour estimate (drivers: forensics + SPEC accuracy + 3 SPEC-DISCOVERY items handled honestly via fallbacks rather than fabrication). **What's still fragile / forward-deferred:** **Sentry env still not wired for production runs** — operator pre-deploy must add `EXPO_PUBLIC_SENTRY_DSN` to `.env` + EAS Secrets for active monitoring (DSN provided in IMPL report §C). Cycle 16a shipped TRANSITIONAL with env-absent guard until then; 17a leaves that as-is. **Supabase Magic Link template UNVERIFIED** — operator §A.5 dashboard check confirms email body contains `{{ .Token }}` (the 6-digit code) and not `{{ .ConfirmationURL }}` (the magic link URL). 30-second Dashboard check before next OTA. **17b TopBar IA reset queued** — `extraRightSlot` prop refactor will delete the 17a tactical inline cluster + ratify proposed I-37 invariant (TopBar default `[search, bell]` always visible; pages compose, never replace) + wire search and bell to actually do something. **17c WCAG audit queued** — D-CYCLE17A-IMPL-5 36px touch target is below WCAG 44pt minimum across the kit (TopBar.tsx default cluster, IconChrome primitive defaults). Pre-existing kit-wide pattern, not introduced by 17a; needs separate ORCH for systematic audit. **17d perf pass queued** — covers DoorPaymentMethod imports possibly unused after §B.5 lift (D-CYCLE17A-IMPL-6) + bundle size baseline + AsyncStorage version audit. **Apple JWT D-IMPL-46** closed as already-mitigated — autorotate spec + scheduled remote agent + GitHub Actions workflow already cover this. **Launch posture:** Mingla Business Refinement Pass FIRST MINI-CYCLE COMPLETE. Phase 5 polish closer ongoing — 17b/17c/17d sequential remain in pipeline. EAS dual-platform OTA pending operator post-CLOSE.

> **2026-05-04 (close++++++) — ✅ Cycle 16a (BIZ Cross-Cutting Polish quick wins) CLOSE Grade A on operator device smoke "all passes".** Plain-English: Mingla Business now monitors itself (Sentry captures any component crash), shows a friendly 404 when someone hits a typo'd URL, transitions smoothly from splash to welcome instead of flashing through a spinner, and gives users a way to recover when they've denied photo permission ("Open Settings" deeplink). Four small but load-bearing polish wins. NO new dependencies. NO schema changes. **What's now strong:** Sentry was installed 7 months ago but never actually initialized — `Sentry.init()` was never called anywhere in the code. Every component crash since Cycle 0a has silently disappeared. After 16a, Sentry captures every uncaught throw with React component-stack context (operator wires the DSN env post-deploy → next OTA picks up the active SDK). The ErrorBoundary primitive built in Cycle 0a Sub-phase C.3 was finally wrapped at the app root — codified as NEW invariant I-36 ROOT-ERROR-BOUNDARY with CI grep gate so this regression can't return. Image-picker permission denial now offers an "Open Settings" path instead of dying with a toast (closes Cycle 14 J-A1 silent-recovery gap). 404 page is on-brand. Cold launch is now seamless instead of a 3-state visual flash. **Most operator-trust-building moment:** Phase 0 forensics ingest caught that the original D-IMPL-37 framing ("Sentry org/project not provisioned") was understated. The actual gap was bigger: even WITH a provisioned project, the SDK still wouldn't fire because `Sentry.init()` was never called. Honest re-classification: D-CYCLE16-FOR-1 root cause + D-CYCLE16-FOR-2 contributing. Plus D-CYCLE16-FOR-3 closed Cycle 14's D-CYCLE14-IMPL-3 NSPhotoLibraryUsageDescription concern as a false alarm — `photosPermission` was already configured in app.config.ts since Cycle 7. Two paper-trail corrections in one cycle. Pipeline ran cleanly: forensics surfaced 7 discoveries → DEC-098 split into 16a (OTA-able) + 16b (native rebuild) → SPEC verbatim contracts → IMPL with 9 sequential tsc-checkpointed steps + ui-ux-pro-max pre-flight + 6/6 grep gates → device smoke "all passes" → CLOSE Grade A. **Most surprising during the cycle:** Cycle 14's NSPhotoLibraryUsageDescription concern was already closed before it was raised — `photosPermission: "Mingla Business uses your photo library to upload brand and event imagery."` had been in `app.config.ts` plugins block since Cycle 7. Forensics caught this on file-read; D-CYCLE14-IMPL-3 closes via D-CYCLE16-FOR-3. Implementor honestly skipped scanner refactor (D-CYCLE16A-IMPL-1) because scanner already had graceful full-screen permission UI; ConfirmDialog overlay would have regressed UX. SPEC author's intent for "consolidation" met for edit-profile (real value); scanner left untouched. **What's still fragile / forward-deferred:** **Sentry env not yet wired for production runs** — operator pre-deploy must add `EXPO_PUBLIC_SENTRY_DSN` to `.env` + EAS Secrets (`SENTRY_AUTH_TOKEN` + `SENTRY_ORG` + `SENTRY_PROJECT`) for active monitoring + readable stack traces. Cycle 16a ships TRANSITIONAL with env-absent guard until then. **Cycle 16b deferred indefinitely** — offline detection (`@react-native-community/netinfo`) + force-update prompts (`expo-updates`) require new dependencies + native rebuild via `eas build`. Family-and-friends MVP scale doesn't justify offline + force-update infra yet; revisit when traffic warrants. **Notifications + location permission UX** speculative (D-CYCLE16-FOR-6) until B-cycle OneSignal SDK + a future location-using surface land. **Apple JWT expiry tracker** D-IMPL-46 reminder due ~2026-10-12 (Cycle 0b carry-forward; persistent). **Launch posture:** Mingla Business Phase 5 (Account + Polish) is now FEATURE-COMPLETE on the UI side. Phases 4 (Event Management — Cycles 9c→10→11→12→13) + 5 (Cycles 14 → 15 → 16a) compose the entire operator-facing app baseline. **B-cycle backend wires** is the next major slot — closes 6+ TRANSITIONAL banners simultaneously (Stripe + Resend + OneSignal + audit_log writers + 5 invite/scan/door edge functions). Or operator can pick from the open queue: ORCH-0713 Phase 0.5 runtime verify (place-intelligence calibration) + ORCH-0721/0722/0723 chain (PR #62 close) + ORCH-0712 forensics + ORCH-0708 IMPL + Refinement Pass mini-bundle. **Cycle 16b** stays on the slot list but deferred until operator commits to native rebuild window.

> **2026-05-04 (close++) — ✅ Cycle 15 (BIZ Organiser Login + Email-OTP) CLOSE Grade A on operator device smoke "all pass".** Plain-English: organisers without Google or Apple accounts can now sign in to Mingla Business with just their email. Tap "Continue with Email" on the welcome screen → type your email → tap Send code → check your inbox for a 6-digit code → paste the code (it auto-submits when you type the 6th digit) → you're signed in. Same auto-create-on-first-sign-in behavior as Google and Apple. Works identically on iPhone, Android, and the web bundle — Supabase's email-OTP API is platform-agnostic, no native SDK needed. Recovery if you previously deleted your account works exactly the same way for email sign-ins as it does for Google/Apple — Cycle 14's 30-day window applies regardless of provider. **What's now strong:** the universal sign-in option closes the cold-traffic gap (anyone with an email can become an operator) without compromising security (passwords are never stored — Supabase emails a one-time code each time). The UX includes 60-second Resend cooldown with live countdown, "Wrong email? Edit" preserves the email if they typed it wrong, rate-limit copy is explicit ("Too many attempts. Wait a minute before trying again."), wrong/expired code copy is honest ("That code didn't match or has expired. Try again."), and keyboard-discipline is applied so the input stays visible above the soft keyboard on native. **Most operator-trust-building moment:** the dev-server SyntaxError crash that surfaced post-IMPL (D-CYCLE15-IMPL-1) was honestly diagnosed as pre-Cycle-15 paste-debris from the Cycle 13 v2 export rework — orchestrator initially mis-classified it as "probably dead code" in REVIEW; operator's runtime hit it; orchestrator adjusted the threshold for future REVIEWs (any tsc parse error reaching HEAD is by definition NOT dormant, just unobserved-yet). The combined v2 rework caught a SECOND duplicate further down the same file + a Cycle 15 IMPL completeness gap (missed second `BusinessWelcomeScreen` consumer at `app/auth/index.tsx`) — both fixed at the source within v2 dispatch scope. The pipeline ran cleanly end-to-end: forensics → 6 operator decisions locked in batch → SPEC verbatim contract → IMPL with 7 sequential tsc-checkpointed steps + ui-ux-pro-max pre-flight + 8/8 grep gates → dev-server crash → hotfix → tsc surfaced 2 more issues → combined v2 rework → device smoke "all pass". **Most surprising during the cycle:** the original Cycle 15 epic was 56 hours; final IMPL was ~12-16 hours. Phase 0 ingest caught WEB2 already shipped in Cycle 0b (operator's "email and login already fixed" referred to OAuth web flow), and `mingla-marketing/` (Next.js) is the founder's separate workstream per DEC-086 owning J-L1..J-L4 marketing surfaces. So Cycle 15 collapsed to ONE thing: add email-OTP to the welcome screen. ZERO schema changes; ZERO new dependencies; ZERO mutations to existing stores. Schema accepted email-OTP-created users without modification (creator_accounts RLS already permissive on `auth.uid() = id`). **What's still fragile / forward-deferred:** **Operator pre-deploy gate** — Supabase Dashboard → Auth → Email Templates → "Magic Link" body must contain `{{ .Token }}` somewhere visible (renders the 6-digit code); if template is link-only, the email arrives but the code isn't visible — UX breaks. ~30-second Dashboard check before EAS OTA. **First email-OTP user `display_name` fallback** — `ensureCreatorAccount` falls back to `user.email.split('@')[0]` (e.g., "joe" from "joe@example.com") because Supabase doesn't populate `user_metadata.full_name` for email-OTP users. Acceptable initial UX; operator can change in J-A1 edit-profile post-sign-in. **Resend SMTP wire** — Cycle 15 uses Supabase default email provider; B-cycle wires Resend alongside notification infra. **Apple JWT expiry tracker** — D-IMPL-46 reminder due ~2026-10-12 (Cycle 0b carry-forward). **Launch posture:** Mingla Business sign-in surface is now universal (Google + Apple + Email). Phase 5 polish (Account hub from Cycle 14 + Email sign-in from Cycle 15) compose the last operator-facing baseline UX before B-cycle backend wires close 6+ TRANSITIONAL banners simultaneously. **Cycle 16 (cross-cutting polish: offline + force-update + error + 404 + splash + permission-denied)** OR **B-cycle backend wires** are the operator's next slot.

> **2026-05-04 (final) — ✅ Cycle 14 (BIZ Account hub) CLOSE Grade A on operator device smoke + v2 rework retest "all pass".** Plain-English: the operator's Account tab transformed from a Cycle 0a placeholder (just sign-out + email + brand list) into a real settings hub. Three new screens: **Edit profile** (change your name and upload a profile photo — photo lands in a new Storage bucket scoped tightly so you can only upload to your own user-id path), **Notifications** (4 toggles for what to be notified about — for now they save locally; the actual push/email delivery wires up in B-cycle when OneSignal + Resend ship), and **Delete account** (a 4-step flow: warning → cascade preview showing exactly what will be deleted → type your email to confirm → soft-delete with 30-day recovery window). The existing sign-out button got renamed "Sign out everywhere" with a caption clarifying it signs you out on every device — Supabase already does this by default in v2.39+ (we verified the source), so the rename is purely a UX honesty fix. **What's now strong:** the operator finally has GDPR-compliant account control (Strategic Plan R4 satisfied — minimum scope) without forcing them to email support. Soft-delete with auto-recovery on sign-in: change your mind within 30 days, just sign in again, account un-deletes itself, you see "Welcome back" toast on the Account tab. The cascade preview is honest — only shows rows where you actually have data (no fake "0 brands deleted" noise). The avatar Storage RLS is path-scoped tighter than the consumer-app pattern: upload only allowed if `split_part(name, '.', 1) = auth.uid()::text` — meaning the file MUST be named after the operator's user-id, no spoofing possible. **Most operator-trust-building moment:** the device smoke caught a real Const #3 violation (Bug A — `void signOut()` fire-and-forget race meant the user got "redirected to home page, not signed out" after delete) AND a latent silent-killer (Bug B — recovery helper running on every auth event with s.user, which would silently un-delete the account if a token refresh fired between deleted_at=now() and signOut). Operator caught Bug A on smoke; orchestrator forensic trace surfaced Bug B as a sibling on the same surface; v2 rework fixed both at the source with 2 surgical edits totalling ~+15/-6 LOC. The pipeline ran cleanly: forensics → 14 operator decisions locked in batch → SPEC verbatim contract → IMPL with 15 sequential tsc-checkpointed steps + ui-ux-pro-max pre-flight + 8/8 grep gates → migration deploy hotfix (decorative COMMENT ON POLICY rejected by Postgres because storage.objects is owned by supabase_storage_admin — stripped) → commit + push → device smoke caught Bug A → tight rework → retest "all pass". **Most surprising during the cycle:** PR #59 already shipped 80% of the schema. `creator_accounts.deleted_at` + `account_deletion_requests` audit table + `marketing_opt_in` boolean were all present from a prior PR. Cycle 14 was mostly UI wiring on top of stable schema; only one NEW migration (the avatar Storage bucket — D-14-2 SPEC-pivot when forensics caught that the originally-proposed `brand_covers` bucket doesn't exist; Cycle 7 brand "cover" is hue-based via EventCover.tsx, not image-upload). **What's still fragile / forward-deferred:** **B-cycle backend deferrals close 6+ TRANSITIONAL banners simultaneously** — `request-account-deletion` edge fn + 30-day hard-delete cron + `auth.admin.deleteUser` CASCADE + Resend confirmation email + OneSignal SDK + `user_notification_prefs` table all defer to B-cycle. The 4 notification toggles save to Zustand TRANSITIONALLY; marketing toggle alone double-wires to schema column. **Account deletion is soft-only client-side** — mobile UPDATEs `creator_accounts.deleted_at = now()` via self-write RLS; the audit row + 30-day cron + hard-delete cascade defer to B-cycle (mirroring consumer-app `delete-user` edge function pattern). **`expo-image-picker` first-use on iOS** may need NSPhotoLibraryUsageDescription declared in app.config.ts (D-CYCLE14-IMPL-3) — operator's Step 3 smoke worked, so it's likely already configured speculatively from Cycle 7, but unverified-by-grep. **Brand transfer feature blocked by `brands.account_id` IMMUTABLE constraint** (D-CYCLE14-FOR-4) — `biz_prevent_brand_account_id_change` trigger forces Option B "soft-orphan" pattern; future "transfer brand to another operator" UX requires schema change. **Launch posture:** Mingla Business Phase 5 (Account) is now feature-complete on the UI side. Phases 4 (Event Management — Cycles 9c→10→11→12→13) + 5 (Account — Cycle 14) compose the operator-facing app baseline. **Phases 6+ open with whichever cycle the operator picks next** — likely B-cycle backend wires (closes 6+ TRANSITIONAL banners simultaneously) per cycle epic alignment, or Refinement Pass mini-bundle to clear the small P3 backlog before next major cycle.

> **2026-05-04 (late) — ✅ Cycle 13 (BIZ End-of-Night Reconciliation Report) CLOSE Grade A on operator device smoke + v2 rework retest.** Plain-English: organisers now have ONE screen at end-of-night that adds up all 4 ledgers (online ticket sales + door sales + comp guests + scans) and tells them whether the night made money + whether everyone got in. Tap "Reconciliation" tile on Event Detail (visible to finance_manager+ rank only) → see headline that adapts per event status (Live / Upcoming / Ticket sales ended / Event cancelled), then 4 sections: how many tickets sold (online + door + comps), how much money came in by payment method (Card, Apple Pay, Google Pay, Cash, Manual + B-cycle placeholders for Card reader and NFC tap), how many people scanned in (with no-show count on past events), and any data-integrity warnings (silent when everything reconciles cleanly — only fires on rare edge cases like door-sale-without-scan or method-sum-drift). One tap to export the night as a CSV with refund attribution per row + 5-line summary preamble for accountants. Cycle 12's door-only reconciliation card stays put on the Door Sales screen — Cycle 13 is the cross-source SUPERSET that you can also reach via a small "View full reconciliation report" CTA at the top of the door route. **What's now strong:** the operator finally has finance closing tools that don't require manually cross-referencing 4 lists in a spreadsheet at 2am. Permission gate at finance_manager+ matches existing refund actions — same authority cluster + same UX pattern. Discrepancy detection is honest and silent when clean (no false-positive warm-orange noise pretending issues exist). Settlement-stub split was corrected on this route per D-13-10 (online×0.96 + door×1.0 fixes the over-deduction the Event Detail KPI card still has — that's a separate small ORCH for a future polish). Native CSV export now toasts honestly post-v2 (silent on share-sheet dismiss; "shared" when user picks Mail/Notes; "downloaded" on web). **Most operator-trust-building moment:** the v2 rework caught a Const #3 violation that had been silently lying for 3 cycles (Cycle 10 J-G6 + Cycle 12 J-D5 + Cycle 13 J-R3 all shared the same "success on dismiss" lie) — operator caught it at device smoke; fix at the source mended all 3 simultaneously without scope expansion. The orchestrator pipeline ran cleanly: forensics → 11 operator decisions locked in batch → SPEC verbatim contract → IMPL with 12 sequential tsc-checkpointed steps + mandatory ui-ux-pro-max pre-flight + 9/9 grep gates → device smoke caught real Const #3 → tight rework → retest PASS. **Most surprising during the cycle:** D-CYCLE13-RECON-FOR-3 — the existing Event Detail KPI card payout calculation (`revenueGbp × 0.96`) is wrong for door-heavy events because cash sales have zero Stripe fee. Cycle 13 reconciliation route uses the correct split locally; Event Detail KPI stays simplified per D-13-10 lock; orchestrator can backlog a small ORCH to align them later. **What's still fragile / forward-deferred:** **PDF export DEFERRED to B-cycle** — "Email PDF report" CTA visibly disabled with caption "B-cycle"; full file-share UX (`expo-sharing` + `expo-file-system` install per pre-existing TRANSITIONAL D-CYCLE10-IMPL-1) ships when B-cycle does the native build. **Audit log integration DEFERRED entirely** (D-13-11) — the audit_log brand-admin SELECT policy from Cycle 13b exists but no client writers populate rows yet; reconciliation route has no operator-timeline section until B-cycle wires audit_log writers. **VIEW_RECONCILIATION rank gate is mobile-UX only** — no server RLS counterpart yet because Cycle 13 is read-only over local Zustand stores; I-32 amendment honestly documents this; B-cycle reconciliation RPC must mirror the gate. **I-27 store-level enforcement gap** still open (D-CYCLE13-RECON-FOR-1) — Cycle 13 dedupes scans defensively at aggregator layer; B-cycle scan-ticket edge function adds DB UNIQUE constraint to close it permanently. **Launch posture:** Mingla Business Phase 4 (Event Management) is now feature-complete from operator perspective. Cycles 9c (orders) + 10 (guests) + 11 (scanner) + 12 (door sales) + 13 (reconciliation) compose the full event-night operations toolkit. Phase 5 (Account) opens with Cycle 14 = edit profile / settings / delete-flow / sign out per cycle-14.md. **Cycle 14 is the next BIZ slot.**

> **2026-05-04 — ✅ Cycle 13b (BIZ Permissions UI Depth) CLOSE Grade A on operator device smoke + clean `supabase db push`.** Plain-English: cleanup cycle that took out two pieces of dead scaffolding (the `permissions_matrix` DB table that no code read + the `canManualCheckIn` toggle that gated nothing) and added one tiny safety policy (brand admins can now see all team members' actions on their brand in the audit log, not just their own). Mobile UX: scanner invite sheet now shows ONE toggle (Accept payments at the door) instead of two; scanner team list rows have one less pill; audit log banner reads honestly ("Brand admins see all team actions; team members see their own"). Database: one fewer table; one new SELECT policy on audit_log; existing self-only policy still in place via PostgreSQL multi-policy OR-merge. **What's now strong:** the permissions architecture has a single source of truth at both layers — `biz_role_rank` SQL function on server-side (PR #59 line 11-30) and `MIN_RANK` constants on mobile-side (Cycle 13a permissionGates.ts) — both were already canonical, but Cycle 13b's matrix drop removes the stale duplicate that could have tricked future implementors. The forward-compat invariant I-33 locks the permissions_override jsonb shape as a deny-list, so when the operator validates a "restrict X without changing role Y" use case, the editor ships in <1 day on stable contracts. **Most surprising during the cycle:** rare zero-rework BIZ cycle — verbatim contracts in SPEC paid off; implementor delivered exactly what was specified with no mid-cycle ambiguity surfacing. The cycle ran clean from forensics through deploy in one session. **Most operator-trust-building moment:** the YAGNI discipline — Q2 (override editor) + Q3 (per-event roles) deferred WITHOUT reluctance, with shape locks (I-33-DRAFT-now-ACTIVE) and decision locks (DEC-093) preserving forward-compat. Const #8 (subtract before adding) applied consistently — Q4 mirrored DEC-092 J-A9 subtract precedent. **What's still fragile / forward-deferred:** **B-cycle backend writes still owed** — Cycle 13a + Cycle 13b's audit log viewer have empty rows until B-cycle ships writers (refund / role-change / brand-edit / invite edge functions writing audit_log rows in same transaction). Cycle 13b's brand-admin SELECT policy is correctly authored + applied but unobservably useful until B-cycle starts populating data. **F-1 scanner shape translator deferred to B-cycle** — when scanner backend writers ship, ~10 LOC translator at the read/write boundary maps mobile camelCase ↔ DB snake_case (`canScan ↔ scan` + `canAcceptPayments ↔ take_payments`). Cycle 13b's drop of `canManualCheckIn` simplified this — no third key to translate. **Audit log writers themselves deferred** — every operation that should record (refund, role change, brand edit, invite, scanner change) needs its edge function to INSERT audit_log rows in the same transaction; Cycle 13b sets the SELECT policy but writers ship in B-cycle. **`permissions_override` editor deferred to 13c+** — locked shape (I-33 ACTIVE deny-list) but no UI; ships when operator validates a real "restrict X" ask. **Per-event roles deferred indefinitely** (DEC-093). **Launch posture:** Mingla Business permissions architecture is now lean + single-source-of-truth on both layers (mobile + backend). Cycle 13a + 13b together cover all empirically-surfaced operator workflows on the team-management + audit-log surface; Cycle 13c becomes use-case-driven rather than scope-driven. **Cycle 13c is the next BIZ slot** if/when operator surfaces a real per-member override or per-event role need. **Most surprising forensics finding:** PR #59 author shipped permissions_matrix as runtime-mutable scaffolding that nothing ever consumed — Cycle 13b finally dropped it after 6+ months of being a permissionless trap door. Pattern lesson codified in DEC-094 + D-CYCLE13B-FOR-3 (PR-time frontend coordination on new client-readable tables).

> **2026-05-04 — ✅ Cycle 13a (BIZ Permissions UI Foundations) CLOSE Grade A on operator device smoke.** Plain-English: organisers can now invite real teammates. From their brand profile → Team & permissions, they see a list with themselves auto-recognized as Account Owner, plus any pending invitations they've sent. Tap "+" → fill name + email + pick from 6 roles (Account owner / Brand admin / Event manager / Finance manager / Marketing manager / Scanner) → submit → pending row appears with role pill + relative time. Brand admins also get an "Audit log" entry in the menu showing recent recorded actions. **The invitations are stored locally for now** (TRANSITIONAL banner on every team surface honestly says so) — emails + acceptance flow ship in B-cycle. Underneath it all, 7 critical operator surfaces are now hidden or disabled for sub-rank users so a "scanner"-role teammate can't accidentally cancel an event, refund money, or change ticket prices. The role-rank thresholds mirror the SQL `biz_role_rank` function exactly (CI grep gate enforces) so what mobile shows = what RLS enforces server-side. **What's now strong:** the operator can finally hand over part of their workflow to another human without giving them root. Solo operators on first launch are auto-recognized as account_owner via a synthesis fallback chain (DB lookup with stub-mode bridge for local-only brands). The Cycle 11 scanner invitation pattern + Cycle 12 sub-sheet picker pattern were both generalized cleanly — code reuse across cycles is paying off. **Most surprising during the cycle:** the forensics agent missed an entire pre-existing J-A9 BrandTeamView cluster (5 components + 2 routes + 6 currentBrandStore types). Operator caught it mid-IMPL when SPEC tried to create files at colliding paths. Locked DEC-092 Path A (subtract J-A9 entirely + replace with 13a contracts). Then operator device smoke caught 3 P0 bugs across 2 reworks: (1) synthesis fallback didn't handle stub brands → "+" buttons missing on Team page + Events tab + 7 cascading gates locked the operator out of basic actions; (2) RolePickerSheet wouldn't open visibly because it was rendered as a Fragment sibling instead of inside the parent Sheet (RN Modal nesting) — Cycle 12 verbatim pattern was the fix. Both reworks were single-file mechanical patches. Memory rule recommendation: codify "RN sub-sheets MUST render inside parent Sheet, not Fragment siblings" so future implementors don't repeat. **What's still fragile / forward-deferred:** **Brand invitations TRANSITIONAL** — `useBrandTeamStore` is client-side persisted only; B-cycle wires `invite-brand-member` + `accept-brand-invitation` edge functions writing to the existing `brand_invitations` + `brand_team_members` tables (PR #59 schema + ORCH-0706 hardened). **Audit log shows your own actions only** today — RLS scopes SELECT to `user_id = auth.uid()`. Brand-admin-can-read-all-rows-for-brand RLS policy is queued for 13b/B-cycle (SPEC §10.4). **Audit log will fill as B-cycle wires server-side recording** — refund / role change / brand-edit / invite edge functions need to INSERT audit_log rows in same transaction. **F-1 scanner permissions shape mismatch** — mobile keeps 3-key shape (canScan/canManualCheckIn/canAcceptPayments); DB stays 2-key (scan/take_payments). 13a doesn't write to DB scanner tables; 13b decision per Q2 lock. **`permissions_override` jsonb editor** — empty {} default stays untouched in 13a; 13b ships per-member fine-grained override UI when operators have 3+ team members with edge cases. **PublicBrandPage "Verified host since YYYY" pill suppressed in 13a** — restoration is B-cycle task once `creator_accounts.created_at` is wired through React Query. **D-CYCLE13-IMPL-2/3 (P3 polish)** queued for Refinement Pass. **Launch posture:** Mingla Business operator product now has the full PRD §11.1 6-role enum + brand-level team management foundation + audit log viewer foundation + 7 critical action gates wired. **Cycle 13b is the next BIZ slot** (F-1 scanner permissions shape fix + permissions_override editor + per-event role assignments for non-scanner roles + audit_log brand-admin-can-read-all RLS policy expansion). **Most operator-trust-building moment:** the synthesis fallback CRITICALITY — without it, every existing solo operator would have lost access on the next deploy. The operator-device-smoke catch surfaced this before it could ship to real hands. The whole orchestration cycle (specer + implementor + 3 device-smoke loops + orchestrator REVIEW gates) earned its keep on this one cycle alone.

> **2026-05-03 (late evening) — ✅ Cycle 12 (BIZ Door Sales / In-Person Payments) CLOSE Grade A on operator device smoke.** Plain-English: organisers can now sell tickets at the door at a real event. They flip a per-event "In-person payments" toggle in Settings, set per-tier "Available at" (Online only / Door only / Online and at the door), and a new "Door Sales" tile appears on Event Detail. Tap it → scan-mode-style screen with a TESTING MODE banner up top (Card reader and NFC tap-to-pay are visible-but-disabled stubs until B-cycle Stripe Terminal SDK ships) + a reconciliation card showing live cash + manual + refunded + NET totals + a by-scanner expandable + Export CSV button. Tap "+" → multi-line cart sheet (one buyer can pay for friends), pick payment method (Cash + Manual enabled today), optional buyer name/email/phone/notes, hit Record sale → buyer is auto-checked-in (door sale fires `via: "manual"` scan record per ticket sold; activity feed picks them up as "Tunde checked in" entries). Refunds work too (per-line stepper + 10..200-char reason) — but with a hard architectural lock: refund is **money-only**, the buyer stays CHECKED IN regardless. They were physically at the door; financial event ≠ attendance event (OBS-1, mirrors I-19 spirit). Refund history surfaces in the door sale detail, in the J-G2 guest detail, AND in the Recent Activity feed as `Tunde — refunded £10.00 (door)` warm-orange. **What's now strong:** buyer-side honest UX — public event page now shows door-only tiers as info-only rows with a "Door only" pill + greyed-out "Pay at the door" button (no more silent disappearance in the cart). Operator-side rich tier chips — `formatTicketBadges` surfaces 6 categories (Door only / Online only / Sale window scheduled / Min N per buyer / Max N per buyer / Transfers disabled) on every tier card across wizard Step 5, Step 7 preview, EditPublishedScreen, PreviewEventView, and the public buyer page from a single helper edit. Vanilla tiers stay chip-free. **2 NEW invariants (I-29 + I-30)** enforce architectural boundaries: door sales NEVER as phantom OrderRecord rows (separate `useDoorSalesStore` + anon-tolerant buyer routes have 0 useDoorSalesStore imports); door-tier vs online-tier separation via `TicketStub.availableAt` enforced by 4 filter chains (J-C1 cart + J-D3 picker + AddCompGuestSheet comp + PublicEventPage Buy gate). **Most surprising during the cycle:** operator device smoke caught 2 P0 EditPublishedScreen bugs that headless tester missed (silent-failure in `editableDraftToPatch` — implementor wired the door-sales toggle into 3 of 4 spots in `liveEventAdapter.ts` but forgot the patch builder; crash in `formatValueForKey` on undefined values from pre-Cycle-10 events whose `privateGuestList` field was never migrated). Both fixed in rework v1 (commit `977d0ad1`) including a defense-in-depth migrate backfill that heals Cycle 10's schema-bump-without-migrate gap. Then a second smoke caught 3 more user-facing gaps (tier cards under-communicated config + public page advertised tiers buyers can't buy + activity feed missed door refunds) — all fixed in rework v2 (commit `8d457528`). **What's still fragile / forward-deferred:** **Card reader + NFC tap-to-pay payment options stay TRANSITIONAL stubs** until B-cycle wires Stripe Terminal SDK + platform NFC integration (Apple Tap to Pay on iPhone is US-only as of late 2025; UK rollout pending). **Door sales backend writes deferred** — `useDoorSalesStore` is client-side persisted only; B-cycle wires `record-door-sale` + `refund-door-sale` edge functions writing to the existing `door_sales_ledger` table (PR #59 schema + ORCH-0706 CHECK constraint already live). **Per-scanner identity resolution deferred** — every door sale's `recordedBy` is the operator's `auth.users.id` until B-cycle scanner-team functional flow ships (reconciliation by-scanner shows 1 row currently). **D-QA-CYCLE12-1 (P2 silent-failure in DoorRefundSheet null-fallback)** queued for B-cycle small ORCH — trigger is unreachable in client-only Cycle 12 (no path can make `recordRefund` return null in single-user mode) but worth fixing before backend race conditions land. **2 pre-existing tsc errors** flagged from main→Seth merge (events.tsx duplicate toastWrap + brandMapping Brand drift) — ride-along ORCH candidate. **Launch posture:** Mingla Business operator product now has the full PRD §7 in-person-payments surface + door-sales reconciliation + cross-cycle integration with Cycle 8 cart + Cycle 10 guest list + Cycle 11 scanner permissions. **Cycle 13 (Permissions UI per BUSINESS_PRD §14 sequence) is the next BIZ slot.** **Most operator-trust-building moment:** the OBS-1 hard lock — refund-doesn't-undo-attendance — was the right semantic call. Buyer was physically there; that's a separate event from "did they keep the money." Edge case "operator wants to undo BOTH money + check-in" deferred to a future cycle.

> **2026-05-03 (evening) — ✅ ORCH-0700 Phase 2 + Phase 3B CLOSE Grade A — admin Place Pool dashboard restored + 6 dead AI columns removed.** Plain-English: the admin Place Pool dashboard had 3 categories (Brunch+Lunch+Casual, Movies+Theatre, Groceries) showing 0 places for all of today after a morning matview rebuild — 6 hours of admin trust hit. Tonight's fix restored the real numbers (567/53/35 in Baltimore) by rewriting the SQL category-derivation helper to emit Mingla's canonical 10 user-facing slugs instead of 11 invented helper-only slugs that no consumer recognized. Same fix dropped 6 dead AI-categorization columns from `place_pool` (an entire decommissioned subsystem from before ORCH-0707) with a 30-day archive backup for safety. **What's now strong:** every place's category is derived at read-time from Google's raw type data — no more competing stored-interpretation columns, no more 3-way ambiguity (Constitution #2 restored). 4 legitimate parallel category taxonomies (seeding / bouncer / signal-scoring / display) are now mapped end-to-end with documented purpose for each. **Most surprising during the cycle:** the orchestrator (me) made a Phase 1 helper-taxonomy mistake that took 6 hours to surface because matview cron runs every 10 min and the dashboard cells just silently rendered 0 instead of erroring — caught only when operator did the visual smoke. New 3-gate I-CATEGORY-SLUG-CANONICAL invariant prevents recurrence (SQL probes + matview probe + TS unit test all enforce that any helper output must be in DISPLAY_TO_SLUG canonical set). **What's still fragile / forward-deferred:** the 7 standard orchestrator trackers got a fast-pass sync entry tonight but a deeper grade audit is appropriate next session; ORCH-0597/0598 display-name split (10 → 12 slugs eventually) remains incomplete in DISPLAY_TO_SLUG but unrelated to this fix; mingla-categorizer skill marked RETIRED (the system it served is gone — file a NEW ORCH if AI classification re-emerges); `useMapCards.ts:8-12` still has a stale 12-category list from before ORCH-0434 (separate cleanup queued, low priority). **Launch posture:** admin DB cleanup story is COMPLETE — `place_pool` is lean (only Google's raw type data + bouncer + scoring outputs as live signals), helper is canonical, regression gates wired. Next ORCH at operator's choice.

> **2026-05-03 — ✅ Cycle 11 (BIZ QR Scanner + Check-in + Scanner-Team UI + Per-Ticket QR) CLOSE Grade A pending operator device smoke.** Plain-English: organisers can now run the door at a real event. They tap "Scan tickets" on Event Detail → camera opens → buyer presents QR → success/duplicate/wrong-event/refunded feedback + buyer name + check icon. Or they can manually mark a guest checked in from the Guest Detail screen — one tap per ticket. Or they can invite door staff via the new "Scanners" tile on Event Detail (UI shipped now, emails ship in B-cycle). Buyers now get one QR per seat instead of one shared QR for the whole order — swipe between them in a horizontal carousel. **What's now strong:** entire 8-journey door experience surface ships (J-S1..J-S8); persisted scan ledger survives logout + cold-start; activity feed shows a green "Tunde checked in" entry for every successful scan; J-G1 row pill transitions cleanly grey → orange → green as multiple tickets get scanned. **Most surprising during the cycle:** tester caught a P0 React Rules of Hooks violation that would have crashed the Guest Detail screen on cold start (4 useMemos placed AFTER an early-return shell instead of before). Implementor's tsc-only verification gate didn't catch it. **D-T3 process improvement registered:** add `eslint-plugin-react-hooks` to implementor verification gate going forward. **What's still fragile / forward-deferred:** **the scanner cannot validate any production ticket today** (ORCH-0711). Reason: orders are persisted to the local device's Zustand store via the buyer's checkout. In production with two devices, the operator's store is empty for buyer orders → every scan returns "Ticket not found." Cycle 11 IMPL retry added an honest TESTING MODE banner above the camera viewport — copy: *"Testing mode — scanner only validates orders made on this device. Cross-device scanning lands when the backend ships in B-cycle."* Const #7 honored. B-cycle's `scan-ticket` edge function is what unlocks production scanning. **Other forward-deferred:** scanner-payments cluster (PRD §6.2 — door sales, NFC, card terminal) hard-line OUT to B-cycle; cross-device duplicate prevention deferred to B-cycle DB partial UNIQUE index (I-27 DB-enforcement); 7 P2/P3 polish items (weak email validation, type-leak on canAcceptPayments, camera-on-background battery drain, etc.) deferred to B-cycle polish wave; SC-1/2/3 device smoke (camera permission OS prompt, Settings deeplink, haptic feedback) covered by operator post-deploy smoke — low risk. **Launch posture:** Mingla Business operator-side door experience is now UI-complete + deployable for dev/staging single-device demos. B-cycle backend (already in PR #59 schema, just not yet wired functionally) flips this to production-ready cross-device. The TESTING MODE banner is the honesty bridge between "shipped" and "production-ready." **Forward queue:** B-cycle (scan-ticket edge function + invite-scanner edge function + accept-scanner-invitation route + auth gate at /scanner) closes the cross-device gap; Cycle 12+ per BUSINESS_PRD pipeline.

> **2026-05-03 — ✅ ORCH-0706 (PR #59 B1.5 backend hardening) CLOSE Grade A — Mingla Business backend foundations are LIVE on the production database for the first time.** Plain-English: the entire Mingla Business backend that's been sitting in the repo as PR #59 since 2026-05-02 finally hit the actual database. We discovered along the way that PR #59 had a function-ordering bug that would have caused it to fail apply on any clean DB (function `biz_is_brand_member_for_read_for_caller` defined at line 1718 but referenced at line 290 — PostgreSQL fails at statement 40). Multiple Copilot reviews + one independent forensics review missed it because none actually applied the migration. We fixed it locally on Seth, applied both PR #59 + ORCH-0706 hardening atomically via `supabase db push`, then ran 8 SC probes via Supabase Management API SQL — all 5 hardening fixes verified. **What's now strong:** brand slugs + event slugs + events.created_by are DB-enforced FROZEN (renaming throws exception); refunds + door_sales_ledger CHECK constraints reject typo values; audit_log + scan_events COMMENT ON TABLE honestly discloses service-role carve-out. **What's still fragile / forward-deferred:** main branch still has the PR #59 ordering bug (Seth has the local fix; recommend separate PR-back-to-main); pre-flight verification gap exposed (independent forensics review claimed HIGH confidence without `supabase db reset` against test DB — process gap). **Most surprising:** the migration history table on production had 18 stale entries that didn't match schema state (15 ledger-only orphans + 3 with local files that were unintentionally re-marked reverted by orchestrator before correction). Cleaned via `supabase migration repair`. Root cause unknown but most likely a DB restore that preserved migration history. **Launch posture:** Mingla Business backend is now production-ready at the schema level — 19 tables with full RLS + immutability triggers + CHECK constraints + Option B audit-log carve-out. Cycle 11 (QR scanner) is next on the queue and unblocks D-9c-V3-3 (ticket scans in activity feed) + Cycle 10 check-in column live data.

> **2026-05-02 — ✅ Cycle 10 (Mingla Business Guest List) CLOSE Grade A.** Plain-English: organisers can now see who's coming to their event. Tap "Guests" on Event Detail and you get a real screen — no more toast stub. List shows everyone (paid orders + comps), search by name/email/phone, tap any row for a detail view with their contact + ticket info + (for paid orders) cross-event purchase history scoped to your brand. Tap "+" to add a comp guest manually with a required audit reason — appears at top of the list with a "COMP" pill, and the action gets logged to the event's Recent Activity feed. EditPublishedScreen now has a "Private guest list" toggle (hides attendee count from buyers — no buyer surface honors it yet, but the flag is operator-controlled and will bind when the buyer-side guest preview ships). Tap the download icon to export the full list to CSV (web: file downloads; native: Share sheet with CSV text). **What's now strong:** entire PRD §5.3 surface that's buildable without backend ships — list / detail / search / manual-add comp / private toggle / export. Buyer-as-attendee model degrades cleanly when B-cycle wires per-attendee identity. Activity-feed integration selective per locked decision (manual-add + private-toggle log; search + export don't). I-25 + I-26 invariants enforce the architectural boundaries (no phantom comp orders; no buyer surfaces in Cycle 10). **What's still fragile / forward-deferred:** approve/reject pending guests stays gated on B4 (Stripe payment-hold backend); manual check-in stays gated on Cycle 11 (QR scanner — Cycle 10 renders a "NOT CHECKED IN" placeholder pill from day 1 so Cycle 11 just plumbs data); native CSV currently shares as text (Share sheet) not a file artifact — discovery D-CYCLE10-IMPL-1 recommends future install of expo-sharing + expo-file-system to enable file-share UX (~30min OTA-eligible dispatch). **Launch posture:** Mingla Business operator product now has 6 of 10 PRD §5.3 features shipped + the placeholder column for the other 2 Cycle-11-gated. Approval flow remains the only B-cycle-gated functional gap. **Most surprising:** PR #59 schema (`tickets.attendee_name/email/phone` + `tickets.approval_status` state machine + `scan_events` table) is FAR more complete than the consumer code uses — consumer-side built ahead of backend across Cycles 1-9c. B-cycle is the natural cutover. Buyer-as-attendee Cycle 10 model degrades cleanly to per-ticket-attendee when the backend lands. **Forward queue:** Cycle 11 (QR scanner) unlocks 3 things in one go (Cycle 10 check-in column live data + D-9c-V3-3 ticket scans in activity feed + manual check-in PRD §5.3 feature). After Cycle 11: Cycle 12+ per BUSINESS_PRD pipeline.

> **2026-05-02 — ORCH-0704 v2 CLOSE (Mingla Business).** Plain-English: organisers can now edit any field on a published event — name, date, address, ticket prices, tier capacity, settings — through a sectioned screen that slides up from the manage menu. Existing buyers stay protected: if you try to delete a tier that has sales, lower the capacity below tickets sold, or change the price for a sold tier, the screen says "Refund first" and gives you a button to open Orders (ledger lands Cycle 9c — for now it shows a placeholder toast). Switching from "single date" to "recurring weekly" works freely — but if the change drops a date someone bought a ticket for, you get the same "Refund first" gate. Every save asks for a reason (10–200 chars; "Venue change due to weather" is the canonical example) which gets logged permanently and flows into the email/SMS/banner notification copy. Email + SMS are stubs that log to console for now; B-cycle wires real Resend + Twilio. **What's now strong:** any LiveEvent field is editable post-publish; buyer-protection guard rails have full coverage (8 reject reasons each mapped to refund-first dialog with "Open Orders" CTA); reason audit trail is permanent (Zustand persist, append-only, logout-cleared); notification stack architecture is in place — only the wires to real Resend/Twilio + buyer order detail surface are missing (all explicitly marked TRANSITIONAL with EXIT CONDITIONs). The narrow Cycle 9b-2 (description + cover hue only) is fully gone — `liveEventToEditPatch.ts` deleted, `updateLiveEventEditableFields` removed, the narrow EditPublishedScreen replaced with sectioned accordion reusing existing wizard step bodies. **What's still fragile / forward-deferred:** in stub mode, sold-count is always 0 — operator can edit anything freely until Cycle 9c builds `useOrderStore`; "Open Orders" reject CTA shows toast "lands Cycle 9c" until the route exists; SMS stub fires only when `webPurchasePresent=true` which is hardcoded false until Cycle 9c populates from useOrderStore; buyer order detail page (where the material-change banner renders) is forward-deferred to Cycle 9c. **Launch posture:** Mingla Business operator product is now feature-complete for edit-after-publish — Cycle 9c (Orders ledger + buyer order detail) closes the loop on buyer-side rendering, then B-cycle wires real notification delivery. ORCH-0705 (no-show + post-event ops + dispute paths) registered as deferred until post-Stripe integration. **Most surprising:** operator overrode all six v1 spec defaults (silent-push for cosmetic, lock-destructive for sales) in favor of the more aggressive industry-standard pattern (notification stack everywhere · whenMode unlocked · refund-first as unified destructive pattern · reason on every change). v2 took ~+1180 LOC net to ship — substantial but the architecture is decoupled (step bodies have zero coupling to draftEventStore, verified by investigation OBS-704-1) so each phase compiled cleanly. New invariants ratified: I-19 (immutable order financials) + I-20 (edit reason mandatory + audit log permanence). DEC-087 logged.

> **2026-04-29 — ORCH-0696 CLOSE.** Plain-English: the popup that opens when you tap any card in the app (across 8 different places) used to be a centered floating white card. Now it's a bottom sheet that slides up — same pattern as Apple Maps, Spotify, Uber. Two themes per render branch: tapping a Ticketmaster event opens a dark sheet with the new event-specific layout (poster hero, artist name in orange, "Get Tickets" as the dominant action, save/share/add-to-calendar as smaller chips); tapping any other card (restaurant, curated experience, Stroll, Picnic) opens a white sheet with the same content layout you've always had — just bottom-sheet presentation. Drag handle at top works on both, drag-down dismisses, scrim tap dismisses. The chat-shared "save card from a friend" flow no longer needs the workaround Modal hack from ORCH-0685 cycle-3 — toasts now appear above the sheet naturally. **What's now strong:** ExpandedCardModal F→A across all 8 mount surfaces (chat / Discover / Saved / Calendar / friend profile / collab session / solo deck / collab deck); event experience is now genuinely event-shaped (artist + show time + tickets first, not jammed into a place template); place experience preserved exactly like before but with modern bottom-sheet presentation; chat-shared Save flow verified end-to-end on iOS + Android (toast renders above sheet via React-tree z-ordering, no Modal-portal hack needed); Constitution #7 (label temporary fixes) STRONGLY UPHELD — Shape 2a's exit condition met and the transitional Modal hack was deleted as originally designed. **What's still fragile:** ViewFriendProfileScreen still hardcodes USD currency + `isSaved={false}` (open as ORCH-0697); the same 4 sub-component design lessons surfaced during operator field-test (D-IMPL-6/7/8 from earlier IMPL report) remain candidates for follow-up if any feel off in production use; the `glass.surfaceDark` token is now orphan post-dual-theme (could delete in future cleanup, low priority). **Launch posture:** unchanged-strong. ORCH-0696 was S2 design-quality + cross-surface UX consistency, not a launch blocker. **Most surprising win:** the dual-theme architecture (events dark / places light, conditional on render branch) was NOT in the original spec — operator caught the "all dark" assumption at first device smoke and the conditional approach emerged from that course-correction in 4 hotfixes. Three cycles in a row (ORCH-0670 / ORCH-0698 / ORCH-0696) confirm the same lesson: design-spec architectural assumptions need operator confirmation BEFORE bulk-IMPL begins. D-PROC-3 process invariant candidate registered.

> **2026-04-29 — ORCH-0670 SLICE A + ORCH-0698 BUNDLED CLOSE.** Plain-English: the Concerts surface (now renamed "Events") had five always-broken defects every user hit regardless of language or platform — error/empty/no-match screens showed raw code strings instead of words, the price-tier chips (Chill/Comfy/Bougie/Lavish) tapped but didn't actually filter, the top-bar chip labels and screen title were hardcoded English so non-English users always saw English, and the screen title's autoshrink prop made the Android header visibly smaller than iOS and smaller than the Friends-screen header. All five fixed in one bundled commit: 7 missing i18n keys added with native translations across all 29 languages (280 translations), filter chips now actually contract the grid via canonical `TIER_BY_SLUG` lookup (5 dead switch cases deleted), all 6 chip labels + title now translate, title renamed to "Events" per operator brand-language preference, autoshrink removed and aligned to Friends-screen baseline. Bonus cleanup: 14 orphan i18n keys deleted (392 dead translations across locales). On top of that, the Map view (which was already orphaned — no nav tab, no JSX render, no callers) got its entire 17-file subtree deleted along with `useMapSettings.ts` and 3 native dependencies (`react-native-maps`, `@maplibre/maplibre-react-native`, `react-native-map-clustering`); 15 npm packages removed total. Net: -2,068 LOC across both fixes. **What's now strong:** Discover surface F→A (no dead taps, real copy in all states, locale parity, cross-platform header consistency); native bundle smaller (3 heavy deps removed); EAS pipeline simpler (`react-native-maps` was the web-bundle blocker preventing `--platform all`). **What's still fragile:** `ViewFriendProfileScreen.tsx` hardcodes USD currency and passes `isSaved={false}` always (D-OBS-1 + D-OBS-2 from forensics audit — registered as ORCH-0697 + Slice B currency cleanup); CardInfoSection composition unverified for whether 5 imported sub-components actually render (D-OBS-3 — spec-phase question for ORCH-0696). **Launch posture:** unchanged-strong. ORCH-0670 was S1 (always-broken UX visible to every user worldwide, regardless of locale). ORCH-0698 was S2 tech debt + native bundle bloat. Both closed in single OTA. **Most surprising win:** ORCH-0698 was a render-first verification catch by operator — designer + forensics both propagated "MapBottomSheet/PersonBottomSheet are live" without grep-verifying. Operator caught it. D-PROC-2 codified — every architectural claim about existing code MUST be JSX-render-grep-verified before forensics or designer treats it as live.

> **2026-04-27 — ORCH-0690 CLOSED — Schedule date-picker now respects user intent on both iPhone and Android, across every place it shows up.** Plain-English: tapping Schedule on any card used to hijack the user — on iPhone, spinning the date wheel one notch silently committed the date and jumped to the time wheel before they could finish picking. On Android, pressing OK on the calendar dialog immediately opened the time clock with no preview. Once in time-mode, no way back without losing all progress. Plus four quieter problems in the same code: past times accepted, places with unknown hours silently scheduled, Schedule button could lock greyed-out after errors, and iOS/Android scheduling logic was copy-pasted twice and slowly drifting. Today's fix: deleted the iPhone auto-flip line, added an Android preview dialog ("You picked Friday, November 7. Continue to time?" with [Change Date]/[Pick Time]), added a "← Back to date" button on iPhone time-mode, blocked past-time scheduling, and made unknown-hours places ask the user before committing. All eight ExpandedCardModal mount surfaces inherit the fix via the shared `ActionButtons` component — chat-shared cards, Discover deck, Saved tab, Calendar, paired person view, collab session, plus solo + collab swipeable decks. 9 new alert messages translated into all 29 supported languages = 261 translations. **Cycle-2 fix landed in same commit:** operator field-tested cycle-1 and caught the iPhone Modal header overflowing with the new third button — surgical CSS fix (gap + padding tightened, title flex-shrinks, ellipsis truncation) added to the same commit before any user saw it. Total cycle-1 + cycle-2 wall time: well under 3 hours from intake to operator device PASS. **What's now strong:** the Schedule flow is structurally correct end-to-end (state-machine cause addressed at source, not symptom-masked), Constitution #1 (no dead taps) and #3/#9/#12 all RESTORED, blast radius understood (8 surfaces inherit), and the shared `confirmAndSchedule` helper kills ~46 lines of copy-paste drift between iOS and Android branches. **What's still fragile:** none from this fix — operator device PASS confirmed all 11 cycle-1 visual SCs + 2 cycle-2 V-checks. The pre-existing `fetch_local_signal_ranked` CI invariant gate (ORCH-0691, S3) still false-fails on every branch protection run; that's orthogonal tech debt, not ORCH-0690. **Launch posture:** unchanged-strong. ORCH-0690 was an S2 cosmetic-spirit fix (UX correctness), not a launch blocker; closing it removes a real frustration on a high-frequency interaction. Combined with ORCH-0694 closing earlier today (deck ghost-card after swipe-left), two more S2/S1 grade promotions this session.

> **2026-04-26 — ORCH-0684 CLOSED — paired-person view is now actually showing real cards instead of placeholders, for every paired user.** Plain-English: when a Mingla user opened a friend's profile any time in the last ~3 days, the birthday hero + custom holidays + upcoming holidays sections all rendered card-shaped placeholders with "Unknown" titles, missing photos, default "Chill" prices, and zero combos. The data was actually flowing from the database correctly — but the translator that turns database rows into mobile cards was forked from a deleted table (`card_pool`) and never rewired when ORCH-0640 repointed the source to `place_pool` ~3 days ago. Net effect for users: the most personal social/relationship surface in the app served meaningless content. ORCH-0684 rewrote the translator to read the actual database fields, brought back curated combos with a per-holiday rule (birthday gets a celebration combo + 5-8 singles; Valentine's gets a romantic combo + 4-6 singles; etc.), and pushed joint-pair history into the database ranking — so a place either you OR your friend has saved bubbles up, and a place you've BOTH saved bubbles up the most. Plus 3 structural CI gates that prevent the same bug class from ever shipping again. **Live-fire happened in this orchestrator session:** founder applied migration via `supabase db push` (one DDL hiccup needed `DROP IF EXISTS` since the function gained 3 return columns vs ORCH-0668; safe inside the transaction), founder pushed the edge fn deploy → v92 ACTIVE, perf budget passed at 168ms warm vs 500ms target. Tester independently INSERTed 3 saved cards for a test viewer, watched all 3 bubble straight to ranks 1/2/3 of the deck, then rolled back — proving personalization is no longer fake. Backend went live BEFORE the mobile commit shipped — so users on the OLD app build are already seeing the fix on the next view (the edge fn does all the substantive work). Mobile commit + EAS Updates iOS + Android ship the type-widening + cleanup + null-priceTier UI guard. **What's now strong:** the paired-person view is structurally correct end-to-end (RPC + edge fn + mobile mapper + UI), joint-pair personalization is observable via `boost_reasons` telemetry array, the auto-bilateral mode kicks in automatically when both users have ≥10 confident preference rows (currently nobody meets that threshold; will populate as users use the app), and 3 new CI gates structurally prevent the ghost-field-reads class of regression. **What's still fragile (operator-side environmental gates only):** the combo branch end-to-end (T-A4) needs a real-client curl with JWT to confirm `generate-curated-experiences` returns viable combos for the holiday composition's anchor signals; if no combos, the response carries `summary.emptyReason: "no_viable_combo"` which is acceptable degraded behavior per spec. Real-device check on iPhone or Android (T-C1) confirms the EAS Update propagated and the new CompactCard hides the price line when `priceTier === null`. Discover regression spot-check (T-C10/T-C11) confirms the singles deck and curated deck are unchanged (different RPCs/edge fns, byte-unchanged in this dispatch — VERY HIGH static-confidence). **Launch posture:** materially better. Paired-person view is a primary social surface; before today it shipped placeholder cards; now it ships real-content cards with joint-pair-aware ranking. Combined with ORCH-0686 closing earlier today (photo-backfill write surface), two F→A grade promotions this session.

> **2026-04-26 — ORCH-0686 CLOSED — Lagos photo backfill is actually downloading photos for real, right now.** Plain-English: yesterday's ORCH-0678 fix to the photo-pipeline deadlock had a hidden hole — the database's allowed-labels list (`CHECK` constraint on `photo_backfill_runs.mode`) was never updated to match the new TypeScript enum value `pre_photo_passed`. Result: every "create photo run" click in the admin returned generic toast "Failed to create run" with no diagnostic, and 14,401 pre-bouncer-approved places sat stranded for ~24 hours waiting to download Google photos. ORCH-0686 amended the constraint to permit the new value, kept legacy `initial` valid for the 18 historical rows, flipped the default, and shipped a small admin helper that unwraps real Postgres error bodies into the toast description so the next bug like this is self-diagnosing. Most importantly: a structural CI gate (`I-DB-ENUM-CODE-PARITY`) now prevents this exact rename-without-SQL-update class of failure from ever shipping again — the same class as ORCH-0540's PL/pgSQL drift; two occurrences was enough. **Live-fire happened in this orchestrator session:** founder applied migration via `supabase db push`, founder clicked "Create photo download run" on Lagos in the admin, real run row appeared with `mode='pre_photo_passed'`, 20 places downloaded photos in the first 2 batches, zero failures, run still going as of CLOSE. **What's now strong:** the photo-backfill write path is structurally permanent (gate prevents recurrence), Lagos's 1,039 pre-photo survivors are draining for real photos at $0.035 each (matches ORCH-0678 cost projection), the admin error toast experience improves silently for every future failure across both Place Pool Photos tab and Signal Library one-shot, and the operational recovery for the 8 still-stuck cities (Paris/NYC/Berlin/Barcelona/Toronto/Chicago/Dallas/Miami totalling ~38k rows) is now executable on demand. **What's still fragile (paperwork only):** SC-9 — the helper's actual unwrap behavior on a forced 500 — is unverified. ~30-second test in admin browser at operator's convenience: dev-console-invoke with garbage mode, confirm toast description contains "violates check constraint" rather than the generic SDK message. Non-blocking; CONDITIONAL Grade A converts to unconditional Grade A on that single observation. **Launch posture:** materially better. Photo pipeline is the second-pass quality filter that controls launch-city deck inventory; before today it was structurally deadlocked, now it's structurally permanent. The 1,039 = 1,039 forensics-projection match (third occurrence in this program — Bouncer projection in ORCH-0678, then in tester verification, now in operational recovery) reinforces that the pipeline math is reality-anchored, not aspirational.

> **2026-04-26 — ORCH-0671 + ORCH-0678 BUNDLED CLOSE — Lagos can finally serve real inventory; admin Photo Pool ghost page is gone.** Plain-English: the deadlock that made Lagos serve zero places is fixed by design (not patched), and the long-promised deletion of the bouncer-blind Photo Pool admin page (which read pre-bouncer counts and confused operators about real photo coverage) has shipped. The Bouncer pipeline is now a clean two-pass — pre-photo check first (weeds out places lacking websites/hours/types/google photos), photo download in the middle (only for survivors — 75% Google API savings vs the naive flow), final post-photo check last (catches download failures). All three steps run from a single 3-button admin panel that replaces the old single Bouncer button. **What's now strong:** the empirical Lagos test produced **EXACTLY 1039** pre-photo survivors — matching the forensics-projected 1039 with byte-for-byte rejection-reason atomic counts. This is the highest-confidence forensics→spec→impl chain Mingla has shipped. Single-writer column ownership is structurally enforced by 3 new CI-gated invariants (any drift fails CI + names the file path). Migration auto-promoted 13,362 healthy-city rows so Raleigh/London/Washington/Brussels/Baltimore/Cary/Durham/Fort Lauderdale don't need re-bouncing. Photo Pool admin surface cleanly removed: 5 RPCs DROPped, 1 view DROPped, 4 historical photo_backfill runs archived with service-role-only RLS, CHECK constraint shrunk to a single allowed value. **What's still fragile (paperwork only):** ~5min admin browser smoke + ~50min Lagos Step-2/Step-3 completion (= ORCH-0682 Lagos-portion operational recovery). On those: both ORCHs flip from CONDITIONAL → unconditional Grade A. Zero P0/P1/P2 findings; 2 P3 deferred. **Launch posture:** materially better. Photo-pipeline deadlock that would have re-fired on every future city seed is dissolved. The international-markets policy decision (ORCH-0681) is now empirically grounded — Lagos's actual 24.6% pre-photo pass rate (vs US/EU baselines of 40-62%) tells us the cluster-A website rule structurally rejects ~68% of Nigerian SMEs. User can now pick A/B/C policy with data instead of projection. 8 stuck cities (Paris/NYC/Berlin/Barcelona/Toronto/Chicago/Dallas/Miami totalling ~38k rows) await operator-paced recovery via the new admin three-button flow at the operator's convenience. **Cost win locked in:** Lagos $148→$36 (75% savings) per the spec's design; cross-city ~$1,500 → ~$400-600 saved if/when operator recovers all 9 cities.

> **2026-04-25 — ORCH-0669 cycle-2 CLOSED — home + chat chrome looks like glass instead of white seams.** Plain-English: the white-line edges around the home header pills, preferences chip, notification bell, bottom nav, "+" create pill, and chat input capsule were too bright (12% white alpha) — they read as visible seams against the dark frosted-glass backdrop on every render. Lowered to 6% (50% reduction). Same token, single value change, all 7 surfaces consume by reference so no per-component edits. Chat input separators (the 1px dividers between attach + text + send sections) become near-invisible at 6% — explicitly accepted per founder's Option A 2026-04-25 (the capsule still reads as clearly bounded sections via spacing and button positioning). Dead `topHighlight` token from the deleted V5 layer also removed. **What's now strong:** Home + chat chrome reads as premium glass with sub-perceptible edges; new structural invariant `I-CHROME-HAIRLINE-SUB-PERCEPTIBLE` + CI gate prevents the regression class going forward (any future contributor inlining `borderColor: 'rgba(255, 255, 255, 0.X)'` ≥ 9% on a chrome consumer file fails CI). **What's still fragile:** founder hasn't visually confirmed yet (T-04..T-09 home + T-12.1..T-12.3 chat) — non-blocking but recommended within 24h to upgrade CONDITIONAL → unconditional Grade A. If 0.06 reads as "still a seam" or "too floating", spec §14 rollback path is a single-line revert to 0.08 (no structural undo). **Launch posture:** still pre-launch. Zero new S0/S1 issues. ORCH-0669 was the last design-debt polish dispatchable without founder steering — Wave 5 remaining items (ORCH-0671 admin Photos / ORCH-0670 concerts-events / ORCH-0661 pending pills smoke) all require either founder decision or device smoke.

> **2026-04-25 — Wave 4 CLOSE Grade A: chat-domain bundle fully verified.** Plain-English: 4 user-facing fixes verified and on production for an hour. (1) Friends' incoming DMs arrive in real time again — no more silent drops requiring close+reopen. (2) Friends-modal "Add to Session" + DM "Add to Board" both invite for real — no more fake "Sent!" toasts that did nothing. (3) DM "Share Saved Card" sends a real card bubble that the recipient can tap to expand — replaced 5-month-old toast-only stub. (4) Deck cards show real distance ("0.4 mi") and travel-time ("8 min walk") with mode-matching icons — no more fake "Nearby" labels. **What's now strong:** chat domain end-to-end production-grade (DM realtime + saved card share + add-to-session + paired-profile recs from ORCH-0668 yesterday-close). 4 new structural invariants prevent the regression class for each. **What's still fragile:** founder Phase 4 device live-fire (~25 min, 2-3 devices) is the only outstanding evidence — non-blocking but recommended within 24h to upgrade CONDITIONAL → unconditional Grade A. **Launch posture:** still pre-launch. Zero S0/S1 blockers in the chat-domain bundle post-Wave-4. Wave 5 parked items (home-chrome hairline / photos admin / concerts-events / pending pills) are S2/S3 polish. Parallel chat's ORCH-0677 picnic-dates regression is the only S1 currently in flight — separate investigation.

> **2026-04-25 — ORCH-0668 CLOSED Grade A — paired-profile recommendations work again for every paired user.** Plain-English: the recommendation rows under every paired friend's profile (birthday hero, "Your Special Days" anniversaries, "Upcoming Holidays") were all silently broken — every user saw "Couldn't load recommendations" because the database query was timing out at 25 seconds. The fix rewrote that query so it now returns in ~120 milliseconds (220× faster). Tester independently verified across 5 runs at the worst-case 11-signal Raleigh workload — p95 ~135 ms vs the 2000 ms budget = 14× under. Edge function logs confirm production users are already successfully loading recommendation cards at sub-150 ms warm. **What's now strong:** paired-profile experience is production-grade end-to-end; the structural rule (`I-RPC-LANGUAGE-SQL-FOR-HOT-PATH`) that prevents this exact bug class is registered + CI-gated; CI gate negative-control proven (inject synthetic plpgsql migration → fires + names violator). **What's still fragile:** 4 chat-domain ORCHs (0664 DM realtime / 0666 add friend / 0667 share saved card / 0659-0660 deck distance) are IMPL-COMPLETE on production OTAs but await Wave 4 tester verification — all 4 tester prompts pre-written and parallelizable. **Launch posture:** still pre-launch. ORCH-0668 was the only S0 in flight; closing it leaves zero S0s in the chat-domain bundle. T-18 device smoke (~5 min founder action) is recommended but not blocking. 

> **2026-04-25 — ORCH-0672 CLOSED.** Plain-English: the dev build was bricked for ~hours today — every cold start crashed at module load because UI code was reading design tokens that never made it into the codebase. Hotfix landed in ~25 min (INTAKE → CLOSE) via single-file pathspec commit `d566dab7`. App boots again. **Strong/fragile unchanged structurally** — Home pill bar already graded A under ORCH-0589/0661; this restored that prior state, not a new gain. **One process gain:** new invariant `I-COUPLED-DIFF-NEVER-PARTIAL-COMMIT` makes the regression class that caused this S0 structurally preventable in future. **Launch posture:** still pre-launch. Build no longer bricked. 4 mobile-commit waves now queued (ORCH-0664 / 0666 / 0667 / 0659-0660) plus 1 backend-only deploy (ORCH-0668). All 5 are code-complete; founder-driven deploy + commit + OTA + tester dispatch sequence ready.

> **2026-04-23 late-night — ORCH-0641 FORMAL CLOSE.** Tester returned CONDITIONAL PASS. Evidence chain complete. The deck works again for 7 previously-dead chip types — users selecting Brunch, Drinks, Fine Dining, Casual, Icebreakers, Movies, or Play now see real cards instead of empty decks. **Nothing else strong/fragile has moved since the informal closure earlier tonight.** One clarifying note for the snapshot: curated cards are more fragile than previously thought — `filterCuratedByStopHours` in `discover-cards` was assumed dead code but is actually called on every request with a schema bug that makes it a silent no-op ("assume open" default). Curated cards can currently include stops closed at the user's scheduled hour. This was already scoped under ORCH-0644; D-1 reclassification merges ORCH-0645 into 0644 rather than dispatching separately. **Launch blockers unchanged:** none directly from this closure, but ORCH-0643 (picnic-dates empty because groceries signal never scored) is next up and cheap to ship.

> **2026-04-23 night — ORCH-0640 (Great Demolition and Rebuild) CLOSED Grade A. ORCH-0641 (filter schema fix, S0) CLOSED Grade A.** User device smoke 5/5 PASS. **What this means for the product:** the app now has ONE card-generation system instead of three parallel systems that drifted against each other. Every card the user sees — whether a single place, a curated date-night experience, or a saved card — comes from the same `place_pool` table through the same 3-gate quality filter (is_servable + signal score + real photo). This closes ~2 years of architectural debt where `saves`/`experiences` (2025-era) + `card_pool`/AI-validation (early 2026) + `place_pool` (late 2026) all coexisted. **What's now strong:** one owner per truth (Constitutional #2 fully satisfied on serving surface), 7 new invariants CI-enforced via `scripts/ci-check-invariants.sh`, 7 of 10 chips re-serving real cards after 8-day silent regression (ORCH-0641 fix), idempotent save path via verified upsert + unique index. **What's still fragile:** ORCH-0644 (curated cards don't filter by opening hours — can recommend a dinner spot that closed at 3 PM) unblocked and ready for dispatch; 11 other deferred follow-ups queued (mostly admin cleanup + docs). **Rollback safety net intact** — `_archive_card_pool` + `_archive_card_pool_stops` live until 2026-04-30 (7-day soak). **Launch blockers:** none from this closure. No migration pending. OTA already live on both platforms.

> **2026-04-22 — Build-readiness snapshot:** ORCH-0636 (TS Build Hygiene) closed Grade A code-level — `tsc --noEmit` now clean for the first time in the Seth branch. ORCH-0637 (Empty State Redesign) closed Grade A code-level pending device verification. ORCH-0598.11 (Launch-City Pipeline) closed code-level for the SINGLE-card two-gate + RLS hardening, but has a LIVE curated-card serving bug (tracked as **ORCH-0639, deferred to separate chat, Const #13 violation, P1, pool-only enforcement still holds**). **Current posture: OTA-safe to ship ORCH-0636 now. Binary build should wait for ORCH-0639 to land.** "What's strong": TS build clean, Seth-branch empty states coherent with ORCH-0626 dark canvas tokens, single-card two-gate preventing Cary/Durham leaks into Raleigh. "What's fragile": curated-card pool serving currently returns 0 in production (auto-regenerates via pool-only path, but at 1-3s latency vs <500ms cache hit) — ORCH-0639 owns the fix. See WORLD_MAP.md for full detail.

> **2026-04-21 ~23:30 UTC — ORCH-0635 (Coach Mark Refresh) CLOSED Grade A.** Device-verified PASS after iterative cutout tuning. Commit `f284215d`. Tour rewritten from 10 steps to 9 to match post-glass-refresh app layout. 3 orphaned steps (old 2, 5, 10) fixed or dropped, 1 duplicate cutout merged (old 1+3), step 6 retargeted from FilterChip to header panel with centered bubble, NEW step 3 added for Likes tab (closes core-loop save-destination gap), NEW step 9 added targeting BetaFeedbackButton. New `bubblePosition?: 'auto' | 'center'` field on CoachStep. Legacy `coach_mark_step` values normalized to TOUR_COMPLETED on first post-deploy fetch (idempotent). New `__DEV__` warning in `useCoachMark` fires within 500ms if a step's targetRef never attaches — catches future refactor orphans. 14 files, +286/-151. Pure presentation + state-machine. OTA-safe for both platforms. Non-beta feedback universalization flagged as candidate follow-up (BetaFeedbackButton gate).

> **2026-04-21 ~22:00 UTC — ORCH-0627.1 (Profile Core Glass Refresh, Phase 1) CLOSED Grade A.** Device-verified PASS. Commit `e3dfb380`. Converts the last white-on-light screen to warm-charcoal glassmorphic bento. 5 floating cards (Hero elevated + Interests + Stats + Account + Footer) on `rgba(20,17,19,1)` canvas with orange radial glow breathing 0.85↔1.0 over 8s (static with reduce-motion). New `<GlassCard>` primitive (base + elevated) handles iOS BlurView + Android API<31 fallback + Reduce Transparency fallback. New `glass.profile.*` token namespace (~260 tokens: canvas, hero gradient + glow, card base/elevated, 14 text roles, avatar + dual ring, chips intent/category, stat tiles, level ring, tier badge, settings row, sign-out, sheet, motion). Stats card restructured from flat 3+3 to level-ring hero + 2×2 bento grid. Count-up short-circuits on zero. Phase 2 (ORCH-0627.2: sheets + ViewFriendProfileScreen + PairedProfileSection) QUEUED. 9 files, +1396/-452. Pure presentation + token layer. OTA-safe both platforms. Discoveries: placesVisited/streakDays still hardcoded to 0 (ORCH-0629/0630 candidates); BetaFeedbackButton still light-themed (ORCH-0634); AccountSettings nested screens still light (ORCH-0628).

> **2026-04-21 ~21:15 UTC — ORCH-0626 CLOSED Grade A.** Empty-state text legibility restored on dark-canvas screens — a launch-quality concern (users hitting Connections with no friends, DM with no messages, or the deck in error mode previously saw effectively invisible text). Now matches the canonical post-glass-refresh contract. Strengthens the app's "Premium" feel on first-run and error paths. Does not shift any launch blockers. **Next queued: ORCH-0627 (Profile page glass refresh)** — closes the last white-on-light holdout, bringing the full mobile surface into the dark-glass design language.

> **2026-04-21 ~08:30 UTC — ORCH-0595 Slice 3 (Upscale Brunch) CLOSED Grade A.** 3 signals shipped + tuned on family/friends device (fine_dining + drinks + brunch). All calibrated via visible device feedback loop (Slice 2 did 2 cycles, Slice 3 did 3). **What's Strong:** signal-library architecture + per-slice calibration workflow is proven extensible — 3rd signal cycle took ~3 hours end-to-end including calibration. **What's Fragile:** Google types data-quality still has edge cases (Oak/Sitti mislabeled wine_bar for drinks, bar_and_grill chains surface where you don't want them for brunch). 14 signals remaining. Launch blockers: unchanged (none).

> **2026-04-21 ~07:45 UTC — ORCH-0590 Slice 2 (Drinks signal) CLOSED Grade A.** Signal-library architecture now ships TWO signals end-to-end (Fine Dining + Drinks) with generalized cohort branch + auto-listing admin + retroactive chip rename + one-shot DB UPDATE replacing irrelevant AsyncStorage migration mandate. Family/friends device serving real drinks-ranked cards (cocktail bars / wine bars / breweries above restaurant-with-bars). Fine Dining chip rename visible on user device post-OTA. **What's Strong:** unified signal model proven extensible (Slice 3 adding Romantic = one SQL INSERT + one map entry + one mobile chip edit). **What's Fragile:** Google types data-quality edge cases (Oak Steakhouse + Sitti labeled wine_bar when they're restaurants) — data-cleaning work, not scoring flaw. Launch blockers: unchanged (none). Constitutional 14/14 preserved.

> **2026-04-21 03:45 UTC — ORCH-0558 CLOSED Grade A — device verified on iOS + Android.** Collab match promotion is now production-grade reliable. 4 ORCH-IDs bundled-closed: 0558, 0532, 0556, 0557, 0534 (ORCH-0534 historical rows backfilled). Collaboration Sessions surface moves 1 A → gains 1 more A (match-promotion path now structurally proven); 0 C/D/F regressions introduced. New invariants registered: I-MATCH-PROMOTION-DETERMINISTIC, I-BOARD-SAVED-CARDS-EXPERIENCE-ID-NOT-NULL, I-CHECK-FOR-MATCH-COLUMN-ALIGNED, I-MATCH-NOTIFICATION-FAILS-OPEN, I-REALTIME-COLD-FETCH-PARITY, I-COLLAB-MATCH-OBSERVABLE. Constitutional #2 (one-owner-of-truth) + #8 (subtract-before-adding) IMPROVED. Launch blocker on collab match reliability — cleared.

> Last updated: 2026-04-18 (**ORCH-0503 v3 CLOSED A** — user device retest 6/6 PASS; mixed-deck interleave now correct; commit + two-platform OTA pending user action; Phase 2.4 dispatch next)
> Generated by: Orchestrator Post-PASS Protocol

## Operational Alert (2026-04-18 — ORCH-0503 v3 SHIPPED on device, OTA pending)

**Mixed-deck interleave bug is dead. Three fix cycles, one root cause.**

- **What changed:** Provider sync-effect's 3-way "growing cardinality" branch at `RecommendationsContext.tsx:1127-1141` collapsed into a single `const merged = deckCards` adoption line (new line 1118). The prev-preserving `[...prev, ...toAppend]` fallback was destroying React Query's authoritative interleave whenever observer batching collapsed partial-2 + queryFn-resolve into one sync-effect fire (always, under React 18).
- **Why the earlier two fixes failed:** AH-139's spec assumed 3 sync-effect fires (partial-1, partial-2, final-resolve as separate notifications). Device DIAG logs during AH-143 captured the actual 2-fire sequence. The growing branch fired on the collapsed Fire 2 with `deckCards` = interleaved 68 cards and `prev` = 50 singles from Fire 1; `[...prev, ...toAppend]` rebuilt `[50 singles + 18 curated appended]` instead of adopting `deckCards`.
- **User device retest:** 6/6 PASS. T-01 top-of-deck alternates category/curated. T-03 mid-swipe preserved (ORCH-0498 no-regression — `removedCards` is ID-keyed, position-independent). T-04/T-05 single-pill decks unchanged. T-06 3-pill mix interleaves correctly. T-13 dev-console guard silent on happy path.
- **Structural safeguards landed:** (1) 28-line protective comment at call site citing AH-143; (2) `__DEV__` runtime guard fires `console.error` if merged[0..3] ≠ deckCards[0..3]; (3) new invariant `I-PROGRESSIVE-DELIVERY-INTERLEAVE-AUTHORITATIVE` registered in `INVARIANT_REGISTRY.md`; (4) grep invariant `[...prev, ...toAppend]` → 0 live matches in file.
- **Ship posture:** mobile OTA only (no migration, no edge fn, no native). `FEATURE_FLAG_PER_CONTEXT_DECK_STATE = __DEV__` still controls the path; flag-off legacy branch byte-stable. Kill switch = flag flip + OTA.
- **Deploy:** two separate `eas update` commands per memory rule (iOS and Android are distinct invocations — `--platform ios,android` comma syntax is invalid; `--platform all` fails on web due to react-native-maps).
- **Prod users unaffected:** flag off in prod. Fix ships dark until ≥1 week clean DEV telemetry.
- **Still open in this cluster:** ORCH-0510 silent catches (S3 observability, defer to Phase 4 lock-in); ORCH-0508 no test framework in app-mobile (S2 process-debt, blocks automated regression tests for this and similar sync-effect bugs).
- **Next program action:** Phase 2.4 dispatch — user-scoped cross-mode rejected-cards service per DEC-025. Closes ORCH-0493 RC#2 and restores Constitutional #13 exclusion consistency.

## Operational Alert (2026-04-19 very late — Phase 2.3 FAILED RETEST [SUPERSEDED by 2026-04-18 ORCH-0503 v3 CLOSE])

**Phase 2.3 did NOT ship. Do not commit. Do not flip flags.**

- **What failed:** AH-139's equal-cardinality expansion branch did not resolve ORCH-0503 on device retest. User's mixed-deck symptom inverted from "only singles shown" to "only curated shown" — confirming the visible top-of-deck tracks whichever side wins Promise.race in deckService.fetchDeck, not the final interleaved order. Orchestrator hypothesis: the sync effect's `deckIdsKey` uses sorted IDs, so partial-2 and final produce identical keys → sync-effect inner block skips on final → AH-139's new branch never runs. AH-139 implementor trace + orchestrator REVIEW both missed this upstream guard. Quality gap registered.
- **Also discovered (user report):** ORCH-0504 — solo cold-launch resets deck to card 0. ORCH-0505 — collab WAITING_FOR_PARTICIPANTS lying state flashes when ≥2 participants actually exist. Both registered in MASTER_BUG_LIST.
- **Current posture:** Phase 2.3 code remains in working tree, uncommitted. Three stacked reworks (AH-136 parent + AH-138 bridge + AH-139 failed interleave) + new symptoms. Forensic investigation AH-140 dispatched to root-cause all four symptoms before ANY more code attempts.
- **Prod users unaffected:** flag off in prod. Zero user-visible regression in the shipped app.
- **Investigation scope:** four symptoms, HIGH rigor, five-layer cross-check, six-field evidence per root cause. No solutions in the report — spec cycle follows after orchestrator review.
- **ORCH-0491 status unclear** — preservation claim relied on user's earlier Test 1 PASS; given the mixed-deck regression, the cross-mode preservation path may also have issues that weren't exposed by the narrow test. Forensics will re-validate.

## Operational Alert (2026-04-19 late — Phase 2.3 CONDITIONAL PASS [SUPERSEDED])

**Solo↔Collab swipe preservation + mixed-deck double-wipe both closed on device.**

- **What changed:** New `DeckStateRegistry` (`app-mobile/src/contexts/deckStateRegistry.ts`) holds one `DeckState` per `(mode, sessionId)` context. `useDeckCards` split into three calls (legacy + flag-solo + flag-collab) — `enabled` gates select which fires. Mode toggle swaps the registry's active-context pointer; SwipeableCards' swipe state mirrors into the registry and restores from it on toggle. Strict-superset check in the expansion signal closes ORCH-0498. Rework (AH-138) wired the missing save/restore bridge + ordering race guards that the parent phase deferred.
- **User device retest:** Test 1 (Solo swipe 3 → Collab → Solo) PASS — position + removedCards preserved. Test 2 (mixed-deck pref change + swipe during partial window) PASS — swipes preserved through curated merge. No regressions observed.
- **Ship posture:** `FEATURE_FLAG_PER_CONTEXT_DECK_STATE = __DEV__` → prod ships DARK. Kill switch = flip constant + OTA. Coupled-exit with `FEATURE_FLAG_PROGRESSIVE_DELIVERY` — both flip to unconditional `true` after 1wk clean telemetry; flag-off shims removed in cleanup commit.
- **QA:** user device retest + orchestrator code-read REVIEW substituted for the full tester cycle. Device-dependent SCs not formally exercised (SC-2.3-03 two-device collab mid-swipe, SC-2.3-04 Devtools parallel-hooks check, SC-2.3-06 p50/p95 latency measurement) are covered by code-read + the Phase 2.2 CONDITIONAL PASS precedent.
- **Deploy:** no migration, no edge fn change. Mobile OTA (two separate commands — iOS + Android).
- **Next program action:** Phase 2.4 — user-scoped cross-mode rejected-cards service. Absorbs ORCH-0493 RC#2 (`sessionServedIdsRef` replace on batchSeed===0) and restores Constitutional #13 (exclusion consistency). Closes DEC-025's design direction.
- **Still carrying forward:** ORCH-0493 RC#1 **partially closed** — superset subset works; non-superset collab realtime pref change awaits Phase 2.6 per spec line 475.

## Operational Alert (2026-04-19 — Phase 2.2 CONDITIONAL PASS)

**Deck preference-change latency: structural fix complete; production ships DARK pending telemetry.**

- **What changed:** `deckService.fetchDeck` now races singles + curated via `Promise.race`. Whichever resolves first with ≥1 card paints immediately. `useDeckCards` merges (not replaces) via `mergeCardsByIdPreservingOrder`, preserving existing card positions. Zero-singles + non-empty-curated no longer waits on curated's 20s ceiling — curated delivers at its actual settle time. Fixes ORCH-0485 RC#2 (singles-first hardcoded) + RC#3 (zero-singles skip) + ORCH-0486 (mixed-deck serverPath carry-through).
- **Ship posture:** code shipped; production behavior unchanged. `FEATURE_FLAG_PROGRESSIVE_DELIVERY = __DEV__` means DEV builds get the race path, prod builds get the verbatim pre-2.2 sequential-await fallback. Flip to unconditional `true` after 1-week clean telemetry — OR leave gated and Phase 2.3 can flip it explicitly.
- **QA:** `outputs/QA_ORCH-0490_PHASE_2.2_PROGRESSIVE_DELIVERY_REPORT.md` (0 P0/P1/P2/P3, 2 P4 doc-nits). All 7 SCs + 10 regressions + 6 invariants + 4 constitutional principles PASS structurally. TS compile clean (5 pre-existing, 0 new). Device-dependent measurements (warm p50 ≤ 1000ms, visual wipe counts, cross-device cache byte-identity) deferred — tester lacks hardware; structural determinism chain fully traced.
- **Deploy:** no migration, no edge function redeploy. Mobile OTA via two separate `eas update` commands (iOS + Android) per memory feedback. Pending user commit + OTA.
- **Next program action:** Phase 2.3 — per-context deck state + ORCH-0498 (mixed-deck double-wipe closure). Phase 2.3 converts the first-5-IDs wipe from "fires on every cache write" to "fires only on true reorders, not additive merges." Prompt prep pending.
- **Still carrying forward:** Phase 2.1 OTA is still pending user action (committed as 984950fa but iOS/Android OTAs not yet published).

## Operational Alert (2026-04-18, corrected post env-clarification)

## Operational Alert (2026-04-18, corrected post env-clarification)

**Admin Place Pool page is UNBLOCKED on production. Only auto-refresh remains broken.**

- **Environment clarification:** Mingla-dev IS the production Supabase project (single-env setup, user confirmed 2026-04-18). All 4 ORCH-0480/0481 migrations are already live in prod.
- **Primary goal achieved:** The original S1 — admin Place Pool page returning 500s — is RESOLVED. The 3 RPCs that were timing out now run in 107ms / 87ms hot / ~400ms. Admin users can see the page load. **ORCH-0480 closed A.**
- **Residual:** pg_cron auto-refresh of `admin_place_pool_mv` does not work (3-cycle stuck-in-loop, DEC-023). MV data is frozen at 2026-04-17 18:45 UTC initial populate. Admin stats will drift from reality unless someone manually refreshes. No user-facing error surfaced — Constitutional #3 silent-failure partial violation.
- **Path to close ORCH-0481 (D→A):** Dispatch ORCH-0489 implementor. Single-file admin UI change adds "Refresh Stats" button calling existing `admin_refresh_place_pool_mv()` RPC. Converts silent failure into explicit user-controlled refresh. ~2-3 hrs implementor + 1 hr tester. Prompt: `prompts/IMPL_ORCH-0489_ADMIN_REFRESH_BUTTON.md`.
- **Deferred follow-ups:** ORCH-0487 (city_overview covering-index for <500ms target), ORCH-0488 (cache-sensitivity on cold MV reads), ORCH-0484 (776 empty-array orphan rows).
- **Production deploy:** Complete for ORCH-0480/0481 (they're in prod already since Mingla-dev = prod). ORCH-0489 will be the next prod deploy (admin web dashboard — no migration, no EAS OTA).

## App Readiness Summary

| Metric | Value |
|--------|-------|
| Total items tracked | 303 |
| Grade A (launch-ready) | 101 (33%) |
| Grade B (solid, minor gaps) | 26 (9%) ↑ +2 (ORCH-0469, ORCH-0472) |
| Grade C (functional, incomplete) | 4 (1%) |
| Grade D (fragile) | 4 (1%) |
| Grade F (broken/unaudited) | 169 (56%) ↓ -2 |
| Deferred | 1 (<1%) |
| Deck hardening passes complete | 12 (46 bugs fixed) |
| Place-pipeline passes complete | 1 (8 bugs fixed in single ORCH-0460 bundle) |

## Can a User Complete the Core Loop?

| Step | Status | Confidence |
|------|--------|------------|
| Install + Auth | PARTIAL — Phone OTP (B), Google (C), Apple (B), Sign-out (A) | Medium |
| Onboarding | PARTIAL — preference save + skip work (A), but state machine + GPS + resume all F | Low |
| Explore (deck swipe) | STRONG — 37 items at A, 7 at B, deck contract deterministic, cross-page dedup fixed, place→card sync live | High |
| Save experience | UNVERIFIED — save/unsave at F | Low |
| Schedule | PARTIAL — scheduling bugs exist (picker behind modal, no confirmation) | Medium |
| Invite friends | UNVERIFIED — friend request send at B, but collaboration invites at F | Low |
| Collaborate | PARTIAL — UI consolidated (A), but creation/voting/sync all F | Low |
| Go (use experience) | UNVERIFIED — post-experience + review all F | Low |

**Verdict: Core loop is NOT completable end-to-end.** The card deck (middle of funnel) is excellent, but entry (auth/onboarding) and exit (save/schedule/review) are unverified.

## Top 5 Launch Blockers

1. **Avatar impersonation (ORCH-0250)** — Avatars bucket has no user-scoping RLS. Any user can overwrite another's avatar. S1.
2. **Onboarding state machine at F (ORCH-0008)** — Users may get stuck. Completion rate unknown. S0.
3. **Account deletion at F (ORCH-0102)** — Apple/Google require it. App Store rejection if missing. S0.
4. **Save/unsave at F (ORCH-0094)** — Core loop requires saving. Cannot verify user value delivery. S1.
5. **Chat entirely unaudited (ORCH-0127)** — Social feature with zero verification. DM broken = hollow connections. S1.

### Recently Resolved Blockers
- **Place Pipeline Accuracy Overhaul (ORCH-0460 bundle — 8 items)** — CLOSED 2026-04-17. 5 categories were leaking wrong places into users' feeds (restaurants in Creative & Arts, bars in Movies & Theatre, bars/play/tobacco in Brunch Lunch Casual, sports parks/farms/kids centers in Play, garden stores in Flowers). Root cause: every filter in the pipeline only checked `primary_type` — the real identity was often in the `types` array. Fix: expanded all filters to check the full types array, split casual_eats into 3 configs (50-type Google limit), added 58 new world cuisine types to seeding (Cuban, Filipino, Persian, dim sum, soul food, etc.), split FLOWERS_BLOCKED_TYPES into primary/secondary sets (after retest P0 caught a 168-supermarket false-positive regression on `food_store`), created UPSCALE_CHAIN_PROTECTION whitelist. Removed upscale↔casual mutual exclusivity (Nobu-style restaurants now get both). Closed ORCH-0460/0461/0462/0463/0464/0465/0471/0477. QA retest 11/11 SC PASS. Live SQL confirms 139 canonical supermarkets preserved.
- **Photo Backfill Job System (ORCH-0274)** — CLOSED. Broken global photo download replaced with city-scoped batch system. 2 tables, 9 actions, auto-advance, persistent progress, job status bar. Phase 1 (backend) + Phase 2 (UI). 23/23 tests PASS.
- **place_pool → card_pool Sync (ORCH-0273)** — CLOSED. 13+ fields drifted silently between master record and served cards. Unified trigger now syncs 16 fields for single cards + curated composites. Old website trigger replaced. 10/10 tests PASS.
- **Cross-Page Dedup (ORCH-0272)** — CLOSED. Impression recording broken since Mar 29 due to partial index mismatch. Fixed ON CONFLICT predicate, added error throw + degraded mode, client circuit breaker. Infinite scroll restored. 7/7 tests PASS.
- **Deterministic Deck Contract (5 fixes)** — ORCH-0266 (double pagination), ORCH-0267 (travel time), ORCH-0038 (custom location GPS), ORCH-0268 (NULL price tier), ORCH-0048 (category interleave). All CLOSED at A. Solo mode (ORCH-0065) upgraded F→B, collab parity (ORCH-0066) upgraded C→B.
- **ORCH-0258 (admin_users privilege escalation)** — CLOSED. All permissive policies dropped, is_admin_user() gating. QA passed.
- **ORCH-0252 (get_admin_emails exposed to anon)** — CLOSED. Revoked anon access, replaced with is_admin_email() boolean. Fixed with ORCH-0258.
- **ORCH-0224 (Admin auth 3-layer)** — Upgraded B to A. Admin email exposure fixed as part of ORCH-0258.
- **ORCH-0253 (PII exposure via USING(true) on profiles)** — CLOSED. RLS policy tightened. QA passed.

## Top 5 Quality Risks

1. **60% of items at F** — Unknown bug count in production code
2. **Map surface entirely unaudited (16 items at F)** — Large feature with zero verification
3. **Chat entirely unaudited (8 items at F)** — Social feature with zero verification
4. **Calendar entirely unaudited (8 items at F)** — Scheduling is core loop step
5. **10 unverified invariants** — Structural guarantees without proof

## What's Strong (Grade A/B Surfaces)

- **Place Pipeline Accuracy (NEW 2026-04-17)** — 8 A-grade closures. Types-array filtering across 5 categories, 58 new cuisine types seedable, upscale chain whitelist, flowers primary/secondary split preserves canonical supermarkets. Constitutional invariant #13 restored to PASS. Pool: 41,727 approved places; 1,354 (~3.2%) will be correctly re-categorized post-deploy.
- **Card deck pipeline** — 35 items at A, 7 at B. Pool-first architecture, exclusions, balancing, photo integrity. Hardened across 10 passes.
- **Deck Contract** — All 200 pool cards reachable, travel time hard-filtered, categories interleaved, custom location deterministic, NULL price tiers excluded. Solo and collab modes verified.
- **Notification infrastructure** — 6 items at A. Push delivery, realtime subscriptions, app badge.
- **Hardening utilities** — withTimeout, mutation error toast, query key factory. All A.
- **Chat responsiveness** — 4 items at A. Instant open, background fetch, block timeouts.
- **Preferences-to-deck contract** — Race condition killed, prefsHash matching, atomic save.
- **Auth & Session** — 2A/4B/1C. Sign-out hardened. OAuth flows verified.
- **Admin Security** — Admin auth upgraded to A. Privilege escalation fixed, admin email exposure closed, is_admin_user() gating enforced.
- **Payments & Subscriptions** — 8A/6B/1C. Paywalls, RevenueCat, tier gating, swipe limits, referral expiry, trial abuse prevention all verified.
- **State Persistence** — Instant tab switching (always-mounted tabs), PreferencesSheet opens from cache, all tabs always live on foreground resume. 4 items closed at A.

## What's Fragile (Grade D/F Surfaces)

- **Map & Location** — 16 items, 100% at F. Entire feature unaudited.
- **Chat / DM** — 8 items, 100% at F. No verification of send/receive, presence, realtime.
- **Calendar & Scheduling** — 8 items, 100% at F. Core loop step, unverified.
- **People Discovery** — 10 items, 100% at F.
- **Sharing & Invites** — 10 items, 100% at F.
- **Post-Experience** — 9 items, 100% at F.
- **Booking** — 2 items, 100% at F.

## Engineering Velocity

- **10 deck hardening passes** completed (2026-03-20 to 2026-03-24), 44 bugs fixed
- **5 card pipeline audit passes** completed (2026-03-24 to 2026-03-25)
- **2 notification passes** completed (2026-03-21 to 2026-03-23)
- **9 cross-cutting passes** completed (2026-03-23)
- Last commit with hardening work: 2026-03-26 (AI Quality Gate Phase 2)
- Wave 1b payment fixes: 7 bugs closed 2026-03-31 (ORCH-0143/0144/0145/0146/0147/0148/0149)
