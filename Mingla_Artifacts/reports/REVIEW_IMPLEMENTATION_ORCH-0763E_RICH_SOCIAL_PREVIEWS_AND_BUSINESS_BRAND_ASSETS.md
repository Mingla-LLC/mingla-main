# REVIEW - ORCH-0763E Rich Social Previews + Mingla Business Brand Assets Implementation

Date: 2026-05-09
Mode: Orchestrator review
Verdict: Accepted for tester/deploy gate, not closed

## Plain-English Finding

The implementation moved the right problem forward. It did not just change the visible share text. It added the missing business-logo asset layer, native/web app branding assets, crawler-facing metadata routes, and dynamic OG image routes needed for iMessage/WhatsApp/social preview cards.

This is still not proven in production. The local implementation can pass tests and export, but link previews are generated by external crawlers hitting deployed `business.usemingla.com`. The app icon/splash also cannot be proven until a native iOS/Android rebuild is installed.

## Evidence Reviewed

Implementation report:

- `reports/IMPLEMENTATION_ORCH-0763E_RICH_SOCIAL_PREVIEWS_AND_BUSINESS_BRAND_ASSETS.md`

Reported verification:

- Focused Jest passed: 4 suites / 15 tests
- TypeScript passed
- Expo public config passed
- Expo web export passed
- New server/API JS syntax checks passed
- Scoped ESLint passed for the new preview/server routes and touched public page surfaces
- OG image smoke returned a real PNG buffer
- Asset dimensions checked

Scope check:

- The reported implementation is scoped to `mingla-business`.
- No explorer/consumer app or admin app changes were reported for this pass.
- There is a dirty worktree with other pre-existing `mingla-business` changes from adjacent ORCH work; close/commit must stage only scoped files later.

## Accepted Implementation Shape

Accepted direction:

- Repo-owned Mingla Business logo assets under `mingla-business/assets/brand/` and `mingla-business/public/brand/`
- Regenerated app icon/splash/adaptive-icon/favicon assets
- Visible logo reference updates for welcome/login, landing, and 404 surfaces
- Vercel bot/crawler rewrites for public event and brand pages
- Dynamic OG routes:
  - `/og/event/:eventId.png`
  - `/og/brand/:brandSlug.png`
- Client-side Twitter image metadata additions for parity

The bot-only rewrite approach is acceptable for this stage because it preserves the normal Expo public event/brand pages for human users while giving social crawlers metadata-friendly HTML.

## Remaining Gates

Do not close ORCH-0763E yet.

Required before close:

1. Web deploy proof
   - Deploy `mingla-business` to Vercel.
   - Prove deployed `/og/event/...png` and `/og/brand/...png` return `image/png`.
   - Prove deployed public event/brand routes return crawler-visible title, description, canonical URL, OG image, Twitter image, and no wrong domains for bot user agents.
   - Prove ordinary browser user agents still load the public event/brand experience.

2. Real social preview proof
   - Share a fresh or cache-busted event link through iMessage and WhatsApp.
   - Confirm preview includes event name and Mingla branding.
   - Confirm link opens the public event page.

3. Native rebuild proof
   - Install a fresh iOS build and confirm app icon/splash use the new business logo.
   - Confirm equivalent Android behavior when Android release testing is available.

4. Independent tester report
   - Dispatch `$tester` with `prompts/TESTER_ORCH-0763E_RICH_SOCIAL_PREVIEWS_AND_BUSINESS_BRAND_ASSETS.md`.

## Next Action

Recommended next sequence:

1. Deploy the business web app to Vercel.
2. Dispatch `$tester` with:

`prompts/TESTER_ORCH-0763E_RICH_SOCIAL_PREVIEWS_AND_BUSINESS_BRAND_ASSETS.md`

3. Rebuild/install native iOS/Android business app when ready to prove app icon and splash changes.

If the tester fails on deployed preview metadata, route to `$implementor` rework. If tester passes with only platform preview cache delay, orchestrator may accept with a documented cache note after a fresh-event proof.
