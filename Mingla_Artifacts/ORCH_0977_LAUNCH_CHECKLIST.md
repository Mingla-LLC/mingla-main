# ORCH-0977 — Consumer App Store + Play Store Production Launch Checklist

**Status:** REGISTERED 2026-05-25 · IN PROGRESS
**Affected Surfaces:** Consumer iOS (`app-mobile/` on iOS), Consumer Android (`app-mobile/` on Android).
**NOT in scope:** business-iOS, business-Android, business-web-preview, admin-web, buyer-web.
**Worktree:** `~/Desktop/mingla-orchs/ORCH-0977-[consumer-app-store-launch]/`
**Branch:** `ORCH-0977-consumer-app-store-launch`
**Base commit:** `3b5364192` (carries autocomplete cleanup + dead-booking-integration removal — done pre-spawn)

---

## Scope

Ship a true production AAB to Google Play Store AND a true production IPA to Apple App Store for the consumer app, off current `main`. Last shipped AAB is build 12 (2026-04-22, commit `b1499138`) — 5+ weeks behind. No production iOS IPA has ever been built off current `main` (all recent EAS iOS builds are `development` profile).

When every item below is done and verified, the orchestrator closes this ORCH via a single PR from `ORCH-0977-consumer-app-store-launch` to `main`, then reaps the worktree.

---

## To-do list

Items marked ✅ are done. Strike-through indicates resolved/no-longer-applicable.

### Shared (both platforms)

- ✅ ~~Add 6 missing EAS production env vars~~ — resolved 2026-05-25 (4 confirmed dead + deleted, 2 set: Mixpanel + Mapbox in EAS prod; Mixpanel also in preview + dev; client-side Google autocomplete fallback deleted entirely, replaced with single-path OSM)
- ✅ ~~**#2 — Bump `appVersion`** in `app-mobile/app.json` from `"1.0.0"` to `"1.1.0"`~~ — done 2026-05-25 in commit landing this update; `runtimeVersion` policy is `appVersion` so all OTA channels now resolve to `1.1.0`, decoupling the new production binary from build-12 installs.
- ✅ ~~**#3 — Confirm META-ORCH-0972 fully closed**~~ — operator confirmed 2026-05-26: META-ORCH-0972 (brand-kind decommission) is fully closed. The production build off current `main` will not ship a partially-converted brand-kind state. No commit pinning needed.
- ✅ ~~**#4 — Confirm `google-services.json` project `mingla-dev`**~~ — operator confirmed 2026-05-26: `mingla-dev` is the only Firebase project. Single-project setup (dev + prod share the same Firebase). Android FCM token registration + OneSignal credentials remain coherent. No swap needed.
- ✅ ~~**#5 — Confirm Mingla Plus IAP products** active on both Play Console + App Store Connect~~ — operator verified 2026-05-26 (screenshots): ASC has `mingla_plus_weekly` / `_monthly` / `_annual` all "Ready to Submit"; Play Console has the same 3 product IDs with 1 active base plan each (last updated 2026-05-08). Pro/Elite were never real products (5-axis probe: zero git launch commits, zero memory entries, zero decision-log entries, 0/37 subscription rows, 0/41 revenue-log rows reference them). The legacy entitlement constants were deleted from `revenueCatService.ts` in commit `c853dc873`.
- ✅ ~~**#6 — Review Sentry `sendDefaultPii: true` flag**~~ — operator decision 2026-05-26: keep `true` (better crash diagnostics). Decision recorded inline at `app-mobile/app/_layout.tsx:27-32` (replaces the prior `TODO ORCH-0679-D3` placeholder). **Downstream gating:** items #27 (Play Data Safety) + #34 (Apple Privacy Nutrition Labels) MUST declare Approximate Location (via IP), User ID, and Diagnostics.
- ✅ ~~**#7 — Confirm RevenueCat dashboard** has ASC Shared Secret/API key (iOS) + Service Account JSON (Google Play)~~ — operator verified 2026-05-26 on RC dashboard: both credentials populated. If TestFlight or internal-track smoke testing surfaces a purchase that charges but doesn't grant entitlement, this is the first place to recheck.

### Android only

- ✅ ~~**#8 — Strip or declare `SYSTEM_ALERT_WINDOW`** in `app.json`/native overrides~~ — done 2026-05-26: new Expo config plugin `app-mobile/plugins/withoutSystemAlertWindow.js` strips the permission from the merged AndroidManifest at every prebuild. Source was Expo's `@expo/config-plugins/withAndroidBaseMods` default template (Mingla has zero overlay API usage in code). Net: Play Store listing no longer shows the confusing "Display over other apps" entry. Item #28 (Play Console Permissions Declaration) no longer needs to cover `SYSTEM_ALERT_WINDOW`.
- [ ] **#9 — Flip `eas.json` submit profile** — `track: production`, `releaseStatus: completed` (after internal testing done)
- ✅ ~~**#10 — Remove dead `WRITE_EXTERNAL_STORAGE`** permission from `app.json`~~ — done 2026-05-26 in this commit. Also removed the paired `READ_EXTERNAL_STORAGE` since both were deprecated on API 33+ and shared the same disclosure-noise problem.
- ✅ ~~**#11 — Fix `android/app/build.gradle` release block** (proper release signing config)~~ — MOOT 2026-05-26. Same as #12: `app-mobile/android/` is gitignored; Expo regenerates the native Android project on every prebuild and EAS injects its own managed upload keystore at cloud-build time. The local `signingConfig signingConfigs.debug` stub in the regenerated file is irrelevant for cloud builds. No action needed.
- ✅ ~~**#12 — Remove literal `versionCode 1`** line in `build.gradle`~~ — MOOT 2026-05-26. `app-mobile/android/` is fully gitignored (Expo regenerates the entire native Android project on every `expo prebuild` / EAS build), so there is nothing tracked to delete. The literal `versionCode 1` is regenerated identically every build and EAS overrides it via `appVersionSource: remote` + `autoIncrement: true`. No action needed.
- ✅ ~~**#13 — Confirm Play Console service account** still has Android Publisher API access~~ — verified 2026-05-28. Service account `eas-submit@mingla-dev.iam.gserviceaccount.com` (key at `~/.mingla-secrets/playstore-mingla.json`) authenticated via RS256 JWT → OAuth2 token exchange succeeded → created an edit transaction on `com.mingla.app.v2` (edit id `08568930748305934540`, left uncommitted so it auto-expires). Proves full Android Publisher API write access to the Mingla package. `eas submit --platform android` will work.
- [ ] **#14 — Full smoke pass on physical Android** after new AAB built
- [ ] **#15 — Eyeball every screen for edge-to-edge layout bugs** on Android 15

### iOS only

- ✅ ~~**#16 — Add `NSUserTrackingUsageDescription`** to `app.json` → `expo.ios.infoPlist`~~ — done 2026-05-26 in this commit. String: "Mingla uses this permission to measure ad performance and improve your experience. You can change this in Settings." Prevents the iOS hard-crash when ATT fires on first launch and satisfies App Review's AppsFlyer-presence-without-ATT rejection trigger.
- ✅ ~~**#17 — Decide ATT strategy** (fire prompt vs strip IDFA collection from AppsFlyer init)~~ — operator decision 2026-05-26: fire the ATT prompt post-auth. Implemented in commit `b678c82ab`: installed `expo-tracking-transparency@6.0.8`, added Expo plugin to `app.json` with the `userTrackingPermission` string, refactored `appsFlyerService.ts` to use `manualStart: true` + new `startAppsFlyer()` export, wired a new `useEffect` in `app/index.tsx` that fires `requestTrackingPermissionsAsync()` 1.5s after the user gets a session and then calls `startAppsFlyer()` regardless of result. Replaces the never-implemented ORCH-0349 plan.
- ✅ ~~**#18 — Decide iPad support** — ship iPad screenshots OR set `supportsTablet: false`~~ — operator decision 2026-05-26: iPhone-only at launch. `app.json` `ios.supportsTablet` flipped `true` → `false`. iPad users can still install the binary, but it runs in iPhone-compat mode (letterboxed) and App Store Connect will not require iPad screenshots. Downstream effect on item #38 (App Store screenshots): only iPhone sizes needed.
- ✅ ~~**#19 — Verify Apple Sign-In button** renders equal-prominence to Google in WelcomeScreen~~ — audit 2026-05-26 PASSES Apple Guideline 4.8. Apple button renders FIRST (top of action zone), identical `100% / s(400) / vs(56)` dimensions, identical `radius.lg` / `gap: 12` / font size 17 / semibold / letter-spacing 0.3 / 22px icon. Apple has slightly deeper shadow (`shadows.md` vs Google's `shadows.sm`) — within the "equivalent or more prominent" rule, favors Apple. Black background + white text follows Apple HIG for the "Sign in with Apple" black variant. iOS-only conditional render via `Platform.OS === "ios"` is correct (Apple Sign-In is iOS-specific). App Review will not reject for 4.8 violation. Evidence in `app-mobile/src/components/signIn/WelcomeScreen.tsx:317-404` (render) + `:486-526` (styles).
- ✅ ~~**#20 — Run `eas credentials --platform ios`** — confirm cert + provisioning profile valid~~ — probed 2026-05-27 via `eas credentials --platform ios` (production profile) driven through expect. All production iOS credentials VALID and tied to Apple Team `782KVMY869 (MINGLA LLC)`: Distribution Certificate serial `3EA1AD008F0C9F2592639D817AAFF16` expires **Wed Mar 3, 2027 11:00:18 EST**; App Store Provisioning Profile (Developer Portal ID `9D64D7YYLP`) status active, same expiration; Ad Hoc Provisioning Profile (Developer Portal ID `HH6JQ37H98`) status active, same expiration; Push Key entry present in EAS for Apple Team `782KVMY869` (live key ID `3S2UH53R86` confirmed via OneSignal probe under #22); OneSignalNotificationServiceExtension target's bundle `com.mingla.app.v2.OneSignalNotificationServiceExtension` also has matching cert + profile, same expiration. ~9 months of runway before any credential refresh needed. Submission to App Store Connect is unblocked from a credentials standpoint.
- ✅ ~~**#21 — Verify AASA file + assetlinks** at `usemingla.com/.well-known/`~~ — shipped 2026-05-27 via PR #222 (marketing-well-known). Both files live: AASA returns 200 + `Content-Type: application/json` with appID `782KVMY869.com.mingla.app.v2` scoped to `/invite/*`, `/board/*`, `/orders/*`, `/chat/*`; assetlinks.json returns 200 + `application/json` with both production SHA-256 fingerprints under `com.mingla.app.v2`. Apple CDN cache (`app-site-association.cdn-apple.com/a/v1/usemingla.com`) confirmed serving payload. Google Digital Asset Links API confirmed both fingerprints parse into separate statements (maxAge 3599s). iOS Universal Links + Android App Links infrastructure GREEN end-to-end. **Live-test caveat (deferred as [[ORCH-0979]]):** Universal Link cold-start opens the app correctly (proves AASA works) but lands on Expo Router "Unmatched Route" 404 because the single-screen app has no `app/invite/[code].tsx` route file — incoming URL never reaches `deepLinkService.parseDeepLink`. Fix is small (`app/+not-found.tsx` catch-all) but operator deferred to post-launch; does NOT block store submission since both stores only verify AASA + assetlinks file content during review, not in-app routing.
- ✅ ~~**#22 — Verify APNs auth key** in OneSignal dashboard not expired~~ — independently probed 2026-05-26 via OneSignal v2 API. Token-based authentication (.p8); `apns_env=production`; `apns_team_id=782KVMY869`; `apns_key_id=3S2UH53R86`; `apns_bundle_id=com.mingla.app.v2` matches `app.json`. .p8 keys do not expire on Apple's side (only revoke-able from Developer Portal). Liveness signal: `messageable_players: 59` of `players: 164` (~36% notification grant rate) confirms `permissionOrchestrator.requestPushPermission()` IS firing post-coach-marks for real users. **Security note:** the API probe response surfaced the raw .p8 private key + FCM service account JSON in chat scrollback + Anthropic logs. Treat as a credential rotation candidate before public launch (regenerate .p8 in Apple Developer Portal + FCM service account in Google Cloud + re-upload to OneSignal; ~15 min total).
- ✅ ~~**#23 — Confirm App Store Connect API key** valid~~ — closed 2026-05-27. Operator created the ASC API key in App Store Connect → Users and Access → Integrations → Team Keys and dropped the `.p8` into the central `Key Details For Mingla/` vault (gitignored). All three identifiers wired into `app-mobile/eas.json` `submit.production.ios`: `ascApiKeyPath: /Users/sethogieva/Desktop/mingla-main/Key Details For Mingla/AuthKey_B39RMRV6D8.p8`, `ascApiKeyId: B39RMRV6D8`, `ascApiIssuerId: ee78d0ff-158c-4326-80ef-aec69745fc2d`. End-to-end validation: built an ES256-signed JWT from the .p8 + key ID + issuer ID, hit `GET https://api.appstoreconnect.apple.com/v1/apps/6760440898` → **HTTP 200**, returned canonical app metadata: name `Mingla–Date Plans & City Gems`, bundleId `com.mingla.app.v2` (matches `app.json`), sku `mingla-ios-v1`, primaryLocale `en-US`. EAS Submit will use this credential for App Store uploads — no Apple ID + 2FA needed per submission. Apple ASC API keys do not expire; only revocable from the ASC dashboard.
- ✅ ~~**#24 — Clean up deprecated `NSLocationAlwaysUsageDescription`** in `app.json`~~ — done 2026-05-26 in this commit. Deprecated since iOS 11; `NSLocationAlwaysAndWhenInUseUsageDescription` already present remains the canonical key.
- ✅ ~~**#25 — Audit `ios/Mingla/PrivacyInfo.xcprivacy`** for newer required-reason categories~~ — MOOT 2026-05-26. `app-mobile/ios/` is gitignored; Expo regenerates the iOS project on every prebuild. The 4 reasons currently declared (UserDefaults, FileTimestamp, SystemBootTime, DiskSpace) cover the app-level direct API usage; each third-party SDK ships its own `PrivacyInfo.xcprivacy` in its Pod (Sentry, AppsFlyer, RevenueCat, OneSignal, Stripe, Mixpanel, etc. — verified earlier). Any missing reason would surface as a specific App Review rejection at submission, fixable via a Expo config plugin then. Superseded by pre-submit verification items #43–#46.
- [ ] **#26 — Full smoke on TestFlight** on real iPhone (+ iPad if `supportsTablet: true`)

### External — Play Console manual (operator)

- ✅ DONE 2026-05-28 (operator pasted) — **#27 — Complete / refresh Data Safety form** — **ANSWER SHEET WRITTEN 2026-05-27; awaiting operator paste into Play Console.** Full audit at `Mingla_Artifacts/ORCH_0977_PLAY_CONSOLE_DATA_SAFETY.md` with per-section answers covering: encryption (yes, HTTPS everywhere), deletion mechanism (in-app Settings → Delete Account), Personal info (name/email/phone/user IDs all collected + shared with named third parties), Financial (NO payment data — Stripe/Apple/Google process; Mingla only sees tokenized purchase via RevenueCat), Messages (in-app board messages yes; SMS/email no), Photos+Audio (yes via Cloudinary), Calendar (Mingla writes only, doesn't read — declare NO), Contacts (NO, never accessed), App activity (Mixpanel + AppsFlyer events), App info+performance (Sentry crash + diagnostics, with `sendDefaultPii: true` → linked to user identity), Device IDs (IDFA + Android Ad ID via AppsFlyer), Location (precise = optional via permission; coarse = required for personalization). Side finding: Privacy Policy at `usemingla.com/privacy-policy/` lists only 5 service providers (Supabase/Twilio/Vonage/Google/Apple) but code uses 12+ — both stores reject if declarations don't match the policy. Flagged for follow-up.
- ✅ DONE 2026-05-28 (operator pasted) — **#28 — Submit Permissions Declaration** for `ACCESS_FINE_LOCATION` (+ `SYSTEM_ALERT_WINDOW` if kept) — answer sheet at `Mingla_Artifacts/ORCH_0977_COMPLIANCE_QUESTIONNAIRES.md`. Location is foreground-only (no `ACCESS_BACKGROUND_LOCATION`), so the easy declaration path applies — no background-location video/justification needed. Paste-ready declaration text provided. `SYSTEM_ALERT_WINDOW` already stripped by the withoutSystemAlertWindow plugin (#8), so not in scope. Awaiting operator paste into Play Console.
- ✅ DONE 2026-05-28 (rated 12/Teen, confirmed in Play declarations) — **#29 — Complete Content Rating questionnaire** — IARC answer table at `Mingla_Artifacts/ORCH_0977_COMPLIANCE_QUESTIONNAIRES.md`. Category: Social Networking. All violence/sex/gambling/horror = No; alcohol = References only (Infrequent/Mild, from "drinks places" venue suggestions); users-communicate = Yes; location-sharing = Yes; digital purchases = Yes. Expected rating: Teen 13+ / PEGI 12 (matches Terms §3 minimum age). Awaiting operator paste into Play Console.
- ✅ ~~**#30 — Verify Privacy Policy URL** live + listed on store listing (`https://www.usemingla.com/privacy-policy/`)~~ — shipped 2026-05-27 via PR #223 (marketing-privacy-policy-page). Live verified: apex `https://usemingla.com/privacy-policy/` and www `https://www.usemingla.com/privacy-policy/` both 308-redirect (trailing slash stripped by Vercel default) → 200 OK serving the 12-section Privacy Policy. Production deploy `dpl_Dt7JbmgfqBpV6h3hST2em3MmiU2d`. Architecture: extracted `PRIVACY_SECTIONS` + `PRIVACY_EFFECTIVE_DATE` from `hero.tsx` into `mingla-marketing/lib/privacyContent.ts` so the home page modal AND the standalone page share one source of truth (no drift). Consumer app's `LEGAL_URLS.privacyPolicy` (used from ProfilePage, CustomPaywallScreen, OnboardingFlow → opens browser) now resolves instead of 404ing. **Minor follow-up:** HTML `<title>` doubles to "Privacy Policy — Mingla — Mingla" because root layout's `title.template` appends "— Mingla" automatically. One-line fix in `app/privacy-policy/page.tsx` metadata (change `title: 'Privacy Policy — Mingla'` → `title: 'Privacy Policy'`). Not blocking submission but visible in browser tabs + search results. Bundling with next marketing PR.
- [ ] **#31 — Complete at least one closed/internal testing release**
- 🟡 **#32 — Draft release notes per locale** — DRAFTED 2026-05-28 at `Mingla_Artifacts/ORCH_0977_RELEASE_NOTES.md`. Play "What's new" (≈430/500 chars) + App Store "What's New" (≈700/4000 chars), en-US primary (both stores fall back to default locale). Launch-flavored (first public release), experience-app positioning (no dating language), no prohibited promo terms, points users to in-app Contact Support. Awaiting operator paste into both dashboards' release-notes fields.
- ✅ ~~**#33 — Confirm IAP/subscription SKUs** match RevenueCat exactly~~ — implicitly confirmed via #5: ASC + Play Console + RevenueCat all carry the same 3 product IDs (`mingla_plus_weekly` / `_monthly` / `_annual`) attached to the only entitlement that now exists (`Mingla Plus`). No legacy entitlements to mismatch against post-`c853dc873`.

### External — App Store Connect manual (operator)

- ✅ DONE 2026-05-28 (operator pasted) — **#34 — Complete Privacy Nutrition Labels** for v1.1.0 — **ANSWER SHEET WRITTEN 2026-05-27; awaiting operator paste into App Store Connect → My Apps → Mingla → App Privacy.** Full audit at `Mingla_Artifacts/ORCH_0977_APP_STORE_PRIVACY_NUTRITION_LABELS.md` using Apple's three-bucket taxonomy: 🔴 **Used to Track You** (Device ID/IDFA via ATT prompt for AppsFlyer install attribution — the ONLY tracking category Mingla triggers), 🟡 **Data Linked to You** (Contact Info + Location + User Content + Identifiers + Purchases + Usage Data + Diagnostics — all linked because tied to Supabase account ID, and Sentry `sendDefaultPii: true` includes IP + user_id in crash payloads), 🟢 nothing in **Not Linked to You** because every collected category is tied to identity. ATT prompt declaration covered with `NSUserTrackingUsageDescription` string already in `app.json`. Item #35 (Encryption Export Compliance) auto-clears via `ITSAppUsesNonExemptEncryption: false` already in `app.json`. Same Privacy Policy mismatch flagged for #27 applies here.
- ✅ ~~**#35 — Answer Encryption Export Compliance** (should auto-skip via `ITSAppUsesNonExemptEncryption: false`)~~ — verified 2026-05-28. `app.json` `ios.infoPlist.ITSAppUsesNonExemptEncryption` = `false`, which auto-populates the ASC encryption question and skips the manual CCATS classification-document upload at submission time. No manual action needed; clears automatically when you submit.
- 🟡 **#36 — Fill App Review Information** (demo account, contact, notes) — UNBLOCKED + drafted 2026-05-28 at `Mingla_Artifacts/ORCH_0977_APP_REVIEW_INFORMATION.md`. Root blocker found + fixed: onboarding has a mandatory phone+OTP gate with no skip, so reviewers (Apple/Google OAuth login) were stuck at phone verification. Shipped a test-OTP bypass (reviewer number `+12015550199` + code `123456`, live in deployed `send-otp`/`verify-otp`). Paste-ready ASC App Review notes + Play App access notes provided. **Operator still must:** (1) fill ASC Contact Information (name/phone/email monitored during review), (2) paste the notes into both dashboards, (3) optionally self-test the reviewer flow on TestFlight.
- ✅ DONE 2026-05-28 (operator pasted) — **#37 — Complete Age Rating questionnaire** — ASC answer table at `Mingla_Artifacts/ORCH_0977_COMPLIANCE_QUESTIONNAIRES.md`. All content types None except Alcohol/Drug References = Infrequent/Mild; no unrestricted web. Expected rating 12+. **⚠️ MAJOR FLAG: Apple Guideline 1.2 requires UGC apps (Mingla has friend messaging + board discussions + reviews) to have in-app report + block + content-filter + published contact. Contact is now published (support@/legal@/privacy@).** ✅ **UGC 1.2 fully resolved 2026-05-28:** report ✅ + block ✅ (both pre-existing) + published contact ✅ (in-app Settings link added) + content filter ✅ (NEW — OpenAI Moderation via `moderate-content` edge fn, wired pre-insert at board messages, direct messages, and profile bio; reviews are rating-only so no text to filter; deployed + verify-first-call confirmed flagging works). The single biggest App Store risk is now closed.
- ✅ ~~**#38 — Upload App Store screenshots** (iPhone 6.7"/6.1"/5.5" + iPad 12.9"/11" if applicable)~~ — closed 2026-05-27. 10 source PNGs (3240×5760, 9:16) resized inline via `sips` into App Store iPhone 6.7" set (1284×2778, 0.6–4.7 MB each) + Play Store phone set (1080×1920, 0.4–2.8 MB each) under `~/Desktop/mingla-main/App Screenshots/appstore/` + `/playstore/`. Apple's display-dimensions ratio rule covers all iPhone sizes via the single 6.7" upload; iPad N/A (#18 `supportsTablet: false`). Same pass produced Play Store **Feature Graphic** (1024×500, 5 stitched tiles at 205×500 each, ImageMagick `+append -extent 1024x500`, 545 KB) + **App icons** (App Store 1024×1024 + Play Store 512×512, Mingla orange wordmark on white, no alpha, both stripped of metadata). All assets uploaded by operator to ASC + Play Console. Implementation report at `Mingla_Artifacts/reports/IMPLEMENTATION_ORCH-0977_APP_STORE_SCREENSHOTS_RESIZE.md`.
- ✅ ~~**#39 — Verify subscription products** "Ready to Submit"~~ — verified 2026-05-28 via ASC API (`GET /v1/apps/6760440898/subscriptionGroups` → group `Mingla Plus` id 22000661). All 3 products `state: READY_TO_SUBMIT`: `mingla_plus_annual`, `mingla_plus_monthly`, `mingla_plus_weekly`. Matches the RevenueCat + Play Console SKU set confirmed under #5. Products will attach to the first submission automatically.
- ✅ DONE — **#40 — Draft App Store metadata** (name, subtitle, description, keywords, URLs) — completed by Mingla marketing team (operator confirmed 2026-05-28). Live name in ASC: "Mingla–Date Plans & City Gems". No orchestrator action needed.
- [ ] **#41 — TestFlight build** pushed + tested before App Store submission

### Pre-submit verification

- [ ] **#42 — Build off `main` HEAD** with `eas build --profile production --platform all`
- [ ] **#43 — Read EAS build logs** — confirm Mixpanel + Mapbox + Stripe envs all inject
- [ ] **#44 — Install signed AAB on physical Android + IPA on TestFlight iPhone** (+ iPad)
- [ ] **#45 — Walk full first-run flow** (install → onboarding → sign-in → ATT iOS → location grant → home → search/autocomplete → paywall → IAP sandbox → push tap → deep-link)
- [ ] **#46 — Watch Sentry dashboard** for zero fatal crashes during smoke walk
- [ ] **#47 — `eas submit`** to closed/internal tracks, then promote

---

## Reference files

- `eas.json` — build + submit profile config
- `app-mobile/app.json` — Expo config, plugins, native permissions
- `app-mobile/app.config.ts` — runtime config + Google OAuth client IDs
- `app-mobile/android/app/build.gradle` — Android Gradle config
- `app-mobile/android/app/src/main/AndroidManifest.xml` — merged Android manifest
- `app-mobile/ios/Mingla/PrivacyInfo.xcprivacy` — Apple required-reason API declarations
- `app-mobile/src/services/oneSignalService.ts` — push init
- `app-mobile/src/services/appsFlyerService.ts` — attribution + ATT timing
- `app-mobile/src/services/revenueCatService.ts` — IAP / paywalls
- `app-mobile/src/services/mixpanelService.ts` — product analytics
- `app-mobile/app/_layout.tsx` — Sentry init

---

## Close criteria

This ORCH closes only when:

1. All 46 remaining checklist items above are ✅
2. Production AAB submitted to Google Play (either to a closed track or promoted to production)
3. Production IPA submitted to App Store Connect (either to TestFlight or in App Review)
4. Operator confirms both submissions are accepted by the stores (i.e. no validation rejection at upload time)
5. Single PR from `ORCH-0977-consumer-app-store-launch` to `main` opened, pre-merge gate satisfied, squash-merged
6. Worktree reaped per CLOSE Step 1.7

Post-close, the World Map entry promotes from REGISTERED → CLOSED with PASS grade, and the Launch Readiness Tracker for both surfaces flips to "submitted, awaiting store review."
