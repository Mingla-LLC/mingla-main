# Outputs Legacy Archive

Historical B2/B2a Path C materials copied from the ignored `outputs/` root during ORCH-0750C.

The original `outputs/` directory is ignored and was not deleted in ORCH-0750C. These archive copies are the durable, versionable references for future docs.

Current authority for business/payment implementation state must be revalidated against current specs, reports, code, schema, and decisions before use.
