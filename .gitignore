# === Environment (real secrets only — .env.example is fine to track) ===
.env
.env.local
.env*.local

# === Operator credential stashes (never commit) ===
*Appsflyer Dev Key*
*AppsFlyer Dev Key*
*Dev Key.md
*REST API Key*
*Service Token*
*Project Token*
*API Key.md
*Secret Key*
*Auth Token*
*sentry*.md
*Sentry*.md

# === macOS ===
.DS_Store

# === Expo ===
.expo/

# === Node ===
node_modules/
# Also ignore a `node_modules` SYMLINK (a file, not a dir) — the trailing-slash
# pattern above only matches directories, so a stray symlink could otherwise be
# committed (ORCH-1006: one was, and broke a checkout's node_modules).
node_modules

# === Build output ===
dist/
build/
web-build/

# === IDE / Editor ===
.idea/
.vscode/
*.swp
*.swo
*~

# === Debug logs ===
npm-debug.*
yarn-debug.*
yarn-error.*

# === TypeScript ===
*.tsbuildinfo

# === AI tool configs (private) ===
.claude/
.codex/

# === Orchestrator artifacts (local operational state) ===
outputs/
Mingla_Artifacts/prompts/

# === Native (signing keys) ===
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem

# === MCP server config (contains live access tokens — per-dev local only) ===
# NOTE 2026-04-23: historical commits leaked access tokens in .mcp.json.
# Rotate those tokens in Supabase Dashboard. Local file stays for dev use.
.mcp.json

# === Supabase CLI local state (per-dev, auto-generated) ===
supabase/.temp/

# === Ad-hoc local scratch (one-off test harnesses, debug scripts) ===
tmp/

# === Stripe credentials scratch (RAK + secret key copy-paste files; never commit) ===
stripe-values.md
stripe-values*.md
*-stripe-keys.md
*-stripe-secrets.md

# === Provider credentials scratch (never commit) ===
Mingla_Artifacts/cloudinary_details.md
resend api key.md
resend-api-key*.md
*-resend-keys.md
*api key*.md
*api-key*.md
*api_key*.md
pexels_giphy_values.md
ticketmasterdetails.md
ticketmaster-details*.md
*-ticketmaster-keys.md
*-ticketmaster-secrets.md

# === RevenueCat credentials scratch (added 2026-05-14 — caught Revenuecat-value.md exposed at repo root) ===
Revenuecat-value.md
Revenuecat-value*.md
revenuecat-value*.md
RevenueCat-value*.md
*revenuecat*.md
*RevenueCat*.md
*-revenuecat-keys.md
*-revenuecat-secrets.md

# === Generic credential-value naming patterns (catches future "<provider>-value(s).md" leaks proactively) ===
*-value.md
*-values.md
*_value.md
*_values.md

.vercel

# === Test/retest/QA evidence directories (heavy screenshots + Metro logs; local-only) ===
# Added 2026-05-14 after audit found ~103 MB of evidence in working tree.
# The text reports referencing this evidence ARE committed; the raw evidence
# stays on disk for local reference, never git history.
Mingla_Artifacts/evidence/
Mingla_Artifacts/reports/orch-*-retest*/
Mingla_Artifacts/reports/orch-*-retest-*/
Mingla_Artifacts/reports/orch-*-investigation/
Mingla_Artifacts/reports/orch-*-rescoped/
Mingla_Artifacts/reports/orch-*-qa/
Mingla_Artifacts/reports/orch-*-retest*.log
Mingla_Artifacts/reports/*.mov
Mingla_Artifacts/evidence/**/*.mov
vercel-token.md

# Local-only personal secrets file — never commit
TokensNeeded.md
