# Mingla Comms Ledger — MOVED

**This ledger is retired (2026-07-19, issue #974).** The live comms table is now [`COMMS.md`](COMMS.md) — same protocol (BLOCK/WARN/FYI rows, ack on read, one-file direct-to-`main` commits), same numbering continued from COMMS-0118.

There are no active entries here. If your instructions point you at this file, read and write `COMMS.md` instead.

The full historical ledger (COMMS-0001 → COMMS-0117, active + archive) is preserved at git tag `pre-avengers-archive`:

```bash
git show pre-avengers-archive:COMMS_LEDGER.md
```

## Active entries

*(none — see COMMS.md)*
