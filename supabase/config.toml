project_id = "gqnoajqerqhnvulmnyvv"

[functions.refresh-fx-rates]
verify_jwt = false

[functions.manage-brand-discovery-currency]
verify_jwt = true

[functions.manage-stay-inventory]
verify_jwt = true

[functions.stay-reservations]
verify_jwt = true

[functions.stay-reservation-sweep]
verify_jwt = false

[functions.admin-stay-operations]
verify_jwt = true

[functions.payout-release-sweep]
verify_jwt = false

# issue #1179 (initiative #1013 sub-issue J) [cancel-refund-fanout] — refunds every
# order-buyer of a cancelled event exactly once. Invoked by the backstop pg_cron
# (service-role Bearer, not a user JWT) AND by best-effort client cancel kickoffs, so
# verify_jwt=false. Authorization is NOT a user gate — it is the status-first
# precondition inside cancel_event_refund_prepare (event_not_cancelled ⇒ zero refunds
# regardless of caller); the twin RPCs it calls are GRANTed to service_role only.
[functions.event-cancel-refund-fanout]
verify_jwt = false

# ORCH-1201 — hourly API-health probe; auth via service-role Bearer (pg_cron),
# not user JWT. verify_jwt=false so the cron POST is accepted (the fn enforces
# its own service-role-Bearer auth-guard).
[functions.api-health-probe]
verify_jwt = false

# ORCH-1220 [business-reviewer-bypass] — App-Store / Play reviewer email-login
# bypass. PUBLIC (verify_jwt=false) but the function enforces its own secret-code
# gate (REVIEWER_BYPASS_CODE) and only ever mints a session for the locked
# REVIEWER_EMAIL. Not a "log in as anyone" oracle — see the function header.
[functions.reviewer-signin]
verify_jwt = false

[functions.weather]
verify_jwt = false

[functions.events]
verify_jwt = false

[functions.send-message-email]
verify_jwt = true

[functions.send-collaboration-invite]
verify_jwt = true

[functions.ticketmaster-events]
verify_jwt = false

# ORCH-0824: server-side fan-out for consumer Discover (business events +
# Ticketmaster). Anon-callable; reads via service-role with explicit
# public-visibility WHERE clause as defense-in-depth.
[functions.discover-merged-events]
verify_jwt = false

# ORCH-1079: the Google `places-autocomplete` proxy was RETIRED (business address
# autocomplete now flows through `mapbox-geocode`). GOOGLE_MAPS_API_KEY is RETAINED
# — six other edge fns still consume it (admin-seed-places / admin-refresh-places /
# admin-place-search / backfill-place-photos / get-companion-stops /
# get-picnic-grocery). Do NOT delete the key.

# Ve2 — place_pool name search for venue claim comparison (public-safe fields).
[functions.claim-search-pool]
verify_jwt = true

# Issue #1648 — resolve a PICKED address to a place we already hold, so a venue
# already in our directory cannot duplicate itself when the NAME search missed.
# verify_jwt = true is mandatory, not cosmetic: every call spends money at
# Google and reads the pool, so it can never be anonymous. Rate-limited per user
# on the same bucket shape as claim-search-pool.
# BOUNDARY (ORCH-1079): this does NOT autocomplete anything. Mapbox still owns
# every suggestion the brand sees. Google is asked one question, once, AFTER the
# brand has already picked — "which place is this?" — and the answer is used
# only to look up our own directory.
# GOOGLE MAY RESOLVE IDENTITY; IT MAY NEVER DRIVE SUGGESTIONS.
[functions.venue-address-pool-match]
verify_jwt = true

# Ve1 — venue operator confirmation after biz_create_venue_brand_pending_review.
[functions.venue-claim-submitted-email]
verify_jwt = true

[functions.venue-claim-decision-email]
verify_jwt = true

# ORCH-0770: Cloudinary is a third-party webhook sender and cannot provide
# Supabase user JWTs. The gateway is public; the function authenticates
# requests with Cloudinary's signed notification headers.
[functions.event-cover-video-webhook]
verify_jwt = false

# META-ORCH-1270 (Phase 2) — cover-video storage reaper; invoked by pg_cron with
# the service-role Bearer (not a user JWT). verify_jwt=false so the cron POST is
# accepted; the function enforces its own service-role-Bearer guard.
[functions.event-cover-video-reaper]
verify_jwt = false

[functions.event-cover-pexels-search]
verify_jwt = true

# ORCH-0737 v2 patch (post-smoke 2026-05-06): wall-clock limit bump to 200s
# DEFERRED — `max_request_duration_seconds` is not recognised by the local
# supabase CLI version; removed 2026-05-06 to unblock ORCH-0734 deploy.
# Re-evaluate at ORCH-0737 CLOSE cycle: either upgrade CLI to a version that
# supports this key, OR set the timeout via the Deno function code, OR via
# request headers. Default 150s applies in the interim.
[functions.run-place-intelligence-trial]
verify_jwt = true

# ORCH-0777 live-fire 2026-05-11: Stripe webhooks have no Supabase JWT.
# Signature is verified inside the function via STRIPE_WEBHOOK_SECRET.
[functions.stripe-webhook]
verify_jwt = false

# Twilio status callback also third-party — function authenticates via
# TWILIO_STATUS_CALLBACK_SECRET query param.
[functions.twilio-message-status]
verify_jwt = false

# #1770 — OneSignal authenticates with a rotating bundle-only bearer token.
[functions.onesignal-event-stream]
verify_jwt = false

# Buyer status endpoint serves anonymous buyers reading their own checkout
# session by buyerStatusToken; no Supabase JWT involved.
[functions.ticket-checkout-status]
verify_jwt = false

# ORCH-0852 — synchronous buyer-owned confirmation path. Anon-tolerant;
# access gated by buyerStatusToken hash match (NOT Supabase JWT). Verifies
# Stripe PaymentIntent status directly and idempotently invokes
# biz_ticket_checkout_finalize so the buyer never depends on Stripe
# webhook timing to see their order.
[functions.ticket-checkout-confirm]
verify_jwt = false

# ORCH-1388 (fold-in D-C) — reconcile-stuck-checkouts sweep, cron-invoked via
# pg_cron → net.http_post with the service-role bearer (a valid JWT, so
# platform verification passes); the function additionally enforces its own
# service-role check at entry. verify_jwt = true documents the CURRENT live
# posture explicitly with ZERO behavior delta (the fn previously ran on the
# platform default).
[functions.reconcile-stuck-checkouts]
verify_jwt = true

# ORCH-0948 — anon buyer waitlist signup. Function validates ticket/event
# eligibility and writes via service-role; no direct anon table INSERT policy.
[functions.waitlist-signup]
verify_jwt = false

# #1615 — POST authenticates the user in-function; GET serves only a validated,
# opaque snapshot and never exposes source tables.
[functions.shared-card]
verify_jwt = false

# #1775 — authenticated brand contact CSV inspection/import.
[functions.contact-import]
verify_jwt = true

# ORCH-1027 — public launch-city point-in-bbox gate. Consumer-callable BEFORE
# sign-in (the ORCH-1028 onboarding location gate runs at app launch, possibly
# pre-auth). Exposes only non-sensitive city geometry; reads the live subset via
# the service-role client. No PII, no auth-scoped data. Per SPEC_ORCH-1027 §C.1.
[functions.check-launch-city]
verify_jwt = false

# ORCH-1045 — public "Get Beta Access" lead-submit. The organiser marketing site
# is unauthenticated; anon callers POST a lead. The fn re-validates all input,
# soft-throttles by salted IP hash, and writes via the service role (anon has no
# table policy — deny-by-default). No JWT required. Per SPEC_ORCH-1045 §3.3.3.
[functions.beta-access-lead-submit]
verify_jwt = false

# ORCH-1319 — the ORCH-1216 explorer "Get the app" lead-submit edge function was
# DECOMMISSIONED (the app is now LIVE on both public stores; the marketing CTA
# links straight to the store / desktop QR, so there is no lead form to POST).
# public.explorer_app_leads table + rows are PRESERVED; only the writer is retired.
# (`supabase functions delete explorer-app-lead-submit` at deploy.)

# META-ORCH-1222 — public careers application endpoint. career.usemingla.com is
# unauthenticated; anon callers POST an application + CV. The fn re-validates all
# six fields + the CV, resolves the role (must be status='open'), uploads the CV
# to the private career-cvs bucket and inserts via the service role (anon has no
# table policy — deny-by-default). No JWT required. Per SPEC_META-ORCH-1222 §4.B.
[functions.careers-apply]
verify_jwt = false

# META-ORCH-1222 — admin CV signed-URL endpoint. Returns a 60s signed URL to a
# private career-cvs object. ADMIN-gated (admin_users lookup) so JWT verification
# stays ON — only an active admin may fetch a CV. Per SPEC_META-ORCH-1222 §4.D.
[functions.careers-cv-signed-url]
verify_jwt = true

# ORCH-0880 [Tr5 Traveler Intake Forms]: anon-tolerant signed-URL endpoint
# per feedback_anon_buyer_routes.md. Buyer is anonymous when uploading intake
# files; authentication happens at the trip/schema/question lookup layer +
# storage RLS layer (anon-write-own per migration §4). NO JWT required.
[functions.trip-intake-upload-signed-url]
verify_jwt = false

# ORCH-0897 — direct user send path and cron/service-role path both provide
# Authorization headers. Keep JWT verification enabled at the function gateway.
# #2510 — Resend posts with no Supabase JWT; authenticity is the Svix
# signature, verified against RESEND_WEBHOOK_SECRET inside the function.
[functions.resend-webhook]
verify_jwt = false

[functions.marketing-send]
verify_jwt = true

# ORCH-0914 — organiser-triggered installment actions; RPCs read auth.uid().
[functions.manual-charge-installment]
verify_jwt = true

[functions.send-installment-reminder]
verify_jwt = true

# ORCH-0897 — consumer-app post-install claim flow. Must be auth-gated.
[functions.claim-pending-trip-chat-participation]
verify_jwt = true

# ORCH-0986 — paired profile batched cards. Auth-gated; function resolves the
# paired friend's last-known GPS server-side and never returns coordinates.
[functions.get-paired-profile-cards]
verify_jwt = true

# META-ORCH-1059 Sub-A — Mapbox Search Box proxy for every business address
# picker (experiences, events, trips, brand, venue claim). Keeps
# MAPBOX_ACCESS_TOKEN server-side. Auth-gated: the creation wizards are always
# authenticated; anonymous geocode traffic is not a legitimate use. ORCH-1079
# retired the legacy Google places-autocomplete proxy — this is the sole
# business address-autocomplete edge fn now.
[functions.mapbox-geocode]
verify_jwt = true

# ORCH-1165 [Mapbox static map server-proxy]. Server-side proxy for the public
# "Where you'll be"/"Where you'll start" static map (trip / event / experience).
# verify_jwt = false: the buyer-web public page is anonymous (same posture as the
# other public web fns); the strict input validation + style/zoom/size allowlists
# in index.ts are the abuse guard in lieu of auth. Reuses the EXISTING
# MAPBOX_ACCESS_TOKEN secret — keeps the token + the upstream map-vendor host out
# of all client network traffic and strips the logo/attribution server-side.
# NOTE: the PUBLIC function name is intentionally vendor-NEUTRAL (`static-map`),
# so the client `<Image>` URL `/functions/v1/static-map?…` reveals nothing about
# the upstream map vendor. Internal var/log names stay descriptive (server-side).
[functions.static-map]
verify_jwt = false

# ORCH-1050 [Brand team invite + accept + ownership transfer]
# META-ORCH-1048 sub-B. Both endpoints require an authenticated caller — the
# invite endpoint reads auth.uid() for the brand_admin+ rank check, and the
# accept endpoint resolves auth.uid() → creator_accounts.id for the RPC.
[functions.invite-brand-member]
verify_jwt = true

[functions.accept-brand-invitation]
verify_jwt = true

# ORCH-1384 [partner brand-management verbs]
# Resend / correct-email reissue for a pending partner_brand_links row.
# Requires an authenticated caller — the fn reads auth.uid() to verify link
# ownership, then drives the service_role-only
# partner_reissue_brand_invitation RPC (expire-now + fresh invitation +
# link email/invited_at update, atomically).
[functions.partner-reissue-invitation]
verify_jwt = true

# ORCH-1111 [Surface pending invites in-app]
# Both require an authenticated caller. list-my-pending-invites resolves the
# JWT email → that user's pending invites (service-role; invitee has no
# membership so RLS can't serve them). decline-brand-invitation writes the new
# 'declined' terminal state under the same login-email-trusted identity proof.
[functions.list-my-pending-invites]
verify_jwt = true

[functions.decline-brand-invitation]
verify_jwt = true

# ORCH-1051 [Scanner invite + brand-scoped scanner]
# META-ORCH-1048 sub-C. Both endpoints require an authenticated caller — the
# invite endpoint reads auth.uid() for the event_manager+ rank check, and the
# accept endpoint resolves auth.uid() → creator_accounts.id for the RPC.
[functions.invite-scanner]
verify_jwt = true

[functions.accept-scanner-invitation]
verify_jwt = true

# ORCH-1052 [Partner identity + account-level Stripe Connect + currency-match
# invite gate]. META-ORCH-1048 sub-D. Partner-Stripe onboarding + account-
# session minter require an authenticated caller (auth.uid() → partner gate).
[functions.partner-stripe-onboard]
verify_jwt = true

[functions.partner-stripe-account-session]
verify_jwt = true

# ORCH-1331 [partner Paystack payout rail] — partner Transfer-Recipient
# onboarding requires an authenticated caller (auth.uid() → partner gate;
# mirrors partner-stripe-onboard).
[functions.partner-paystack-onboard]
verify_jwt = true

# ORCH-1331 — cron sweep; no Supabase JWT (pg_cron net.http_post sends the
# service-role key as a Bearer token; the fn's first statement gates on it —
# mirrors reconcile-stuck-checkouts).
[functions.partner-paystack-split-retry]
verify_jwt = false

# META-ORCH-1076 [Paystack Africa / Nigeria] — proof slice.
# paystack-webhook MUST be verify_jwt=false (Paystack sends no Supabase JWT;
# it is authenticated by the x-paystack-signature HMAC-SHA512 check instead).
[functions.paystack-webhook]
verify_jwt = false

# paystack-checkout-create is a TEST-ONLY harness (verify_jwt=false for easy
# sandbox calls); Phase 1 folds this into the auth-gated ticket-checkout-create.
[functions.paystack-checkout-create]
verify_jwt = false

# META-ORCH-1104 Phase 0 [Support live-chat + tickets]. The caller-facing support
# fns require an authenticated caller (auth.uid() → is_support_staff()/is_admin_user()
# gate, or requester = own ticket). verify_jwt = true.
[functions.support-claim]
verify_jwt = true

[functions.support-send]
verify_jwt = true

[functions.support-set-status]
verify_jwt = true

[functions.support-grant-staff]
verify_jwt = true

# notify-support is an INTERNAL push fan-out producer invoked server-to-server by
# support-send (and later support-claim) with the service-role bearer. Like the
# webhook fns, the gateway does not require a Supabase user JWT — the fn
# authenticates the producer itself by matching the service-role bearer.
[functions.notify-support]
verify_jwt = false

# ORCH-1150 — public-submit-rsvp is the anon-capable guest RSVP write (Going/
# Not-going) for event_type=rsvp events. Public route (verify_jwt=false): the
# /e/ public page + the consumer deck call it. It runs under service-role and
# validates the event is a published RSVP + rate-limits anon writes.
[functions.public-submit-rsvp]
verify_jwt = false

# ORCH-1291 [rsvp-chip-in] — rsvp-contribution-create is the anon-capable
# voluntary-gift charge for a chip-in-enabled RSVP event. verify_jwt=false
# (mirrors public-submit-rsvp + the ticket guest path): an optional Authorization
# bearer resolves user_id, else an anon buyer with name/email. It runs under
# service-role, prices via the shared all-in engine at tax_cents=0, and creates a
# Stripe direct-charge / Paystack initialize before returning.
[functions.rsvp-contribution-create]
verify_jwt = false

# ORCH-1291 [rsvp-chip-in] — rsvp-contribution-refund issues an organiser/admin
# refund of a paid contribution (discretionary keeps-cut / event-cancellation
# make-whole). verify_jwt=true (organiser identity gates the refund).
[functions.rsvp-contribution-refund]
verify_jwt = true

# ORCH-1150 — rsvp-notify is the INTERNAL multi-channel transactional fan-out
# (push + SMS + email) invoked server-to-server with the service-role bearer.
# It is NOT a public anon route.
[functions.rsvp-notify]
verify_jwt = true

# Issue #1447 — organiser/scanner RSVP admission. The gateway verifies the JWT;
# the RPC independently uses auth.uid() for event-scoped authorization.
[functions.scan-rsvp]
verify_jwt = true

# Issue #1447 — authenticated owner or anonymous fragment-token RSVP pass/PDF.
# Anonymous recovery is validated inside the function, so the gateway is open.
[functions.rsvp-pass-fetch]
verify_jwt = false

# META-ORCH-1161 Sub-A.2 — record-consent is the SHARED bundled-consent writer
# called by BOTH the consumer onboarding (authenticated) AND the anonymous buyer
# checkout (anon), so verify_jwt=false (anon-tolerant). It writes consent_records
# via the service-role client and computes ip_hash server-side. DEC-186.
[functions.record-consent]
verify_jwt = false

# META-ORCH-1148 2.1b — send-venue-sms is the OPERATOR-triggered "table's ready"
# waitlist SMS. verify_jwt=true: the gateway verifies the caller's user JWT; the
# fn ADDITIONALLY gates on manager-plus brand membership, validates E.164, honors
# the SMS opt-out (STOP) ledger, and sends ONLY via the approved toll-free
# Messaging Service. Twilio creds are Supabase secrets (set at deploy).
[functions.send-venue-sms]
verify_jwt = true

# META-ORCH-1148 2.2a — venue-reservation-create serves BOTH an authenticated
# consumer-app user AND an anonymous web guest, so verify_jwt=false (the fn
# handles auth internally, mirroring ticket-checkout-create's guest path);
# re-validates the slot against pg_venue_available_slots + enforces capacity
# server-side; free path mints directly, fee path uses the shared all-in engine.
[functions.venue-reservation-create]
verify_jwt = false

# META-ORCH-1148 2.2a — venue-reservation-confirm finalizes a FEE reservation
# after the charge (idempotent: FOR-UPDATE the session + return-existing).
# verify_jwt=false: callable from the anon web redirect-return + the app.
[functions.venue-reservation-confirm]
verify_jwt = false

# META-ORCH-1148 2.2g — venue-reservation-cancel: the signed-in guest cancels
# their OWN reservation; the SECURITY DEFINER pg_cancel_my_reservation (called as
# the user) enforces ownership + legal transition + computes refund eligibility
# (paid && fee_refundable && before cancel cutoff). When eligible, this fn
# EXECUTES the Stripe deposit refund on the connected account (mirrors
# refund-order). verify_jwt=false: the fn validates the JWT + auth.uid() itself.
[functions.venue-reservation-cancel]
verify_jwt = false

# META-ORCH-1161 Sub-A — notify-outbox-drain: pg_cron drains notification_outbox
# and forwards each row to notify-dispatch v2. verify_jwt=false: invoked by cron
# with the service-role bearer.
[functions.notify-outbox-drain]
verify_jwt = false

# META-ORCH-1161 Sub-C — notify-reminders: ~15-min pg_cron enqueues buyer event +
# reservation reminders (24h + 2h buckets) into notification_outbox via
# pg_notify_reminders_enqueue(). verify_jwt=false: invoked by cron with the
# service-role bearer.
[functions.notify-reminders]
verify_jwt = false

# ORCH-1203 GAP-C1 — backfill-rsvp-qr-codes: every-15-min cron re-mints + delivers
# signed QR passes for going RSVPs left with qr_code=NULL (transient missing-pepper).
# verify_jwt=false: invoked by cron with the service-role bearer; the fn enforces a
# service-role-Bearer auth guard internally.
[functions.backfill-rsvp-qr-codes]
verify_jwt = false

# META-ORCH-1161 Sub-A — twilio-inbound-sms: the STOP/HELP inbound webhook.
# verify_jwt=false: Twilio cannot present a Supabase JWT; gated on ?secret=
# (same posture as twilio-message-status).
[functions.twilio-inbound-sms]
verify_jwt = false

# ORCH-1227 (DEC-192) — termii-delivery-status: the Nigeria SMS delivery webhook.
# verify_jwt=false: Termii cannot present a Supabase JWT; the function FAIL-CLOSED
# verifies the X-Termii-Signature HMAC-SHA512 over the raw body
# (TERMII_WEBHOOK_SECRET ?? TERMII_API_KEY).
[functions.termii-delivery-status]
verify_jwt = false

# META-ORCH-1161 COMPLIANCE-PAGES — self-serve-unsubscribe: the PUBLIC no-login
# opt-out endpoint behind the marketing-site /unsubscribe page + CAN-SPAM email
# footers. Accepts {email?, phone?, scope, token?} and writes BOTH
# channel_suppressions (per channel) AND legacy marketing_unsubscribes (global)
# via the service role. verify_jwt=false: CAN-SPAM requires the page to work
# standalone with no login; per-IP throttle blunts burst abuse (mirrors
# record-consent).
[functions.self-serve-unsubscribe]
verify_jwt = false

# ORCH-1271 — admin-write-primitive is the audited-write PRIMITIVE prover (and
# the wave-2 skeleton for Stripe-touching admin actions). verify_jwt=true: the
# gateway rejects callers with no JWT (401); the fn then re-verifies the JWT is
# a real user AND an active admin (admin_users status='active' → else 403) before
# writing an admin.edge_probe audit row via admin_write_audit. Mirrors
# careers-cv-signed-url's admin-gated service_role contract.
[functions.admin-write-primitive]
verify_jwt = true

# ORCH-1278 — admin money-act edge fns (Stripe-touching). verify_jwt=true: the
# gateway rejects no-JWT callers (401); each fn then re-verifies the JWT is a real
# user AND an active admin (admin_users status='active' → else 403) before any Stripe
# or DB write. admin-refund-order calls the service_role refund twins + Stripe
# refunds.create (TEST-mode safe today; LIVE needs Seth's go). admin-stripe-connect-
# action does refresh (accounts.retrieve) / onboarding_link (account_sessions.create)
# over an EXISTING account only — no account creation, no money movement.
[functions.admin-refund-order]
verify_jwt = true

# ISSUE-1221 — typed Venue/RSVP refund control plane. Provider posting is
# service-worker only; read/action surfaces authenticate again inside the Edge.
[functions.source-refund-sweep]
verify_jwt = false

[functions.checkout-sale-revocation]
verify_jwt = false

[functions.venue-reservation-refund-status]
verify_jwt = false

[functions.source-refund-action]
verify_jwt = true

[functions.source-refund-attention]
verify_jwt = false

[functions.admin-source-refund-action]
verify_jwt = true

[functions.admin-source-refund-operations]
verify_jwt = true

[functions.admin-stripe-connect-action]
verify_jwt = true

# ISSUE-1173 (sub-issue D of #1013) — admin-gated batch migration of existing
# Stripe organiser accounts onto (or off) a manual event-anchored payout
# schedule. verify_jwt=true: the gateway rejects no-JWT callers (401); the fn
# then re-verifies the JWT is a real user AND an active admin (else 403).
# Moves NO money; creates/replaces NO Stripe account.
[functions.admin-payout-hold-migrate]
verify_jwt = true

# ISSUE-862 WP1 [Full Rooms Ad Engine] — admin-ad-* edge fns (Meta channel +
# 4 fail-close stubs). verify_jwt=true: the gateway rejects no-JWT callers (401);
# each fn then re-verifies the JWT is a real user AND an active admin
# (admin_users status='active' → else 403) before any platform call or DB write.
# All platform tokens live ONLY in Edge Function Secrets, resolved via
# ad_connections.token_env_var (env-var NAMES in the DB, never values).
# Everything is created PAUSED; launch is an explicit admin action.
[functions.admin-ad-connect]
verify_jwt = true

[functions.admin-ad-preflight]
verify_jwt = true

# Issue #1928 — exact Explorer/Business public-identity readiness. Read-only
# provider discovery plus Meta validate_only; never creates or persists objects.
[functions.admin-ad-app-identity-preflight]
verify_jwt = true

[functions.admin-ad-app-readiness]
verify_jwt = true

[functions.admin-ad-app-readiness-event]
verify_jwt = true

[functions.admin-ad-create-campaign]
verify_jwt = true

[functions.admin-ad-campaign-action]
verify_jwt = true

[functions.admin-ad-campaign-sync]
verify_jwt = true

# ISSUE-866 WP3 [Full Rooms Ad Engine — Creative Library] — records a canonical
# creative row behind the A1-6 server-side byte-probe + the §2 validation matrix
# + the COMMS-0102 crawler-permissiveness check. Same gate as the WP1 fns:
# verify_jwt=true, then the in-code admin_users active gate (403). This fn never
# calls an ad platform and never reads a platform token — platform upload is
# lazy, at ad-create, via _shared/adCreative.ts resolveCreativeRef.
[functions.admin-ad-creative-upload]
verify_jwt = true

# ISSUE-1184 [Campaign Builder video Phase A] — one-platform-at-a-time media
# preparation plus read-only provider previews. Gateway JWT and the in-function
# active-admin gate both run before input handling or provider access.
[functions.admin-ad-creative-prepare]
verify_jwt = true

[functions.admin-ad-preview]
verify_jwt = true

# ISSUE-989 [Campaign Builder real targeting] — the admin-gated, READ-ONLY
# targeting resolver the Audience step calls to turn a typed city/interest NAME
# into per-platform targeting keys/ids (Meta adgeolocation+adinterest, TikTok
# tool/region+tool/interest_category, Google geoTargetConstants:suggest, Reddit
# name passthrough). Creates NOTHING — every branch is a search. Same gate as the
# WP1 fns: verify_jwt=true, then the in-code admin_users active gate (403).
[functions.admin-ad-targeting-search]
verify_jwt = true

# ISSUE-1267 [Google Ads — the 5th server-side conversion lane] — the admin
# one-shot that provisions the Google conversion action (customers/{id}/
# conversionActions:mutate, category PURCHASE / UPLOAD_CLICKS / ONE_PER_CLICK) and
# stores the returned resource name in ad_connections.extra (CONFIG, not a secret),
# plus validateOnly dry-runs of both the mutate and the uploadClickConversions
# lane. Same gate as the WP1 fns: verify_jwt=true, then the in-code admin_users
# active gate (403). NO token/secret ever appears in a response, row, or log.
[functions.admin-ad-google-provision-conversion]
verify_jwt = true

# ISSUE-865 WP-A [Attribution & reservation-conversion tracking] — the anon
# buyer-surface capture endpoint. verify_jwt=false because it is called from the
# anon public web landing AND (post-finalize, server-to-server) from the WP-B
# finalize hook; it authenticates to the DB as service_role internally (RLS is
# admin-read + service-write) and is FAIL-OPEN + IDEMPOTENT (unique event_id) so
# it can never block a purchase. In-fn rate-limit + PII hashed, no PII echo.
# Same anon pattern as discover-merged-events. Reads env-var NAMES only — never a
# token VALUE (the CAPI send is WP-B).
[functions.attribution-capture]
verify_jwt = false

[functions.venue-organic-capture]
verify_jwt = false

# ISSUE-1003 [Venue Website Grader growth tool — test cut] — the anonymous
# grader page's run endpoint (place_pool typeahead search + report generation).
# verify_jwt=false because the marketing tool surface is UNAUTHENTICATED. The
# fn re-validates every field server-side, rate-limits by salted IP hash (raw
# IP never stored, 8 runs/24h), SSRF-guards the site fetch, and talks to the DB
# as service_role only (tool_leads + place_pool have no anon policies —
# deny-by-default).
[functions.growth-tools-run]
verify_jwt = false

# ISSUE-1003 — the email gate: POST {run_id, email} captures the lead email and
# sends the generated report as a branded HTML email (shared shell, system
# sender, reply-to support@). Same anon posture + service-role-only DB access
# as growth-tools-run.
[functions.growth-tools-gate]
verify_jwt = false

# ISSUE-1003 — the public render-data read for the "after" homepage preview
# (template engine). GET ?run_id / POST {run_id} returns the storefront-render
# subset (name, tagline, vibes, occasions, photos) of a saved run so the
# /tools/venues/preview page can render a redesigned homepage to screenshot.
# No sensitive fields; service-role-only DB access.
[functions.growth-tools-preview]
verify_jwt = false

# ISSUE-1003 — token-gated full-report read. POST {run_id, token} returns the
# full report ONLY when the token matches the one emailed to the verified
# address (growth-tools-gate). Entering an email never reveals the report; the
# emailed link does. Service-role DB access.
[functions.growth-tools-report]
verify_jwt = false

# ISSUE-2725 — machine-only competitor weekly worker. Gateway verification is
# disabled because it accepts the service-role/CRON secret and performs its
# own constant-time authorization before parsing or claiming work.
[functions.competitor-intel-worker]
verify_jwt = false

# ISSUE-1078 — real Google Calendar booking. POST {action:"slots"} returns free
# 20-min slots on seth@usemingla.com's calendar; POST {action:"book"} creates the
# event with a Meet link + report context. OAuth refresh token (calendar scope),
# no service account. Public (no user data mutated without the calendar API).
[functions.growth-tools-book]
verify_jwt = false

# ISSUE-1004 — Event Turnout Predictor. POST {action:"run", input, budget}
# forecasts event turnout + what ad spend buys, grounded in LIVE external
# research (competing events, historical turnouts, weather, event-ad CPC) via
# Gemini google_search, then a deterministic budget engine + structured
# synthesis. Writes tool_leads(tool='events'). Same anon + service-role posture.
[functions.growth-tools-events]
verify_jwt = false

# ISSUE-1096 — city autocomplete for the event predictor intake. POST {q}
# proxies Mapbox Search Box /forward (server-side token) and returns real city
# candidates so the city field is validated, not free text. No DB access.
[functions.growth-tools-geocode]
verify_jwt = false

# ISSUE-1005 — Quote Any Trip. POST {action:"run", input} builds a fully-costed
# trip quote grounded in LIVE external research (real named hotels + activities
# with current rate estimates, comparable trips, demand, weather, trip-ad CPC)
# via Gemini google_search, then a deterministic cost-sheet + pricing engine
# (recommended per-person price, margin ladder, profit-max ad budget) + a
# structured day-by-day itinerary. Writes tool_leads(tool='trips'). Same anon +
# service-role posture as the other growth tools.
[functions.growth-tools-trips]
verify_jwt = false

# ISSUE-1006 — The Undercharging Audit. POST {action:"run", input} runs a pricing
# audit for experience hosts grounded in LIVE research (named comparable
# experiences + prices, a fair hourly rate for the host's time) via Gemini
# google_search, then a deterministic audit engine (true cost per head INCLUDING
# their time → profit/loss, recommended price, break-even seats, upside) + a
# structured verdict. Writes tool_leads(tool='experiences'). Same anon +
# service-role posture as the other growth tools.
[functions.growth-tools-pricing]
verify_jwt = false
[functions.claim-attendance]
verify_jwt = true

[functions.attendance-claim-link]
verify_jwt = false

# #2217 — the identity reconnect. verify_jwt = true is the gate, not decoration:
# the function matches the CALLER'S OWN provider-verified identifiers, so an
# anonymous caller has nothing to match and must not reach the claim RPC.
[functions.attendance-claim-identity]
verify_jwt = true

[functions.attendance-claim-backfill]
verify_jwt = false

# Issue #1789 (#1767 Phase 1) — venue QR print sheet (SPEC #1788 P-27). The
# gateway is open so the 401 body is ours, but the function REQUIRES a Bearer
# in-code (userIdFromAuthHeader) and gates on rank >= event_manager for the
# brand before it reads a single spot code. Renders on the shipped ticket-PDF
# rail into the private `venue-qr-sheets` bucket and returns a 60s signed URL.
[functions.venue-qr-sheet]
verify_jwt = false

# Issue #1770 / parent #876 — bounded source-key worker. The gateway is open to
# cron/service-role and the function independently checks that bearer.
[functions.brand-person-ingest-worker]
verify_jwt = false

# Issue #1770 — organiser invite preview/confirm and private export surfaces.
[functions.offering-invite-dispatch]
verify_jwt = true

[functions.guest-roster-actions]
verify_jwt = true

[functions.brand-people-export]
verify_jwt = true

[functions.brand-people-export-worker]
verify_jwt = true

# Issue #1772 — enabled support/privacy staff only. The gateway verifies the
# JWT and the handler independently proves support_staff before service RPCs.
[functions.support-brand-person-erasure]
verify_jwt = true

# Issue #1790 (SPEC #1788 §S6) — venue-order-create serves BOTH an authenticated
# consumer-app user AND an anonymous web guest, so verify_jwt=false and the fn
# handles auth internally via userIdFromAuthHeader (the venue-reservation-create
# pattern). Every money number is computed server-side through the shared all-in
# engine; a price sent by a client is a validation error, not a hint.
[functions.venue-order-create]
verify_jwt = false

# Issue #1790 — venue-order-status is the anon-possession read: access is gated
# by a sha256 hash-match on buyerStatusToken, NOT a Supabase JWT (the shipped
# ticket-checkout-status posture).
[functions.venue-order-status]
verify_jwt = false

# Issue #1790 — venue-order-staff. verify_jwt=false because a Bearer is REQUIRED
# IN-CODE: the token is verified via auth.getUser, then the brand-membership
# floor is checked, and the rank-gated tab mutations enforce >= event_manager
# inside their SECURITY DEFINER RPCs.
[functions.venue-order-staff]
verify_jwt = false

# Issue #1791 (SPEC #1788 §S6, P-25) — venue-order-guest-action. verify_jwt=false
# because it serves BOTH an anonymous web guest holding a `guestCancelToken` and
# a signed-in app user who owns the order. Both proofs are checked IN-CODE: the
# token by a sha256 hash-match inside the RPC, the user by auth.getUser followed
# by a buyer_user_id comparison. A cancel before acknowledgement is an instant
# full refund; afterwards it becomes a request the VENUE decides (D-7a).
[functions.venue-order-guest-action]
verify_jwt = false

# Issue #1791 (SPEC #1788 P-55) — venue-order-escalation-sweep. Called ONLY by
# pg_cron every minute with the service-role key, which the function compares
# in-code before doing anything. verify_jwt=false is the sweeper posture
# (notification-retry-sweeper / reconcile-stuck-checkouts). Its entire output is
# push notifications and durable in-app rows: it never refunds, never cancels,
# never pauses a venue's ordering, and never sends an SMS (D-7a/D-7b/D-7c).
[functions.venue-order-escalation-sweep]
verify_jwt = false

# Issue #2075 — public, read-only native minimum-version policy. The function
# validates app/platform itself and reads the private table with service role.
[functions.app-version-policy]
verify_jwt = false

# Issue #2107 — public, read-only JavaScript (OTA) update policy, keyed by the
# runtime the caller is actually running. Separate from app-version-policy on
# purpose: shipped clients key-set-validate that response, so extending it would
# make every installed build reject it and fail open.
[functions.app-ota-policy]
verify_jwt = false

# #2830 — customer Website control plane. User identity and monotonic role
# authority are re-derived inside Core; provider-neutral failures are returned.
[functions.brand-site-control]
verify_jwt = true

# #2830 — signed CMS-to-Core service gateway. Supabase JWT verification is not
# the authority; the function verifies the directional, expiring HMAC envelope
# and consumes its nonce before any work.
[functions.brand-site-cms-callback]
verify_jwt = false

# #2830 — signed public-runtime-to-Core exact-host resolver. Anonymous browser
# traffic never calls this route; only the server runtime owns its sender key.
[functions.brand-site-runtime-resolve]
verify_jwt = false

# #2830 — consent-gated first-party attribution issue/consume bridge. It uses
# an exact Mingla Sites Origin allowlist and stores only a peppered token digest.
[functions.brand-site-attribution]
verify_jwt = false
